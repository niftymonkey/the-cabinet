dist/
node_modules/
playwright-report/
test-results/
pnpm-lock.yaml

# The design record is hand-shaped prose. Prettier's table padding makes it worse to edit.
docs/

# Vendor agent skills dropped by the Upstash marketplace install. Not ours to format.
.agents/

# Session handoffs from /continue. Local state, never committed.
continue.md
continue-*.md

# The v1 roadmap's source, its build scripts and the page they generate. Local
# iteration notes, excluded from git in .gitignore.
local/

# Assetpack's cache and its output. Generated on every typecheck and build, and
# git ignores all three (apps/hungry-grave/.gitignore).
apps/hungry-grave/.assetpack/
apps/hungry-grave/public/assets/
apps/hungry-grave/src/manifest.json

