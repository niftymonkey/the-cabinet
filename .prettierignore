dist/
node_modules/
playwright-report/
test-results/
pnpm-lock.yaml

# The design record is hand-shaped prose. Prettier's table padding makes it worse to edit.
docs/

# Vendor agent skills dropped by the Upstash marketplace install. Not ours to format.
.agents/

# Session handoffs from /continue and task branches from /fork. Local state,
# never committed. Both are hidden from git in .git/info/exclude, which prettier
# does not read, so an open handoff would otherwise fail the gate for its author
# alone.
continue.md
continue-*.md
fork-*.md

# Local iteration notes and one-off verification instruments, excluded from git
# in .gitignore.
local/

# The v1 roadmap's source, its build scripts and the page they generate. The
# yaml and template are hand-shaped; prettier would churn both.
scripts/roadmap/

# Assetpack's cache and its output. Generated on every typecheck and build, and
# git ignores all three (apps/hungry-grave/.gitignore).
apps/hungry-grave/.assetpack/
apps/hungry-grave/public/assets/
apps/hungry-grave/src/manifest.json

