# Continue: The Hungry Grave

> **Last updated:** 2026-09-07T22:30:00Z
> **Branch:** `hungry-grave-v1` in worktree `/home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1` (46f32d4930 chore(hungry-grave): add the Neon agent skills the marketplace install wrote), not pushed; main is 41b7a5854c
> **Working tree:** clean apart from gitignored per-checkout files (`.env.local`, `.vercel/`, node_modules). This handoff and the push records are tracked on the branch under `apps/hungry-grave/docs/push/`, alongside `.claude/rules/push-working-agreement.md`; both are removed before merge.

## FIRST ACTION: enter the worktree

Locally: run EnterWorktree with `path: /home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1` before anything else. In the cloud: the branch is already checked out. Either way this file, the decisions, the open-ticket summary, the path draft and the pre-authorization list are the tracked copies in `apps/hungry-grave/docs/push/`, and the rules that bind the push are in `.claude/rules/push-working-agreement.md`. Mark's personal tally stays gitignored at the main checkout's `local/what-the-first-build-taught.md`. Locally, the isolation guard refuses writes to the main checkout and refuses compound Bash that mentions git; use plain commands and the file tools. Working mode for everything from here: `one-push-build-mode` memory (lean main thread, subagents on Opus or cheaper, the stuck rule, Discord for stuck notes only).

## The V1 grill is complete; the prerequisites for the push are nearly done

The prompt is `apps/hungry-grave/docs/push/v1-rethink-prompt.md` and the compass is `apps/hungry-grave/docs/v1-first-build-learnings.md`. Read both, then `apps/hungry-grave/docs/push/decisions.md` (decisions 7 to 19, Mark's repositioning and his fun goal, verbatim, plus the carried-forward items; 13 to 19 landed 2026-09-07) and `apps/hungry-grave/docs/push/open-tickets.md` (all 19 open tickets summarized with the assumptions each rests on, quoted). Then the reading list the prompt names (influences, VISION, game-concept, ADRs 0047 0034 0045 0046 0003 0008 0002, CONTEXT.md, `scripts/roadmap/v1.yaml`). Do not restart the grill from question one. Pick up at the next step below.

How the grill runs, from the prompt and from Mark across four sessions: one question at a time, wait for the answer. Frame design questions as moments of play. **Research before asking**: a craft question gets researched against industry standard first, from primary sources, written under `apps/hungry-grave/docs/research/`, and comes back as two or three options each tied to named games, with a recommendation. Where the record already holds a value, it is one option with its origin named, never settled. **Research guides, Mark decides.** Take his idea, find how close games made it work, name any documented failure mode with evidence, then build and measure it if nothing says it fails. **An ADR is never a cost.** The cost of reversing one is the property of play it protected, said as a moment of play; if that property is worth nothing there is no cost (Mark, 2026-09-02, after I named "we change ADR 0002" as a cost). The term is "industry standard", never "the world". Nothing becomes a ticket during the grill. Never measure the current fixed stage to answer a question about the new systems (Mark stopped a scratch tally 2026-09-02; the old stage is the old game). Settled and off the table: the core loop, the shmup crossing, replay from seed plus inputs, the ADRs as canonical record.

### Decisions 1 to 6 (all Mark's, recorded nowhere but here)

1. **The V1 line is redefined.** V1 is the concept proven as a game Mark can keep building. It is done when a real person's read of the game means something, and the read has happened. The line is now: *one stage, a final boss, tunable, played by a harness that moves, dodges, feeds and chooses in several styles, read by a person, on desktop and phone.* "Dressed" leaves the V1 line; Halloween art is a hope for later, not a promise. Halloween as a date is explicitly not a focal point. This changes the box in `game-concept.md` and `v1_in_one_line` in `v1.yaml`, both untouched until the grill ends.
2. **Both systems get designed inside V1** (density and progression), and **the ladder gets made visible inside V1**, because without them a person's read is dice.
3. **The playing harness is in V1, at the right place, not first.** Mark's words: a mechanism for the computer to play the game, dodge, power up, make choices, and test several variations of a feature or fix before handing him a final human read. The harness measures, it never says whether the game is fun; it runs the variations and Mark plays the winner. Hard dependency: a harness that chooses needs a choice to exist first. Added 2026-09-02: it runs many plays, repeated, faster than real time (the sim is headless and fixed-tick already), and human play data lands in the same place, told apart; automated play is never the only source of truth.
4. **Progression first, then harness, then density.** Reasons: the harness needs a choice to exist and the harness is what tunes the director without Mark as the choke point; Left 4 Dead's director was tuned against players whose power never changes, ours reads pressure on a player whose power moves with every offer, so landing the director before the offer means tuning it twice; a wildcard pause is a hole in the pressure signal the director must know about. Decision 10 has since cut the director out of power entirely, which weakens the second reason; re-check this order at the path step.
5. **The choice-sphere wildcard stays parked**, per ADR 0034. The density design carries one line, "a pause may exist," so it is not surprised later.
6. **Compass corrections** landed in 1fc7a5d06d. One waits on purpose: ADR 0047 says "the tuning pass (#39) runs first and #85 carries the measurement", which is the ordering the grill has not settled; it gets fixed when the grill fixes the order.

### Decisions 7 to 10, in `apps/hungry-grave/docs/push/decisions.md` (one line each here)

7. Stage length stays five minutes (basis `docs/research/shmup-stage-design.md`); two follow-ons queued: the 17 second pre-boss silence and the one minute final boss.
8. The offer of three stays; the spin goes. Three bodies fall side by side, the grave swallows one, the other two vanish. Basis `docs/research/reward-delivery-models.md` (committed df5985fe0a). Mark saw a spinning multi-option pickup in an unnamed shmup; recorded as an open item in that file.
9. One live offer at a time; further paid drops bank toward the next, the bank visible on the offer. Chained menus are the one thing Mark has kept out of the game since Vampire Survivors is the influence. Whether a burst can cross several prices is unknowable until density is directed; the harness checks that path.
10. **Power is metered by carriers, not by kill count.** Authored mobs carry the offer; the director never spawns carriers, so it cannot touch power, which cuts the loop. Carrier placement per stage is data and is now the main progression knob, read as time-of-each-drop and power-by-minute. Mark's fun goal, verbatim in the decisions file: the player reaches the point where "there's just so many projectiles flying out of you." Supersedes ADR 0002's kill-priced curve and retires drops.ts's price table at the ADR step; decisions 8 and 9 ride on the carrier's drop unchanged.

11. A missed carrier is missed; the carrier schedule carries slack, a number in the stage data.

### Decisions 13 to 19 (2026-09-07, all Mark's, one line each; full text and rejected options in the decisions file)

13. **The stage grows to eight to ten minutes, in two or three named sections.** Supersedes decision 7. Basis `docs/research/stage-length-with-a-director.md`. Mark's words on the cost: "with you authoring new things as necessary (mobs, shapes, temporary-before-art-backgrounds, music, etc)". He has stand-in assets (music, mob art, backgrounds); they enter V1 as section markers, not as dressing, so decision 1 holds. **Asset location deferred by Mark to implementation time, when he is at his computer; do not ask for it before then.**
14. **Three sections: Banshee near 2:00, a swarm set piece near 5:00, the Undertaker near 8:00.** Ikaruga's short-first, longest-middle, short-last shape. The set piece is a heaven moment (the storm shreds a big authored swarm, corpses are the payout), not a second hell dose. What the set piece is remains design work. Closes "are both bosses inside V1": yes.
15. **The pre-boss drain-out becomes a slow, sparse last row; the boss arrives as the last body leaves; no drain-out before the set piece.** Only the two boss boundaries need an empty field. The sparse row is the authored valley the director relaxes into.
16. **The Undertaker runs about 90 seconds to two minutes across three chunks.** One new pattern chunk inside his own grammar (curtains, spirals). Health per chunk is tuning data.
17. **Harness: one policy over many seeds first, with the offer built into the existing dodge policy; then two error knobs (stale-command dexterity error, shorter look-ahead strategy error), nine configurations; the named roster waits for human tapes.** Every harness question is a comparison, never a threshold; batches report distributions, never means. One header field names which policy steered. Basis `docs/research/playing-harness-precedent.md`.
18. **The visible ladder is both channels: the storm carries each line's level by count, and a slim strip confirms it with score and level pips; a loss shows on the field (the line's projectiles blow up) and on the strip (pip goes dark).** Halls of Torment's shipped shape. The rim carries no ladder marks. Basis `docs/research/visible-ladder-precedent.md`; note its rejection of a strip rested on "the glance the designer asked to avoid", which has no ruling behind it (the only record line, learnings line 18, is about drops). Open play question: is a stripped rung a catchable field object.
19. **The director gets a finite budget per phase (Risk of Rain 2's credits), spent only when pressure is low, with a quiet interval after each add; engine caps are re-derived above floor-plus-budget and go back to being bug detectors; the corpse cap never evicts and faults if it binds.** Closes the caps question and, in part, the felt-payoff question (the storm wins a phase when the budget runs out). Basis `docs/research/director-knobs-and-signal.md`.

**Wording rule from Mark this session:** the game is bullet heaven with doses of bullet hell from bosses. Say it in that order. I wrote "the game's own promise is bullet hell in separated doses" and he stopped me; the record (VISION.md line 13, CONTEXT.md Storm) is clean.

Progression's "tunable" closed without a ruling: the knobs are data tables (what each level buys, the carrier schedule, later what an offer can hold once the pool passes four), the read is the harness by style plus a person's pick when shown.

### The four AFK research records (committed and pushed; consumed by decisions 13 to 19)

`stage-length-with-a-director.md` (f97c318ee1), `director-knobs-and-signal.md` (d17462deb1), `playing-harness-precedent.md` (a6218d31e3), `visible-ladder-precedent.md` (41b7a5854c). Each ends with an Open items list naming what came back empty; do not re-search those. Facts from them the later questions still need: Fatshark deleted the near-kill term when it rewrote the director for Darktide (read damage and floor events, never nearby kills); the signal's constants get set once and what gets retuned is the gating and population; a deterministic policy needs one run per seed; relative difficulty is invariant to the policy, so a weak bot ranks correctly; no shipped game subtracts score directly on a hit.

### The next step (as of 2026-09-07, end of session 6): finish the prerequisites, then the push

The grill's questions are all answered (decisions 1 to 24, in `apps/hungry-grave/docs/push/decisions.md`; 22 to 24 are craft calls I took with Mark's confirmation). The path is drafted in `apps/hungry-grave/docs/push/path-draft.md` and Mark has read the summary; he has not objected to any disposition yet (the three he was warned about: closing #26, closing #37, #38 off the road). The pre-authorization list is drafted in `apps/hungry-grave/docs/push/pre-authorizations.md`, not yet shown to him in full (he has seen the stuck rule, the one-writer rule and the lean rule, all confirmed). Decisions 25 and 26 (the waking set piece on a slow parallax ground layer; the locked-overlap third chunk placed last) closed the last craft calls on 2026-09-07, so step 3 below is done.

Prerequisites, in Mark's order, state:
1. Assets: done and agreed (below).
2. Store: done and smoke-tested (below).
3. Craft calls: three of four done (22 to 24). The fourth is the set piece identity and the Undertaker's third chunk; a research agent on Opus was writing `apps/hungry-grave/docs/research/set-piece-and-final-boss-chunk.md` when the session ended. **Check whether that file exists.** If yes, read only its recommendations and put the two choices to Mark as moments of play (the set piece is a heaven moment, never a second boss). If no, re-run that research (the prompt shape is in the sibling records; Opus; under 30 KB; no dashes).
4. Usage limits and models: done (skill rewritten, plan B, lean by default).
5. Stuck rule: done.
6. Then: show Mark the pre-authorization list, take his edits.
7. Then: cloud or local. Facts: the Agent tool's `isolation: "remote"` does NOT go to the cloud on this account (probe 2026-09-07 ran in a local worktree, saw Mark's home, 12 CPUs, the WSL disk), so "main thread here, subagents in the cloud" is not available. Mark is locked out of the Vercel dashboard by an unexpected 2FA prompt (support ticket open, holiday), so no Vercel token can be minted; the local CLI login still works, so all Vercel steps (deploys, env pull) run from this machine. Recommendation given: run the push as a cloud session with the two deploys deferred to the end and done locally; alternative is all local. Mark has not decided.
8. If cloud: dry run first (install tools, `pnpm verify`, store smoke test, a CodeRabbit review with `--api-key`, one Discord webhook message; note `usage.sh` needs a credentials file the cloud lacks). Done already: the working agreement is in `.claude/rules/push-working-agreement.md` and the push records are tracked under `apps/hungry-grave/docs/push/` (both removed before merge). Still to do: set the cloud environment variables: `CODERABBIT_API_KEY` (Mark has instructions), store keys copied from `apps/hungry-grave/.env.local`, a Discord webhook URL. Mark was told not to paste tokens in chat.
9. Then the push: docs first (ADRs, learnings, tickets, roadmap) as one review stop, then code to the done line, per the path draft and the pre-authorization list.

Older detail on the questions this section used to list follows; all are closed.

- **Where human play data lands in V1.** Carried since session 4: friends' tapes and harness tapes go to one place, told apart (the header already carries author and inputDevice; decision 17 adds the policy field). What has no home is where tapes are stored. Not yet researched; a small question, possibly operational rather than design.
- **The rest of the learnings doc's open questions:** the two-touch tie-break on the offer for the tape (the spin is gone per decision 8, three bodies side by side, so this is now "which body wins when the grave covers two"); how fast a stripped player recovers at the thinner birthright; whether the gas becomes the new lean and whether boss patterns ever complete uncancelled. The dodge-only bot question is closed by decision 17; the caps and felt-payoff questions by decision 19.
- **Which offer body the base policy walks to** (nearest, lowest-level line, or per-line preference); the third is the roster's territory.
- **Whether a stripped rung is a catchable object** (decision 18's open play question).

Then the path step: re-check decision 4's order (progression, harness, density) now that decision 10 cut the director out of power and decisions 13 to 16 made the stage rework authored content inside the density design; place the ladder (after the carrier work, before a person plays); place the harness. Then ticket by ticket using `apps/hungry-grave/docs/push/open-tickets.md`. Before any progression or density answer becomes an ADR, the game design gate reviews it.

### The questions after that, in whatever order the answers force

Answered this session: the pre-boss silence (15), the final boss length (16), the harness shape and styles (17), the ladder shape (18), both bosses in V1 (14), the caps (19). Still open: listed under the next step above. Mark on human play data: "we're going to need to figure out where that does land ... once we redesign what V1 looks like".

Facts found that the later questions need:
- Roadmap card n85 (directed density) is `when: next` with no `blocked_by` edges; n86 also has none.
- `v1.yaml` promise p26 (a pinned seed replays the same run cross-device) is superseded by ADR 0047; CONTEXT.md already defines a pinned run as pinning the dice, not the stage.
- ADR-step touch list from decisions 13 to 19: `game-concept.md` line 44 (stage clock, drain-out), line 64 (Undertaker's two chunks); VISION.md's "a boss arrives alone" boundary wording (one boundary is now a swarm event); ADR 0047's off-limits list (set piece added, drain-outs shrink to the sparse row) and its "tuning pass runs first" ordering; ADR 0002 and drops.ts's price table (decision 10); ADR 0034's spin wording (decision 8); `caps.ts` (decision 19); the research record's unsourced "designer asked to avoid the glance" line (decision 18).
- `game-concept.md` still says the birthright is the skull stream plus Territory; ADR 0045 names that sentence as needing correction. `VISION.md` still carries the authored-stage bet and the "not a survivors game with a director" line; #86 owns that. `game-concept.md` line 44 holds the five-minute stage and line 79 the deferred wave director.
- From the ticket summary: #26's map is stale against its own comments and against #85 (mermaid still shows #36 as frontier, lists 15 nodes, none of the newer tickets); #68's ruling comment and #26's gist still say "soul stream"; #37 (labelled ready-for-agent, zero comments) still lists "the wave director" as out of scope. `gh issue view --comments` in plain mode dropped titles and bodies, and #37 came back empty; use `--json`.
- #31's body names a tester and says the date gets written in; never ask Mark for tester names or dates.
- The four end outputs the prompt wants, in order: a redefined path with order and dependencies; a keep/rewrite/close disposition for every open ticket with the surviving learning named; ADRs for the grill's decisions plus the learnings doc updated; then tickets and roadmap cards. Draft everything for review before creating, closing or editing anything.
- `local/what-the-first-build-taught.md` is Mark's personal tally (what he set out to build versus what building it showed), in a plain over-lunch voice. Five entries as of 2026-09-02 (the fifth: power comes from who you kill, not how many). An entry qualifies only if a stranger would describe the project differently from the heading alone; propose additions at the end of a session, one at a time.

## How we got here, in one line each

2026-08-31: belch-thread grilling produced ADRs 0034, 0008, 0045, 0046 and the skull stream rename, all unbuilt. 2026-09-01 (first session): ADR 0047 landed in a25e711b2c; Mark called the replan; the learnings record landed in 886782ebb3 and is the durable home for the six unbuilt rulings, the homeless designs, what the plan gets wrong, and the open questions. Do not rebuild those lists here. 2026-09-01 (second session): decisions 1 to 6, two questions in. 2026-09-01 (third session): two research records written and committed (d010921e66), decision 7, the delivery-model repositioning, and the research-before-asking rule. 2026-09-02 (fourth session): reward-delivery-models research committed (df5985fe0a), decisions 8 to 10, progression closed, the coupling question handed to the density design, harness speed and human-data wants recorded. Later the same session: decision 11 (missed carrier is missed), director-precedent research committed (e00074fba2), entry 12 (Mark's longer-stage question, to be evaluated; first miswritten as a decision), fifth tally entry. 2026-09-06 (fifth session): the AFK research block ran end to end, four records written by four parallel agents, checked, committed and pushed. No decisions taken and no questions put to Mark; the grill is exactly where the fourth session left it, with the research for its next step now in hand. 2026-09-07 (sixth session): decisions 13 to 19, seven questions asked and answered one at a time from the four records, each recorded in the decisions file as it landed; the bullet-heaven wording correction; the asset location deferred by Mark.

The roadmap source is `scripts/roadmap/v1.yaml`; a Stop hook rebuilds the page on change and the session publishes it to https://claude.ai/code/artifact/c6bc397f-f700-49cc-914f-5ed88f2cb420. The `tally` block is a build tripwire. Expect the yaml to be substantially rewritten by what the grill produces.

## The bell arcs, designed and awaiting its record

Unchanged and still waiting; the grill's ADR step is its likely home. Mark said 2026-08-31 there is "a reasonable-size chance" an upcoming ticket removes the bell from the initial four; nothing ruled since hardcodes it anywhere. The pinned design, all Mark's answers from 2026-08-31, recorded nowhere else:

- The clock and cadence stay. ADR 0036's core (timed pulse, damage falloff with distance) stands untouched. Only the shape changes: the 360° ring becomes cones, one forward at level 1, maybe 1-2-3-4-5 per level, wrapping toward the sides as they multiply, so max level earns "around me at field scale" back and never occupies "ahead of me".
- Early level-1 blindness accepted knowingly; reach grows to compensate, damage and push concentrated. The push must be felt; the measured case: `BELL_PUSH_BY_LEVEL` `[0,0,0,0,20,40]`, #79 runs total push 42/51/0 field units, bell kills 4/3/2 of ~267.
- Amplified self-starving accepted eyes-open; it feeds the storm by shoving mobs back into stream fire.
- Exact angles, reach, push per level are tuning data, never ruled numbers.
- The supersession must name: what replaced the ring wording (2026-08-27 pool ruling, restated in `weapon-pool-review.md`), what stood (timer, falloff, not-close-defence), what the old ruling could not have known (no repel reading until #79). Verbatim original in `local/design/bell-arcs.md` (gitignored). Conditioned tapes at bell 4-5 via the committed recorder for tuning.

## Open items with no other home

Small, real, operational rather than design.

1. **#82 open**: tape commit labels miss a dirty marker (`vite.config.ts:21`); candidate `git describe --always --dirty`.
2. **#37 stays open deliberately** (boss stories 12-14, fog story 22).
3. **540x760 records disagree** (`layout.test.ts:67`, `field.ts:4`, `CONTEXT.md:85`), docs-only fix pending.
4. **Stamp-order trigger** on PR #84's stamp thread: a second output in `v1.yaml` `outputs:` moves the stamp into the hook.
5. **Stream flank narrowness**, Mark: "the edge thing could be an issue". Watch item, arithmetic in `soulStream.ts` JSDoc.

## Standing rules and facts, still true

- **One-push build mode (Mark, 2026-09-07):** prerequisites walked through together first (assets done, store done, usage limits and models done, stuck rule done; open craft calls in research now; then the pre-authorization list; then cloud-or-local decision and, if cloud, a dry run), then one autonomous push on one branch in its own worktree; CodeRabbit CLI before every code commit; gates as relevant; ADRs are my call, reviewed on the branch before merge. Memory: one-push-build-mode.md (carries the stuck rule and the lean-by-default rule in full).
- **Usage limits (2026-09-07):** the `stay-within-limits` skill in the config repo (`/home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/`) was rewritten: `usage.sh` reads the real account limits (session, weekly all, weekly Fable) from the same endpoint the `/usage` screen reads; thresholds session and weekly-all caution 75 stop 85, Fable caution 85 stop 95; main thread lean by default (plan, orchestrate, review, gates; everything else on Opus/Sonnet/Haiku subagents). Those two files are uncommitted in the config repo; the worktree guard blocks git there from this session.
- **Stuck rule (Mark, 2026-09-07):** two failed honest attempts, or a decision only he can make, or an unauthorized irreversible step: stop the thread, write a stuck note (doing, two attempts and why each failed, one-sentence question), send it over Discord, move to independent work; if none, write the handoff and pause. Discord only for stuck notes and the done message.
- **Cloud facts (2026-09-07, from official docs via the guide agent):** cloud sessions share the same usage limits, no machine charge; fresh Linux VM (4 vCPU, 16 GB) clones the repo at the branch; Bash, package installs, network behind a configurable allowlist, commits and pushes on the session branch, PRs all work; repo `.claude/` loads, personal `~/.claude` does not; no local `.env`, secrets via environment variables on the cloud environment; long background jobs die on idle; browser automation not documented. A session can hand off to a new one via a cloud routine (run once, now) or `claude --bg`; a user can `claude --resume <id> --model opus`. Recommendation given: cloud, after a dry run that installs tools, runs `pnpm verify` and the store smoke test. Mark has not decided cloud vs local yet.
- **Assets, agreed 2026-09-07:** `~/downloads/free-horror-music-pack` (20 wav loops, eleven end at full level), `~/downloads/crawling-depths` (16 px dark navy dungeon tileset with dressing and creatures, one look, three sections by dressing and tint), `~/downloads/scarymobs` (32 px sprites; usable subset is skeletons, ghosts, black cats, the core-lit flesh golems, the tall grey figure, the statue; the brown werewolves, bears, gorillas and zombies are out). Mapping: skeleton shambler, ghost revenant, cat ghoul, golem carrier, big ghost Banshee, tall figure Undertaker, eldritch thing for the set piece. Licenses: itch.io free packs, personal and commercial use; raw packs stay out of git, only game-ready output (mp3 loops, composed background strips, one atlas) is committed. Pixel art draws nearest-neighbour. Asset sourcing recipe agreed (to land as a project rule at the pre-authorization step): vector placeholder first, then the three packs, then Kenney CC0 by direct zip from the asset page (scripted download verified, license file inside the zip), raw packs in a gitignored staging folder, stop and ask only if all three fail. Butler is itch's publishing tool, not a download path.
- **Branch and worktree (2026-09-07):** branch `hungry-grave-v1` from main, worktree at `/home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1`, session entered via EnterWorktree. First commit 46f32d4930 (the Neon agent skills the marketplace install wrote). The worktree carries its own `.vercel/` link copy, `.env.local` copy and node_modules; the smoke test passes from there. This handoff and the push records were moved onto the branch on 2026-09-07 so cloud sessions can read them; the main checkout keeps only a pointer file. The primary checkout's memory dir is outside the repo and still the memory home. All project agents, rules, hooks, skills and settings.json are tracked, so the branch has them; only `.claude/settings.local.json` is per-checkout. The worktree guard refuses compound Bash that mentions git; use the file tools or plain commands.
- **Blob read cache finding:** `get(url, { access: 'private' })` returned null right after a fresh `put` to a path deleted a minute earlier; `useCache: false` fixed it. Run ids are unique so the upload function never reuses a path, but any read-after-write check must pass `useCache: false`.
- **Store, provisioned and smoke-tested 2026-09-07:** Neon Postgres via `vercel integration add neon --name hungry-grave-runs --no-claim` (no browser step needed) and a private Blob store `hungry-grave-tapes` (store_hnGloGEqUSe3V8Nx, iad1) linked to the project; env pulled into `apps/hungry-grave/.env.local` (gitignored): DATABASE_URL and friends, BLOB_READ_WRITE_TOKEN. Smoke: table write/read/drop OK; a real tape gzipped (737 KB to 68 KB, eleven times) round-tripped through Blob byte-exact with `get(url, { access: 'private' })`. Shape: rows in Neon (runs with header columns; readings keyed by run and computing build), gzipped bytes in Blob by run id, one upload function in the app used by the game at run end and by the harness script.
- **Mark's operating rule (2026-08-28):** batch mechanical already-ruled work; stop only for a new product/architecture decision, an invariant conflict, scope expansion, or invalidating evidence. Commit, push, deploy, and merge each need their own explicit yes.
- **Edit-in-place ADR pattern (ratified 2026-08-31):** same question, new answer → rewrite the ADR in place, supersession note inside, filename slug follows the new title, number stays. New number only for a new question.
- **Supersession convention (confirmed 2026-09-01 across all 47 ADRs):** dated inline bold prose in the record being retired, plus a what-stood / what-it-replaced / what-it-could-not-have-known triple in the retiring one. No YAML frontmatter anywhere.
- **Conditioned tapes:** from apps/hungry-grave, `pnpm vite-node --config vite.headless.config.ts scripts/record-conditioned.ts <out> <seed> <ticks> soulStream=N territory=N wisps=N bell=N`.
- **Measure:** `pnpm vite-node --config vite.headless.config.ts scripts/measure.ts <tape>`; stdout is the whole report. Tapes refuse across sim changes; fresh tapes per iteration.
- **Test command** plain `pnpm vitest run`; repo gate `pnpm verify`. Editor diagnostics produce phantoms (a `'require' is declared but never read` warning in `scripts/roadmap/build-v1.mjs` is one); only `pnpm typecheck` judges.
- **Local play:** `cd apps/hungry-grave && pnpm build && pnpm exec vite preview` → http://localhost:4173/. Mark saves tapes to `/mnt/c/Users/markd/Downloads/*.tape`.
- **Deploy deliberately stale** at `6eb5cd1741` on hungry-grave.vercel.app. Phone deploy: `cd apps/hungry-grave && vercel build --prod && vercel deploy --prebuilt --prod`.
- ADR set 0001-0047 canonical; no ADR carries a combat magnitude; spec-as-comment ruling (/to-spec spec lands as a comment on the existing ticket).
- **The bot is dodge-only**: it never levels anything, so it cannot measure drop-choice or the difficulty question at all. Decision 3 replaces it with a real harness, inside V1.
- Reading big files: `cat` output over roughly 30KB gets saved to a file instead of shown; use the Read tool on the saved path. In zsh, a bare `echo ====` fails (equals expansion) and `--include=*.ts` needs quoting.
- The scratchpad is per session; anything a later session needs goes into `apps/hungry-grave/docs/push/` on the branch during the push, not the scratchpad and not the gitignored `local/`.

## Suggested skills

`/grilling` + `/domain-modeling` stay paired for this work; `/domain-modeling` means challenging terms against `CONTEXT.md` as the grill runs, and recording the grill's decisions as ADRs at the end per the prompt's step 3. `/research` for each craft question before it is asked. `/triage` for the tickets that fall out at the far end (drafts to Mark first; body carries problem and need only, approach in a follow-up comment). `/to-spec` output lands as a ticket comment. `/tdd` and `docs/agents/feature-playbook.md` bind any coding; dispatches go to subagents with the dispatch contract, and a non-coding dispatch prompt must open with "Non-coding dispatch:".
