# Continue: The Hungry Grave

> **Last updated:** 2026-09-16, **session 27 closes here with slice J-fix landed and deployed and three new belch follow-ups ruled, and Mark has ordered them behind the meter.** Branch `hungry-grave-v1`, tip `72eb0eb2e1`, tree clean. Round two's slices R-fix, H, I, H2, J and J-fix have all landed, leaving **K and L**. **Slice J-fix made the belch catch the crowd** (`6caa2fa72b`, note `aed6d4568a`): `BELCH_BURST_RADIUS` is now `FIELD_WIDTH / 2`, `ERUPTION_REACH` reads that row and the diagonal derivation is gone, and the ring centres on the grave and drifts at `SCROLL_SPEED`. The median share of live bodies caught per press went **10.8 to 43.5 percent on seed 902, 16.1 to 35.0 on seed 17 and 12.3 to 48.9 on seed 5150**, and a real tape shoved 40 bodies against slice J's 19. `step.ts` and the bell were untouched, `GOLDEN` and all three versions held, CodeRabbit was clean, and it is deployed. **Mark played it and gave three things.** He ruled the belch's picture moves too fast (**R12**, recorded at `72eb0eb2e1`): not a blast, not shrapnel, not a flash of light, and *not* a spreading gas either, which is the lower wall his first reading of this ruling crossed and which is corrected in place. He reported bodies at the same distance where **some are pushed and some are not**, which the record already predicted as R10's held separate-clocks lever. And he made the larger point that **a tape must be able to show it**, which today it cannot. **He ordered the three follow-ups explicitly: slice K the meter first, then R12's belch motion, then the dying-body fix.** Nothing is running in the tree.
> **Branch:** `hungry-grave-v1`, worktree `/home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1`. Tip is **the commit above `762f98abe7`**, which is this handoff's own docs commit; **`762f98abe7` is the resisted-push ruling** (ADR 0008 and ADR 0007 re-ruled in place) and `d8c862db72` is the boss-tell research record behind it. `10fe54a29a` is **slice J's docs commit** over its code commit **`eda21401a0`**, which is the last code commit; `7a26dbe277` is **slice H2's docs commit** over its code commit **`d6794f9836`**; `ee82d23500` is the prompts commit that wrote H2 and rewrote J. **`58efd4a159` is the supersession of R2 and R3 and the new R10**, and `81702a49a7` is the watched-pushback research record. `72b307d353` is slice I's docs commit over `157946940c`. **Slice H is three commits**: `2e90597cad` the fold, `ed369353e2` the bell rewired, `2056264082` the note. Slice R-fix is `eddb32c4cb` with its note `b25def3a01`. Slice G is `7f18ec83fe` with its docs commit `cd07ffb3f5`; slice F is `982536a5d5` with its docs commit `524bb68447`. Every earlier slice's hashes are in the progress note's section 1 table; read them there rather than here. Step 4's plan is `apps/hungry-grave/docs/design/step-4-mow-ladder-director-dispatch.md`, its step order is that file's section 10, and the running detail is `apps/hungry-grave/docs/push/step-4-progress.md`. **Round two's own three files are `apps/hungry-grave/docs/design/round-two-wall-belch.md` (the record, with rulings R1 to R10 and the gate outcomes in its section 7), `apps/hungry-grave/docs/push/round-two-slice-prompts.md` (sections ADR commit, R-fix, H, I, H2, J, K, L) and `apps/hungry-grave/docs/push/round-two-progress.md`.** Cite their sections rather than restating them; this file is the state of play and the standing rules, nothing more.
> **Working tree:** **clean at `762f98abe7` and nothing is running in it; `pnpm verify` was not rerun on the ruling commit because it is docs only.** `pnpm verify` green at slice J: 146 test files, **2063 passed, 21 expected fail, 2 todo and 2084 test names**, against slice H2's 146 files, 2055 passed, 21 expected fail, 2 todo and 2076 names, and slice I's 2050 passed and 2073 names. **`WITNESS_VERSION` is 8**, moved in slice H's fold commit `2e90597cad` and nowhere else; **`READINGS_VERSION` is 5**, moved in slice I's `157946940c` and nowhere else; **`FORMAT_VERSION` is 4 and holds through round two**; **`GOLDEN`'s checksum is `-145039082`**, re-pinned in `2e90597cad` and held through H2 and J. **Round two's `GOLDEN` budget of two ends with one spent**: slice H took it, **slice J's permit went unused** because the canonical scenario tolls no bell and never belches, and R-fix, I, H2, K and L are permitted none. **Every tape recorded before `2e90597cad` is refused at the decode**, on the witness version. Run `git log --oneline -25` first anyway.
> **Deployed:** **slice J's tip is live on https://hungry-grave.vercel.app** (bundle `index-hTQ81tlj.js`), which is Mark's own phone-play address, deployed 2026-09-15 by session 26 **so he can feel the belch**; slice H2's tip was deployed before it, and he played the toll on localhost and called it very good. The tree stayed clean through both. **Two build warnings, both pre-existing and both already known**: `@pixi/sound` sits in the main chunk whatever the dynamic import says (open item 11 below), and one chunk is over 500 kB. No ticket for either, and the trigger for both is bundle size mattering. **The next deploy is after K, so he sees the meter, or after L.**
> **Session shape:** the orchestrator runs on Fable and makes every hard call itself or in a Fable subagent; everything else is an Opus or cheaper subagent. Usage at session 27's close: **session 6, weekly all 29, weekly Fable 16**; the session window resets 2026-09-16 12:00 UTC and both weekly windows 2026-09-19 18:00 UTC; FIRST ACTION item 2 is the live reading. **Mark waived the caution lines; only the stops apply** (weekly all 85, Fable 95). One code slice per wave, `bash /home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/usage.sh` between waves. The deploy recipe is `apps/hungry-grave/docs/deploy.md` and it runs from the branch worktree between coders, never from a scratch one. **Session 27 ran on Opus rather than Fable and it worked**, so the Fable-never-reads rule bit less than usual; the orchestrator read the tree directly.

## FIRST ACTION

1. **Enter the worktree.** Run EnterWorktree with `path: /home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1` before anything else.
2. **Run the usage script**, `bash /home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/usage.sh`, and read it against the stops above.
3. **Run `git log --oneline -25`, then `git status --short`**, one plain command per Bash call, and confirm the tip and the clean tree this file claims.
4. **Slice K, the belch meter, is next and Mark ordered it first himself** (2026-09-16, when three belch follow-ups appeared and he was asked what should go ahead of the meter; his answer was the meter, then R12's motion, then the dying bodies). Dispatch it to one Opus coder after a usage check: the belch meter shows its fill and moves to the bottom-left corner (#127, the K section of `apps/hungry-grave/docs/push/round-two-slice-prompts.md`, **progress note section 13** after session 27's renumbering). The rulings behind it are R1 to R10 in the record, and the meter's own is R7, the radial fill of the ring, corner to bottom-left. He asked for it *"soon so we can start feeling like we're getting towards real game"*. **The K prompt is already written and already renumbered**; it needs nothing before dispatch.
5. **Then R12's belch motion, and its speed is researched before any coder is dispatched.** R12 is in the record at `72eb0eb2e1` and it is a **band with two walls, both Mark's**: not a blast, not shrapnel, not a flash of light at the top, and not a spreading gas at the bottom, aimed nearer the speed of matter thrown out of a body, his words being *"barf speed"*. **The lower wall exists because session 27's first draft of R12 crossed it** and told him he had ruled that gas spreading was nearer the thing than a blast; he had not, and he said so plainly. The slice has two halves. **The honest half**: `eruptionFrontsAt` grows its radius linearly while the shove itself decays, fast at the start and slow to settle, so the ring outruns the bodies early and waits for them at the end; the front reads the shove's own decay so the ring is where the bodies are on every tick. **The expression half**: thickness, edge and fill, so it reads as gross matter rather than a clean hoop, taken against shipped precedent and never from a description. **The front's duration stays R3's and does not move.** J-fix's own slowdown already took the front from 3.45 field widths a second to 1.00, under the Blank's 1.67, and **whether that is already inside the band, short of it, or past the lower wall is Mark's read after playing the deployed build**, which he has not yet given.
6. **Then the dying-body fix, which is R10's held separate-clocks lever and Mark's own sighting is its trigger.** On 2026-09-16 he reported seeing *"12 to 15 mobs get pushed and easily 5 or 6 that were right next to those other ones that did get pushed did not get pushed"*, all at about the same distance. **Session 27 proved the belch itself is fair**: twelve bodies placed on one ring at one distance were handed identical impulses, `shoved: 12`, identical `firstStep`, `ticksLeft` and `shovesLeft` (`local/jfix-equal-distance.ts`, uncommitted). **What is not fair is what happens next**: over the 90 ticks of flight, six of the twelve died and stopped dead where they died, at flight ticks 18, 31, 31, 33, 66 and 66, because a corpse carries no impulse. R10 already names this, already defers it, and already names the trigger: *"a rung where no shambler is ever seen travelling is a finding for round two's close and the trigger for the separate-clocks ruling at the next witness move, and it is the orchestrator's under one-push mode"*. **It costs a `WITNESS_VERSION` move to 9**, which is the fourth time this step makes saved tapes a dead baseline, and that is taken eyes open. **A second cause is named and not yet ruled out**: the reach is a hard edge with no falloff and nothing drawn to mark it, so before J-fix a body just inside and one just outside looked like the same distance; J-fix makes the drawn ring and the real reach one circle, so his next play is the evidence on whether that was part of it.
7. **And with it, the tape's blind spot, which is Mark's own larger point and is his standing rule for the harness.** His words on 2026-09-16: *"Literally everything that we need to care about and measure should have some representation in our tape. That's the whole point of it so that we can measure things and iterate."* Today the `belched` event carries `cancelled` and `shoved`, two counts of what happened, and **nothing at all about the misses**: no record of which bodies were in the frame at the press and were not pushed, nor why. So the exact thing he watched leaves no trace in any tape and cannot be answered from one. **The belch's record must carry, per press, every body in the frame with pushed-or-not and the reason: out of reach, not entered, not pushable, already dead.** It is a `READINGS_VERSION` move and it rides with item 6 rather than alone, because that slice is already moving a version.
8. **Then slice L**, one Opus coder, a usage check between: the Wall becomes a real wall only a belch opens (#123, the L section, **progress note section 14**). Its input list already names J-fix's reach as what its curtain measurement is taken against.
9. **Then round two's close**: the three gates on round two's diff, CodeRabbit on the tip, and a batch. A deploy after L if K's was the last one.
10. **Then the tuning-record step**, whose draft design record is `apps/hungry-grave/docs/push/drafts/step-5-tuning-record-draft.md`. Its eleven decisions are unruled and the orchestrator rules them before any coder is dispatched. The findings below marked as tuning-step inputs are its first sweep, and **slices H2, J and J-fix added five more**: the flattened push ladder, the Waking's corpse property, the living fringe's depth at the 0.7 ratio, a named third configuration for a tape that can carry a belch, and **the belch's net travel being direction-dependent, 123 up the field and 237 down against a nominal 180**, because the scroll composes with the shove and R11's own closing paragraph rules that it stays that way.
11. **#131 and #132 are outside round two and wait for Mark to schedule them**, and **#130, the typed agent roles, is his too**: he wants it at a pause point between pushes and never mid-push.
12. **No asking**: the work is ruled, and the stops are the stuck rule and the usage stops and nothing else. **Mark waived the caution lines and said to keep going past the context watchdog; the handoff is still written when it fires**, and only the stop and the `/clear` ask are waived.

Push records are the tracked copies in `apps/hungry-grave/docs/push/`. The isolation guard refuses writes to the main checkout and refuses compound Bash that mentions git even in quoted text; use plain separate commands. The stop hook error about `local/publish-instruction.mjs` is harmless (open item 0 below).

**Fable never reads (Mark, 2026-09-09).** Whether Fable is the main thread or a subagent, cheaper agents do the searching, reading and investigating and hand Fable the result. A small Fable job runs in the main thread; a big one runs as a Fable subagent spawned with those results in its prompt. Opus subagents gather, code, gate and write.

**This file is the continue document and it is ephemeral.** It is tracked on the branch only because the worktree cannot see the main checkout's gitignored `continue-hungry-grave.md`, which is now a pointer here. Delete it when the push merges. Sessions 1 to 19 are in git history: `git show b0d41583be:apps/hungry-grave/docs/push/handoff.md`, for citation only.

## STEP 4 BRIEF: the mow, the ladder, the director

**What step 4 is.** Re-author everything authored so the game starts from a better place, then put the director on top. Mark's rulings that bind it, all 2026-09-09: (1) **the mow**: bullet heaven is mowing and mowing, getting more power and seeing more and harder enemies as you get it; density is bought with weak bodies, never tough ones. (2) **The ladder**: reaching the end with every line maxed should be rare and for the best players, but the ceiling stays high and visible; rarity and skill expression, never a lower cap. (3) The three changes (authored schedule, progression curve, director) ship in one pass, not sequenced. (4) A density ADR passes the game design gate before filing. ADR 0047 (director reads pressure, never power; four off-limits moments; rows are the floor; own seeded streams) and ADR 0056 (finite purse per phase, quiet interval after an add, signal reads harm and never a nearby kill, caps re-derived and never evicting) stand and are Mark's. Gates never overrule rulings.

**Where we are.** Steps 0 through 7 of the plan's eighteen are done, in this order: step 0 the baselines (no commit, `local/step4/tests-baseline.txt` and its `.json` sibling, 1834 names, progress note section 6); step 0a slice A0, the build identity (`26a064a064`, cwd fix `136a349aeb`, sections 8 and 10); step 1 round 0, the frame budget (`c23be6156c`, the pool refusal `3594fe154a`, section 9), **both halves now done**, the phone half being Mark's reads below; step 7 slice A, the mob table and fire (`d4dedf6d9a`, section 11), which is the mow measured as one skull to a shambler and enemy shots in the air down from a peak of 70 to 8 and 12; then steps 2 to 6, the records grilled, the three gates' second round folded, and the ADRs filed (`9cba278131`, then `2120369437` for the growth rate's word and `cb230c765c` for ADR 0060 re-ruled, progress note section 12). The weapon growth research record landed in session 21's own docs commit.

Session 22 landed five slices and session 23 landed the last two, so step 4's coding is complete. Each is closed and fully recorded, and each is one line here with its citation:

- **Step 8a, slice B0, the glossary realignment** (`2ec6ee5efc`, progress note section 13): ADR 0061 files the ruling and the six words land, Chunk to phase, Phase to section, Row to wave, Template to formation, Drop to power-up, Strip to HUD, with `READINGS_VERSION` 3 to 4 and `GOLDEN` unmoved. The one fact from it that outlives the slice is `STREAM_SALTS`, in the findings below.
- **Step 8, slice B, the standing waves and the re-authored waves** (`480b207fdf`, follow-up `4607280db1`, progress note section 14): standing waves arrive as `Repeat` fields on `StageWave` and the three section tables are re-authored, landing 2393 bodies on seed 77 with the grave held where they landed 663, with all three refusal counters at zero.
- **Step 9, slice C, the economy waves, the burst cadence and the weapon climb** (`7bc43700a5`, follow-up `01c9a6f32d`, progress note section 15): every rung buys damage per the weapon growth record's section 4, `CORPSES_TO_CEILING` goes 80 to 400 and the reservoir 9 to 300, and `GOLDEN` re-pinned.
- **Step 10, slice D, the caps become derivations** (`5393987f6c`, re-ruling `2fb33ee5de`, progress note section 16): `MOB_CAP` 481, `CORPSE_CAP` 704 and `MOB_FIRE_CAP` 434 are derivations, the card table and the three section purses land in `waves.ts` with nothing spending them, which is slice F's, and a sixth fence says the cap derivation reads tables and never the stage.

- **Step 11, slice E, the witness fold, widened once** (`2b4e849720`, progress note section 17): `WITNESS_VERSION` goes 6 to 7 in exactly one commit declaring all nine folded fields it gained, the run holds eight seeded streams where it held five, and the wisp volley clock lands with its 30-tick floor. The one fact from it that outlives the slice is that **`WISP_CAP` no longer binds**, measured with the floor forced on and off at the same tip, rung 1 going 64 of 64 to 5 of 64, which is the trigger decision 6 below waits on.
- **Step 12, slice F, the director's signal and spend** (`982536a5d5`, progress note section 18): the run holds a director that reads one number, rising on the grave being hit and the floor ladder's two rungs alone, and while that number reads low it buys a card off slice D's table out of the section's purse and goes quiet for four to eight seconds. `GOLDEN` took its fourth re-pin, checksum `-489751710`. Deployed clean at `524bb68447`.
- **Step 13, slice G, the instrument and the lock** (`7f18ec83fe`, progress note section 19): the batch reads the director's own run back off a tape, plus arrivals as a rate, a mobs-alive distribution, ticks to ceiling, belch intervals and hits-to-kill per minute per type. `FORMAT_VERSION` moves once to 4 for the signal lock in the header, and every tape before this commit is refused at the decode. `READINGS_VERSION` 4, `WITNESS_VERSION` 7 and `GOLDEN` all held. **Deployed clean at `97fc911527`.**

**Steps 14 and 15 are closed** and their outcomes are the two sections below, the gates and the batch read. **Step 16, round two, is building**: its docs landed at `ec87a2e9e7` and `40adc7edd5`, and **R-fix, H, I, H2 and J have all landed**, leaving K and L. Each of the five is one line here with its citation, and the detail is the round two progress note's own sections.

- **Slice R-fix, the three tech gate findings** (`eddb32c4cb`, note `b25def3a01`, round two note section 7): findings 4, 5 and 6 closed, CodeRabbit clean, no printed figure moved and nothing visible changed.
- **Slice H, the shove is a body travelling** (`2e90597cad` the fold, `ed369353e2` the bell, note `2056264082`, round two note section 8): a toll starts an impulse and the body carries itself over seven ticks instead of being teleported once, `WITNESS_VERSION` 7 to 8 in the fold commit alone, `GOLDEN` re-pinned to `-145039082`, and the bell is the shove's only caller. **The one thing that outlives the slice is the push at the top rungs**, in the findings below and it is for Mark's read.
- **Slice I, the shove is measurable** (`157946940c`, note `72b307d353`, round two note section 9): `mobShoved` says which push threw it, the repel reading splits into a toll arm and a belch arm, `READINGS_VERSION` 4 to 5, and the report gains the three refusal counters, the tick every directed add landed on, and a `measure.test.ts` that is tree-stable. Nothing a player sees changed.
- **Slice H2, the toll's push is watched and outreaches its damage** (`d6794f9836`, note `7a26dbe277`, round two note section 10): `SHOVE_TICKS` 7 to 30, the push column 55 / 63 / 71 / 80 / 90 by rung (the reach over 45 times 15.5, in whole units), and a second damage reach beside it at 0.7 of the push reach, 112 / 128 / 145 / 162 / 183, so a struck body at the fringe travels and lives. A conditioned tape reads 116 shoves and 1277.8 units where slice I read 2 and 20.1. `GOLDEN` held at `-145039082` and witness 8, readings 5 and format 4 all held. **Deployed, and Mark's localhost read is that the toll feels very good.** The three things that outlive the slice are in the findings below.
- **Slice J, the belch is three waves of pushback and takes health off nothing** (`eda21401a0`, note `10fe54a29a`, round two note section 11): `BELCH_SHOVES` 3, `BELCH_SHOVE_THROW` 60 and `BELCH_SHOVE_SPACING` 30, so 180 units against a 160 reach, with `ERUPTION_TICKS` derived as 90 and `drawEruption` drawing three staggered fronts. On seed 902, 19 bodies shoved, median and max travel 180.00, and 14 of the 19 clear the reach; `damage.belch` and `fatalBlows.belch` are zero throughout. Determinism came free, because a 90-tick push always crosses a 60-tick checkpoint. **`GOLDEN`'s second permit went unused. Deployed.**

**Where the game was before all this** is the dead baseline and it is in the record (`docs/design/mow-ladder-director.md`), not here: 1.13 arrivals a second, mean 4.4 mobs on screen, enemy shots peaking 70, all four lines maxed by the end, and Mark's felt run of boring, then dodging without the power to kill, then super easy. What must not move at any point: the fences, the invariants, replay determinism.

**Mark's rulings of 2026-09-09, verbatim where he gave words, because they change how the rest of step 4 is run.**

1. *"I don't know how to build the game. You should be using research and data to answer this question."* Naming and every craft call is settled by research off the main thread. Never asked of him.
2. *"Please don't lean towards keeping something we have because of uncertainty... you're gonna have to break things."* When research says precedent disagrees with an ADR this session filed, re-rule it to precedent. Never amend it to permit both.
3. *"If we have to adjust skulls and rate of fire and all of that stuff, so be it; the point of this is to get a design that makes sense."* His rulings are the feel: the mow, the ladder, heaven with doses of hell. The numbers under them, one-skull, hit counts, fire rates, are the session's arithmetic and move to precedent freely.
4. **The glossary, his pick taken:** *"I'll go with your pick for slice B words now."* The six terms realigned to the genre's words and slice B0 landed all six; the system layer uses the genre's words and the flavor stays at the player surface, which is the Hades split. ADR 0061 holds it.

**Decisions the session made under the pre-authorizations, each already in the tree.** ADR 0060 re-ruled to stepped growth and one row construct, and a standing row ends at the next row of any kind. The per-minute enemy health step is struck on evidence: the figures were Mad Forest's Inverse mode, which is that stage's hard mode, and ADR 0059 rules the direction anyway; it is kept as a batch question with `timeToKill.ts` in slice G as its trigger (progress note section 12, design record section 12 item 6). Weapon growth per rung stays per line, read from `docs/research/weapon-growth-per-level-precedent.md`: the damage lane adds 25% of rung 1 per rung to a ceiling of x2, so skull 8/10/12/14/16, wisp 10/12/15/17/20, bell near about 40/56/72/88/104, and Territory flat per ADR 0044 because it buys area and repel. That record's own health-step proposal is superseded by the strike above and stays a batch question.

**Decisions the session made under the pre-authorizations, session 22.** Each is recorded in the progress note or the slice prompts file; these are the ones a later session would otherwise re-litigate.

1. **`MOB_CAP`'s derivation is pulled forward from slice D into slice B** (progress note section 14). Slice B's authored Crowd rate, twelve bodies a second against a 13.8 second transit, holds about 165 bodies in flight before a single shaped beat and with nothing killed, and it could not fit the constant 160; the measured runs sat at exactly 160, which is the pool saturating rather than a peak. ADR 0056 rules that content prices caps and never that caps price content, so lowering the rate to fit and hand-picking a bigger constant were both rejected. `MOB_CAP` became `peakLive()`. The transit window is bounded by the scroll alone, because `mobs.ts` imports `caps.ts` and reaching back for the mob table would close a cycle, so it over-prices by about three quarters, which is the direction a safety net rounds. CodeRabbit raised this twice against slice B and both were declined on this ruling.
2. **`SET_PIECE_POUR_SECONDS` moved from a fifth of a second to a tenth.** It is a derived value whose inputs moved, not a magnitude: the pour must not arrive thinner than the section it interrupts, and the doubled shaped counts took the densest ten seconds from 36 bodies to 74. The derivation is rewritten in its own JSDoc. Plan section 7 does not list this constant, which is why it is written down here.
3. **The wisp volley clock and its interval floor landed in slice E's fold commit, not slice C**, and both of slice C's `it.fails` tripwires were flipped green there (`2b4e849720`, progress note section 17). Closed; the reason it moved is in the citation.
4. **Commits on this branch carry no trailer of any kind.** Mark's global rule is a single-line conventional message with no trailers, and it overrides the harness's own `Co-Authored-By` reminder. Every commit on the branch is written that way and a later session should not start adding one.
5. **`peakLive`'s director term is re-ruled from the largest single card to the cards a whole transit window holds at the quiet interval's minimum** (progress note section 16). CodeRabbit raised it against slice D and the coder declined it correctly, because spec test 26 pinned the old term by name; the orchestrator then ruled it, because ADR 0056 makes a cap a proof it never binds and a term one card can exceed is not a proof. Both terms now go through one `directedInside(seconds, bodiesAnAdd)` reading `QUIET_INTERVAL_MINIMUM_SECONDS` once, so the mob cap and the corpse cap cannot disagree about the same director again. `MOB_CAP` 445 to **481**, `CORPSE_CAP` 668 to **704**, and `REVENANT_FIRE_PEAK` 152 to 200 so `MOB_FIRE_CAP` 386 to **434**. Spec test 26 was the session's own planned test rather than a ruling of Mark's, so it is retitled, re-pinned and carries the what-stood / what-it-replaced / what-it-could-not-have-known triple in its own comment.
6. **The wisp-cap silence is still unfixed and it is a finding, not a build item.** `launchWisps` takes a null slot from `takeSlot` and returns, so a truncated volley raises nothing and moves no counter, against the code rule that nothing abnormal is ever silent. Slice E ruled it out of its own commit with reasons (progress note section 17): `state.refusals` is excluded from the fold, so a fourth counter is not a folded field and does not belong in the one declaring commit; a truncated volley leaves no trace on a tape, so slice G's readings cannot see it either; and the honest instrument costs a `Refusals` field, a `clearRefusals` line, an `EXCLUDED` entry, a new fault identity with a wire number and a severity row, and a new invariant check. **The trigger was the next time either line cap is measured binding.** **Slice I did not close it and it is still open** (round two note section 9). What slice I built is a reading over the three counters that already exist, `tuning.refusals.food`, `.carriers` and `.offers`, so the counters now print on every batch and all three read zero across fourteen runs. **There is still no fourth counter for a truncated wisp volley**, no new fault identity and no new invariant check, so a truncated volley is still silent and still leaves no trace on a tape. The trigger is unchanged. No ticket was ever filed and none is needed.
7. **Deploys run from the branch worktree between coders, and never from a scratch worktree.** The scratch-worktree recipe that slices C and D used is retired: the isolation guard refuses git inside a scratch worktree when a subagent runs it, and `git worktree remove` is denied to subagents, so a scratch worktree can be created and then neither used nor cleaned up. Mark removed `deploy-slice-c` himself on 2026-09-14. Deploying from the branch worktree means the deploy happens **between** coders, with the tree clean, which is also what makes the build identity clean. Two facts from the scratch era are kept only because they explain old stamps: a single root `node_modules` symlink stamps a build dirty, because the root `.gitignore` carries `node_modules/` with a trailing slash and that does not match a symlink; and `vercel build` runs its own `pnpm install`, which wrote through those symlinks into the branch worktree.
8. **Step 4's coder contract is lifted into its own file**, `apps/hungry-grave/docs/push/step-4-coder-contract.md` (`257a2fe5d5`). It carries what every coding slice repeats: how to work in the worktree, the commit and review rules, the progress note, the ten verification commands, what must not move in any slice, what is never a slice's job, and the stuck rule. **The slice F and G prompts name it and hold only what is their own; slices B0 through E carry all of it inline because they were dispatched before the file existed**, and their blocks are left exactly as those coders were given them. Mark asked for the lift-out to save context, and it matches how steps 2 and 3 were run.
9. **Slice G ran a few seeds only and the batch is the orchestrator's**, which held: G's own batch was six seeds under each configuration and it proved the instrument rather than tuning anything (progress note section 19).
10. **#118 was read by slice G and it is the orchestrator's to rule** (progress note section 19). G's reading came back **empty on seeds 900 to 905 under both `steady-far` and `loose-far`**: no run spent the tick budget and none reached no ending, so the threshold the fix was gated on was never reached and **#118 stands exactly as it is**. G's own honest statement is that six seeds is an eighth of the sample the defect was found in, so an empty result proves nothing either way. No boss-section exception was built, no boss section gained a purse, and ADR 0047's first off-limits moment is intact. **The 48-seed batch answered it and #118 still stands**: zero ceiling stops in 108 bot runs and the repro seed sealed under both hands, so the ticket stays open on Mark's own sighting. See the batch read below, finding 4.
11. **The four tickets from Mark's reads, #122 to #125, are filed as round-two inputs** and none of them is any slice's work. They are the next section, and his rulings of 2026-09-15 below now bind three of them.

**Decisions the session made under the pre-authorizations, session 23.** Each is in the progress note; these are the ones a later session would otherwise re-litigate.

1. **`GOLDEN`'s fourth re-pin was spent on slice F and the fifth is spare.** Nothing left in step 4 is owed one, so a re-pin in round two is a decision rather than a budgeted move.
2. **The signal lock is a bare `-1` on the wire and not a symbol**, because the header is bytes and a symbol survives no encoding (progress note section 19). What makes it safe is that `advancePressure` clamps every value into nought to one, pinned by a test that walks four thousand real calls rather than by reading `director.ts`.
3. **`READINGS_VERSION` held at 4 through slice G** because every reading went in beside the existing ones and the flat `mobsAlivePerTick` row keeps its exact name and value, so a step 3 batch and a step 4 batch stay subtractable by name. CodeRabbit asked for a bump and it was **declined**: four of its findings were applied and this one was ruled not the slice's.
4. **The plan's claim that every new reading module joins `harnessStatesNoTarget.test.ts` is false against the tree** and was confirmed rather than acted on (progress note section 19). That list is three dev modules and not one of the sixteen existing reading modules is in it, so adding one would have been inconsistent with all sixteen. What actually holds a new reading is the two declaration guards. The list was left alone.
5. **Slice R-fix landed the three tech-gate findings** (`eddb32c4cb`, eleven files; round two progress note's R-fix section). Finding 4: `Section.liveFormationCeiling` is now `1 | null`, so a 2 is a compile error, pinned by a `@ts-expect-error` test. Finding 5: `holdableSignal` and `SIGNAL_FULL` moved into `signalLock.ts`, which still imports nothing and is asserted to; `records.ts` no longer reaches the director, and a narrow directional fence guards the recurrence. Finding 6: the signal and quiet-interval refusals now sit above the affordable filter in `directorSpend`. CodeRabbit found nothing; witness 7, format 4 and readings 4 all held, and the 163 batch readings are flat either side.
6. **Two claims in the R-fix prompt were false against the tree and the coder corrected them** rather than following them (same progress note section). The ceiling column lives in `stage.ts` and not `waves.ts`; and `boundary.test.ts` fences `src/tape` folder-wide onto `src/game`, **which is why the `records.ts` import slipped past it in the first place** and why finding 5's guard had to be a narrow directional fence rather than a tightening of the existing one. A later prompt writer should expect to be corrected the same way.
7. **The session process restarted once mid-R-fix** and the coder was resumed from its transcript and finished cleanly. Nothing was lost; it is written down only so a reader of the transcripts is not surprised by the seam.

## Mark's reads, 2026-09-14, on the slice C build, on his phone

None of this is a ruling. These are his reads and his sketches, and the batch reads them before any options come back with data.

**It works and it plays the same as desktop.** That is the whole of the platform question for now.

**The crowd is mostly mowing, not dodging**, in his words, which closes the mow read: the thing step 4 set out to make is on the screen.

**The offer blends into a thick crowd but is still readable.** His sketch is a sphere or a bubble around the offer with the nearby bodies pushed out of it, force-directed rather than drawn. **#122**, and it is path step 5's rather than round two's.

**The Wall does not read as a wall**, it is *"just more stuff put in a line"*, and he is not sure it should stay at all. **#123**, round two.

**He rarely belches, and it does not feel like a big boom that means something.** It may not survive. His sketch is a bigger, more ambitious pushback, two or three waves of it, instead of damage. **#124**, round two.

**Territory does nothing under the mow**, because things die too fast to be slowed by it. **#125**, round two.

**His history note, which binds #123 and #124 together.** The Wall was created as the answer to why the belch is useful: something to save the belch for, a learning moment, back when the belch also damaged mobs. The damage was later removed and the Wall kept. So the two are one question and they get decided together.

**The phone half of round 0 is done, on the slice E build, and it closes.** The largest field the build offers, the 200 mobs and 500 corpses row, reads sim tick mean 0.23 ms and p95 1.00 ms, render CPU mean 2.72 ms and p95 4.00 ms. The phone stands it with room under a 16.7 ms frame. The 400 / 1000 row is refused by the caps and needs the frame-budget build to be measured at all. The derived cap field, 481 mobs and 704 corpses, sits between the two and scales to somewhere near 8 ms render p95, which is under budget.

## Mark's rulings, 2026-09-15, all binding

These are rulings and not reads. They set what round two is, what comes after it, and they bind #123, #124, #126 and #127.

1. **Step 4 closes as "density round one" after the batch.** Round two fixes only what is plainly broken, plus the Wall and belch work below. It is not an open tuning pass.
2. **The step after round two is tooling**, and it is one thing: a tuning record holding every tunable number, overridable from the batch command line and later from the URL, stamped in the tape header, plus a sweep runner over variants by hands by seeds. The point is that the orchestrator iterates from numbers and asks Mark for a feel check only when the numbers say a candidate is ready. In his words, he built the V1 rewrite, so he is not the feel check on every iteration. **The draft design record is `apps/hungry-grave/docs/push/drafts/step-5-tuning-record-draft.md` and its eleven decisions are unruled**; the orchestrator rules them before any coder is dispatched.
3. **The belch is a big pushback with no damage, two or three shoves.** Its charge rate is fine as it stands. This is #124 ruled, and it answers his 2026-09-14 read that the belch does not feel like a big boom.
4. **Every shove must feel like a force going out**, and today's does not: the bell's repel jumps a body in one or two frames and reads as a glitch. **The push feel is fixed first and the belch then reuses it.** This is **#126**, new.
5. **The belch meter must show its fill and move to the bottom-left corner.** A handedness setting is future work and not this. This is **#127**, new.
6. **The Wall becomes a real wall the grave cannot pass, and only a belch opens it.** If research shows a pure pushback belch cannot open it, **the Wall is cut**. This is #123 ruled, and it holds his history note that #123 and #124 are one question.
7. **These build in round two, push feel first, one Opus coder per slice, and they deploy before the tooling step.** He does not want them on a back burner.
8. **A push must run long enough and far enough to be watched, which reopened R2 and R3 and then settled them.** His read on the shipped seven-tick shove: *"7 ticks is basically still nearly the same as a frame to the human eye. The intent is that you see them: those mobs get pushed back because they're moving towards you. Each wave should be about a half second's worth of animating push backwards, maybe longer."* And the half the first record never asked: *"It also should answer the question of how far back they should be pushed, because if you nudge them just the tiniest bit, it's not going to be clear what the value of that belch or that toll is."* **He then picked option 2 of `docs/research/watched-pushback-duration.md` section 5**: a wave of 30 ticks with a fast start and a slow settle, the toll throwing 90 units, the belch three waves of 60 for 180, the eruption three fronts over 90 ticks, and the bell's damage reach narrower than its push reach. **The magnitudes are data in rows and never ADR text.**

9. **The Waking's source takes a resisted push from a toll, and bosses stay unpushed and get a tell instead** (2026-09-15 evening, his option 1 of three). A toll moves the Waking's source by a fraction of the push a mob takes, and **the fraction is a tuning row rather than ADR text**, research favouring about one third. **ADR 0007 stands for bosses**: a boss takes the bell's damage and never its push, and what it gains instead is a **per-toll tell on the body plus the shed armor pieces of decision log entry 4**. ADR 0008 is re-ruled in place with the triple and ADR 0007 amended in place with the triple, both at `762f98abe7`, with the round-two record's shove promise corrected inline and `CONTEXT.md`'s Burst, Waking and Boss entries updated to match. **This is not round two's work**: it is **#131** (the boss tell) and **#132** (the Waking push), each carrying a context comment with the research figures and the seam facts.

The rulings on #123 and #124 are comments on those tickets; #126, #127, #131 and #132 are new tickets. **His issue format**, which the triage skill is not the source of: three one-line paragraphs, Problem, Need, Done when, and a follow-up comment posted right after creation opening with a Placement line.

## Mark's reads, 2026-09-15 evening, on the H2 and pre-J builds

None of this is a ruling; his rulings of the same day are the section above.

**The toll's push *"feels very good"***, played on localhost at slice H2's tip. That is the felt half of #126 answered: a watched pushback reads as a force going out, which is what his ruling 4 asked for.

**The belch on the build before slice J *"does not seem to do anything."*** That read predates J, which landed the three-wave shove, and K, which makes the meter visible; **his read of the deployed J build is still owed** and K's deploy is the natural moment for it.

**He wants the belch meter and the boss damage indicators *"soon so we can start feeling like we're getting towards real game."*** The meter is slice K, next. The boss indicators are #131, which is outside round two and his to schedule.

**He asked whether damage meters for bosses had been agreed.** They had not: **no health bar was ever ruled**, and what the record actually carries is phase chunks with a flash at the breaks plus the shed armor pieces of decision log entry 4. That is what #131 builds, and the answer was given to him rather than a bar being designed.

## Findings filed for Mark's read, not applied

Each is a finding against something he ruled, so it is written down and built past. **Ten of them are waiting on his read as this is written and none of them blocks anything**: the ladder read, #129, the third-fewer-hits reading, the `WITNESS_VERSION` plan change, and the six that slices H2 and J produced, all marked below.

- **Superseded, closed by slice H2 (`d6794f9836`): the bell's push collapse at the top rungs.** `proximity` gave damage and push one shared falloff, so a shambler never survived a toll at rung three or higher; R10 split the two reaches, damage now falls off over 0.7 of the push reach, and a conditioned tape reads 116 shoves and 1277.8 units where slice I read 2 and 20.1. The reasoning is `docs/research/watched-pushback-duration.md` section 1 and round two note sections 8 and 10.
- **The push ladder has flattened and the Waking's corpse property has become dominant** (round two note section 10). Rung one's toll now throws 55 field units where it threw 6, so the ladder from rung one to rung five went from 6.7 times to 1.6 times, and the Waking's corpse property moved from 1.51 times to 6.8 times, 413 corpses against 61. **It follows from R2 exactly as he picked it**, so nothing was built past; it is an input to the tuning step and it is the clearest number for him to react to when he next plays a high bell.
- **A toll's damage now steps to nothing at a line inside the cone that is never drawn** (round two note section 10). R10 as built makes the damage reach 0.7 of the push reach, and a body just outside it is shoved and takes nothing at all, with **its travel as the only tell**. That is R10 working, and whether the step wants a visible edge is a feel question for his read rather than a defect.
- **At bell rungs four and five the storm kills bodies before a belch shove can carry them** (round two note section 11): 35 shoves at rung 1 falling to 0 at rung 5, so the hand loses exactly the carriers the belch paid for. Seed 303's offer stands unreached and its baseline is re-measured into a named third set. **`record-conditioned.ts` can never carry a belch at all**, so measuring one wants a new named configuration, which is the tuning step's.
- **The living fringe is wide but shallow**: bodies that survive a toll are carried only 9 to 13 units, and 61.7, 61.3 and 62.0 percent of struck shamblers travel alive at rungs 1, 3 and 5. **A damage reach of 0.47 in place of 0.7 would roughly double the depth.** The ratio is a tuning row, so this is a sweep rather than a build.
- **The blind keyboard hand cannot fill the reservoir**, peaking at 36 percent over 9146 ticks (round two note section 11), which is why **no hand tape carries a belch** and why slice J's rendered check landed on the replay route instead. It bears on #124's *"he rarely belches"* from the harness side: the instrument has the same problem the player reported, and a hand that can belch is what a belch measurement needs.
- **`src/dev/readings/repel.ts` carries a stale sentence**, "push only exists at bell levels 4 and 5", in its JSDoc at line 19 and again in `__tests__/repel.test.ts` at line 54. It is **pre-existing rather than H2's**, it is wrong at every rung now, and it belongs to the next slice that touches the readings.
- **Slice I confirmed slice H's figures to the digit** (round two note section 9): a conditioned tape at every line's rung 5, seed 77, 6000 ticks, verified, **33 tolls, 2 shoves and 20.09 field units**. The two builds agree exactly and agreeing is the pass, so the finding above is a property of the sim and not a measurement artefact of either slice.
- **CodeRabbit asked slice I to fold `impulse.source` and bump `WITNESS_VERSION` with it, and it was declined** (round two note section 9). The decline stands on slice I's own fourth ruling and the design record's section 5: the witness moves exactly once in round two and that move was slice H's. The reasoning beside the ruling is that the field is provenance, written once at the start of a shove and read by no rule, which is `mobFire[].kind`'s precedent, and a divergence in it shows through the seven impulse fields the walk already folds. Slice G's declined finding is the worked precedent. **It is written down because it is a reviewer's major finding built past, and that is his to see at merge.**
- **The ladder does not open across 96 bot runs** (the batch read above, finding 2). Median one rung a run, nothing bought at all in 20 and 19 of 48, and level 5 reached on no line in any run. The bot does not hunt carriers so it is a floor rather than the truth, but the offers themselves also fell from about 1.96 a run to 0.83 against the step 3 batch. **This one is for him: next time he plays, did he reach rung 3 on any line.**
- **A body touching the grave does the dominant harm and there is no tell at all** (the batch read above, and the design gate's own DECIDE-NOW item). Contact overtook shots as the way a run is hurt when the mow landed, and nothing on screen says it is happening. **#129, and it is for his read.**
- **Round two folded per-body impulse fields onto `Mob` and moved `WITNESS_VERSION` 7 to 8, and it has landed** in slice H's fold commit `2e90597cad` (round two note section 8). The plan did not carry a witness move in round two; the orchestrator ruled it under Mark's push ruling because the shove has to be per body and the fold is where a per-body field lives. The cost is stated rather than discovered: every tape recorded before that commit is refused at the decode, the fourth time this step has made saved tapes a dead baseline. **This is a plan change and it is still for his read.**
- **A body costs about a third fewer hits by minute six** (slice G, progress note section 19). A ghoul goes from 3 hits to about 2 and a revenant from 8 to about 5.9. This is the evidence the design record's section 12 item 6 asked for when the per-minute enemy health step was struck, and that record marks the call as Mark's. **This one is for him.**
- **The section purses are priced above what the quiet interval lets a section spend** (slice F, progress note section 18; the batch read above, finding 3). The Crowd's 140 seconds hold about 23 quiet intervals at a mean card cost of 5.8, so at most a third of its 388 is reachable. **The batch closed it from the other side: about 0.88 of both the Procession and the Crowd goes unspent across 96 runs, and the quiet interval is what binds.** It is now a **tuning-step input**, not round two's, and it is the tech gate's findings 3 and 7 as well.
- **The director takes three quarters of the Procession's empty ticks** (slice F, progress note section 18). Folded into the same tuning-step sweep as the purse finding above.
- **Closed by round two: ADR 0042's Wall lost its loaded half under the mow.** At 8 health the storm thinned the curtain enough for a lane to open and the grave crossed untouched, and a curtain of silent bodies put no shots in the air for a belch to be worth spending on. Progress note section 4 item 7, plan verification step 22. Mark's #123 read arrived at the same place from the player's side, and **his ruling 6 plus ADR 0042's re-ruling in `ec87a2e9e7` answer it: the Wall becomes impassable, it is built from a new durable silent row, and only a belch opens it.** Slice L builds it.
- **Territory's bottom rung inverts against his #79 dwell ruling.** Two pulses is a shambler now and a level-one crossing lands five, so the mow body dies in the slowest ground there is. Progress note section 4 item 8, plan verification step 23. Nothing in the plan owns Territory's rungs, and his #125 is the felt half of it.
- **The shipped caps cannot stand three of the six frame-budget fields.** The constants `MOB_CAP` 160 and `CORPSE_CAP` 233 against fields of 100/250, 200/500 and 400/1000. Slice B's derivation answered the constant half; slice D answered the rest, and the build now stands five of the six and refuses only 400/1000, on the corpse cap. Progress note sections 4 item 3 and 16.
- **The derived `MOB_CAP`, now 481, is above the 400 mobs round 0 measured on desktop, and the phone now says that is fine.** The pool is built at its cap, so a build stands 481 mob slots where round 0's largest measured field stood 400. The phone reading above closes this: at 200 / 500 the phone sits at 1.00 ms sim p95 and 4.00 ms render p95, and the cap field scales to near 8 ms render p95 against a 16.7 ms frame. This is now informed rather than open; what is still unmeasured is 400 / 1000, which the caps refuse and which needs the frame-budget build. Progress note sections 14 and 16.
- **Before the wisp floor, the wisps were doing nearly all of the mowing at the bottom of the ladder.** With the grave held at the ceiling on seed 77, the floor takes a rung 1 run's kills from 1772 to 187, a tenth, and a rung 5 run's from 1936 to 1795, seven percent. The line that was meant to be a dive's reward was the whole storm for a low build, which is the clearest evidence the floor was ruled for the right reason, and it is also a number to look at from the other side, because a rung-1 build now kills a tenth of what it killed. **The batch read it: all 96 runs used the birthright rig, and the answer is the ladder finding above**, a median of one rung a run and level 5 on no line. Mark's next phone play is still the felt check and slice G is what is live for it. Progress note section 17.
- **The offer is not separable at a glance in a Crowd-density field.** Slice B's named shot, the replay at tick 18464 with 70 live mobs and 96 corpses and an offer standing two bodies wide: the offer bodies are small pale shapes in the same size band and a similar value as the corpses and the mobs, and the one thing that picks them out is that they sit against the grave's own outline. Mark's phone read above is the same finding from his side and it is now #122. Progress note section 14.
- **Closed by slice F: the Vigil's `liveBodyCeiling` was stale at 4 against a doubled table and is now 28**, twice the fourteen shaped bodies the section stands, which is the relation the old figure was set on. **It gates nothing today**, because `VIGIL_PURSE` is zero and a section with nothing to spend never reaches a ceiling, and that fact is in the column's own comment so nobody later reads it as a gate that was tested. Progress note section 18.
- **`STREAM_SALTS` is a new small commitment in code:** a stream's salt is decoupled from its name so a rename never re-seeds a run. It was forced by a real divergence in slice B0 rather than chosen, and it is the kind of thing that reads as incidental until someone tidies it away. Progress note section 13.
- **The revenant's Territory cost climbs while its skull cost falls**, a consequence of ADR 0044 holding Territory flat while the damage lane grows. From the weapon growth record.
- **A maxed bell deletes trash anywhere in its cones.** The far edge at rung 5 carries 13 against a mow body's 8, so one toll clears a curtain of twenty-two shamblers across the field's width, which is the Wall's own width. It arrives at the ADR 0042 finding from the weapon side, it is ADR 0059's one-touch mow body meeting the ruled damage lane, and it stands as an `it.fails` tripwire carrying its cause in the test's own comment. Progress note section 15.
- **A full build's doubled throughput puts the nominal run at about six and three quarter minutes against ADR 0049's eight.** Both boss fights take about half their old nominal. CodeRabbit asked twice for boss health or `BOSS_STORM_SHARE` to be retuned back inside the band and both were declined, because the damage lane is ruled and neither number belongs to a slice in this step. It stands as an `it.fails` tripwire. Progress note section 15.
- **A hit costs about 46 seconds of mowing at rung 1, against about 5 on the dead baseline.** `CORPSES_TO_CEILING` at 400 makes a hit cost 29.63 fresh trash corpses where 80 made it 5.93, and what a low build actually kills is 0.64 bodies a second; at rung 5 the same hit costs 12 seconds. A hit is a far bigger setback for a low build than it was and a smaller one for a high build. Progress note section 15.
- **The trash's `mobFire` stream is now drawn by nothing at all in a whole run.** The Banshee's nudge was its only drawer and slice E gave the Banshee its own stream, and every mob row's `firstShotJitter` is 0. The stream is folded, checked and named, and it costs nothing, but a reading that treated a moving `mobFire` cursor as evidence of anything would be reading a stream nothing spends. Progress note section 17, and it is a reading for whoever runs the batch.
- **`@pixi/sound` is imported dynamically by `src/app/sound.ts` and statically by `src/engine/engine.ts` and `src/main.ts`**, so Vite keeps it in the main chunk and the dynamic import buys nothing. A ticket if bundle size ever matters; not now.

**Owed to the next slices, and each is that slice's job rather than a ticket.** Slice B's guard test landed, that a body carries exactly its `MOB_TYPES` row, which is the deliberate absence of any stat step (`mobs.test.ts:830`). Of the two stale "ramp" comments, `screenLifecycle.test.ts`'s is gone and `digest.ts`'s survives, now at `digest.ts:316`, so it passes to the next slice that touches that file. Slice C rewrote the stale damage comments in `skullStream.ts`, `territory.ts`, `bell.ts` and `wisps.ts`.

**Dispatch shape.** The gate agents are `gate-game-design`, `gate-product-vision` and `gate-tech-architecture`; they are exempt from the dispatch hook, and their prompts must tell them not to edit and not to leave markers. A non-coding dispatch prompt opens with the literal line `Non-coding dispatch: <reason>`. A coding prompt names `docs/agents/feature-playbook.md` and carries the six contract items; a coder handed a slice missing any of them stops and reports. **Step 4's per-slice prompts live in `apps/hungry-grave/docs/push/step-4-slice-prompts.md`, one section per slice, and the standing rules live in `apps/hungry-grave/docs/push/step-4-coder-contract.md` from slice F on**; a slice F or G dispatch names both, and the earlier slices carry the rules inline because they predate the file. **That file is now spent: every slice A through G has been dispatched and landed. Round two's prompts are their own file, `apps/hungry-grave/docs/push/round-two-slice-prompts.md`, one section each for the ADR commit, R-fix, H, I, J, K and L, all written ahead at `40adc7edd5`**, and a round two dispatch names that file and the step 4 coder contract, which still holds unchanged. A prompt is drafted by an Opus agent into the scratchpad while the previous slice builds, then appended and committed as docs; **the orchestrator reads only the decision parts of a draft** and rules on any stop it names before the dispatch goes out. **A coder is never dispatched in the last hour of a session**, because it dies with the context switch.

**Where Fable is worth spending (Mark, 2026-09-09).** The balance itself, the interaction of the three changes, reading each batch and picking the next move, and resolving the growth-versus-pressure split if the design gate rejects it. Not: writing records, slice prompts, coding, gates, batches, tickets, commits, deploys, the follow-along list, ADR drafting once the decision is made.

**Facts learned in session 21 that would otherwise be re-derived.** `.claude/rules/push-working-agreement.md`, which the main checkout's pointer file names, does not exist; this handoff and the coder contract are the rules. `coderabbit review --agent --uncommitted` from the worktree root is the working form, and it does not see untracked files, so `git add` by path first. Mark's Downloads holds 2 format 3 tapes, 18 format 2 and 11 format 1. `git worktree list` shows prunable scratch worktrees from earlier sessions, left alone deliberately. The step 0 baseline was captured by a scratch worktree at `e9957eebfb` with `node_modules` symlinked in rather than installed.

**Facts learned the hard way in round two, each of which cost a slice real time.**

- **Superseded, and the blind spot is closed: a tape checkpoint could not catch a bell shove in flight while `SHOVE_TICKS` was 7.** Seven ticks against a 60-tick checkpoint grid and a 180-tick `BELL_PERIOD` pinned the mid-shove ticks into a 27-to-51 residue band that never reached 0. **Slice H2's 30-tick shove closed it** (`d6794f9836`, round two note section 10): all 60 residues are present and zero is reached 56 times. **Slice J closed it for the belch too** (`eda21401a0`), where a 90-tick push always crosses a 60-tick checkpoint, so determinism with shoves in flight came free and the incommensurate-numbers tuning decision is not needed. What still stands from it is the stronger proof itself, folding the witness on every flying tick across two plays of the same seed rather than only on the grid.
- **A headless browser cannot photograph a seven-tick shove** (round two note section 8). It draws this build at 3 to 5 frames a second and the app answers by running twenty to thirty sim ticks per drawn frame, so the whole shove finishes inside one frame. **Three attempts are written down there rather than dressed up**: screenshotting a live toll, driving the replay route's `?at=` across a shove, and stepping a Playwright fake clock frame by frame. The across-ticks property is real in the sim and pinned by tests, and **it is human-checkable only, at 60 frames a second on a real device**.
- **`measure.test.ts` is now tree-stable** (round two note section 9), by lifting out `buildMismatch.running` alone into a fixed marker while still asserting each side names a build. `buildIdentity` itself was not touched, because a tree with untracked work is uncommitted work and that is #82's ruling. It was proved under a process writing an untracked file into the worktree every fifty milliseconds, which is what a docs agent on this branch actually does; churning `local/` proves nothing because git ignores it.
- **Research that answers "how long is X in shipped games" instead of "how long must X be for the player to see it" produces a confident wrong ruling.** `docs/research/push-feel-precedent.md` found Vampire Survivors' 120-millisecond knockback and it became `SHOVE_TICKS` 7, and **that knockback is a felt nudge inside a permanent scrum and never the event the player looks at**, so a duration taken from it could not produce a watched one. The same record measured duration and never distance, so 40 field units went forward untested against the field it has to read on. The replacement is `docs/research/watched-pushback-duration.md` and its section 0 is the post-mortem. **Frame a craft-value question in perception terms**, what the player must be able to see and against which ruler already on screen, and convert every candidate into the field's own units before it is a number in the tree.
- **A subagent's malformed curl left a stray file named `-o` in the worktree root**, from a redirect flag the shell took as a filename. It was deleted. **If `git status` ever shows an untracked `-o`, that is what it is**; remove it with `rm ./-o`, because a bare `rm -o` is parsed as a flag.
- **The context watchdog fired in session 24 at about 150k tokens**, after two code slices, one deploy and three brief agents. **A session that reads no files itself still fills, on agent reports and on pasted prompts**, so the Fable-never-reads rule buys less headroom than it looks like it should. **Two slices per session is the observed pace** and a third should not be started on the assumption there is room.

**Slice J-fix missed its second pass line and nothing was tuned to reach it.** The share of a still grave's ticks with nothing at all inside the belch's reach fell from 58.0 to **37.4 percent**, against a line of "below half of 58", 29.0. Decomposed: 7.5 percent of those ticks have no body alive at all, and the rest, bodies alive with none in reach, fell 50.6 to 29.9. The circle's on-field area rose 2.4 times but the grave sits at 0.8 of the field's height while bodies enter at the top, so it buys less than its area. **A tuning-step input, and a candidate reason the belch can still feel like it does nothing on a given press.**

**Two harness baselines moved under slice J-fix and were re-measured rather than re-pinned blind.** Seed 101 now ends holding all four lines above the birthright and is the one seed still unsealed at 27409 ticks.

**The hand tape can verify but cannot carry a belch, and the reason is now measured.** The bot's first belch lands at ticks 10097 to 15534 across all twelve batch runs while a blind driver seals at 8589, so a hand tape ends before a belch is ever spent. This sits beside the already-filed fact that `record-conditioned.ts` scripts `belch: false` on every tick, so no conditioned tape can carry one either. **Together they are why Mark's own point about the tape bites: the belch is the least observable thing in the game and it is the thing being changed.** A tuning-step input, and item 7's instrument work is the other half of it.

## The three implementation gates, plan step 14, read and closed

They ran read-only on step 4's code, changed no tracked file and left no markers. **Their findings files are gone with the job's scratch folder**, so the surviving record is `apps/hungry-grave/docs/design/round-two-wall-belch.md` section 7 and the tickets below. **None of the three said `gate disagrees` and none of them blocks.**

- **Vision.** Nothing blocking. Its one correction, `game-concept.md`, is fixed in `ec87a2e9e7`.
- **Tech.** Nothing blocking. Findings 4, 5 and 6 were **slice R-fix** and are **landed at `eddb32c4cb`**. Finding 1, the renderer walking every pool slot at the derived caps, is **#128**. Finding 2, `measure.test.ts` not tree-stable, is **landed in slice I** (`157946940c`, round two note section 9). Findings 3 and 7, the purse accounting, are **tuning-step inputs** and match the batch read below.
- **Design.** Nothing blocking. Its three DECIDE-NOW items are placed, in order: the ladder under the bot is a **tuning-step input**, contact harm with no tell is **#129**, and the offer not being separable is **#122**. Its remaining ask, that Mark play the tip and hunt carriers himself, is noted for his read and is not a blocker.

## The 48-seed batch, plan step 15, read and closed

**What ran.** 48 seeds from 1000 under each of `steady-far` and `loose-far`, birthright rig, recorded at `cd07ffb3f5`, **all 96 verified**. Reports are `apps/hungry-grave/local/batches/steady-far-birthright-1789459315771` and `loose-far-birthright-1789459319726`. The later pair, `steady-far-birthright-1789460928374` and `loose-far-birthright-1789460932104`, is not a second batch: it is the #118 repro seed 20260913 replayed once under each hand, one tape each.

**The orchestrator's read, which closes density round one** per Mark's ruling 1 above. The four headline findings:

1. **The mow is real.** Arrivals a second go from the dead baseline's 1.13 to a median 4.55 and 4.36; mobs alive sit at a median-of-run 39 and 35 against the baseline's mean of 4.4; a shambler dies to one hit. This is the thing step 4 set out to make and the batch confirms Mark's felt read of 2026-09-14.
2. **The ladder does not open for the bot.** Rungs per run median 1; 20 and 19 of 48 runs buy nothing at all; **0 of 96 runs reach level 5 on any line**; 12 and 20 offers are never taken; 27 and 48 power-ups fall off the bottom edge. The bot does not hunt carriers, so part of this is the policy rather than the game, but **offers per run also fell from about 1.96 to 0.83 against the step 3 batch**, which is a sim fact and is the tuning step's first sweep.
3. **The director works but barely spends.** About 9 adds a run; the purse ends with about 0.88 of both the Procession and the Crowd unspent; the gate reads open about 0.79 of the run; the signal is 0 for over half the ticks and peaks at 1.0 in every run. **The quiet interval is what binds**, and ADR 0056's "the storm is seen to win" is unreachable as priced. A tuning sweep, not a round-two build.
4. **#118 did not reproduce and the ticket stays open.** Zero ceiling stops in 108 bot runs, and the repro seed 20260913 replayed under both hands ends sealed at 21294 and 19469 ticks, before the Undertaker. **It stays open on Mark's own sighting**, which the bot cannot contradict; the reasoning is on the ticket's comments.

**The rest of the read, each already placed:**

- **Contact hits are now the dominant harm and they carry no tell.** **#129**, and it is also the design gate's second DECIDE-NOW item.
- **No trash arrives inside any Banshee span across 96 runs.** That is ADR 0047's off-limits moment working by design, and it is the mechanism behind #118 rather than a defect.
- **Belch charge splashes past full, mean 2.4 and 3.0 a run.** Bot policy, and it is exactly what #127's visible fill addresses.
- **The refusal counters live on `RunState` and appear in no reading.** **Closed by slice I** (`157946940c`, round two note section 9): `tuning.refusals.food`, `.carriers` and `.offers` print on every batch out of a new `src/dev/readings/refusals.ts`, and all three read zero on all fourteen runs measured. **A non-zero counter is a fault and a finding and never a cap to raise**, which is written into the module's own JSDoc. The wisp-cap half of decision 6 under session 22 is **not** closed by this; see that decision.
- **`compare-batches` withholds every ordering across hands by design**, so a cross-hand comparison has to be made by hand from the two reports.
- **Shots in the air at peak sit exactly at `MOB_FIRE_CAP` 70 in 75 of 96 runs.** That is the boss, and the cap binding there is the cap doing its job.

## Round two, started

**Its docs landed in two commits.** `ec87a2e9e7` re-rules **ADR 0008 in place to the shove** (the filename slug follows) and **ADR 0042** for the Wall, and corrects `game-concept.md` on the vision gate's finding. `40adc7edd5` adds the record `docs/design/round-two-wall-belch.md`, the research `docs/research/push-feel-precedent.md`, the prompts `docs/push/round-two-slice-prompts.md` and the progress note `docs/push/round-two-progress.md`.

**The orchestrator's rulings R1 to R9 are in the record's own sections.** The ones a later session would otherwise re-litigate:

- **Per-body impulse fields fold onto `Mob` with `WITNESS_VERSION` 7 to 8, inside slice H as its own declaring commit.** **Landed in `2e90597cad`**, seven fields declared in one paragraph. This is a change to the plan, made under Mark's push ruling, and it is **filed for his read** below.
- **R2, R3 and the new R10 as superseded and ruled at `58efd4a159`**: read them in the record's section 2 rather than here. **Superseded as a to-do: H2 and J have both built them** (`d6794f9836`, `eda21401a0`), so the tree now carries 30-tick shoves, the split reach and the three-wave belch rather than the seven-tick figures.
- **The Wall opens by geometry**, blocked by cost and never by an impassable body.
- **A new durable silent mob row** carries the Wall.
- **Radial fill of the ring**, corner to bottom-left, for the meter.
- **One shove module, two callers.**
- **`GOLDEN` may re-pin once in slice H and once in slice J**; `READINGS_VERSION` 4 to 5 in slice I; `FORMAT_VERSION` 4 holds. **Slice H spent its re-pin** (`-489751710` to `-145039082`, the checksum the only field that moved) and **slice I moved readings to 5**. **The second re-pin was slice J's and it went unused**, because the canonical scenario tolls no bell and never belches, so round two ends with one of its two spent and K and L permitted none.

**The slice order is now R-fix, H, I, H2, J, J-fix, K, L**, then the three gates, CodeRabbit on the tip, a batch and a deploy. **R-fix, H, I, H2, J and J-fix are all landed and deployed, and slice K is next** (FIRST ACTION item 4). **Two belch follow-ups sit behind K and ahead of L by Mark's own ordering of 2026-09-16**, R12's motion and the dying-body fix, and they are FIRST ACTION items 5 to 7. After round two comes the tuning-record step, draft at `docs/push/drafts/step-5-tuning-record-draft.md`, eleven decisions still unruled.

**A left-over `/tmp/hg-tickets` folder** from a ticket agent is harmless and can be ignored or deleted.

## Open items with no other home

0. **Stop hook error in this worktree, harmless, not to be chased.** `scripts/roadmap/on-v1-change.mjs:23` imports `local/publish-instruction.mjs`, which is gitignored and lives only in the main checkout, so every stop in the worktree prints a non-blocking module-not-found error. Roadmap edits are struck for the push, so the hook has nothing to do here. The hook is configured in the main checkout's untracked `.claude/settings.local.json` as `node "$CLAUDE_PROJECT_DIR"/scripts/roadmap/on-v1-change.mjs`; it started failing in session 9 when the harness re-homed the project folder from the main checkout to the worktree, so it will fail in any session that launches inside the worktree. Fixed 2026-09-08 by copying that one file into the worktree's gitignored `local/` folder; if the error comes back, the copy is missing again.
1. **Mark's follow-along doc, https://md.niftymonkey.dev/api/raw/CTtB5BJJ, is current through slice G.** Rows F and G both read "done and live" and a sentence on round two was added, revision **`rv_9nmkwbrL`**. **It next owes round two's own rows** as those slices land, and **R-fix, H, I, H2 and J have now landed with both the toll's watched push and the three-wave belch deployed**; whether a session has since added those rows was not checked at this write, so check the doc before writing to it. Updated through the md.niftymonkey.dev targeted-edits API with `MD_API_KEY` read from the environment, which is how every later update should go. It is written for a regular human: what it is and what it means for the game, no hashes, no counts, no process jargon.
2. **Closed.** Both things owed with Mark as the actor are done: the phone half of round 0 and the felt check of whether the mow feels like a mow, both in his reads of 2026-09-14 above.
3. **Open tickets against step 4, none blocking.** #118 the Undertaker deadlock (a real defect Mark has seen; boss phases author no adds, so the food economy starves there and the mow makes it worse). **The 48-seed batch could not reproduce it and it stays open on his sighting**, decision 10 above and the batch read's finding 4, with the reasoning on the ticket's comments. #117 is Mark's alone (whether the sloppy corner must reach the bosses; evidence on the ticket). #116, #121, and the deferred gate findings on #39 and #109.
3a. **Two new tickets from the implementation gates**, both filed in Mark's format: **#128**, the renderer walks every pool slot at the derived caps (tech gate finding 1); **#129**, a body touching the grave has no tell (tech and design gates, and the batch's own read). Neither is any round two slice's work as ruled. **His issue format is now known and recorded**: three one-line paragraphs, Problem, Need, Done when, then a follow-up comment opening with a Placement line.
3b. **Three tickets filed in session 26, none of them round two's and all three Mark's to schedule.** **#131**, a boss takes a per-toll tell on its body plus the shed armor pieces, and **#132**, the Waking's source takes a resisted push, are his ruling 9 above; each carries a context comment with the research figures and the seam facts, the sharpest being that **`stormTargets.ts` has no path to move a set piece today** (a `slot.mob !== null` guard and `Impulse` living on `Mob` alone), with three comments and one test pinning the old no-push rule on ADR 0007. **#130**, typed agent roles for the `.claude/agents/` folder (coder, gatherer, researcher, doc-writer, drafts in its comment), is not v1 at all: **he wants it at a pause point between pushes and never mid-push**.
4. **The tickets from Mark's phone reads are round two's inputs, and six of them now have rulings on them.** #123 the Wall and #124 the belch are ruled by his 2026-09-15 rulings above and are decided together per his history note; **#126 the push feel and #127 the belch meter are new tickets filed from those same rulings**. #125 Territory is round two's and still unruled. **#122, the offer bubble, is the path's step 5 rather than round two**, and the design gate's third DECIDE-NOW item is the same finding. The build order is R-fix, then H, I, H2, J, K, L, and **R-fix, H, I, H2 and J are all landed and deployed, leaving K and L**. **#126 is done**: its done line was Mark's read of the retuned build and he gave it on localhost at H2's tip, *"feels very good"*. **#124's done line is still owed**, because his belch read was taken on the build before J; **#127's is slice K**, next.
5. **The human-tape replay divergence** on `b1c3a584d1` is undiagnosed and the evidence is preserved at `docs/push/divergence-b1c3a584d1.md`, because after slice A the tip cannot reproduce that run at all. Mark: it cannot be allowed to fail, and a test holds the cause once known. Format 2 tapes are refused at the decode, which is informative rather than a defect.
6. **#82 stays open on GitHub deliberately.** The dirty marker and the cwd fix both shipped (`26a064a064`, `136a349aeb`), but the fix is on this branch only, so the ticket closes at merge and not before.
7. **#37 stays open deliberately** (boss stories 12-14, fog story 22) until the path's step 2 absorbs them.
8. **540x760 records disagree** (`layout.test.ts:67`, `field.ts:4`, `CONTEXT.md:85`), docs-only fix pending.
9. **Stamp-order trigger** on PR #84's stamp thread: a second output in `v1.yaml` `outputs:` moves the stamp into the hook. Dormant while roadmap edits are struck.
10. **Stream flank narrowness**, Mark: "the edge thing could be an issue". Watch item, arithmetic in `soulStream.ts` JSDoc.
11. **`@pixi/sound` is in the main chunk whatever the dynamic import says**, per the finding above. No ticket; the trigger is bundle size mattering.
12. **`local/grill/` at the main checkout is stale** (moved onto the branch 2026-09-07); Mark can delete it.
13. **Config repo**: the rewritten `stay-within-limits` skill files are uncommitted at `/home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/`; the worktree guard blocks git there from this session.
14. **Cloud push playbook** at https://md.niftymonkey.dev/v/NCQUpIOq (memory `cloud-push-playbook`); the three dry runs and the local ruling are its inputs when Mark next wants it refined.
15. **The Cone glossary entry moved in slice H2 as R10 required and it reads correctly in the tree**, and the resisted-push ruling updated the Burst, Waking and Boss entries at `762f98abe7`. **The shove and the impulse still have no `CONTEXT.md` entry of their own**, though: slices H, I, H2 and J have each flagged it and none added one, each saying the file gained nothing and lost nothing. **It is a glossary decision for the orchestrator**, folded into slice K or L's dispatch or filed as a ticket, and **never asked of Mark cold**.
16. **Candidate ADR: a reading may not inherit a meaning from the shape its value happens to have.** It is already the stated reason behind three separate declines, slice G's readings bump (`step-4-progress.md` section 19), slice H's start-firing shape for `mobShoved` (round two note section 8) and slice I's inferring the shove's source from `spacing` (round two note section 9), and today it lives only in `comparisonDeclared.test.ts` and in three progress notes. **The orchestrator files it under one-push mode when a slice next touches the readings**, and Mark reviews it on the branch before merge.
17. **The wisp-cap silence is still open and slice I did not close it**, which is session 22's decision 6 above and the reason is there. `launchWisps` takes a null slot and returns, so a truncated volley raises nothing and moves no counter, against the rule that nothing abnormal is ever silent. Slice I built a reading over the three counters that already exist and added no fourth one, no fault identity and no invariant check. **The trigger is unchanged: the next time either line cap is measured binding.** No ticket, and none is needed.

## Standing rules and facts, still true

- **Craft calls are settled by research, and precedent outranks our own record (Mark, 2026-09-09).** He does not know how to build the game and will not be asked to: naming and every craft number comes from research off the main thread, a record that disagrees with precedent is re-ruled to precedent rather than amended to permit both, and his rulings are the feel while the numbers under them are the session's arithmetic and move freely.
- **Gates never overrule Mark (Mark, 2026-09-09).** A gate can find, never rule. A finding that would reverse, defer, narrow or reconsider something he decided (an ADR, a decision-log entry, a ticket ruling, the path order, the done line) is outside the gate's authority: file it as a ticket, list it under "Needed from Mark" as "gate disagrees with ruling X", and build on his ruling. Gate prompts are scoped to consistency with his rulings, never their worth; the harness (ADR 0053) is a thing he absolutely wants whatever a vision gate thinks of it. Questions to him only when the session cannot make the call and it is hard to reverse.
- **One-push build mode (Mark, 2026-09-07):** one autonomous push on one branch in its own worktree; CodeRabbit CLI before every code commit; gates as relevant; ADRs are my call, reviewed on the branch before merge. Memory `one-push-build-mode.md` carries the stuck rule and the lean rule in full.
- **Stuck rule:** two failed honest attempts, or a decision only Mark can make, or an unauthorized irreversible step: stop the thread, write a stuck note (doing, two attempts and why each failed, one-sentence question), send it over Discord (`DISCORD_WEBHOOK_URL`, a curl POST with `{"content": ...}`), move to independent work; if none, write the handoff and pause. Discord only for stuck notes and the done message.
- **Usage limits:** `stay-within-limits` thresholds: session and weekly-all caution 75 stop 85, Fable caution 85 stop 95; main thread lean by default. Mark waived the caution lines for this push, so only the stops apply. At a Fable stop, write the handoff, commit it, chain a new session on Opus.
- **Cloud facts** (official docs, 2026-09-07) are recorded in this file's copy on the archive branch `hungry-grave-v1-cloud`; the cloud is not used for this run.
- **Vercel:** Mark is locked out of the dashboard (2FA prompt, support ticket open) but the CLI login works and `vercel tokens add` minted a token. Project `hungry-grave`, org `team_rDwpau77qippLMDxVoyeq5Ev`; the worktree's `.vercel/` link and `.env.local` are gitignored copies. hungry-grave.vercel.app is Mark's own phone-play address and carries the latest landed slice; deploy over it freely.
- **Store (provisioned and smoke-tested 2026-09-07):** Neon Postgres `hungry-grave-runs` and private Blob store `hungry-grave-tapes` (store_hnGloGEqUSe3V8Nx, iad1), keys in `.env.local`. Shape: rows in Neon (runs with header columns; readings keyed by run and computing build), gzipped bytes in Blob by run id (a real tape went 737 KB to 68 KB), one upload function used by the game at run end and by the harness. Read-after-write must pass `useCache: false` to `get(url, { access: 'private' })`. The clients (`@neondatabase/serverless`, `@vercel/blob`) are added by dry-run step 3.
- **Assets, agreed:** mapping skeleton shambler, ghost revenant, cat ghoul, golem carrier, big ghost Banshee, tall figure Undertaker, eldritch thing for the set piece; the brown werewolves, bears, gorillas and zombies are out. Pixel art draws nearest-neighbour. Every asset in the build is a placeholder and will be replaced before the real game, so licensing is never a question to raise (Mark, 2026-09-08, repeated); for the record only, the music is void1gaming's free horror music pack at https://void1gaming.itch.io/free-horror-music-pack and the pixel packs are itch.io free packs. Asset location in the app is deferred by Mark to implementation time.
- **Mark's operating rule (2026-08-28):** batch mechanical already-ruled work; stop only for a new product/architecture decision, an invariant conflict, scope expansion, or invalidating evidence. Commit, push, deploy, and merge each need their own explicit yes outside the pre-authorized list.
- **Edit-in-place ADR pattern:** same question, new answer: rewrite the ADR in place, supersession note inside, filename slug follows the new title, number stays. New number only for a new question. Supersession convention: dated inline bold prose plus a what-stood / what-it-replaced / what-it-could-not-have-known triple. No YAML frontmatter anywhere.
- **Commands:** conditioned tapes `pnpm vite-node --config vite.headless.config.ts scripts/record-conditioned.ts <out> <seed> <ticks> skullStream=N territory=N wisps=N bell=N`; the frame budget `pnpm vite-node --config vite.frame-budget.config.ts scripts/frame-budget.ts`; measure `pnpm vite-node --config vite.headless.config.ts scripts/measure.ts <tape>`; tests `pnpm vitest run`; repo gate `pnpm verify`; only `pnpm typecheck` judges diagnostics. Local play: `cd apps/hungry-grave && pnpm build && pnpm exec vite preview`. Mark saves tapes to `/mnt/c/Users/markd/Downloads/*.tape`.
- ADR set 0001-0061 canonical; no ADR carries a combat magnitude; a /to-spec spec lands as a comment on the existing ticket.
- Reading big files: `cat` over roughly 30KB gets saved to a file; use the Read tool on the saved path. In zsh, a bare `echo ====` fails and `--include=*.ts` needs quoting. The scratchpad is per session and shared between subagents, so a generic filename gets clobbered; anything a later session needs goes into `apps/hungry-grave/docs/push/` on the branch.

## How we got here, one line

Sessions 1 to 6 (2026-08-31 to 2026-09-07) ran the V1 grill to 26 decisions with eight research records; session 7 (2026-09-07) reviewed the pre-authorization list with Mark, staged the assets, created the cloud environment and ran three dry runs that ended in the local ruling; session 8 (2026-09-08) confirmed the done line, filed the ADR step, and rebuilt the branch clean off `main`; session 9 (2026-09-08) swept the tickets, planned and dispatched step 1, gated its plan, and planned step 2, ending on the context watchdog with the coder still running; session 10 (2026-09-08) landed step 1's last two slices, ran the two implementation gates and the tip review, applied and filed their findings, and stalled only on the push; session 11 (2026-09-08) landed step 2 slices 5 to 12a and started deploying each slice; session 12 (2026-09-08) landed 12b, 13a and 13b, fixed the deploy recipe, filed #101 from Mark's first phone play, and took his boss-art ruling; session 13 (2026-09-08) landed and deployed ground adjustment 1 on his three phone notes and surfaced the Waking's drift as his call; session 14 (2026-09-08) built and deployed adjustment 2 on his ruling and stopped at the context limit before its design gate; session 15 (2026-09-08) ran that gate, built and deployed adjustment 3 on its finding, filed #103, folded the empty slice 15 into 16 and wrote their prompt, and stopped on the context watchdog before dispatching it; session 16 (2026-09-08) closed step 2; session 17 (2026-09-09) built, gated and closed adjustment 4 (#104), planned step 3 (design record, dispatch plan, three gates, a fixer), landed slices 1 to 4b one Opus coder each with a deploy after every one, rewrote Mark's follow-along list to be his only, and paused at Mark's request at the end of half A; sessions 18 to 20 (2026-09-09) closed step 3 and deployed it, wrote and gated step 4's design record and dispatch plan, and filed ADRs 0059 and 0060; session 21 (2026-09-09) ran step 4's baselines, landed slices A0 and A and round 0's frame budget, folded the second gate round, re-ruled ADR 0060 to precedent, struck the enemy health step on evidence, took Mark's four rulings above, and stopped at slice B0; session 22 (2026-09-14) resumed after the weekly reset, landed and deployed slices B0, B, C, D and E with one Opus coder each and their prompts drafted ahead by Opus agents (D's deploy folded into E's), ruled the `MOB_CAP` pull-forward, the `peakLive` window term, the wisp clock's deferral into slice E and the lift-out of the coder contract, took Mark's four phone reads and his phone frame budget, filed #122 to #125, and closed with slice F undispatched for a context reset; session 23 (2026-09-15) landed slices F and G with one Opus coder each, deployed both, took Mark's seven rulings on round two and the tooling step after it, filed #126 and #127, ran and read back the three implementation gates and the 48-seed batch, closed density round one on that read with #118 still standing on his sighting, filed #128 and #129, re-ruled ADRs 0008 and 0042 in place, wrote round two's design record, its push-feel research, its seven slice prompts and its progress note, and landed round two's first slice, R-fix, closing the three tech-gate findings with CodeRabbit clean and nothing visible changed, which is where session 23 ends with slice H next; session 24 (2026-09-15) landed slices H and I with one Opus coder each, taking the shove from a one-tick teleport to a body travelling over seven ticks and then making it measurable by source, moved `WITNESS_VERSION` to 8, `READINGS_VERSION` to 5 and `GOLDEN` to `-145039082`, deployed slice I's tip early so Mark can feel the bell's shove, declined a CodeRabbit major on slice I under its own ruling, and filed the push-at-the-top-rungs finding for his read, on which he reopened rulings R2 and R3 that same evening, saying seven ticks is still nearly a frame to the eye and that the question of how far a body is pushed had gone unanswered, so the session researched a watched pushback properly, found the shared falloff to be what actually killed the push, costed three options, and took his pick of option 2 the same evening, which is where session 24 ends with the supersession unwritten and slice J waiting on a rewritten prompt.

Session 25 (2026-09-15 evening) wrote the supersession of R2 and R3 and the new R10 into the record on Mark's option 2 pick, ran both gates on it and applied every finding, held the `GOLDEN` budget at two on the digest's own evidence, and committed it at `58efd4a159`; it stopped on the context watchdog before writing slice H2's prompt.

Session 26 (2026-09-15) wrote slice H2's prompt and rewrote slice J's on the supersession, correcting the Blank ratio to the shipped prefab from a decompile of the source and running both gates on the diff, then landed H2 and J with one Opus coder each and deployed both: the toll's push now runs thirty ticks and reaches further than its damage, and the belch takes health off nothing and shoves in three waves, with CodeRabbit clean on both and `GOLDEN` never moving; Mark played the toll on localhost and called it very good, asked for the meter and boss damage indicators soon, and said the pre-J belch did not seem to do anything; the session then researched the boss hit tell and the resisted push, took his ruling of option 1, re-ruled ADR 0008 and amended ADR 0007 in place at `762f98abe7`, filed #131 and #132 for the work outside round two and #130 for typed agent roles at a pause point, and filed six findings from the two slices for his read, which is where session 26 ends with slice K next. Session 27 (2026-09-16) wrote slice J-fix's prompt, ran both gates on it and folded every finding, and landed and deployed the slice with one Opus coder: the belch's reach became half the field's width, the eruption's front stopped at that same circle with the diagonal derivation deleted, and the ring was re-centred on the grave and made to drift with the field, lifting the median share of live bodies caught per press from 10.8 to 43.5 percent on seed 902 and shoving 40 bodies on a real tape against slice J's 19, with `GOLDEN` and all three versions held and CodeRabbit clean; the session also closed the scroll finding the other way after the tech gate found that any general fix would silently retune the bell, recorded that and R3's withdrawn clear-the-reach property as two closing paragraphs on R11, and took three things from Mark's play: R12 bounding the belch's picture between a flash of light and a spreading gas, his sighting of bodies at one distance where some are pushed and some are not, and his rule that a tape must be able to show anything worth measuring. **The session also got a ruling wrong and was corrected.** It told him he had ruled that gas spreading is nearer a belch than a blast is; he had said the opposite wall, *"closer to barf speed"*, and said plainly *"don't tell me that I said something that I didn't"*. R12 is corrected in place with the two-walled band and the misreading named. **He then ordered the three follow-ups himself: the meter, then the motion, then the dying bodies**, which is where session 27 ends.

## Suggested skills

`/domain-modeling` for the ADR step (challenge terms against `CONTEXT.md`, record decisions as ADRs). `/triage` for the tickets (drafts to Mark first), but **the triage skill is not the source of his issue format**: three one-line paragraphs, Problem, Need, Done when, and a follow-up comment opening with a Placement line, per his ruling of 2026-09-15. `/to-spec` output lands as a ticket comment. `/tdd` and `docs/agents/feature-playbook.md` bind any coding; dispatches go to subagents with the dispatch contract, and a non-coding dispatch prompt opens with "Non-coding dispatch:". `/stay-within-limits` between waves.
