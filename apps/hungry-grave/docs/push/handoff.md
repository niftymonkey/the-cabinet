# Continue: The Hungry Grave

> **Last updated:** 2026-09-17 (session 29, overnight autonomous run; Mark woke and asked for the scoop at slice 7, no new instruction), tip `6be0c2ed46`. **Step 5 "Show what you have" is closed, deployed and recorded; step 6, the tuning-record step (#142), is the work.** Step 5's eleven slices plus a close fold are the table in `step-5-progress.md` section 1, its close is that note's section 15 (three gates, all PASS WITH FINDINGS; five tickets #137 to #141; two notes in the record's section 7; ADR 0002 clause fixed; five stale bleed sentences fixed; the ladder rig relabelled), and its deploy is `dpl_D1Fpx2RdzBJjdZpTBGxCBvgjhZzf`. Step 6 goes slice by slice per `step-6-slice-prompts.md` (eight slices in run order, drafted at `8714c61251`), every block gated by the tech gate and folded (three gate rounds, `fd9fadf562`, `151ed285b0`, `b77e08c6ec`): **slices 1 to 7 have landed (`step-6-progress.md` sections 6 to 12); slice 8, the sweep runner and the report's tuning identity, is next, then the close.** **This file is trimmed on every rewrite, never only appended** (Mark, 2026-09-16); prune what is recorded elsewhere on the branch and cite where it lives.
> **Branch:** `hungry-grave-v1`, worktree `.claude/worktrees/hungry-grave-v1` (the absolute path is in FIRST ACTION), **tip `6be0c2ed46`** (slice 7's docs commit) on `6f5f98adda` (`src/dev/tuningCandidates.ts` with two rows, `default` and `spendable`, the latter moving `stage.processionPurse` 116 to 42 as a derived figure; `batch.ts` takes `tuning=<name>` and its folder gains the candidate segment; `?tuning=<name>` on the URL repairs an unknown name to the default with one warning; the resolver refuses `score.bossHealthPerKill` at zero), above slice 6 `556869e5aa` on `47c4543d25` (the header's four positional fields replaced by one self-describing block of 17 name-and-f64 rows, `src/tape/startingCondition.ts` the one resolve, `PlaybackOutcome.conditionNotImplemented`, `rigOf` by size, levels and score; a `rig=ladder` tape now verifies and bands `ladder`; **every tape recorded before this commit is refused by version, as ADR 0043's amendment records**), above slice 5 `b402f167fa` on `82e9ab71f6` (seven readers take the record off the run; the five score constants, the three purses and the quiet minimum retired, `QUIET_MIN_TICKS`/`QUIET_MAX_TICKS` as names too; `Section.purse` is `PurseRow | null`; a fence that every row has a reader), above slice 4 `2ac4f9c6b4` on `f5569b2330` (`capsFor(tuning)` inside `createRun`, `RunState.caps`, `StartingConditions.tuning`, `attach(layers, caps)` grows the pools, the storm scratch module-level and grow-only, a new fence that the core reads the record off the run; caps 481 / 434 / 704 unchanged), above slice 3 `a9d2ecad8e`/`25aea8ea1b` (`src/game/tuningRecord.ts`: ten rows, `DEFAULT_TUNING`, `resolveTuning`, `tuningRows`, imports nothing, fenced), above `b996bdc359` (ADRs 0056 and 0043 amended in place), slice 2 `5fa3a9744c`/`83618e4fc6` (`createRun(seed, conditions?: Partial<StartingConditions>)`, `RunState.conditions`, `Rig` as a name plus one record; every step 6 hash is in `step-6-progress.md` section 1), slice 1 `e80c08be1c`, the three fold commits `fd9fadf562`, `151ed285b0`, `b77e08c6ec`, ADRs 0063 and 0064 at `8924d5b397`, the prompts at `8714c61251`, above `b8fe220b2b` (step 5's close docs pass), above the close fold `1d748d14e6`/`7ffcea5db5` (the end screen presents the final score, `hudRow` drops its unused parameter), above slice 10 `602ab1160f`/`6665fad6d4`. Every step 5 hash is in `step-5-progress.md` section 1. **Step 5's files are `docs/design/show-what-you-have.md`, `docs/research/portrait-hud-and-catchable-loss.md`, `docs/research/score-loss-on-a-hit-precedent.md`, `docs/research/score-inputs-precedent.md`, `docs/push/step-5-slice-prompts.md` and `docs/push/step-5-progress.md`. Step 6's are `docs/push/drafts/step-5-tuning-record-draft.md` (the plan: eleven decisions in section 2, every one ruled in section 2a, three prompt-time rulings at its end), `docs/push/step-6-slice-prompts.md`, and `docs/push/step-6-progress.md` once slice 1 creates it.** All under `apps/hungry-grave/`. Cite their sections rather than restating them.
> **Working tree: clean at `6be0c2ed46`, and no agent is running.** `pnpm verify` green from the repo root at `b8fe220b2b` (the script is the root `package.json`'s, run from the worktree root). **`WITNESS_VERSION` 11, `READINGS_VERSION` 9, `FORMAT_VERSION` 5, `GOLDEN` `-2049717150` with `score: 200`**, all read off the tree; step 6's one move, the format 4 to 5, landed in slice 6 and the step's ledger is closed. **Slices 7 and 8 and the close move nothing and re-pin nothing.** **A docs commit that touches a checked file is verified too**: slice 8's docs commit left a doubled blank line in `CONTEXT.md` that turned verify red until slice 9 removed it.
> **Deployed:** **step 5 whole, through the close fold at `1d748d14e6`, is live on https://hungry-grave.vercel.app**, deployment `dpl_D1Fpx2RdzBJjdZpTBGxCBvgjhZzf` READY on production, 2026-09-17, Mark's own phone-play address; deploy over it freely, recipe `docs/deploy.md`, run from `apps/hungry-grave/`. The first deploy attempt was killed by the harness for low memory while three agents ran alongside the dependency install; the rerun alone in the foreground succeeded, so **deploy with no coder or gate running**. Two build warnings, pre-existing and ticketed as #50 and #51. **Step 6 changes nothing a player meets until slice 7 (`?tuning=` on the URL), so no deploy before then; the step's close deploys.**
> **Session shape:** the orchestrator makes every hard call itself or in a Fable subagent, everything else is an Opus or cheaper subagent, one code slice per wave. **The caution lines are waived; only the stops apply** (weekly all 85, Fable 95; last read 65 and 50 at about 11:00 UTC); re-read usage with the usage script before the next dispatch.

## FIRST ACTION

The order below is the whole of it, in this sequence.

1. **Enter the worktree** before anything else: EnterWorktree with `path: /home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1`. Then **run the usage script**, `bash /home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/usage.sh`, against the stops above, then **`git log --oneline -25` and `git status --short`**, one plain command per Bash call, and confirm the tip and the clean tree this file claims.
2. **Slice 8 (E), the sweep runner and a report that says which tuning it read: dispatch one Opus coder on its block now** (the block from `## Slice 8` to the end of the file; its note section is 13 of `step-6-progress.md`). Slices 1 to 7 landed. **Filed by slice 7 and inherited** (note section 12): `GameScreen` still dresses at the default record's caps, untestable until a candidate moves the quiet minimum; no build-time gate exists for any instrumentation URL control and none was built, correcting the draft; `record-conditioned.ts` takes no candidate; the Procession's purse never binds under any hand measured, which is the first real sweep's finding. **Tickets for after the branch, not a slice's job**: `boundary.test.ts`'s import walk reads the words "import" and "from" in prose as imports (four test titles reworded across slices 4 and 6). **Owed to the close docs pass**: `src/dev/digest.ts`'s re-pin paragraph still names `TRASH_KILL_SCORE` in prose. **The gate rulings every step 6 coder inherits** are in the blocks: the resolved record is required field by field and the partial is the caller's; the tuning module imports nothing, fenced; readers take the record as an argument; `rigOf` bands by the rig's own fields and the candidate rides beside it; the storm scratch stays a module-level grow-only array; `rigs.ts` reads the ladder score off the default record; the record is ten rows, the tenth `stage.quietIntervalMaximumSeconds` at today's 8 seconds so the resolver asserts minimum at or below maximum and rejects a failing record as a document.
3. **Every ADR the step needs is filed**: 0063 and 0064 new, 0056 and 0043 amended in place (the purse now travels in the header, so 0056's own 2026-09-09 trigger is spent). Mark reviews all four before merge.
4. **The dispatch wording every coder got**: worktree, the header's overrides, the block in ranges, the contract, the tip, the note section, verify on both commits' trees, and a report under 250 words. **The next deploy is the step's close** (slice 7 put `?tuning=` on the URL; the orchestrator chose to deploy once at the close).
5. **Then step 6's close**: three gates on the step's diff, no CodeRabbit run over the range (Mark, 2026-09-17), a batch under two candidates that differ in one row with the comparison printing that row first, a deploy, and the follow-along document's step 6 rows to done and live.
6. **Then, and only then, the cleanup pass** (Mark, 2026-09-17, "don't try to stuff it in early"), **scoped by Mark on 2026-09-17 to the process documents, not the code**: "If it is code it probably earned its keep. It's all these other ones that I'm concerned about because we don't need a document that talks about how to go build slice number 5 of step 2 or whatever when we've already done that." The suspects are everything under `docs/push/` (this handoff, pre-authorizations, decisions, every slice prompts file, every progress note, coder contracts, planning prompts, drafts, the divergence note, open-tickets, path-draft) and anything else written only so a session had its context. Each is kept, moved, or deleted with the reason in one plain line for his read; a note whose findings are still open moves its open items to a ticket or the design record before it goes. Code is not swept. Deletions are one reviewed commit.
7. **Then the ADR review, before the rundown, as a data-centric condensation and never a per-ADR triage from the orchestrator's head** (Mark, 2026-09-17: "64 ADRs?! That is just bonkers ... ADRs should represent really, really large and important and extremely tough-to-reverse hard-won knowledge-based decisions"; and on method: "read all of the ADRs in some sort of subagent and then propose which ones go together. Maybe a lot of these can be condensed ... speak to the same game component or the same concept ... in a data-centric way based on the data that we have, which is 64 ADRs. If we need to do a workflow that knows how to do that and then does the adjudication and the argumentative subagent, etc., then we need to do that", which is his opt-in to the Workflow tool for this job). **The shape**: (1) extractor agents read every ADR whole, four agents over sixteen each, and write one structured card per ADR into the scratchpad (number, title, the decision in one line, the game component or concept it governs, what it depends on and what depends on it, whether it was amended or superseded and when, what reversing it would cost, whether it was hard-won or made in the moment); (2) one clustering agent reads all 64 cards and groups them by component and concept, proposing for each group how it condenses (merge into one ADR with the surviving decision stated, demote the group to a design record or glossary entry, or stand alone) with the reason drawn from the cards; (3) one challenger agent argues against every proposed merge and demotion, naming what would be lost or which decisions conflict; (4) the orchestrator adjudicates each group in writing, with the challenger's objection answered or upheld; (5) the result is one document for Mark, the groups in order, each with its proposal and ruling, and the full card table appended. Nothing is deleted, merged or demoted before he rules. Memory `adr-bar-is-high` holds the bar and the method.
8. **Then stop.** The store (step 8 on the follow-along list) and later steps are not on this branch. He gets a plain-words rundown in run order with the ADR table, rules on the ADRs, and makes the merge call. Merge waits for his yes.
9. **The follow-along document** is https://md.niftymonkey.dev/api/raw/CTtB5BJJ, edited through the targeted-edits API with `MD_API_KEY` from the environment, a glance list for a regular human: every table in run order, one short line per row, no codes, no hashes, no process jargon. Step 5's rows are all "done and live" with 11b the close fold. **Step 6's three rows there are the draft's old three-slice shape and must be rewritten to the eight slices before slice 1 dispatches** (a row goes in when its prompt is written, Mark 2026-09-16), one line each in his words: the words for the game (a number the game runs on becomes data, a named set of numbers can be played, a recording carries the numbers it was played under, two sets compared). A row's status moves at dispatch ("being built") and at deploy ("done and live"); read live before writing.
10. **#131, #132 and #130 are outside this branch and Mark's to schedule**: the boss's per-toll tell plus the shed armor pieces, the Waking's source taking a resisted push, and typed agent roles, which he wants at a pause point between pushes and never mid-push.
11. **No asking**: the work is ruled, and the stops are the stuck rule and the usage stops, nothing else. **The handoff is still written when the context watchdog fires**; only the stop and the `/clear` ask are waived (Mark, 2026-09-17, before sleeping: "finish out everything we agreed on").

Push records are the tracked copies in `docs/push/`. The isolation guard refuses writes to the main checkout and refuses compound Bash mentioning git even in quoted text; use plain separate commands. The dispatch hook refuses a prompt that looks like coding unless it opens with `Non-coding dispatch: <reason>` or carries the six contract items.

**This file is the continue document and it is ephemeral**, tracked on the branch only because the worktree cannot see the main checkout's gitignored `continue-hungry-grave.md`, now a pointer here. Delete it when the push merges.

## Step 5, closed (#99, and #72 for M2)

**What the step was and every ruling under it**: `docs/design/show-what-you-have.md`; every slice's hashes and its note section are `step-5-progress.md` section 1; the close is section 15. Its batches: slice 10's at `6665fad6d4` is the close batch, twelve birthright seeds and twelve maxed, reproducing sections 12 and 14 per seed. Kills are 85 to 96 percent of gross at today's weights, the cap's share of gross is 13.7 to 3.1 percent, and both are the tuning step's rows, not questions for Mark.

## Step 6, the tuning-record step (#142)

**The plan** is `docs/push/drafts/step-5-tuning-record-draft.md`, rulings in section 2a; **the prompts** are `docs/push/step-6-slice-prompts.md`, eight slices in run order with the old letters in parentheses: 1 the vocabulary and the note; 2 (A1) a run's starting conditions are one record, `createRun(seed, conditions?)`; 3 (A) the tuning record exists and nothing reads it; 4 (B1) the record reaches the run and the three caps derive per run; 5 (B2) every admitted reader takes it off the run, plus a fence that fails on a row nothing reads; 6 (C) the header carries the whole condition, `FORMAT_VERSION` 4 to 5; 7 (D) a candidate named on the command line and on the URL; 8 (E) the sweep runner and the report's tuning identity. **The record carries the nine rows the first sweep list names**, the rest are listed as admissible and unrowed. **Tuning itself, moving numbers against readings, is not this step and not this branch.** The drafter's four resolved conflicts are in the prompts file's header: membership as eligibility, "sweep" unbanned, `stormTargets`' module scratch staying module-level and grow-only, which the step 6 tech gate reversed, and ADR 0022's build-time gate not existing in the tree.

## Round two and step 4, both closed

Every round two slice from the ADR commit through J3 is the table in `round-two-progress.md` section 1; the close is that note's section 17 and the rulings are `round-two-wall-belch.md` section 2. Nothing in it is outstanding.

**What step 4 was** and all eighteen plan steps: `docs/design/mow-ladder-director.md` and `step-4-mow-ladder-director-dispatch.md` section 10. Steps 0 to 15 are closed, A0 through G landed and deployed with every hash in `step-4-progress.md` sections 6 to 19, and step 16 was round two.

**What must not move at any point**: the fences, the invariants, replay determinism.

## Mark's reads

His reads through 2026-09-15 are recorded where they were acted on: the 2026-09-14 ones became tickets #122 to #125, and the 2026-09-15 evening ones on H2 and J are `round-two-wall-belch.md` section 2 under R10 and R12. His 2026-09-16 sighting, bodies inside the eruption not pushed, is answered and shipped as J3 (round two note section 18).

**The mechanical checks are proven by tests and never by his play (Mark, 2026-09-17).** `src/game/__tests__/grave.test.ts` pins the cap's own arithmetic, the lesser-of above the cap and the whole of it below, the rung spent either way and the ladder still finite, and `src/app/screens/game/__tests__/watchedLoss.test.ts` pins the countdown starting at the pre-hit score, falling and never climbing. **Slice 8 is the next dispatch and it stages the ladder in the harness**, so a scenario can walk to the lowest level and the level below it without him.

**The feel questions waiting on his next play, none of them blocking:** the belch's three rings, the Wall as a real wall, the bell's food thrown up-field, the HUD row at a glance on phone and on desktop, the smaller still field, the score seen leaving, the levels seen falling, and whether he chases a fallen rung at all.

**The second cause behind his 12-to-15 sighting is still not ruled out**: the reach is a hard edge with no falloff and J-fix made the drawn ring and the real reach one circle, so his next play is the evidence (`round-two-wall-belch.md` section 4).

## Mark's rulings

His 2026-09-09 rulings are `mow-ladder-director.md` section 2 verbatim, and his 2026-09-15 rulings are `round-two-wall-belch.md` section 2 as R1 to R12, plus ADRs 0007, 0008 and 0042. **Four rulings of 2026-09-16 live here until they have a narrower home, and the trimming rule at the top is the fifth:**

1. **The tuning-record step sits behind step 5**, and **the follow-along document gets a plain-words row for every slice when its prompt is written**, not when it lands. FIRST ACTION carries both.
2. **The score is one number fed by several inputs** (the standing rule below): kills, boss damage, the Waking's source killed, large food after full power, and more he has not named. **A friends' high score list is his wish**, ticket **#135**, after the store. ADR 0002 and decision 10 were superseded in place at `09a1cf3dcb`, M7 carries the other inputs, and memory `score-is-composite` holds it.
3. **An instrument built for one of his sightings is checked on his own play before the slice is called done**, in the standing rules with his words.
4. **"Cap the bleed."** Landed as slice 7 (`f98c01767d`/`5a904b86ca`, ADR 0003 amended, note section 7's M1-fix subsection, research `docs/research/score-loss-on-a-hit-precedent.md`, memory `bleed-is-capped`). **The cap row is re-read after M7's batch**, already folded into M7's prompt.

## Findings filed for Mark's read, not applied

Each is a finding against something he ruled, written down and built past, and anything closed or superseded is cut on every rewrite. The fuller versions live in `show-what-you-have.md` section 7, `round-two-wall-belch.md` section 7, `round-two-progress.md` sections 8 to 18 and `step-4-progress.md` sections 13 to 19.

**Waiting on his read.**

- **A rung dropped above a grave that is standing still drifts back into its swallow box in 8 ticks** (step 5 note section 11's M5-fix subsection), so at the bottom clamp the loss can read as a rung that leaves and returns rather than as a chase. **It is the price of the M5-fix ruling and the alternative was the rung not existing at all.** The levers if he wants one are a longer upfield offset or a group offset on x as well as y, **both data rows**, and M6's grave y per strip says how often the case arises.
- **ADR 0055 records an eyes-open cost the tree disproves**: it says the base harness policy always walks to a body at the bottom of the field, and the measured take rate is zero of ten across twelve runs (step 5 note section 11). The hand's rule is right and the sentence is wrong. **Filed rather than amended**, because M6's declared take rate settles it.
- **Three of the four lines announce a strip in the HUD and nowhere on the field** (step 5 note section 10), because M4 built the field blow-up for the skull stream alone. **A field channel for the other three is a design job per line**, the record's section 7 sixth finding.
- **The second hit while small strips a level off every line, and a dive that grows the grave resets the cushion** (step 5 record section 7). It is the one behaviour in the step he did not ask for and will feel first, and it exists because a kill paying score would otherwise re-arm the bled rung about twice a second. **The lever is one rule, not a number**, read against M6's strips per run.
- **Four Mark-ruled ADRs are amended under one-push mode in step 5**, 0054, 0002 and 0061's sixth term in step 5.0, listed in the step 5 record's section 7, and **ADR 0003 in M1-fix's docs commit**, where the ladder's order stands and only the amount changes. **An amendment to something he ruled is his to overrule before merge.**
- **The ladder does not open across 96 bot runs** (round two record section 7): median one rung a run, nothing bought at all in 20 and 19 of 48, level 5 on no line. The bot does not hunt carriers, so it is a floor rather than the truth. **For him: did he reach rung 3 on any line.**
- **The witness has moved 7 to 11 and the readings 4 to 8 since the plan was written**, none of it in any plan, every move the orchestrator's, and each witness move kills every tape before it. **A plan change is his to see at merge.** M7 takes the readings to 9.
- **The push ladder has flattened and the Waking's corpse property has become dominant** (round two note section 10): rung one to five went 6.7 times to 1.6, the corpse property 1.51 to 6.8. **It follows from R2 exactly as he picked it**, so nothing was built past; it is what to react to when he next plays a high bell.
- **A toll's damage now steps to nothing at a line inside the cone that is never drawn** (round two note section 10), the body's travel its only tell: R10 working, and whether that step wants a visible edge is a feel question.
- **The charging control is now as loud as the ready one used to be** (round two note section 13), because ADR 0054 forbids the old alpha step: whether the corner reads too loud on a real device is his.
- **Under `svh` the belch's control sits over the field on his phone every run**, by 17 to 108 stage units (step 5 note section 5). **M2's even slack split shipped as the mitigation and it does not reach zero.**
- **The play area on his phone gets slightly smaller and stops moving** (step 5 record section 7), because M2 sizes the canvas to `svh` rather than `dvh`. **No test can tell a field that is correctly smaller from one that is wrongly smaller**, so the whole verdict is his phone read.
- **The bank's `+` marker and its 12-unit font are first figures** (step 5 note section 9): `BANK` does not fit the row's 23 units. **If the bank should read larger, the width comes off the score's own budget**, because R1 leaves the row 2.2 units of slack.
- **CodeRabbit's major on slice I was declined** (round two note section 9), fold `impulse.source` and bump the witness with it, and **all seven of M4's were declined too** (step 5 note section 10), the major among them a per-tick event delivery change across both screens. **A reviewer's major built past is his to see at merge.**
- **A thrown corpse is food the hand never gets back**, and **the bell's shoves rose 116 to 220** once cone-killed bodies flew (round two note section 15): 0 swallowed of 72 carried against 15.8 expected, and the design gate filed whether that is the shove's price or a leak, and whether the bell then reads as repel or as theft.

**Tuning-step inputs, its first sweep list.** Each is measured in full where it is cited, none is anyone's build now, and the fuller versions are round two's and step 5's section 7s.

- **A later shove of a press throws fewer bodies than the first**, the ring already cleared, a density question rather than a bug (round two note 18).
- **The purses are priced above what the quiet interval lets a section spend**, and the director takes three quarters of the Procession's empty ticks (round two record section 7, step 4 note 18).
- **At rungs four and five the storm kills bodies before a belch shove can carry them**, and **the living fringe is wide but shallow**, a reach of 0.47 for 0.7 roughly doubling its depth (round two note 10 and 11).
- **The belch is the least observable thing in the game and it is the thing being changed**: no tape of either kind can carry one, so measuring it wants a new named configuration (round two note 11 and 12).
- **Slice J-fix missed its second pass line with nothing tuned to reach it**, and **the eruption's front no longer pictures the field-wide gas** (round two record section 7, note 12): both are candidate reasons a press can feel like it did nothing.
- **A hit costs about 46 seconds of mowing at rung 1 and about 12 at rung 5**, against about 5 on the dead baseline (step 4 note 15).
- **Territory's bottom rung inverts against his #79 dwell ruling**: two pulses is a shambler and a level-one crossing lands five. **#125 is the felt half, still unruled.**
- **Whether 2,000 is the right cost for a floor hit** is a row of this record rather than a question for Mark (2026-09-17). `SCORE_BLEED_CAP` is `20 * TRASH_KILL_SCORE`, annotated as a first figure, read against slice 9's bleeds and strips per run and re-read after slice 10's batch.
- **Whether a capped slice off a large score is seen to leave at all** is a design finding for the same record: on a late score the countdown moves only the trailing three digits, so the fall carries less than it did. **The lever is already named and unbuilt**, a delta readout beside the digits or the skull stream's blow-up firing on a bleed as it does on a strip (open item 7 below).
- **`SCORE_DIGITS` 6 and the cushion mark's fixed x are derived in prose from `TRASH_KILL_SCORE` being 100, and nothing asserts the row's budget against the score rows** (close tech gate). The digit count lives in `src/app/screens/scoreReading.ts` since the close fold, `LadderHud.ts`'s `SCORE_BUDGET` and the cushion's x both derive from it, and the derivation is a JSDoc argument rather than a test: move what a kill pays and the row overruns silently.
- **The kill's own row is about 95 percent of a maxed run's gross**, 94.7 to 96.0, **and the cap's share of gross fell to 13.7 to 3.1 percent after slice 10** (step 5 note section 13), so every other input is tuned against a kill that already swamps them.

**Open against tickets, one line each.** **#136**, the score can be farmed at a boss that never ends, filed with M7's prompt. **#133**, `killableOutright` is a dead rule with no production reader. **#134**, no batch can press on the Wall, so the curtain's only opener is unmeasurable in a sweep. **#135**, the friends' high score list, after the store. #122, the offer is not separable at a glance in a Crowd-density field. #129, a body touching the grave does the dominant harm with no tell. #128, the renderer walks every pool slot at the derived caps. #66, the dev FPS line overprints the score at the narrowest phone, a dev-build-only collision under R12. #50 and #51, the two build warnings.

**Owed to whoever next touches the code they sit in.** `src/dev/readings/repel.ts` carries a stale sentence, "push only exists at bell levels 4 and 5", in its JSDoc at line 19 and in `__tests__/repel.test.ts` at line 54; it is wrong at every rung now, and a stale "ramp" comment survives at `digest.ts:316`. **The trash's `mobFire` stream is drawn by nothing at all in a whole run** (step 4 note section 17), so a reading treating a moving `mobFire` cursor as evidence reads a stream nothing spends.

## Open items with no other home

0. **Stop hook error in this worktree, harmless, not to be chased.** `scripts/roadmap/on-v1-change.mjs:23` imports `local/publish-instruction.mjs`, which lives only in the main checkout; copying that one file into the worktree's ignored `local/` fixes it.
1. **Open tickets against step 4, none blocking.** #118 the Undertaker deadlock, on Mark's sighting and not reproduced in 108 bot runs; #117 is Mark's alone; #116, #121, and the deferred gate findings on #39 and #109.
2. **The human-tape replay divergence** on `b1c3a584d1` is undiagnosed, evidence at `docs/push/divergence-b1c3a584d1.md`. Mark: it cannot be allowed to fail, and a test holds the cause once known.
3. **#82 and #37 stay open deliberately.** #82's fix is on this branch only, so it closes at merge; #37 (boss stories 12-14, fog story 22) waits for the path's step 2.
4. **The rewritten `stay-within-limits` skill files are uncommitted** at `/home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/`, and the worktree guard blocks committing them from this session.
5. **ADR 0062 is filed at `c84b4fd7a5`**, a reading's meaning is declared and never taken from the shape of its value, on the three declines (step 4 note section 19, round two note sections 8 and 9); the guard is `comparisonDeclared.test.ts`. **Filed under one-push mode, so it joins the list of ADRs Mark reviews before merge.** **A `rig=ladder` batch verifies nothing** (slice 9, note section 12): the tape header carries no starting score, so every tape from a rig that starts holding one diverges at its first checkpoint. Slice 9 measured conditioned ladder tapes at score zero instead. **The fix rides in the tuning step's own `FORMAT_VERSION` 4 to 5 bump (draft decision 5): the header's new self-describing block carries the starting score beside the tuning record**, so one bump closes both, and no slice widens the header before then.
6. **The wisp-cap silence is still open.** `launchWisps` takes a null slot from `takeSlot` and returns, so a truncated volley raises nothing and moves no counter, against the rule that nothing abnormal is ever silent. **The trigger is the next time either line cap is measured binding.** No ticket needed.
7. **Step 5's close docs pass landed at `b8fe220b2b`** (five bleed sentences, the ladder rig relabelled with the old condition now "the hit-taking rig", ADR 0002's clause). Two candidates it left filed: widening the tape header so a staged score replays rides step 6's slice 6, and `batch.ts` accepting `rig=ladder` with no divergence warning is moot once the header carries the score.

## Standing rules and facts, still true

- **Craft calls are settled by research, and precedent outranks our own record (Mark, 2026-09-09).** Naming and every craft number comes from research off the main thread, and a record that disagrees with precedent is re-ruled to precedent rather than amended to permit both. His rulings are the feel; the numbers under them are the session's arithmetic and move freely.
- **Decide craft values, never offer Mark a comparison (Mark, 2026-09-16).** His words: *"half of that feedback is solicited... We're still getting caught up on little things... I want a V1. Please help me get there."* **Pick the value with a stated reason, preferring a floor or ceiling an existing rule already implies over a number somebody liked. Where a value is genuinely open, annotate it and move on, and build no comparison knob before V1.** His unsolicited reaction after playing stays welcome and binding; what is banned is manufacturing the question.
- **Slices are numbered in run order (Mark, 2026-09-17).** An inserted slice takes the next number in the order it will run and the later ones shift; the old codes stay in parentheses as anchors. In chat he is told the number and the plain words, never a bare code.
- **Gates never overrule Mark (Mark, 2026-09-09).** A gate finds, never rules. A finding that would reverse, defer, narrow or reconsider something he decided is filed as "gate disagrees with ruling X" and built past. Questions to him only when the session cannot make the call and it is hard to reverse.
- **Mechanical checks are never Mark's (Mark, 2026-09-17): a unit test, a fitting integration test, or a staged harness scenario proves them; a slice prompt's Mark actor carries feel only; a missing harness ability is the work.**
- **The score is one number fed by several inputs, never kills alone (Mark, 2026-09-16).** Every new payer is a row of its own with its weight researched and annotated.
- **A slice gets its row in the follow-along document when its prompt is written, not when it lands (Mark, 2026-09-16).**
- **One-push build mode (Mark, 2026-09-07):** one autonomous push on one branch in its own worktree, CodeRabbit CLI before every code commit, gates as relevant, ADRs my call and reviewed before merge. Memory `one-push-build-mode.md` holds it in full.
- **Stuck rule:** two failed honest attempts, a decision only Mark can make, or an unauthorized irreversible step: stop the thread, write a stuck note (doing, the two attempts and why each failed, one question), send it over Discord (`DISCORD_WEBHOOK_URL`, a curl POST with `{"content": ...}`), move to independent work, and if there is none write the handoff and pause. **Discord carries stuck notes and the done message, nothing else.**
- **Fable never reads (Mark, 2026-09-09).** Cheaper agents search and hand Fable the result; a small Fable job runs in the main thread, a big one as a Fable subagent carrying those results. **Spend it on the balance itself, the interaction of changes, and reading each batch to pick the next move**, never on records, prompts, coding, gates, batches, tickets, commits, deploys or the follow-along list.
- **Dispatch shape.** The gate agents are `gate-game-design`, `gate-product-vision` and `gate-tech-architecture`, exempt from the dispatch hook, and their prompts must tell them not to edit and not to leave markers. A non-coding prompt opens with the literal line `Non-coding dispatch: <reason>`. A coding prompt names `docs/agents/feature-playbook.md`, the prompts file and `docs/push/step-4-coder-contract.md`, and carries the six contract items; a coder handed a slice missing any of them stops and reports. A prompt is drafted by an Opus agent into the scratchpad while the previous slice builds, then committed as docs; **the orchestrator reads only a draft's decision parts** and rules on any stop it names before dispatch. **A coder is never dispatched in the last hour of a session**, because it dies with the context switch.
- **Usage limits:** the thresholds are the `stay-within-limits` skill's and the header carries the live numbers. At a stop, write the handoff, commit it, chain a new session on Opus.
- **Frame a craft-value question in perception terms**, what the player must see and against which ruler already on screen, converting every candidate into the field's own units before it is a number (`docs/research/watched-pushback-duration.md` section 0).
- **An instrument built for one of his sightings is run over a real play against that sighting before the slice is called done (Mark, 2026-09-16).** J2's per-body press record had no tick axis, so it could not see a wave that pushed nothing, and the gap survived to his next play. His words: *"If you created the tape thing to account for this, why did you not check it to make sure you had it right before moving on?"*
- **A headless browser cannot photograph a short-lived state.** It draws at 3 to 5 frames a second against twenty to thirty sim ticks per drawn frame, so such a property is pinned by test in the sim and is **human-checkable only, on a real device** (round two note sections 8 and 13, step 5 note section 11).
- **Deploys run from the branch worktree between coders, never from a scratch worktree**, which the isolation guard makes unusable for a subagent.
- **Commits on this branch carry no trailer of any kind**, Mark's global single-line conventional rule overriding the harness's `Co-Authored-By` reminder.
- **Mark's operating rule (2026-08-28):** batch mechanical already-ruled work; stop only for a new product or architecture decision, an invariant conflict, scope expansion, or invalidating evidence. Commit, push, deploy and merge each need their own yes outside the pre-authorized list.
- **His issue format**, which the triage skill is not the source of: three one-line paragraphs, Problem, Need, Done when, plus a follow-up comment posted right after creation opening with a Placement line.
- **Edit-in-place ADR pattern:** same question, new answer: rewrite in place, slug follows the new title, number stays, a new number only for a new question. Supersession is dated inline bold prose plus a what-stood / what-it-replaced / what-it-could-not-have-known triple. No YAML frontmatter anywhere.
- **Vercel:** Mark is locked out of the dashboard (2FA, ticket open), so the CLI is the only way in and a token is minted. Project `hungry-grave`, org `team_rDwpau77qippLMDxVoyeq5Ev`; the worktree's `.vercel/` link and `.env.local` are ignored copies.
- **The store is provisioned and dormant**, Neon Postgres `hungry-grave-runs` and a private Blob store, keys in `.env.local`; its shape and read-after-write trap are in `docs/push/pre-authorizations.md`. **#135's high score list waits on it.**
- **Assets, agreed**, are the mapping in `step-2-stage-floor-dispatch.md`. **Every asset is a placeholder to be replaced before the real game, so licensing is never a question to raise** (Mark, 2026-09-08, repeated).
- **Commands:** the conditioned-tape, frame-budget, measure and verification lines are in `step-4-coder-contract.md`; only `pnpm typecheck` judges diagnostics and `pnpm verify` is the repo gate. Local play: `pnpm build && pnpm exec vite preview` here. Mark saves tapes to `/mnt/c/Users/markd/Downloads/*.tape`. **`coderabbit review --agent --uncommitted` from the worktree root is the working form**; it does not see untracked files, so add by path first.
- ADR set 0001-0061 canonical, no ADR carries a combat magnitude, and a /to-spec spec lands as a comment on the existing ticket.
- **Reading big files:** `cat` over roughly 30KB gets saved to a file; Read the saved path. In zsh a bare `echo ====` fails and `--include=*.ts` needs quoting. The scratchpad is shared between subagents, so a generic filename gets clobbered; anything a later session needs goes into `docs/push/` on the branch.

## How we got here, one line

Sessions 1 to 29, the V1 grill of 2026-08-31 through round two's close and step 5's first nine slices, are in this file's git history (`git log --follow apps/hungry-grave/docs/push/handoff.md`); `7f5cb0105b` is the last version carrying the session-by-session narrative.

## Suggested skills

`/domain-modeling` for the ADR step, `/triage` for tickets with drafts to Mark first, `/to-spec` output as a ticket comment, `/tdd` bound by `docs/agents/feature-playbook.md`, `/stay-within-limits` between waves.
