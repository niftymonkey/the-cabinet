# Continue: The Hungry Grave

> **Last updated:** 2026-09-09, session 19, written when the context watchdog fired. Mark is awake and has repeated his instruction: write the handoff when the watchdog fires, then keep going; the stop and the /clear ask are waived, the handoff is not. **Step 3's code is complete and pushed.** The next action is the cliff measurement between `loose-far` and `shaky` that Mark's live question needs, then the step 3 close.
> **Branch:** `hungry-grave-v1`, a clean branch off `main`, in worktree `/home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1`. The earlier branch with the cloud scaffolding is archived as `hungry-grave-v1-cloud`, locally and on origin. Last code commit 032b72d794 (the middle rung's reading crash), note b81a6c1a39, then this handoff commit; pushed. Slice 7 was 29a97f2c88, note ad4657276e.
> **Working tree:** clean at the handoff commit. `pnpm verify` green at 032b72d794 (1769 passed). Run `git log --oneline -30` and `git status --short` first anyway. **Mark's instruction for the next session: start it on Opus** (`claude --model opus`), because weekly Fable usage is at 83 against the 85 caution and 95 stop; the coders were Opus already, so nothing changes but the main thread.

## FIRST ACTION: enter the worktree

Run EnterWorktree with `path: /home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1` before anything else. The push records are the tracked copies in `apps/hungry-grave/docs/push/` (this file, `decisions.md`, `path-draft.md`, `pre-authorizations.md`, `open-tickets.md`, `v1-rethink-prompt.md`). The rules that bind the push are Mark's global configuration plus the `one-push-build-mode` memory (lean main thread, subagents on Opus or cheaper, the stuck rule, Discord for stuck notes only); nothing is duplicated on the branch any more. Mark's personal tally stays gitignored at the main checkout's `local/what-the-first-build-taught.md`. The raw asset packs sit gitignored at `apps/hungry-grave/assets-staging/` on disk in this worktree (a copy also lives on the archive branch). The isolation guard refuses writes to the main checkout and refuses compound Bash that mentions git, even inside quoted text; use plain separate commands and scripts written to the scratchpad.

## Where we are (sessions 18 and 19, 2026-09-09)

**Session 19 (2026-09-09) recovered the lost session, landed and deployed slice 7 so step 3's code is complete, fixed a crash the first middle-rung batch exposed, and put three rungs of the ladder side by side. Mark has reopened the hand's error rows and that question is live.**

- **The lost session, and the lesson.** Session 18b ran 01:16 to 02:47 local **from inside the worktree**, so its transcript is filed under the project folder `-home-mlo-dev-niftymonkey-the-cabinet--claude-worktrees-hungry-grave-v1` and `claude --continue` from the main checkout could not list it. Transcript `391907e7-ff88-439c-9f49-2de70494fe26`. It landed slices 5 and 6 and dispatched slice 7, which a machine reboot killed mid-work, leaving only an uncommitted edit to `bot.test.ts`. That edit was discarded and slice 7 was redispatched fresh from its committed block `f3b7ee022e`. **A session started inside the worktree is listed only from inside the worktree.**
- **Slice 7 LANDED: code `29a97f2c88`, note `ad4657276e`** (progress note section 15). No tests added; the only code change is `ONE_WHOLE_STAGE_MS = 30000` on the five whole-stage `dodgePolicy` tests, in that file's own idiom. Every agent-actor verification step green at the tip: suite 1767 passed, typecheck, build, `pnpm verify` exit 0 twice, test-name diff 1670 to 1777 with this slice's own net zero, GOLDEN unmoved, the old-tape refusal exact on both format-2 tapes, the readback verified at 412 checkpoints, the determinism re-run differing in exactly three bytes (the recorded-at stamp). **All five fence files green with no edit.** Step 14's split, which it owed: sharp 62.5 s playing against 54.3 s measuring, sloppy 5.7 s against 6.5 s, so a nine-configuration sweep sits between 2 and 19 minutes. One plan claim false (note item 21): section 3 calls for four fence files and the branch carries five. Deployed and pushed.
- **The middle rung's reading crash, found and fixed: code `032b72d794`, note `b81a6c1a39`.** The first batch ever played on a middle rung (`loose-far`) died on its fourth seed with `mob 2801 died with no damage behind it`. **The defect was in the reading, not the game.** `step.ts` fires the belch at line 237, before `advanceStage`, `advanceBoss` and `advanceSetPiece`, so a belch kill frees a pool slot that a spawn later in the same tick claims and re-stamps with a new id; by end of tick the dead mob's id is gone from the pool, `takeHit` had dropped its damage, and the `mobKilled` one event later hit the guard. The `mobTypeOf` doc comment claiming "a tick runs its spawns before any damage" was false, and `step.ts`'s own tick-order comment already said the opposite. The fix builds one id-to-type map per tick from the pool plus that tick's own `mobKilled` events, which carry the type. **The guard stays and now has its own test**: a kill with no damage behind it still throws. No game code touched; GOLDEN and all three versions unmoved. **Earlier corner numbers are unaffected, confirmed empirically**: both 48-seed reports rebuilt from their tapes under the fix are byte-identical to the ones on disk, and every dropped damage was followed by a kill that threw, so any batch that finished had no drops.
- **THE DROP BELONGS TO THE TWO ERRORS MEETING, not to either knob. This is the reading that matters most, and it corrects an earlier reading of this session's own.** Code `3a6e7eb428`, note `641f657294`, record amendment `1e79b15c94`. Three new hands were added between `loose` and `shaky` (`unsteady` 140/20, `wavering` 175/25, `faltering` 210/30, each with the three head words, eighteen names in all) and swept at 48 seeds. **Reach out of 48: `steady-far` 29, `loose-far` 32, `unsteady-far` 32, `wavering-far` 31, `faltering-far` 36, `shaky-far` 28. On the head axis `steady-short` reaches 38, the highest in the table. Only `shaky-short`, both errors maximal at once, reaches 0.** So the hand axis alone moves reach by nothing across its whole range, the head axis alone moves it upward, and the sloppy corner is not the bottom of a slope in either knob: it is the one place the two errors meet. **The mid-session claim that the ladder was a flat shelf then a cliff between loose and shaky was wrong**, and it was wrong because it read a two-knob corner as a one-knob rung. Two points on the head axis do not map the interaction and mapping it is not yet done.
- **The victories at 48 seeds line up with nothing, and the reason is the batch size. Record amendment `68bf131117`.** Across `steady-far`, `loose-far`, `unsteady-far`, `wavering-far`, `faltering-far`: **7, 7, 17, 5, 17 out of 48.** The two extremes were replayed at 192 seeds from the same first seed and **they converge**: `wavering-far` reach 125 of 192 (65%) and victories 34 (18%); `faltering-far` reach 121 (63%) and victories 38 (20%). At 48 those same two rows read 10% and 35%. **So reach is stable at 48 and a rare outcome is not**, and the session's mid-flight claim that the 5-to-17 gap was too large to be noise was wrong twice: the gap is about three standard errors rather than four, and it was the widest of ten pairwise comparisons across five rows, which is where a three-sigma gap is expected by chance. **The floor is now 192 seeds for any reading whose rate is below about a third, 48 stays adequate for reach**, and the record's section on batch size carries the table and the cost (about 8 minutes a configuration at 192, so nine configurations is a little over an hour).
- **The done line's two corners at 192 seeds, and the evidence is now firm.** `steady-far` reach 117 of 192 (61%), victories 34 (18%). `shaky-short` reach **0 of 192**, victories 0, all 192 sealed. So the sloppy corner's zero is not a small-sample artifact: it genuinely never arrives, over four times the runs. And reach at 48 was already right: `steady-far` read 29 of 48 (60.4%) against 117 of 192 (60.9%). **48 is confirmed adequate for reach and confirmed inadequate for victories, exactly as the record's amendment says.**
- **MARK'S RULING ON MEASUREMENT CADENCE (2026-09-09), and it governs every later step.** The harness exists so work can get done, evaluated and tuned when we need it, **not as something run regularly**. Big sweeps are not the habit. What he asked for: one initial tuning pass now that gets the game feeling roughly right, then one or two tuning iterations after the director is built (step 4), and the rest saved until after the store exists (step 6). Wall clock is the cost he cares about, and a nine-configuration sweep at 192 seeds (a little over an hour) is not something to reach for. **The default working batch stays 48 seeds at about 2 minutes**, which is right for reach and for anything common; 192 is reserved for a decision that turns on a rare outcome such as a victory rate, and on two or three rows, never nine.
- **Three cost fears checked against the tree, because two of them were mine to dispel.** Batches touch **no database and no blob store**: `src/dev` and `scripts/batch.ts` carry no Neon, blob or connection-string reference at all, and tapes land under `apps/hungry-grave/local/batches/`, which is gitignored. Batches spend **no LLM tokens**: a batch is a headless node process playing the game, with no model in it. The only real cost is wall clock, plus local disk, which stands at 255 MB across 17 batch folders and wants an occasional sweep. **OWED at step 6:** pre-authorization item 10 currently says harness batches may upload rows and bytes; given this ruling that should be revisited before the store is wired, and harness batches should probably not upload by default.
- **MARK'S ROUTING LADDER FOR A FINDING (2026-09-09), and it governs the rest of the push.** In order: **fold it into the task at hand** ("I am not done, that is a problem, fix it before I commit this change"); **or write it into the next dispatch** ("the next task needs that to complete, so it goes in that prompt"); **or file a ticket**, only when it does not make sense to address now; **or stop and ask him**, only when none of the above solves it and the decision is hard to reverse. Never a separate dispatch per finding: three findings that need fixing become three items in one existing dispatch. The goal is V1, and anything that gets us there rides with work already planned rather than becoming ceremony between the steps.
- **#118, #119 and #120 are filed but are being fixed now, folded into the step 3 close as its code component.** One coder, three items, in flight at the session 19 pause: (a) #120 restore the test-name baseline the close's own check reads, (b) #119 fix `territoryControl`'s dropped last pulse, because step 4 tunes on readings, (c) #118 diagnose the tick-ceiling runs and make the shortfall visible in the report, with the standing instruction that a genuine game stall is a stop-and-report and not a fix. **Close the three issues once it lands.** The session got here badly, holding them for a nod, then filing them for later, then proposing three dispatches; the ladder above is the correction and it is in memory as `findings-ride-with-planned-work`.
- **OWED, small and not started:** whether `BATCH_SEEDS` (default 48 in `src/dev/batchReport.ts`) stays 48. Under Mark's ruling it should stay 48, because 48 is the right default and 192 is the exception a caller asks for. That is now a no-change decision, recorded rather than done.
- **No existing row moved and it was proved, not asserted.** `steady-far` replayed at the widening commit gave 48 tapes identical from byte 165 to the end, every differing byte inside the header's commit hash and timestamp. Batch folders under `local/batches/`: `loose-far-1788962791962`, `unsteady-far-1788964037430`, `wavering-far-1788964167748`, `faltering-far-1788964284145`.
- **Mark reopened the error rows (2026-09-09) and his question is live.** He said the shaky rows look too harsh for a player who is not already good at these games, and proposed halving them. **The evidence supports his read of the rows**: the design record's own cited research measures human lapse rates at 1 to 2 percent rested and about 9.6 percent under total sleep deprivation, and shaky sits at 250 per mille, two and a half times past any measured human. **But the three-rung table says halving would not give him what he asked for**: half of shaky is 125 per mille and bound 18, which sits just past loose, and loose already plays as well as the perfect hand. Halving buys a second sharp hand, not a weak-player rung. **The open work is to find the cliff edge between loose and shaky.** That needs new named rows in `src/dev/configurations.ts` (a moved rate is a new configuration name, per the design record) and one 48-seed batch each at about 2 minutes a rung. This is also the evidence #117 has been waiting for.
- **Findings filed in the note and not acted on. OWED: tickets, drafted for Mark and awaiting his nod at the session 19 pause.** (a) Runs that never end: seed 20260913 under `loose-far` ran out the harness tick budget at 83577 ticks with no ending, and `unsteady-far` and `wavering-far` each have one of their own. Whether the stage can genuinely stall or the ceiling is just short is unknown. (b) `territoryControl`'s `standsInThePool` drops a mob's last territory pulse, so that reading runs low. (c) `local/step3/tests-baseline.txt` is gone from disk; only the `.json` survives, carrying the same 1670 names.
- **The three new hand names are the session's craft call, not Mark's.** `unsteady`, `wavering`, `faltering`, in `CONTEXT.md`'s Configuration entry with their rows. Renaming is one edit plus the union; say so when he reads them.

**Session 18 (2026-09-09) reopened the hold's shape on Mark's question, made the call, and dispatched slice 5 to the new shape. Slice 5 is in flight as this was written.**

- **What Mark asked.** Whether steady, loose and shaky were too siloed, and whether loose should be "anywhere between steady and loose" and shaky "the whole gamut". Half of that was already true and the session said so: the draw was uniform from 0 to the bound, so the rungs already nested. The half that was not true is the **shape** of the draw. A uniform draw taken on **every** decision makes the sloppy hand evenly mediocre forever, stale about 300 ms every time, rather than sharp most of the time with the occasional disaster. That is the defect he found.
- **The old-shape coder was stopped** before it edited a file; `git status --short` was clean after.
- **Two research passes ran** (both reported in this session's transcript, sources cited in the record). Human error is a tight body plus a rare long tail: the standard descriptive model is the ex-Gaussian whose tail parameter tau is read directly as attentional lapses, and psychophysics fits an explicit lapse rate as its own term. Measured lapse rates run about 1 to 2 percent rested and about 9.6 percent under total sleep deprivation. **The finding that decided the ladder's shape**: on a *simple* response the expert advantage sits in the mode, but on a *choice* response it is carried **entirely by tau** (Classic Tetris experts against novices, BF 39.4). Dodging is a choice task, so the ladder moves the tail and leaves the body at zero. Shipped practice agrees from the other side: Quake III's uniform-scaled aim error is 1999's answer and is what this record used to say; Stockfish multiplies a uniform draw by the gap between the best and second move so a uniform input gives a heavy-tailed output; Maia's authors show depth-limiting "does not pass our test of accurately modeling granular human behavior"; and BotPrize judges spotted bots by their **consistent** mistakes.
- **The call, mine, taken under the default and recorded for Mark's review before merge.** The hold becomes a lapse of attention rather than a standing slowness. Every decision rolls `lapsePerMille` first; only a failed roll draws a hold, uniformly from 0 to `lapseBound`. Rows: steady 0 and 0, loose 100 and 15, shaky 250 and 36. `holdBound` retires. It is recorded as a dated amendment in the design record's section 3 with every source, not as a new ADR: ADR 0053 says only "a drawn number of ticks" and rules the harness, so it does not rule the error model, and the record is the canonical home. **`steady-far` is unchanged and draws nothing at all** (the rate is checked before any draw), so the 48-seed sharp batch recorded against `5f7f365e99` stays comparable and the determinism run still works under the sloppy corner because the sharp corner never touches the stream. The two rates are undefended starting rows; step 4 (#39) moves them, and a moved rate is a new configuration name.
- **Deliberately left out, filed as #116 with its trigger and its approach comment**: a geometric stretch on the lapse depth, which is the ex-Gaussian's real tail. It adds three undefended numbers before any measurement exists. Trigger, either one: slice 6's comparison shows the two corners' bands too tight to separate, or the shaky corner's tape reads as constant mediocrity rather than occasional disasters. A sum of uniforms is explicitly **not** the fallback: Irwin-Hall is a symmetric bell and reproduces the evenly-mediocre failure this amendment removes.
- **Committed and pushed:** `bb882862af` (design record sections 3 and 8, dispatch plan sections 4 and 8) and `9ab5c0a138` (the slice 5 prompt rewritten to the new shape). The superseded prompt block was replaced in place rather than kept beside it; the block itself says it supersedes an earlier draft so a coder reading it cannot follow the old shape.
- **Slice 5 LANDED to the new shape: code `6abdb4255c`, note `8db7b7fee0` (progress note section 13).** Sixteen tests added, one of slice 1's renamed (its name said "four knobs" and a row now has five fields), sixteen mutations with one survivor pinned (the rate compared `>=` against `>`, invisible in any command, now held by a test counting the hand's draws over 30000 ticks). `pnpm verify` green, 1761 passed. GOLDEN, `WITNESS_VERSION` 6, `READINGS_VERSION` 2 and `FORMAT_VERSION` 3 all unmoved. Verification step 9 passed: `shaky-short` twice over six seeds, identical ticks, bytes and report. CodeRabbit one minor applied, re-review clean. `#113` closed by deriving `NAMES` from the run's own streams plus `HAND_STREAM`; the search now covers all thirty ordered pairs of six names and finds no overlap. **`steady-far` proved unchanged** by replaying slice 4a's six seeds to identical figures and an identical report, so the sharp batch is not invalidated by this slice. An editor diagnostic on `harnessRun.ts:151` appeared after the commit and was investigated rather than dismissed: `tsc --noEmit` exits 0, so it is the stale editor state hand-forward (h) predicts.
- **The reading that matters most, and nothing was tuned for it.** Under the new shape the sloppy corner's runs are three to five thousand ticks against the sharp corner's twenty-plus thousand, a seventh of the run, on a six-seed look. The record was written fearing the two corners would sit too close to separate; the first evidence says the opposite may be true, and slice 5's own note raises that **a middling rung may carry the interesting part of the ladder**. The 48-seed measurement of it is slice 6's verification step 11. This is Mark's step 17 territory and no agent judges it.
- **Slice 6 is IN FLIGHT** on one Opus coder against its committed block (`c35b50074f`). It plays **both** corners fresh at its own tip rather than reusing the sharp batch recorded against `5f7f365e99`, because a comparison whose halves carry two commit hashes is a comparison through two instruments. It reports the two reach rates and judges nothing.
- **The game design gate on slice 5 RAN, verdict findings and none blocking, marker on #98.** Its words: the shape is right and better motivated than what it replaced, both knobs attack "position is the whole skill" which is the game's own declared skill, and steady drawing nothing genuinely preserves the sharp batch. Three findings, all filed and none built.
  - **The headline, filed as #117 with its approach comment, and it is Mark's to rule because it touches the done line.** The six sloppy runs, 3457 to 5474 ticks, all ended inside the Procession, before the Banshee at about two minutes of a nine-minute stage. ADR 0053 believes a finding only where the two corners agree on the ordering, so if the sloppy corner never leaves the first section that rule reaches one minute of nine, and every boss reading, the Waking, and story 12's "one belch plus play beats the Undertaker" have one corner rather than two. #39 would then tune the doses of hell against a single hand. The candidate answers are in the ticket's comment, none taken: the agreement corner becomes a middling rung (cheapest, and slice 5's own note reached it independently), or #39 brings the sloppy rates down, or the rule is scoped per section. Slice 6's 48 seeds are the real measurement.
  - **Two corrections to the record I wrote today, and they are mine to make, not a gate overruling anything.** The nesting paragraph in section 3 counts **decisions**, but per **tick** the loose hand acts on a stale command about 43 percent of the time and the shaky hand about 82 percent (rate times mean depth over a cycle of one decision tick plus its stale tail). ALE's sticky actions, the closest shipped formulation, state their probability per frame. And the anchor literature I cited is discrete-trial reaction time and vigilance, where a lapse is one slow trial among many; dodging is continuous manual tracking, whose own literature gives an open-loop correction interval of 250 to 650 ms. The shaky hand re-decides about every 92 ms, three to five times faster than that, so **the ladder is superhuman in update rate at every rung** and the sloppy rungs fail by depth of blind drift rather than by slow reaction: a 36-tick lapse drives the grave about 162 units of a 540-wide field blind, past the body it was walking to. **The mechanism does not change and no number moves**; what changes is the record's claim about what it models. **OWED: amend `playing-harness.md` section 3 in place, dated, with the per-tick figures and the tracking correction, after slice 6 lands** (held back only to avoid editing a record a running coder is reading).
  - **Outside the gate's authority and filed as a comment on #39**: all nine rows share `enoughClearance` 12 and `belchWorthIt` 8, so the ladder never varies risk appetite, which VISION calls the central bet. ADR 0053 already names a third knob as the move, so it is a tuning note and not a reversal.
- **Slice 6 LANDED: code `60f6e652d1`, note `211197eb3d` (progress note section 14).** Six tests added, none removed or renamed (1670 to 1777 against the slice 0 baseline), CodeRabbit zero findings over three files, thirteen mutations with ten caught (the three survivors all sit inside the clearance term, which multiplies out to zero while the separation row is zero). GOLDEN and the three versions unmoved. `pnpm verify` green, exit 0, twice.
  - **The two reach rates, the step's own headline: `steady-far` 29 of 48, `shaky-short` 0 of 48.** Both corners played fresh at one commit, 48 of 48 verified each way. Sharp: 41 sealed, 7 victory, 2 min 3 s. Sloppy: 48 sealed, **47 of 48 ending inside the Procession**, 15 s. Posted on #98 and on #117. **Nothing judged and nothing retuned**: whether that gap is right is Mark's step 17, which is now answerable.
  - **This settles #117's evidence.** The sloppy corner does not reach the bosses rarely, it never reaches them, so ADR 0053's two-corner rule covers about one minute of a nine-minute stage as built. The candidate answers are on the ticket and none is taken.
  - **One plan claim false:** verification step 11 asks these two batches for the agreed and split rows, but `readAcrossCorners` orders one comparison against another and there is only one build, so no corner agreement can exist until #39. Spec test 44 holds the grammar meanwhile.
  - **Verification step 12:** `BAND_SEPARATION` left at zero as shipped; slice 6 **proposes 0.25 of the wider band**, with the measured bands in note section 14 and on #98. Not applied. Independently, the sharp corner is flat on all 180 rows against its own batch from four commits back, which proves both halves were read through one instrument.
- **The record correction the gate asked for is DONE**, `51995936c4`: `playing-harness.md` section 3 gained a second dated amendment. The nesting is now stated per tick (loose about 43 percent of ticks stale, shaky about 82), and the record no longer claims to model a human's reaction, only that it plays worse along a named axis. No number moved.
- **Slices 5 and 6 are DEPLOYED** to hungry-grave.vercel.app at `211197eb3d` (Ready, production, site 200). The deploy checkout was created and removed. Note for the recipe: `rm -rf .vercel/output` is refused by the permission classifier in this session; `mv` the folder aside instead, which works.
- **Slice 7 LANDED** and closed step 3's code. Its results are in the session 19 block above.
- **NEXT: the cliff sweep is done and its answer was that there is no cliff in either knob. What it opened instead is the interaction and the batch size, and neither is this step's to settle.** Both are readings for #117 and step 4, so **the step 3 close is the next action**, in this order: tech architecture and product vision gates on the whole step with markers on #98, findings filed as tickets and never built, CodeRabbit on the whole branch against `main`, the ticket sweep (#107 closes when every figure names its rig; #113 already closed at slice 5; the two new tickets owed above), the #98 resolution comment, close #98, push. Then step 4 per `path-draft.md`. **No merge without Mark's own yes.**
- **Usage at the dispatch:** session 8 percent, weekly all 66, weekly Fable 83 (caution 85, stop 95).
- **Mark's instruction for the night (2026-09-09), which is the authority for everything after this line.** He went to sleep having said: take the research, make the best choice, and keep going toward V1 following the plan and all the rules of this push. So the order from here is slice 5, then slice 6 (the comparison and the done line: steps 11 and 12, the two reach rates reported in the note and on #98 and never judged), then slice 7 (the fences and the full pass, plus the stated budget for the five whole-stage `dodgePolicy` tests in `bot.test.ts`), then the step 3 close (tech and vision gates on the whole step, findings filed as tickets, CodeRabbit on the whole branch, the ticket sweep with #107 and #113, the #98 resolution comment, close, push), then step 4 per `path-draft.md`. Deploy every slice as it lands and update the follow-along list row. He does not merge tonight and no merge happens without his own yes.

---

### Session 17 and earlier

**Session 17 (2026-09-09) dispatched adjustment iteration 4 and planned step 3's design record; both were mid-flight when this was written.** Mark's mood at this point: frustrated that a full day went on step 2's adjustments; he wants the path to move. His standing rule (memory `gate-findings-file-not-build`) is restated as the rule from here: a gate finding becomes a ticket and the path moves on; adjustments are built only when he asks. Adjustment 4 got built because his #104 ruling read as an ask.

- **Adjustment iteration 4 (#104, the pour finishes anyway) LANDED: code 311cc7b8a8, note 39c1eb7e30 (progress note section 30).** Five tests added, three rewritten to the new promise, `bodyGone` excluded from the witness fold with its reason, `WITNESS_VERSION` 6, GOLDEN unmoved, seed 101 identical either side (witness 556547939), property 63 to 25. **Finding, for step 4's tuning pass:** at `SET_PIECE_HP` 2400 no reachable hand can kill the source (a full build parked under the mouth does about 73 damage a second, not the 150 the derivation assumed), so the ruling has no play effect until the row moves; the rendered read needed the row at 200 temporarily. Deployed to hungry-grave.vercel.app (the deploy checkout removed), the follow-along row updated, the 73-against-150 measurement posted on #39, the game design gate run on the landed change (marker on #104: findings, none blocking; the property holds at 63 to 25; filed as **#114** killing the source pays nothing, Mark's call, and **#115** a killed source pours from bare ground, both triggered by the health row being tuned down at step 4), the resolution comment posted and **#104 closed**. Pushed through 1c7b543c04; d9317d7ae1 (the step 3 plan) and later commits still to push. The original dispatch shape, kept: prompt committed at b1c3a584d1 as the last block of `docs/push/step-2-slice-prompts.md`. Shape: `SetPiece` gains a body-gone boolean written in `damageSetPiece`, `setPieceHitbox` null when gone, a new `setPieceKilled` event, `SetPieceClosing` narrowed to spent and scrolled, the renderer hides the sprite, records amended in place, `WITNESS_VERSION` 6, GOLDEN must not move, seed 101 witness may move only if that run kills the source. Commit messages end in `(#104)`. When its two commits are in the log: verify `pnpm verify` green at the tip, deploy per the recipe below, update the follow-along list row `14 adj 4` from "in flight" to done and deployed, run the game design gate on the landed change (as session 15 did for adjustment 2), file any finding as a ticket, post the #104 resolution comment (the two hashes, the record amendments, the measurements) and close #104, then push the branch. If the coder is gone and its commits are not in the log, `git status --short` shows its partial work: read the last section of the progress note and the test file's state before deciding whether to finish it in a fresh Opus coder with the same prompt block or to reset the working tree to b1c3a584d1 and redispatch.
- **Step 3's design record** is written, uncommitted, at `docs/design/playing-harness.md` (39.8 KB, twelve sections). Its planner's brief is the first block of `docs/push/step-3-planning-prompts.md` (new, uncommitted). Craft calls it took: a seventh policy beside the six in `bot.ts` (the live offer's nearest body, else nearest food, else HOME; belches on `belchingPolicy`'s rule); nearest body with `chooseOfferBody`'s tie-break; the knobs as a hold range (steady 0, loose 0 to 6, shaky 0 to 12 ticks, drawn uniformly, a stated departure from decision 17's Gaussian because ADR 0015 forbids the transcendentals) and a look-ahead prefix (far, middling, short), nine names `steady-far` to `shaky-short`; the hand's stream made in `src/dev` from the run's seed and never in `RunState`, so `WITNESS_VERSION` stays 6; batches of 48 seeds, tapes under `local/batches/`, five-number summaries with the extreme seeds named; header field `policy`, a person's run records `person`; five rigs named (#107). **Section 12 holds two ADR-level calls, marked as taken under the default with Mark's review on the branch before merge (the edit landed in session 17):** (1) the base policy belches (widens ADR 0053's verb list; default yes, else story 12 stays open past step 4), (2) the director's budget is pinned to the build, never in the header (closes ADR 0056's open question; default yes, so this step's format bump carries one field). Commit the record with `docs(hungry-grave): playing harness design record (#98)` once the coder's commits are in.
- **The dispatch plan landed** at d9317d7ae1: `docs/design/step-3-playing-harness-dispatch.md`, 93 KB, 45 spec tests, 30 module tests, 7 fences, nine slices (0 to 7, slice 4 in two halves), A through 4b, the knobs as slices 5 and 6. Its planner found: the airborne figure #39 wants is unreadable off a headless tape (the plan adds `shotsAlivePerTick`); the record's batch-cost estimate is about three times optimistic; five modules unowned each with a trigger (store column #100, stripped-rung rate #99, dev autopilot #39, fixture tape #109, persona roster off the road); `comparisonDeclared.test.ts` walks every report leaf so every new reading needs a `READING_COMPARISONS` entry; and the standing line "a `.ts` under `local/` breaks `pnpm build`" looks stale against `tsconfig.json` and `eslint.config.mjs` (two `.ts` files sit there now), so the coder contract's reason may be wrong even if the habit stays.
- **Three gates ran on both records, markers on #98.** Vision gate (comment 5595493848): findings, none blocking; apply-now: (1) record section 5 cites a budget column on the phase (`stage.ts:47-78`) that does not exist, so commitment 2 should stand on ADR 0056's wording and step 4's unbuilt shape, and say a step 4 reversal still bumps before the store; (2) commitment 1 widens ADR 0053's verbs but `CONTEXT.md:201` and the concept box's V1 line still say four, so slice 1 restates both with a citation; (3) spec test 7 in slice 1 reports reach on the pinned seeds and names #39 as where a low sharp reach goes. File later: #107's disposition at step 3's close is unstated (a comment on #107). Design gate (comment 5595495659): findings, none blocking; apply-now: (1) the sloppy corner is a sharp hand: shaky's 0 to 12 tick hold is Counter-Strike's Elite rung and inside Talakat's low-dexterity tail; a shot at 1.83 units a tick cannot cross a 27-wide grave inside the hold, so rows of roughly loose 0 to 15 and shaky 0 to 36 (600 ms) before the first batch; (2) three promised readings (the Waking's property, the spiral-versus-comeback split, grave-to-mob scale) have no mechanism and are not in the declared table; define each as a one-hand reading (swallows inside the Waking span, floor visits and what followed, size series against mob widths) or strike it; (3) the hand's own rows (clearance, belch threshold, holds, samples) are never tuned with the game's at step 4: a changed hand row is a new configuration name; (4) a failure read for the done line: sharp-low is the first tuning reading, never grounds to sharpen the hand. Later, for #39: the spiral split is hand-bound (the base hand dives at any size and never flees), print it as direction only. Wording gaps in its report: the hold delays the belch; the short head moves the wanting's settled point; story 12 reads as a fire count. Tech gate (comment 5595511284): findings, none blocking; every file-and-line claim checks out; apply-now: the airborne figure is the storm's pools per line with mob fire beside (not `shotsAlivePerTick` on mob fire alone); the person's reserved policy names live in `src/tape/tape.ts` so `src/dev` never reaches `src/app`; `DeclaredBatchReading` declares its reduction kinds including not-reduced; one stream name `HAND_STREAM` and test 51 rewritten to it; #113 closes by deriving `NAMES` from `createRun(0).streams` plus `HAND_STREAM` (comment on #113); three seam slips (`recordedAt` source, slice 1's unused `seed`, the shell's fixed output root). Later: rig names exist only in prose (#107). The "`.ts` under `local/` breaks `pnpm build`" line is confirmed stale. **The fixer landed all three:** records c8ee3cb42e (46 spec, 33 module, 7 guards, 86 tests; tests 17 and 18, which sat in no slice, now land in slices 1 and 5), coder contract d3db6f4b31 (`docs/push/step-3-coder-contract.md`), first slice prompt db428611d1 (`docs/push/step-3-slice-prompts.md`, block "Slice 1, as dispatched in session 17"). Pushed. **Slice 1 (the hand) LANDED: code 66dfcea268, note d7a0ba9eb5** (`docs/push/step-3-progress.md` sections 1 to 8). 31 tests added, GOLDEN, `WITNESS_VERSION` and `READINGS_VERSION` unmoved, ADR 0053 amended in place for the belch (Mark's step 15 to review), `CONTEXT.md` gained Configuration, Sharp hand and sloppy hand, Rig. Readings: the hand enters the Undertaker's phase on 3 of 5 pinned seeds where the birthright dodger reaches it on none; it seals on 3 of 5 (clearance row 12); seeds 101 and 404 open no offer at the birthright because no carrier dies on the lane the hand steers (spec test 7's plan claim false; it now pins the measured seed sets; #39's first tuning input). Hand-forwards in section 8: the hand walks at a banked offer's body above the top edge (a reading for 4b, not an edit); reserved names unread until slice 3; slice 5 owns the stream, the seed, the eight rows. Deployed per the recipe (slice 1 changes nothing a player sees). **Slice 2 (the event fields and the readings) LANDED: code c784a356e5, note 18bd764c33** (note section 9). 14 tests, GOLDEN, `WITNESS_VERSION`, `READINGS_VERSION` unmoved, CodeRabbit zero findings. Four plan claims false (in the note): `OfferSite` lives in `src/game/events.ts`, not `src/dev`; `standOffer` takes the site fourth; `readings.ts` has five declaration sites; plan section 4's `measure.ts` block folds in three slice 3 items. Process slip, recorded: the coder wrote the implementation before its tests and paid it back with nine mutations; slice 3's prompt says not to repeat it. **Session's call, no ticket:** five whole-stage `dodgePolicy` tests in `bot.test.ts` time out intermittently under parallel load, never on an assertion, pre-existing since before step 3; slice 7 gives them a stated budget in that file's existing idiom (slice 1 already did so for two). Deployed. **Slice 3 (the header field and the one bump) LANDED: code 4093d4be81, note f254c2551b** (note section 10). Seven tests added, one renamed (the version-2 refusal at version 3, plan section 7's own instruction), `FORMAT_VERSION` 2 to 3 once, GOLDEN, `WITNESS_VERSION` 6 and `READINGS_VERSION` 2 unmoved; verification step 7 passed (both format-2 baseline tapes refused, exit 1, "this tape is format version 2 and this reader is version 3"); ADR 0056 amended in place (the director's budget build-pinned, the session's commitment under Mark's review, with its reopening trigger); CodeRabbit one major applied, three declined with reasons. Three plan claims false in the note; the design record's section 5 "refuses either way" sentence was the session's to fix and is fixed (dated). **Mark's step 15 is now answerable**: both ADR amendments (0053 the belch, 0056 the budget) are on the branch. Deployed. **Slice 4a (the runner) LANDED: code eb41654b11, note ffa8560e87** (note section 11). Eleven tests, four new files and no existing file edited, GOLDEN and the three versions unmoved, CodeRabbit no findings. The first batch: six seeds from 20260909 under `steady-far`, 10.45 s wall clock, about 1.6 s a run (the plan's 45-minute estimate for nine configurations is nearer twelve), tapes at `apps/hungry-grave/local/batches/steady-far-1788934491101/`; two measured by hand, both verified and attested; **seed 20260910 is the first harness run to kill the Undertaker** (victory, four offers, four rungs), a reading for #39, no row moved. One plan claim false: `<count>` is required until 4b creates `BATCH_SEEDS`. Deployed. **Slice 4b (the report) LANDED: code 876aa63f72, note 6f8807b21d** (note section 12). **Half A is complete: the harness plays, records and reports under one hand.** 22 tests added, one renamed to the plan's wording, sixteen mutations (two found real holes, now pinned), GOLDEN and the three versions unmoved, CodeRabbit no findings. **The first 48-seed batch under `steady-far` (seeds 20260909 to 20260956): 48 of 48 verified; 29 reached the Undertaker's phase, 7 killed him; endings 41 sealed, 7 victory; 2 min 25 s to play and measure.** Tapes and `report.json` at `apps/hungry-grave/local/batches/steady-far-1788937370786/`, recorded against 5f7f365e99: that is the sharp half slice 6 compares against. Seven plan claims false, all in note section 4 (the reduction union is seven kinds, not five; `peak` readings are series; `batchReportOf` derives the commit hash; `byLine` and `phaseSpans` are Partial; guard 81 lives outside `src/dev/__tests__` because the fence forbids `node:fs` there; the record's "charge carried into the boss span" is not readable off any tape). Hand-forwards in section 12: `compareBatches.ts` joins guard 81's module list at slice 6; nine configurations cost about 22 minutes measured (play plus measure), verification step 14 still wants the split stated; slice 5 owes no `BATCH_READINGS` row; slice 5 owns `HAND_STREAM`, the seed on `harnessPolicy`, the eight other rows and the widened `ConfigurationName` (read by `BatchOrigin` and `BatchIdentity`, which widen with it). Deployed to hungry-grave.vercel.app. **Mark's step 18 (does the report read) is answerable from note section 12's table; step 17 needs the sloppy corner.** **NEXT: dispatch slice 5 (the two knobs, B begins; closes #113)**: write its block in `step-3-slice-prompts.md` in the shape of the 4b block (the plan's section 10 paragraph for slice 5, the test numbers 10 to 16, 18, 33 to 35, 49 to 51, 53 and guard 82, the hashes so far, the hand-forwards above, the design-gate knob rows loose 0 to 15 and shaky 0 to 36 as the fixer wrote them into the records, verification step 9 the determinism run under `shaky-short`, `CONTEXT.md` gains Dexterity error and Strategy error, `rng.test.ts`'s `NAMES` derived from `createRun(0).streams` plus `HAND_STREAM`), commit the block, dispatch one Opus coder with the slice 1 launch preamble, mark the list row "in flight". Then slice 6 (the comparison and the done line: steps 11 and 12, the two reach rates in the note and on #98, never judged), then slice 7 (the fences and the full pass; gives the five whole-stage `dodgePolicy` tests in `bot.test.ts` a stated budget in that file's idiom, the session's call, no ticket). Then the step end: tech and vision gates on the whole step, findings filed, CodeRabbit on the whole branch, the ticket sweep (#98, #107 closes when every figure names its rig, #113 closes at slice 5), the #98 resolution comment, close, push. **Mark's follow-along list is rewritten (2026-09-09) to be his only**: plain rows, statuses of a few words, no findings or hashes (memory `follow-along-list-plain-words`); keep it that way. Slices go 2, 3, 4a, 4b (A ends; verification step 10's 48-seed batch table goes in the note), 5, 6 (steps 11 and 12; the two reach rates go in the note and on #98, never judged), 7. Gate the whole step at its end (tech architecture and product vision), file findings, CodeRabbit on the whole branch, the ticket sweep, the #98 resolution comment, close, push. If the coder is gone with its commits absent, `git status --short` shows its partial work; decide between finishing in a fresh coder from the same block or resetting to db428611d1 and redispatching.
- **Record claims found false by the planner**, for the plan's section on claims: `bot.ts` has six policies, not four; "the offer goes into the existing dodge policy" describes a hand that never feeds; `record-conditioned.ts` steers a fixed wander so the harness inherits its header and not its steering; no production module writes `inputDevice: 'bot'`, so a scripted wander and a dodge-bot run share a header value today.
- **Filed as #113** (the stream collision guard covers four names by hand while `StreamName` has five; approach in its comment; the fix fits the step 3 slice that adds the sixth stream). Note for the record: `gh issue create` with an inline `--body` was refused by the permission classifier; `--body-file` from the scratchpad went through. The repo has no `hungry-grave` label.
- **Next after the record commits: the dispatch plan**, `docs/design/step-3-playing-harness-dispatch.md`, one Opus planner, the ten-section shape of `step-2-stage-floor-dispatch.md` (what the plan may claim; definition; already built, partly built, absent; verification steps with actors; seams under test; module boundaries; planned test list; constants changed with every reader; craft calls with evidence; new commitments; slices, each one agent's commit). Then the three gates on both records (markers on #98), a fixer for their findings, then slices one Opus coder each with a step 3 coder contract in the shape of `step-2-coder-contract.md`. Add the step 3 rows to the follow-along list when the slices are planned.
- **Usage at the pause (2026-09-09T11:30Z):** session 7%, weekly all 66%, weekly Fable 83% (caution 85, stop 95). Check `bash /home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/usage.sh` between waves. Mark asked that the next session start on Opus.
- **The Cabinet is paused** (its handoff at the main checkout's `continue-cabinet.md`); Mark chose Hungry Grave for this session with "keep going". He also ruled this session: the context watchdog never stops the work and never asks him to clear, but the handoff still gets updated when it fires (memory `watchdog-means-handoff-not-stop`).

**Session 16 (2026-09-08) closed step 2.** Slices 15 and 16 landed as one coder's commit 715575a37d (note b38cfcb687, progress note section 27): the fences green by title, the stale curtain todo in `palette.test.ts` filled, the `rows.test.ts` timeout fixed by splitting one full-build test into three per-seed tests, every agent-actor verification step 1 to 15 re-run at the tip (seven phases in order on seed 101, both endings on all five seeds, the eight rendered reads and the grayscale check obtained, `WITNESS_VERSION` 6, GOLDEN unmoved). Deployed to hungry-grave.vercel.app as b38cfcb687 and the follow-along rows for 15 and 16 updated. The two step-end implementation gates ran (tech architecture and product vision, markers on #97, both findings and none blocking) and every finding was filed under Mark's rule, never built: **#104** (killing the Waking's source forfeits its pour, his question), **#105** (the length band names no hand), **#106** (the victory topple has no code path), **#107** (two rigs under the "maxed dodge bot" label), **#108** (two new draw sites on shared streams), **#109** (tape build identity never compared, no fixture tape), **#110** (`stormTargets.ts` wants an ADR and a fence), **#111** (`tapeStore` one-way failure flip), plus comments on #86, #98 and #38; the `firedRows` sweep is **#112**, because the plan's #59 was closed before the plan was written (item 82). The whole-branch CodeRabbit pass (section 28): 23 then 19 findings, 15 fixed across 5290784bab and d5a637fc25 (two real test defects, the rest records), 27 declined with reasons, no production module touched. Section 29 is the close. Steps 16 to 22 are Mark's and open; the deployed build lets him answer 16, 17, 19, 20, 21 and 22 by playing, and 18 needs a hand that belches, which no bot has. One record gap the pass could not close: the music licence line below names no pack or page, and only Mark has that.

**Session 15 (2026-09-08) ran the design gate on adjustment 2, built and deployed adjustment iteration 3 on its one apply-now finding, filed its other finding as #103, and wrote the slices 15 and 16 prompt.** The gate's verdict (marker on #97): the ruling holds. Five seconds of dormancy is ordinary for the genre (Ikaruga's chapter 4 midboss opens fire about two seconds after arriving; the genre rule is "wait a second before firing"; Nex Machina's postmortem names long waits as the disengaging thing), riding the terrain is the genre's own convention for a ground target, and the choice window did not shrink because the whole cut was dormancy and the pour's fifteen seconds are unchanged. Its apply-now finding: the pour now runs to about y 752 and `resolveMobContact` checked a body the tick it existed, so the tail materialised inside a bottom-parked grave's box, a hit with nothing to see coming. The session's ruling, reviewed by Mark before merge: **a body that appears inside the field cannot touch the grave until its arriving beat has run**; a body crossing the top edge is unchanged; the pour rate row does not move, because the caps were priced against it and a faster pour only moves the band. Adjustment 3, code d58448a6e3, note 3aec16e1ef (progress note section 26): `Mob.appearedInside` written once in `spawnMob`, `canTouchGrave` in `mobs.ts`, read by `resolveMobContact`; no new number, the window is `ARRIVE_TICKS`; `WITNESS_VERSION` stays 6 (the field joins the excluded half beside `mobs[].type`, a CodeRabbit Major declined for that reason); GOLDEN unmoved; the seed 101 maxed-dodge run identical either side. The coder stopped once on a real finding and was ruled on: "zero contact hits for a bottom-parked grave" was never the record's promise (a body falls 42.75 units in its 45-tick beat and a ceiling grave's box is 135 tall, so a body that appears inside a big grave is still inside it when its beat ends, and section 2's parking paragraph already says a grave under the mouth takes contact shrink). The promise is "no hit from a body still arriving"; the run-seam spec test was dropped because `graveHit` names `contact` and never a body, and the two module tests carry the ruling. Measured: ceiling grave 36 to 31 hits over six seeds, floor 30 to 29, property 63 to 25 both sides. Two plan claims false: `digUpBody` in `undertaker.ts` is a second inside spawner and is covered deliberately; the zero-hits reading contradicted the record. `CONTEXT.md`'s Arriving beat entry and `stage-floor.md` section 2 amended in place, dated. Two environment findings in the note: a Blender render from the `cabinet-app` worktree took nine cores mid-verification and reddened the suite twice with timeouts and no assertions; and `prettier --write` on a named path under `docs/` rewrites the file despite the ignore list, so nothing under `docs/` goes to prettier by name. Deployed as of this handoff: the note commit 3aec16e1ef. The follow-along list rows are updated (adjustment 2's gate result, adjustment 3 done and deployed, slice 15 folded into 16).

**Slice 15 has no code left and is folded into slice 16 as its first item.** Fence 110 was extended onto `bosses/` and `setPiece.ts` at slice 6 (progress note section 4, item 41), and the session ran the four fence files at the tip: all green. Two stale todos found there: `palette.test.ts:460` waits for the Undertaker's curtain, which has existed since slice 8 and is drawn since slice 14, so slice 16 fills it; `boundary.test.ts:247` waits for a folder that does not exist and stays. The slices 15 and 16 prompt is the last block of `docs/push/step-2-slice-prompts.md`, committed with this handoff, and carries the `rows.test.ts` timeout fix, the todo, and every agent-actor verification step re-run at the tip.

**Step 2 (#97) is being built, one Opus coder per slice, slices 0 to 14 and three adjustments landed and deployed.** Every hash is in `docs/push/step-2-progress.md` section 1; section 4 holds the plan claims found false, section 7 the verification steps run, and sections 8 onward one section per slice. Read section 1 and the last two slice sections before each dispatch. The dispatch mechanics: the coder contract is the tracked file `docs/push/step-2-coder-contract.md`; each slice's dispatch is a short prompt in the shape of `docs/push/step-2-slice-prompts.md` (the slice's paragraph from plan section 10, the test numbers, the hashes so far, the hand-forwards), launched on an Opus general-purpose agent with a preamble naming the worktree, the prompt file, the one-command-per-call rule for git, and the under-300-word report. Slice 0's baselines are gitignored at the worktree root under `local/step2/`.

**Session 12 (2026-09-08) landed slices 12b, 13a and 13b** (f23d8eb162, 3a7a85d5b1, 7570270a80; progress note sections 20 to 22). Verdicts that still matter: only Chromium was measured for the loop gap, no Firefox or WebKit; the import script desaturates in-process through `pngjs` and applies a percentile stretch because a tint multiplies and the faithful luma import peaked too dark, so the floor draws about a third brighter than the sheet and slice 16's grayscale check judges it; the Waking phase wears the Crowd's dressing; run the test suite alone.

**Session 13 (2026-09-08) landed ground adjustment 1** (21b5de181a, note 88f2280d3c, progress note section 23) on Mark's three phone notes: the ground runs at the field's own scroll, the floor is baked at import from an authored map of the five calm cells, the dressing is a row of 30 in flight. **Session 13 also landed slice 14** (c2a2ac3e52, note c927212fcd, section 24): both bosses as vector silhouettes, the chunk flash at `FLASH_HALF_PERIOD` 12 (5 was over the WCAG 2.3.1 line), boss fire in its own sprites. Verdicts that still matter: the Undertaker's inverted beat sits close to the Vigil's teal ground and only his rim, eye slits and shovel hold his shape, triggered to #38; steps 17 and 19 are Mark's; hand-forwards (b) and (c) below are resolved by slice 14, check the note for test 66 and spec tests 115 and 116 before assuming.

**Session 14 (2026-09-08) landed adjustment iteration 2, the Waking rides the ground** (e636411950, note 820f7c19d7, section 25) on Mark's ruling (a): `SET_PIECE_DRIFT_SHARE` dropped, `SET_PIECE_OPEN_DEPTH` 0.25, `SET_PIECE_PLACED_AT` 135, the eye opens at the same tick and is on screen 20 seconds against 35, dormant 5 against 20. Verdicts that still matter: GOLDEN cannot move for anything past the Procession (its scenario is 600 ticks inside it); the Vigil and the Undertaker arrive 830 ticks later on seed 101 because the bot plays a different trail, so a run crossing ten minutes at slice 16 is a finding to report, not retune; ADR 0050 never said "slower parallax", decision 25 did; the storm seam reads the source's `vx` as zero, now ticket #103.

**Hand-forwards slices 15 and 16 must know.** (a) **Mark ruled on 2026-09-08: the two bosses are vector silhouettes in their palette pairs, as plan section 8 and the design record's dressing section say; the grayscale creature cut-outs slice 13a imported are not used for bosses.** The slice 14 prompt is written to that ruling. (b) `mobFireSprite.ts` still draws every boss shot in `MOB_FIRE.trash` until slice 14. (c) Module test 66 and spec tests 115 and 116 were named todos until a boss phase and the set piece were real; check the note before assuming they are filled. (d) A phase ends the tick its field clears, so a rig that silences one phase rolls into the next section's rows (the `firedRows = ROWS.length` idiom is wrong in thirteen files, left for #59 with the trigger in the note). (e) The hit-taking policy seals before the first carrier, so `STRIPS_A_RUNG` in `bot.test.ts` is pinned empty with its cause. (f) `WITNESS_VERSION` is 6 and must not move again. (g) No dodging birthright run crosses the stage; headless victory runs pin the loadout through `createRun`'s third parameter. (h) Editor diagnostics name scratch files and stale states; `pnpm typecheck` is the judge and has been green at every tip.

**Deploying a slice to hungry-grave.vercel.app (Mark asked, 2026-09-08, that every slice be deployed as it lands).** The recipe that works, and the trap it replaced: `git worktree add --detach <scratchpad>/deploy <note hash>` (later slices: `git -C <that path> checkout --detach <hash>`), copy `apps/hungry-grave/.vercel` and `.env.local` from the worktree into it, then in that checkout `CI=true pnpm install --frozen-lockfile`, then in its `apps/hungry-grave` run `rm -rf .vercel/output`, `vercel build --prod`, `vercel deploy --prebuilt --prod --yes`; the deploy log prints "Aliased https://hungry-grave.vercel.app". **Never symlink the worktree's `node_modules` into the deploy checkout**: `vercel build` runs `pnpm install`, and through the symlink it rewrote every link in the worktree's `node_modules` to point at the deploy folder, which broke the worktree the moment that folder was deleted (slice 12b's note records the repair: `CI=true pnpm install --frozen-lockfile` at the worktree root). The deploy checkout is per session and gone; make a new one (session 15 used the scratchpad's `deploy` folder and removed it with `git worktree remove --force` after). Deployed as of this handoff: adjustment 3's note commit 3aec16e1ef.

**Mark's follow-along list of every step and slice is at https://md.niftymonkey.dev/v/CTtB5BJJ (slug `CTtB5BJJ`, kind `roadmap`).** He reads it to know which slice a report is about. When a slice lands or deploys, update its status cell with the md-niftymonkey skill's targeted edit (a `replace` op on the row), and add rows when a later step gets its slices planned. One URL, never a new upload.

**Found on Mark's first phone play (2026-09-08): an iPhone on silent plays no sound and the game says nothing.** Ticket #101 carries it, with the approach in a comment (a silent HTML audio clip on the first tap un-mutes Web Audio on iOS; WebKit bug 237322; libraries swevans/unmute and feross/unmute-ios-audio). Mark placed it last on the path (`path-draft.md` item 8) and decides at the end of the current v1 work whether it stays in v1. **Found on his second phone play (2026-09-08): a fast slide from the field's edge starts the browser's swipe-back gesture.** Ticket #102, approach in its comment (Android: `overscroll-behavior-x: none`; iOS: standalone web app, or an edge-band `touchstart` preventDefault measured first). Path item 9, beside #101, same end-of-v1 decision.

**Step 1 (#96) is complete, pushed (f1f376719a) and closed.** Slices 9 and 10 landed (fences 8a7ecc5c31, verification pass note 080aa1084b), then the two implementation gates ran (tech architecture and product vision, markers on #96, both "findings, none blocking"), their findings were applied (records 87e9b2268a and the gate-findings commit, lesson 7de1fe64a4, code 6b6f01671d: `TERRITORY_CAP` home in `territory.ts`, the fence exception narrowed to the two safety nets, a fence that only `offer.ts` draws from the drops stream), and CodeRabbit ran on the whole branch against `main` (16 findings: 6 fixed in a02f5980dc and e22a2111df, 10 declined with reasons in `docs/push/step-1-progress.md` section 15). The progress note has 15 sections and is the step's full record. Verification steps 11 to 13 are Mark's and stay open; step 12's number is 308 ticks from a strip to the next take on one seed. The gate deferrals are filed: #31 (the gold carrier tint question), #39 (level-one bell reading, strip recovery), #98 (take-by-slot on banked offers), #99 (the bank's field-side form closes #96's visibility criterion). The step 2 plan gained a block "Added after step 1's implementation gates" (the carrier column is `carries: boolean`, the bank's tick-phase opening site with `Phase.bankOpens`, supply never vanishes at a cap; tests 132 to 135).

**Ticket #96 is closed** with its resolution comment (tip hash, verification steps 11 to 13 open, the #99 note on the bank criterion).

**Step 2 (#97) is planned, gated three ways and committed as 97af4ae66d, not dispatched.** All three gates reviewed the records (markers on #97: game design, product vision, tech architecture; each verdict findings, none blocking). The design and vision findings (thirteen) are applied: Procession, Crowd, Vigil; three loops ruled, six a proposal; the Crowd ends on the eye opening; the Waking's source outlives its pour with an hp row (Moai shape, immune while dormant); every chunk survives one full emit; sprites desaturated on import and coloured by tint; corpse cap counts boss adds and rung bodies; food per second per section is the scarcity measure; the licence item removed. The tech findings (eight) were being applied by an Opus agent when this was written: section ceilings are a director spend gate, never an always-on invariant (the session's reconciliation of the two gates; tests 4 to 6 hold under a stated killing policy); one target seam so no weapon line learns a boss exists; boss fire carries a fire kind with every reader listed; pour rows live in the data module, not setPiece.ts; WITNESS_VERSION moves once with boss and setPiece folded together; audio measures the loop gap first with per-loop start and end rows if needed, no header parse; slices 12 and 13 split so each is one agent's commit; `pngjs` as the fence's PNG reader. The fixer reported and both records landed together as 97af4ae66d, `docs(hungry-grave): stage floor design record and dispatch plan (#97)`. If the plan still says the ceilings are invariants or slice 12 still parses a LAME header, the fixer did not finish: re-read the tech marker on #97 and apply it. The two files in `docs/design/`: `stage-floor.md` (the design record: sections first named the Lane, the Crowd, the Descent from the research record, renamed by the vision gate's collision finding; the set piece is the Waking, a dormant eye on the ground layer that opens mid-field and pours trash, shootable, never touching the grave, its kept property being that committing to the trail pays more than waiting at the bottom edge) and `step-2-stage-floor-dispatch.md` (67 spec, 61 module tests, 7 guards, the counts the plan's own line 890 states after the three gates' findings landed). The planner's section 9 lists nine items it treated as assumptions; the dispatching session reads them, takes the craft ones (section names into `CONTEXT.md`, six loops so a boss never inherits its section's loop, phase end on the sparse row's condition, `Phase.music` as an alias string on the phase, `corpseEvicted` retired, `MobFired.emitter` widened to bosses, scarcity as fewer tougher bodies, #81 kept out), and passes the one that is Mark's (the music licence, below). Three record claims the planner found false: `game-concept.md:48` and `decisions.md:97` say the Descent's kills "pay less food" but `mobs.ts:94` pays the revenant double (the property stands, the mechanism was wrong; #86's sweep corrects the sentence); #50's line citations have drifted (substance holds); `tracer-plan.md:13,46,184,197-198` still read as current (#86 owns). Also: the dodge bot cannot beat a real boss from the birthright, so headless victory runs pin the loadout through `createRun`'s third parameter (`run.ts:154,190`).

### The next steps, in order

1. **Done: step 1 pushed and #96 closed.** Mark allowed the session to push from the worktree (a permission grant on 2026-09-08), so later pushes need no one.
2. **Done in session 13: adjustment iteration 1 on slice 13b's ground** (see "Where we are"; Mark's read of the deploy is open, and his answer on the Waking's drift is the one thing needed). His notes, kept for the record: One Opus coder, one slice-shaped dispatch (contract, prompt, tests first, CodeRabbit, two commits, deploy), before slice 14. His three notes, in his framing: (1) **The territory splotch moves at a different rate than the ground under it**, which makes it read as not real, not useful, not cohesive. Territory is a lobbed thing that lands on the ground and becomes hands pulling at mobs and damaging them, so it belongs to the ground. The splotch is a sim object in field space and the background scrolls on its own parallax, so the two candidate answers are a render-side one (the ground layer scrolls at the rate field objects fall, so a landed patch and the tile under it stay together) and a sim-side one (a landed patch drifts with the ground, which changes play, moves GOLDEN, and wants the design gate). Measure which rates differ before choosing; bring the sim-side one to Mark before building it. (2) **The tiles are placed at random and spun**, and he wants them fitted together so the ground reads as one real piece of ground rather than randomized blocks: use the tileset's own edge and corner cells as a coherent layout, not a random pick per cell. (3) **The dressing is too sparse**; noticeably more statues, urns, rocks and the rest per screen, same families and colours. He likes that the background scrolls; keep that. A rendered check per section, screenshots in the scratchpad, and the deploy, then update his follow-along list.
3. **Done in session 15: the design gate on adjustment 2, and adjustment iteration 3 on its finding** (see "Where we are").
4. **Done in session 16: step 2, the stage floor (#97), complete, closed and pushed** (see "Where we are"). The step's mechanics, kept because step 3 repeats them: one Opus coder per slice with `docs/push/step-2-coder-contract.md`'s shape plus a short slice prompt; the launch preamble (worktree and branch, main checkout never touched, one plain command per Bash call for anything naming git, no stash, no `git add -A`, no em dash, `pnpm` never `npm`, single-line `-m` messages ending in the ticket, `CI=true` if pnpm refuses a non-TTY, under-300-word report) and the playbook line the dispatch hook requires (read `docs/agents/feature-playbook.md`; the contract items are the plan's sections); deploy each slice as it lands (recipe in "Where we are") and update the follow-along list; gate the whole step at its end (tech architecture and product vision), file findings under Mark's rule, CodeRabbit on the whole branch, the ticket sweep, the resolution comment, close, push.
5. **Session 17 landed the #104 adjustment and step 3 through slice 4b (half A). NEXT: slice 5; the exact next actions are in the session 17 bullets in "Where we are".** The original plan for this item, kept: **step 3, the harness, A then B** (`path-draft.md` item 3): the offer in the dodge policy (which body it walks to is an open craft call, nearest body or lowest-level line), a seed batch runner, a distribution report, the policy header field, then the two error knobs (decision 17). Needs steps 1 and 2, both landed. Plan it the way step 2 was planned: a design record and a dispatch plan in `docs/design/` from the record (`playing-harness-precedent.md` is the research; #98 carries the ticket, with the vision gate's comment on story 12's belch), grilled with docs and domain-modeling, then all three gates on the records before dispatch, then slices. **FIRST: the #104 adjustment slice**, Mark's ruling that the pour finishes anyway (the comment on #104 carries the ruling and the slice's shape), one Opus coder in the adjustment-iteration shape of sections 23 to 26 of the progress note (prompt block in `step-2-slice-prompts.md`, contract, tests first, CodeRabbit, two commits, deploy, follow-along row, then the design gate on the landed change). Then step 3's planning. #107 (two rigs under one label) is a step 3 planning input, not a separate fix. Batches write local tapes until step 6 exists.
6. Steps 4 to 7 per `path-draft.md`, one writer at a time, `usage.sh` between waves, to the done line.

### Needed from Mark

- **#117, and it is the one real question waiting for him.** Whether the sloppy corner has to be able to reach the bosses, or whether the corner a finding must agree across stops being the bottom of the ladder and becomes a middling rung. It touches the done line, so no agent rules it. The 48-seed evidence from slice 6 should be on the ticket before he reads it, and session 19 added the piece that reframes the question: `loose-far` reaches on 32 of 48, statistically the same as the sharp hand's 29, so the choice is not between the bottom rung and a middling one. Every rung measured so far is either as good as perfect or dead in the first section, and the cliff between them has never been played. Everything else on this branch continues without his answer, because the nine rows already exist and nothing is blocked on which two of them the agreement rule names.
- **For his review before merge, not a blocker: the hold became a lapse of attention** (session 18 above, design record section 3, dated amendment). His question drove it, the evidence is cited in the record, and the two rates are undefended starting rows that #39 moves.

- **Answered, kept for the record: the Waking's drift.** Mark ruled (a) on 2026-09-08: the source drifts at the field's scroll. Built as adjustment 2, gated clear in session 15. The eye is on screen 20 seconds against 35 and dormant 5 against 20; step 19 (does the Waking read as heaven) is a read of that shorter moment on the deployed build.
- **For his review before merge, not a blocker: adjustment 3's ruling.** The session ruled that a body that appears inside the field (the pour's bodies, and the Undertaker's dug-up bodies) cannot touch the grave until its arriving beat has run, and amended the glossary's Arriving beat entry, which used to say "movement only", to say so. The alternative was a faster pour rate, rejected because it only moves the band where the tail lands. He reviews it on the branch as he reviews ADR-level calls in one-push mode.
- **Answered, kept for the record: #104, what killing the Waking's source buys.** Mark ruled on 2026-09-08: **the pour finishes anyway.** A kill removes the source's body and nothing else; the remaining budget keeps pouring until spent. The ruling and the slice's shape are the comment on #104; it is the adjustment slice that runs before step 3's first slice.
- **Answered: the music pack** is in the licence line under "Settled this session". Assets are placeholders and licensing is never raised again.
- Otherwise nothing. Start the next session from the pointer file with "let's keep going"; the stuck rule and Discord cover anything that needs him while he sleeps.

### Deferred by the tech gate to the store step (step 6 spec)

A failed upload (an offline phone) is a state, not an error: the tape must survive locally until the store acknowledges it, because the store retires the only other way off the phone.

### Settled this session (all Mark's, 2026-09-07)

- **Deploys from the cloud are allowed** (item 4 stands). He minted a Vercel token with `vercel tokens add`; it is on the cloud environment.
- **Item 7 (roadmap) struck.** No roadmap edits, no artifact republish. Tickets are the status source; he asks and the session reads them.
- **Assets staged on the branch** under `apps/hungry-grave/assets-staging/`: 20 music loops converted to mp3 on his machine (`music/manifest.md` has names, durations, sizes; the pack had no license file), and the two pixel packs (`crawling-depths`, `scarymobs`) copied raw. The whole folder is removed before merge. Raw wav never enters git. Music craft note for the push: mp3 loop gaps are a known browser issue; the encoder wrote LAME headers, and the loop path should decode through the Web Audio API and trim by the header, measured before trusting it.
- **itch.io is a sourcing path** via its API, verified on a free pack (three curl calls, in `pre-authorizations.md` item 9). Butler cannot fetch web-uploaded packs; a 2022 forum question on exactly that got no answer. Kenney zips are found by scraping the asset page for `kenney.nl/media/pages/assets/.../*.zip`.
- **Cloud environment `hungry-grave`** exists and is unused; the dry-run records (`dry-run.md`, `dry-run-results.md`) live on the archive branch `hungry-grave-v1-cloud`.
- **The bell arcs get built, option A**, inside path step 1, not only filed. Mark's reason: tuning against a starting weapon that is known to be wrong would tune the wrong game. The design is below.
- **Weapon lines stay extensible**, a standing constraint in `path-draft.md`: tuning numbers are data rows keyed by line, the harness reports by line, policies never name a line, a fifth line is a module plus rows. The tech gate checks it.
- **Personal skills** load from Mark's `~/.claude`; the branch carries none. `usage.sh` reads the local login and works here.

### Decisions 1 to 6 (all Mark's, recorded nowhere but here until the ADR step)

1. **The V1 line is redefined.** V1 is the concept proven as a game Mark can keep building. It is done when a real person's read of the game means something, and the read has happened. The line: *one stage, a final boss, tunable, played by a harness that moves, dodges, feeds and chooses in several styles, read by a person, on desktop and phone.* "Dressed" leaves the V1 line; Halloween art is a hope for later, not a promise, and Halloween as a date is not a focal point. This changes the box in `game-concept.md` and `v1_in_one_line` in `v1.yaml`.
2. **Both systems get designed inside V1** (density and progression), and **the ladder gets made visible inside V1**, because without them a person's read is dice.
3. **The playing harness is in V1, at the right place, not first.** A mechanism for the computer to play the game, dodge, power up, make choices, and test several variations before handing him a final human read. It measures, never says whether the game is fun; it runs the variations and Mark plays the winner. It runs many plays, repeated, faster than real time; human play data lands in the same place, told apart; automated play is never the only source of truth.
4. **Progression first, then harness, then density.** Reasons: the harness needs a choice to exist; a wildcard pause is a hole in the pressure signal the director must know about. (The "tune the director twice" reason weakened once decision 10 cut the director out of power; the path draft keeps the order.)
5. **The choice-sphere wildcard stays parked**, per ADR 0034. The density design carries one line, "a pause may exist."
6. **Compass corrections** landed in 1fc7a5d06d. ADR 0047's "the tuning pass (#39) runs first and #85 carries the measurement" gets fixed at the ADR step to the path's order.

Decisions 7 to 26 are in `decisions.md`. Wording rule from Mark: the game is bullet heaven with doses of bullet hell from bosses, said in that order. Progression's "tunable" closed without a ruling: the knobs are data tables, the read is the harness by style plus a person's pick when shown.

### Research records (committed; each ends with an Open items list, do not re-search those)

`docs/research/`: `shmup-stage-design.md`, `reward-delivery-models.md` (df5985fe0a), `director-precedent.md` (e00074fba2), `stage-length-with-a-director.md` (f97c318ee1), `director-knobs-and-signal.md` (d17462deb1), `playing-harness-precedent.md` (a6218d31e3), `visible-ladder-precedent.md` (41b7a5854c), `set-piece-and-final-boss-chunk.md` (602fb1cf19). Facts the push still needs: Fatshark deleted the near-kill term for Darktide (read damage and floor events, never nearby kills); the signal's constants get set once and what is retuned is gating and population; a deterministic policy needs one run per seed; relative difficulty is invariant to the policy, so a weak bot ranks correctly; no shipped game subtracts score on a hit.

### Open play questions the push must answer (no ruling yet; craft calls unless they touch a rule)

- Where human play data lands: friends' tapes and harness tapes in one store, told apart (header carries author, inputDevice, and the policy field from decision 17). Decision 21 put the store in place; the consent toggle wording is a craft call.
- Which body wins when the grave covers two of the three offer bodies (the two-touch tie-break for the tape).
- How fast a stripped player recovers at the thinner birthright; whether the gas becomes the new lean; whether boss patterns ever complete uncancelled.
- Which offer body the base policy walks to (nearest, or lowest-level line); the per-line preference is the roster's territory.
- Whether a stripped rung is a catchable field object (decision 18's open question; decision 20 says it falls as a body the dive can catch).

### ADR-step touch list and ticket facts

Done: the ADR step in 8c43e70480, the ticket sweep in session 9 (see "Where we are"). Roadmap facts kept for a later rebuild only: n85 and n86 have no `blocked_by`; p26 is superseded by ADR 0047. Standing: use `gh issue view N --json title,body,comments`, plain mode drops bodies; never ask Mark for tester names or dates.

## The bell arcs (landed as ADR 0036 and step 1 slice 6)

Mark's verbatim shape lives in the main checkout's ignored `local/design/bell-arcs.md`; the plan's section 8 carries the per-level headings row the design gate corrected it to. Landed b93d68913d; repel totals per level are in the progress note's section 9. Exact angles, reach and push per level are tuning data for step 4.

## Open items with no other home

0. **Stop hook error in this worktree, harmless, not to be chased.** `scripts/roadmap/on-v1-change.mjs:23` imports `local/publish-instruction.mjs`, which is gitignored and lives only in the main checkout, so every stop in the worktree prints a non-blocking module-not-found error. Roadmap edits are struck for the push, so the hook has nothing to do here. The hook is configured in the main checkout's untracked `.claude/settings.local.json` as `node "$CLAUDE_PROJECT_DIR"/scripts/roadmap/on-v1-change.mjs`; it started failing in session 9 when the harness re-homed the project folder from the main checkout to the worktree (visible in the agents' output paths), so it will fail in any session that launches inside the worktree. Fixed 2026-09-08 by copying that one file into the worktree's gitignored `local/` folder; if the error comes back, the copy is missing again.
1. **#82 open**: tape commit labels miss a dirty marker (`vite.config.ts:21`); candidate `git describe --always --dirty`.
2. **#37 stays open deliberately** (boss stories 12-14, fog story 22) until the path's step 2 absorbs them.
3. **540x760 records disagree** (`layout.test.ts:67`, `field.ts:4`, `CONTEXT.md:85`), docs-only fix pending.
4. **Stamp-order trigger** on PR #84's stamp thread: a second output in `v1.yaml` `outputs:` moves the stamp into the hook. Dormant while roadmap edits are struck.
5. **Stream flank narrowness**, Mark: "the edge thing could be an issue". Watch item, arithmetic in `soulStream.ts` JSDoc.
6. **`local/grill/` at the main checkout is stale** (moved onto the branch 2026-09-07); Mark can delete it.
7. **Config repo**: the rewritten `stay-within-limits` skill files are uncommitted at `/home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/` (`usage.sh` there was synced with the branch copy 2026-09-07); the worktree guard blocks git there from this session.
8. **Cloud push playbook** at https://md.niftymonkey.dev/v/NCQUpIOq (memory `cloud-push-playbook`); the three dry runs and the local ruling are its inputs when Mark next wants it refined.

## Standing rules and facts, still true

- **Gates never overrule Mark (Mark, 2026-09-09).** A gate can find, never rule. A finding that would reverse, defer, narrow or reconsider something he decided (an ADR, a decision-log entry, a ticket ruling, the path order, the done line) is outside the gate's authority: file it as a ticket, list it under "Needed from Mark" as "gate disagrees with ruling X", and build on his ruling. Gate prompts are scoped to consistency with his rulings, never their worth; the harness (ADR 0053) is a thing he absolutely wants whatever a vision gate thinks of it. Questions to him only when the session cannot make the call and it is hard to reverse.
- **One-push build mode (Mark, 2026-09-07):** one autonomous push on one branch in its own worktree; CodeRabbit CLI before every code commit; gates as relevant; ADRs are my call, reviewed on the branch before merge. Memory `one-push-build-mode.md` carries the stuck rule and the lean rule in full.
- **Stuck rule:** two failed honest attempts, or a decision only Mark can make, or an unauthorized irreversible step: stop the thread, write a stuck note (doing, two attempts and why each failed, one-sentence question), send it over Discord (`DISCORD_WEBHOOK_URL`, a curl POST with `{"content": ...}`), move to independent work; if none, write the handoff and pause. Discord only for stuck notes and the done message.
- **Usage limits:** `stay-within-limits` thresholds: session and weekly-all caution 75 stop 85, Fable caution 85 stop 95; main thread lean by default. At a Fable stop, write the handoff, commit it, chain a new session on Opus.
- **Cloud facts** (official docs, 2026-09-07) are recorded in this file's copy on the archive branch `hungry-grave-v1-cloud`; the cloud is not used for this run.
- **Vercel:** Mark is locked out of the dashboard (2FA prompt, support ticket open) but the CLI login works and `vercel tokens add` minted a token. Project `hungry-grave`, org `team_rDwpau77qippLMDxVoyeq5Ev`; the worktree's `.vercel/` link and `.env.local` are gitignored copies. Phone deploy: the per-slice recipe in "Where we are"; hungry-grave.vercel.app carries the latest landed slice.
- **Store (provisioned and smoke-tested 2026-09-07):** Neon Postgres `hungry-grave-runs` and private Blob store `hungry-grave-tapes` (store_hnGloGEqUSe3V8Nx, iad1), keys in `.env.local`. Shape: rows in Neon (runs with header columns; readings keyed by run and computing build), gzipped bytes in Blob by run id (a real tape went 737 KB to 68 KB), one upload function used by the game at run end and by the harness. Read-after-write must pass `useCache: false` to `get(url, { access: 'private' })`. The clients (`@neondatabase/serverless`, `@vercel/blob`) are added by dry-run step 3.
- **Assets, agreed:** mapping skeleton shambler, ghost revenant, cat ghoul, golem carrier, big ghost Banshee, tall figure Undertaker, eldritch thing for the set piece; the brown werewolves, bears, gorillas and zombies are out. Pixel art draws nearest-neighbour. Every asset in the build is a placeholder and will be replaced before the real game, so licensing is never a question to raise (Mark, 2026-09-08, repeated); for the record only, the music is void1gaming's free horror music pack at https://void1gaming.itch.io/free-horror-music-pack and the pixel packs are itch.io free packs. Asset location in the app is deferred by Mark to implementation time.
- **Mark's operating rule (2026-08-28):** batch mechanical already-ruled work; stop only for a new product/architecture decision, an invariant conflict, scope expansion, or invalidating evidence. Commit, push, deploy, and merge each need their own explicit yes outside the pre-authorized list.
- **Edit-in-place ADR pattern:** same question, new answer: rewrite the ADR in place, supersession note inside, filename slug follows the new title, number stays. New number only for a new question. Supersession convention: dated inline bold prose plus a what-stood / what-it-replaced / what-it-could-not-have-known triple. No YAML frontmatter anywhere.
- **Commands:** conditioned tapes `pnpm vite-node --config vite.headless.config.ts scripts/record-conditioned.ts <out> <seed> <ticks> skullStream=N territory=N wisps=N bell=N`; measure `pnpm vite-node --config vite.headless.config.ts scripts/measure.ts <tape>`; tests `pnpm vitest run`; repo gate `pnpm verify`; only `pnpm typecheck` judges diagnostics. Local play: `cd apps/hungry-grave && pnpm build && pnpm exec vite preview`. Mark saves tapes to `/mnt/c/Users/markd/Downloads/*.tape`.
- ADR set 0001-0047 canonical; no ADR carries a combat magnitude; a /to-spec spec lands as a comment on the existing ticket.
- Reading big files: `cat` over roughly 30KB gets saved to a file; use the Read tool on the saved path. In zsh, a bare `echo ====` fails and `--include=*.ts` needs quoting. The scratchpad is per session; anything a later session needs goes into `apps/hungry-grave/docs/push/` on the branch.

## How we got here, one line

Sessions 1 to 6 (2026-08-31 to 2026-09-07) ran the V1 grill to 26 decisions with eight research records; session 7 (2026-09-07) reviewed the pre-authorization list with Mark, staged the assets, created the cloud environment and ran three dry runs that ended in the local ruling; session 8 (2026-09-08) confirmed the done line, filed the ADR step, and rebuilt the branch clean off `main`; session 9 (2026-09-08) swept the tickets, planned and dispatched step 1, gated its plan, and planned step 2, ending on the context watchdog with the coder still running; session 10 (2026-09-08) landed step 1's last two slices, ran the two implementation gates and the tip review, applied and filed their findings, and stalled only on the push; session 11 (2026-09-08) landed step 2 slices 5 to 12a and started deploying each slice; session 12 (2026-09-08) landed 12b, 13a and 13b, fixed the deploy recipe, filed #101 from Mark's first phone play, and took his boss-art ruling; session 13 (2026-09-08) landed and deployed ground adjustment 1 on his three phone notes and surfaced the Waking's drift as his call; session 14 (2026-09-08) built and deployed adjustment 2 on his ruling and stopped at the context limit before its design gate; session 15 (2026-09-08) ran that gate, built and deployed adjustment 3 on its finding, filed #103, folded the empty slice 15 into 16 and wrote their prompt, and stopped on the context watchdog before dispatching it; session 16 (2026-09-08) closed step 2; session 17 (2026-09-09) built, gated and closed adjustment 4 (#104), planned step 3 (design record, dispatch plan, three gates, a fixer), landed slices 1 to 4b one Opus coder each with a deploy after every one, rewrote Mark's follow-along list to be his only, and paused at Mark's request at the end of half A.

## Suggested skills

`/domain-modeling` for the ADR step (challenge terms against `CONTEXT.md`, record decisions as ADRs). `/triage` for the tickets (drafts to Mark first; body carries problem and need only). `/to-spec` output lands as a ticket comment. `/tdd` and `docs/agents/feature-playbook.md` bind any coding; dispatches go to subagents with the dispatch contract, and a non-coding dispatch prompt opens with "Non-coding dispatch:". `/stay-within-limits` between waves.
