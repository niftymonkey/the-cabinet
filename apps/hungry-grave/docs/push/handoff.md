# Continue: The Hungry Grave

> **Last updated:** 2026-09-14, session 22, mid step 4. **Slice C is in flight:** dispatched, one Opus coder, its prompt the third section of `apps/hungry-grave/docs/push/step-4-slice-prompts.md`. Slice B is deployed or deploying to https://hungry-grave.vercel.app.
> **Branch:** `hungry-grave-v1`, worktree `/home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1`. Last code commit `4607280db1`, slice B's follow-up (slice B's own code `480b207fdf`, its docs `2485dc6c58` and `b42f828b1a`; slice B0's code `2ec6ee5efc`, its docs `12b3b4e3ea`). Step 4's plan is `apps/hungry-grave/docs/design/step-4-mow-ladder-director-dispatch.md`, its step order is that file's section 10, and the running detail is `apps/hungry-grave/docs/push/step-4-progress.md`. Cite their sections rather than restating them; this file is the state of play and the standing rules, nothing more.
> **Working tree:** the slice C coder is editing source files in it right now, so run `git status --short` before anything and stage only by path. `pnpm verify` green at `4607280db1`, exit 0: 141 test files, 1872 passed, 20 expected fail, 2 todo, five fences green at 71 tests. `GOLDEN` has re-pinned once, slice A's, and moved in neither B0 nor B, so four of the plan's five re-pins remain and C, E and F may take them. `WITNESS_VERSION` 6, `FORMAT_VERSION` 3, `READINGS_VERSION` 4 (moved by B0). Run `git log --oneline -25` first anyway.
> **Session shape:** the orchestrator runs on Fable and makes every hard call itself or in a Fable subagent; everything else is an Opus or cheaper subagent. Usage at this write: session 17, weekly all 5, weekly Fable 2; the session window resets 2026-09-15 02:30 UTC and both weekly windows 2026-09-19 18:00 UTC. **Mark waived the caution lines; only the stops apply** (weekly all 85, Fable 95). One code slice per wave, `bash /home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/usage.sh` between waves. The deploy recipe is `apps/hungry-grave/docs/deploy.md`; deploy after each slice from a scratch worktree at that slice's tip, because the branch worktree is busy with the next coder. hungry-grave.vercel.app is Mark's own phone-play address and carries the latest landed slice.

## FIRST ACTION: enter the worktree

Run EnterWorktree with `path: /home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1` before anything else. Push records are the tracked copies in `apps/hungry-grave/docs/push/`. The isolation guard refuses writes to the main checkout and refuses compound Bash that mentions git even in quoted text; use plain separate commands. The stop hook error about `local/publish-instruction.mjs` is harmless (open item 0 below).

**Fable never reads (Mark, 2026-09-09).** Whether Fable is the main thread or a subagent, cheaper agents do the searching, reading and investigating and hand Fable the result. A small Fable job runs in the main thread; a big one runs as a Fable subagent spawned with those results in its prompt. Opus subagents gather, code, gate and write.

**This file is the continue document and it is ephemeral.** It is tracked on the branch only because the worktree cannot see the main checkout's gitignored `continue-hungry-grave.md`, which is now a pointer here. Delete it when the push merges. Sessions 1 to 19 are in git history: `git show b0d41583be:apps/hungry-grave/docs/push/handoff.md`, for citation only.

## STEP 4 BRIEF: the mow, the ladder, the director

**What step 4 is.** Re-author everything authored so the game starts from a better place, then put the director on top. Mark's rulings that bind it, all 2026-09-09: (1) **the mow**: bullet heaven is mowing and mowing, getting more power and seeing more and harder enemies as you get it; density is bought with weak bodies, never tough ones. (2) **The ladder**: reaching the end with every line maxed should be rare and for the best players, but the ceiling stays high and visible; rarity and skill expression, never a lower cap. (3) The three changes (authored schedule, progression curve, director) ship in one pass, not sequenced. (4) A density ADR passes the game design gate before filing. ADR 0047 (director reads pressure, never power; four off-limits moments; rows are the floor; own seeded streams) and ADR 0056 (finite purse per phase, quiet interval after an add, signal reads harm and never a nearby kill, caps re-derived and never evicting) stand and are Mark's. Gates never overrule rulings.

**Where we are.** Steps 0 through 7 of the plan's eighteen are done, in this order: step 0 the baselines (no commit, `local/step4/tests-baseline.txt` and its `.json` sibling, 1834 names, progress note section 6); step 0a slice A0, the build identity (`26a064a064`, cwd fix `136a349aeb`, sections 8 and 10); step 1 round 0, the frame budget (`c23be6156c`, the pool refusal `3594fe154a`, section 9), desktop done and **the phone half still owed and Mark's**; step 7 slice A, the mob table and fire (`d4dedf6d9a`, section 11), which is the mow measured as one skull to a shambler and enemy shots in the air down from a peak of 70 to 8 and 12; then steps 2 to 6, the records grilled, the three gates' second round folded, and the ADRs filed (`9cba278131`, then `2120369437` for the growth rate's word and `cb230c765c` for ADR 0060 re-ruled, progress note section 12). The weapon growth research record landed in session 21's own docs commit.

Session 22 added two more. **Step 8a, slice B0, the glossary realignment** (`2ec6ee5efc`, progress note section 13): ADR 0061 files the ruling and the six words land, Chunk to phase, Phase to section, Row to wave, Template to formation, Drop to power-up, Strip to HUD, with `READINGS_VERSION` 3 to 4 and `GOLDEN` unmoved. One surprise in it is worth carrying, because nothing in the research predicted it: `rng.ts` folds each stream's name string into the run seed, so renaming a stream re-seeds it, and every tape recorded before the rename would have stopped reproducing its own run. `STREAM_SALTS` now holds each stream's salt separately, the power-up stream's salt is still the old string `drops`, and a test pins it. **Step 8, slice B, the standing waves and the re-authored waves** (`480b207fdf`, follow-up `4607280db1`, progress note section 14): standing waves arrive as `Repeat` fields on `StageWave` through `repeatingArrivals` over section-local seconds, and the three section tables are re-authored, the Procession at 2, 3.5 and 5 bodies a second with a closing zero at t=111.5, the Crowd at 8, down to 3 for the trough, then 12, and the Vigil authoring no rate at all. The shaped counts are roughly doubled, the twenty-five carriers are unchanged, and the set-piece pour thins by its interval rather than its budget. Measured on seed 77 with the grave held, the three sections land 2393 bodies where they landed 663, and a hand tape verifies with all three refusal counters at zero: `food` 0, `carriers` 0, `offers` 0.

Steps 9 to 16 are ahead: slices C through G, the implementation gates, the batch and round two.

**Where the game was before all this** is the dead baseline and it is in the record (`docs/design/mow-ladder-director.md`), not here: 1.13 arrivals a second, mean 4.4 mobs on screen, enemy shots peaking 70, all four lines maxed by the end, and Mark's felt run of boring, then dodging without the power to kill, then super easy. Slice A already moved it and slice B moves it again. What must not move at any point: the fences, the invariants, replay determinism.

**Mark's rulings of 2026-09-09, verbatim where he gave words, because they change how the rest of step 4 is run.**

1. *"I don't know how to build the game. You should be using research and data to answer this question."* Naming and every craft call is settled by research off the main thread. Never asked of him.
2. *"Please don't lean towards keeping something we have because of uncertainty... you're gonna have to break things."* When research says precedent disagrees with an ADR this session filed, re-rule it to precedent. Never amend it to permit both.
3. *"If we have to adjust skulls and rate of fire and all of that stuff, so be it; the point of this is to get a design that makes sense."* His rulings are the feel: the mow, the ladder, heaven with doses of hell. The numbers under them, one-skull, hit counts, fire rates, are the session's arithmetic and move to precedent freely.
4. **The glossary, his pick taken:** *"I'll go with your pick for slice B words now."* Six terms realign to the genre's words ahead of slice B: Chunk to phase, Phase to a stage word or collapsed into Section, Row to wave, Template to formation, Drop to the genre's upgrade word, Strip to HUD. The rest of the glossary stays. The system layer uses the genre's words and the flavor stays at the player surface, which is the Hades split. This gets its own ADR. The exact words come from `docs/research/six-words-realigned-to-the-genre.md`, and slice B0 landed all six.

**Decisions the session made under the pre-authorizations, each already in the tree.** ADR 0060 re-ruled to stepped growth and one row construct, and a standing row ends at the next row of any kind. The per-minute enemy health step is struck on evidence: the figures were Mad Forest's Inverse mode, which is that stage's hard mode, and ADR 0059 rules the direction anyway; it is kept as a batch question with `timeToKill.ts` in slice G as its trigger (progress note section 12, design record section 12 item 6). Weapon growth per rung stays per line, read from `docs/research/weapon-growth-per-level-precedent.md`: the damage lane adds 25% of rung 1 per rung to a ceiling of x2, so skull 8/10/12/14/16, wisp 10/12/15/17/20, bell near about 40/56/72/88/104, and Territory flat per ADR 0044 because it buys area and repel. That record's own health-step proposal is superseded by the strike above and stays a batch question.

**Decisions the session made under the pre-authorizations, session 22.** Each is recorded in the progress note or the slice prompts file; these are the four a later session would otherwise re-litigate.

1. **`MOB_CAP`'s derivation is pulled forward from slice D into slice B** (progress note section 14). Slice B's authored Crowd rate, twelve bodies a second against a 13.8 second transit, holds about 165 bodies in flight before a single shaped beat and with nothing killed, and it could not fit the constant 160; the measured runs sat at exactly 160, which is the pool saturating rather than a peak. ADR 0056 rules that content prices caps and never that caps price content, so lowering the rate to fit and hand-picking a bigger constant were both rejected. `MOB_CAP` is now `peakLive()` and derives to 439, with `CORPSE_CAP` following to 644. The transit window is bounded by the scroll alone, because `mobs.ts` imports `caps.ts` and reaching back for the mob table would close a cycle, so it over-prices by about three quarters, which is the direction a safety net rounds. **Slice D's other items are unchanged.** CodeRabbit raised this twice against slice B and both were declined on this ruling.
2. **`SET_PIECE_POUR_SECONDS` moved from a fifth of a second to a tenth.** It is a derived value whose inputs moved, not a magnitude: the pour must not arrive thinner than the section it interrupts, and the doubled shaped counts took the densest ten seconds from 36 bodies to 74. The derivation is rewritten in its own JSDoc. Plan section 7 does not list this constant, which is why it is written down here.
3. **The wisp volley clock and its interval floor land in slice E's fold commit, not slice C** (slice prompts, slice C item (d)). A volley clock is a field on `LineState` and every such field is folded, so it moves `WITNESS_VERSION`, which the plan moves exactly once, in slice E, in the commit that declares every new folded field together. Moving the version twice buys nothing, because a tape recorded between C and E is refused after E either way; deriving the clock off the wisps' own `life` fails the moment a whole volley hits early and no wisp survives to carry the time. Slice C authors the 30-tick interval as data and pins the promise as an `it.fails` tripwire naming slice E as its trigger.
4. **Commits on this branch carry no trailer of any kind.** Mark's global rule is a single-line conventional message with no trailers, and it overrides the harness's own `Co-Authored-By` reminder. Every commit on the branch is written that way and a later session should not start adding one.

**Findings filed for Mark's read, not applied.** Each is a finding against something he ruled, so it is written down and built past.

- **ADR 0042's Wall loses its loaded half under the mow.** At 8 health the storm thins the curtain enough for a lane to open and the grave crosses untouched, and a curtain of silent bodies puts no shots in the air for a belch to be worth spending on. Progress note section 4 item 7, plan verification step 22. Slice B owns the Crowd's table and must not re-author that row past his answer.
- **Territory's bottom rung inverts against his #79 dwell ruling.** Two pulses is a shambler now and a level-one crossing lands five, so the mow body dies in the slowest ground there is. Progress note section 4 item 8, plan verification step 23. Nothing in the plan owns Territory's rungs.
- **The shipped caps cannot stand three of the six frame-budget fields.** The constants `MOB_CAP` 160 and `CORPSE_CAP` 233 against fields of 100/250, 200/500 and 400/1000. Slice B's derivation answered the constant half of this (439 and 644); what stands is the next bullet. Progress note section 4 item 3.
- **The derived `MOB_CAP` of 439 is above the 400 mobs round 0 measured.** The pool is built at its cap, so a build now stands 439 mob slots where round 0's largest measured field stood 400, and `CORPSE_CAP` 644 sits inside that row's 1000 corpses. Round 0's desktop half was measured before the mow existed, so the phone half, which is Mark's and runs at `#/frame-budget` on the deployed build, is now the reading that matters. Progress note section 14, open item 2.
- **The offer is not separable at a glance in a Crowd-density field.** Slice B's named shot, the replay at tick 18464 with 70 live mobs and 96 corpses and an offer standing two bodies wide: the offer bodies are small pale shapes in the same size band and a similar value as the corpses and the mobs, and the one thing that picks them out is that they sit against the grave's own outline, so the player finds the offer by knowing where their grave is rather than by seeing it. Whether three bodies still read as a choice inside a mow is his (plan verification step 20). Progress note section 14.
- **The Vigil's `liveBodyCeiling` of 4 is stale against a doubled table.** It gates the director's spend and the director does not exist yet, so slice F re-derives it with the rest of the director's gates. Progress note section 14.
- **`STREAM_SALTS` is a new small commitment in code:** a stream's salt is decoupled from its name so a rename never re-seeds a run. It was forced by a real divergence in slice B0 rather than chosen, and it is the kind of thing that reads as incidental until someone tidies it away. Progress note section 13.
- **The revenant's Territory cost climbs while its skull cost falls**, a consequence of ADR 0044 holding Territory flat while the damage lane grows. From the weapon growth record.

**Owed to the next slices, and each is that slice's job rather than a ticket.** Slice B's guard test landed, that a body carries exactly its `MOB_TYPES` row, which is the deliberate absence of any stat step (`mobs.test.ts:830`). Its other half did not: the stale "ramp" comments are still in the tree at `digest.ts:293` and `screenLifecycle.test.ts:913`, so they pass to the next slice that touches either file. Slice C rewrites the stale damage comments in `skullStream.ts`, `territory.ts` and `bell.ts` that still describe a 40-health shambler, and its prompt adds a fourth the earlier list missed, `wisps.ts`.

**What is next, in order.**

1. **Slice C is in flight**, one Opus coder: the economy waves, the burst cadence and the weapon climb, per the plan's step 9. Weapon growth per rung, `CORPSES_TO_CEILING`, the reservoir and the skull surge cap. Its prompt is the third section of `apps/hungry-grave/docs/push/step-4-slice-prompts.md` and it is the contract; the wisp clock ruling above binds it.
2. **Slice D**, per the plan's step 10. Its prompt was drafted in this session's scratchpad and is lost with the session if it was not appended: if `step-4-slice-prompts.md` has no slice D section, redraft it from the plan's step 10 carrying the two rulings above, that `MOB_CAP` is already derived and that no folded field lands in D.
3. Then E, F, G, the implementation gates, the batch, and round two, per the plan's steps 11 to 16.
4. **A deploy after each slice**, from a scratch worktree at that slice's tip because the branch worktree is busy with the next coder, following `apps/hungry-grave/docs/deploy.md` with the gitignored `.vercel` and `.env.local` copied in.

**Dispatch shape.** The gate agents are `gate-game-design`, `gate-product-vision` and `gate-tech-architecture`; they are exempt from the dispatch hook, and their prompts must tell them not to edit and not to leave markers. A non-coding dispatch prompt opens with the literal line `Non-coding dispatch: <reason>`. A coding prompt names `docs/agents/feature-playbook.md` and carries the six contract items; a coder handed a slice missing any of them stops and reports. **Step 4's per-slice prompts live in `apps/hungry-grave/docs/push/step-4-slice-prompts.md`**, one section per slice with the contract inline, because step 4 has no coder contract file of its own. Each is drafted by an Opus agent into the scratchpad while the previous slice builds, then appended and committed with that slice's docs commit; the orchestrator reads the draft once and rules on any stop it names before the dispatch goes out.

**Where Fable is worth spending (Mark, 2026-09-09).** The balance itself, the interaction of the three changes, reading each batch and picking the next move, and resolving the growth-versus-pressure split if the design gate rejects it. Not: writing records, slice prompts, coding, gates, batches, tickets, commits, deploys, the follow-along list, ADR drafting once the decision is made.

**Facts learned in session 21 that would otherwise be re-derived.** `.claude/rules/push-working-agreement.md`, which the main checkout's pointer file names, does not exist; this handoff and the step 3 coder contract are the rules. `coderabbit review --agent --uncommitted` from the worktree root is the working form, and it does not see untracked files, so `git add` by path first. Mark's Downloads holds 2 format 3 tapes, 18 format 2 and 11 format 1, which corrects the earlier claim of three at the current format. `git worktree list` shows three prunable scratch worktrees from earlier sessions, left alone deliberately. The step 0 baseline was captured by a scratch worktree at `e9957eebfb` with `node_modules` symlinked in rather than installed.

## Open items with no other home

0. **Stop hook error in this worktree, harmless, not to be chased.** `scripts/roadmap/on-v1-change.mjs:23` imports `local/publish-instruction.mjs`, which is gitignored and lives only in the main checkout, so every stop in the worktree prints a non-blocking module-not-found error. Roadmap edits are struck for the push, so the hook has nothing to do here. The hook is configured in the main checkout's untracked `.claude/settings.local.json` as `node "$CLAUDE_PROJECT_DIR"/scripts/roadmap/on-v1-change.mjs`; it started failing in session 9 when the harness re-homed the project folder from the main checkout to the worktree, so it will fail in any session that launches inside the worktree. Fixed 2026-09-08 by copying that one file into the worktree's gitignored `local/` folder; if the error comes back, the copy is missing again.
1. **Mark's follow-along doc, https://md.niftymonkey.dev/api/raw/CTtB5BJJ, is current through slice B0 and needs a slice B row next.** Updated 2026-09-14 through the md.niftymonkey.dev targeted-edits API with `MD_API_KEY` read from the environment, which is how every later update to it should go. It is written for a regular human: what it is and what it means for the game, no hashes, no counts, no process jargon.
2. **Two things are owed with Mark as the actor.** The phone half of round 0: the instrument is at `#/frame-budget` on any deployed build, it needs no flags and no typing, and it will print three of its six fields until slice D lands. And the felt check of whether the mow feels like a mow, after a deploy.
3. **Open tickets against step 4, none blocking.** #118 the Undertaker deadlock (a real defect Mark has seen; boss phases author no adds, so the food economy starves there and the mow makes it worse; the plan folds it into slice F if slice G's reading says the cause is the food economy). #117 is Mark's alone (whether the sloppy corner must reach the bosses; evidence on the ticket). #116, #121, and the deferred gate findings on #39 and #109.
4. **The human-tape replay divergence** on `b1c3a584d1` is undiagnosed and the evidence is preserved at `docs/push/divergence-b1c3a584d1.md`, because after slice A the tip cannot reproduce that run at all. Mark: it cannot be allowed to fail, and a test holds the cause once known. Format 2 tapes are refused at the decode, which is informative rather than a defect.
5. **#82 stays open on GitHub deliberately.** The dirty marker and the cwd fix both shipped (`26a064a064`, `136a349aeb`), but the fix is on this branch only, so the ticket closes at merge and not before.
6. **#37 stays open deliberately** (boss stories 12-14, fog story 22) until the path's step 2 absorbs them.
7. **540x760 records disagree** (`layout.test.ts:67`, `field.ts:4`, `CONTEXT.md:85`), docs-only fix pending.
8. **Stamp-order trigger** on PR #84's stamp thread: a second output in `v1.yaml` `outputs:` moves the stamp into the hook. Dormant while roadmap edits are struck.
9. **Stream flank narrowness**, Mark: "the edge thing could be an issue". Watch item, arithmetic in `soulStream.ts` JSDoc.
10. **`local/grill/` at the main checkout is stale** (moved onto the branch 2026-09-07); Mark can delete it.
11. **Config repo**: the rewritten `stay-within-limits` skill files are uncommitted at `/home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/`; the worktree guard blocks git there from this session.
12. **Cloud push playbook** at https://md.niftymonkey.dev/v/NCQUpIOq (memory `cloud-push-playbook`); the three dry runs and the local ruling are its inputs when Mark next wants it refined.

## Standing rules and facts, still true

- **Craft calls are settled by research, and precedent outranks our own record (Mark, 2026-09-09).** He does not know how to build the game and will not be asked to: naming and every craft number comes from research off the main thread, a record that disagrees with precedent is re-ruled to precedent rather than amended to permit both, and his rulings are the feel while the numbers under them are the session's arithmetic and move freely.
- **Gates never overrule Mark (Mark, 2026-09-09).** A gate can find, never rule. A finding that would reverse, defer, narrow or reconsider something he decided (an ADR, a decision-log entry, a ticket ruling, the path order, the done line) is outside the gate's authority: file it as a ticket, list it under "Needed from Mark" as "gate disagrees with ruling X", and build on his ruling. Gate prompts are scoped to consistency with his rulings, never their worth; the harness (ADR 0053) is a thing he absolutely wants whatever a vision gate thinks of it. Questions to him only when the session cannot make the call and it is hard to reverse.
- **One-push build mode (Mark, 2026-09-07):** one autonomous push on one branch in its own worktree; CodeRabbit CLI before every code commit; gates as relevant; ADRs are my call, reviewed on the branch before merge. Memory `one-push-build-mode.md` carries the stuck rule and the lean rule in full.
- **Stuck rule:** two failed honest attempts, or a decision only Mark can make, or an unauthorized irreversible step: stop the thread, write a stuck note (doing, two attempts and why each failed, one-sentence question), send it over Discord (`DISCORD_WEBHOOK_URL`, a curl POST with `{"content": ...}`), move to independent work; if none, write the handoff and pause. Discord only for stuck notes and the done message.
- **Usage limits:** `stay-within-limits` thresholds: session and weekly-all caution 75 stop 85, Fable caution 85 stop 95; main thread lean by default. Mark waived the caution lines for this push, so only the stops apply. At a Fable stop, write the handoff, commit it, chain a new session on Opus.
- **Cloud facts** (official docs, 2026-09-07) are recorded in this file's copy on the archive branch `hungry-grave-v1-cloud`; the cloud is not used for this run.
- **Vercel:** Mark is locked out of the dashboard (2FA prompt, support ticket open) but the CLI login works and `vercel tokens add` minted a token. Project `hungry-grave`, org `team_rDwpau77qippLMDxVoyeq5Ev`; the worktree's `.vercel/` link and `.env.local` are gitignored copies. hungry-grave.vercel.app is Mark's own phone-play address and carries the latest landed slice; deploy over it freely.
- **Store (provisioned and smoke-tested 2026-09-07):** Neon Postgres `hungry-grave-runs` and private Blob store `hungry-grave-tapes` (store_hnGloGEqUSe3V8Nx, iad1), keys in `.env.local`. Shape: rows in Neon (runs with header columns; readings keyed by run and computing build), gzipped bytes in Blob by run id (a real tape went 737 KB to 68 KB), one upload function used by the game at run end and by the harness. Read-after-write must pass `useCache: false` to `get(url, { access: 'private' })`. The clients (`@neondatabase/serverless`, `@vercel/blob`) are added by dry-run step 3.
- **Assets, agreed:** mapping skeleton shambler, ghost revenant, cat ghoul, golem carrier, big ghost Banshee, tall figure Undertaker, eldritch thing for the set piece; the brown werewolves, bears, gorillas and zombies are out. Pixel art draws nearest-neighbour. Every asset in the build is a placeholder and will be replaced before the real game, so licensing is never a question to raise (Mark, 2026-09-08, repeated); for the record only, the music is void1gaming's free horror music pack at https://void1gaming.itch.io/free-horror-music-pack and the pixel packs are itch.io free packs. Asset location in the app is deferred by Mark to implementation time.
- **Mark's operating rule (2026-08-28):** batch mechanical already-ruled work; stop only for a new product/architecture decision, an invariant conflict, scope expansion, or invalidating evidence. Commit, push, deploy, and merge each need their own explicit yes outside the pre-authorized list.
- **Edit-in-place ADR pattern:** same question, new answer: rewrite the ADR in place, supersession note inside, filename slug follows the new title, number stays. New number only for a new question. Supersession convention: dated inline bold prose plus a what-stood / what-it-replaced / what-it-could-not-have-known triple. No YAML frontmatter anywhere.
- **Commands:** conditioned tapes `pnpm vite-node --config vite.headless.config.ts scripts/record-conditioned.ts <out> <seed> <ticks> skullStream=N territory=N wisps=N bell=N`; the frame budget `pnpm vite-node --config vite.frame-budget.config.ts scripts/frame-budget.ts`; measure `pnpm vite-node --config vite.headless.config.ts scripts/measure.ts <tape>`; tests `pnpm vitest run`; repo gate `pnpm verify`; only `pnpm typecheck` judges diagnostics. Local play: `cd apps/hungry-grave && pnpm build && pnpm exec vite preview`. Mark saves tapes to `/mnt/c/Users/markd/Downloads/*.tape`.
- ADR set 0001-0060 canonical; no ADR carries a combat magnitude; a /to-spec spec lands as a comment on the existing ticket.
- Reading big files: `cat` over roughly 30KB gets saved to a file; use the Read tool on the saved path. In zsh, a bare `echo ====` fails and `--include=*.ts` needs quoting. The scratchpad is per session and shared between subagents, so a generic filename gets clobbered; anything a later session needs goes into `apps/hungry-grave/docs/push/` on the branch.

## How we got here, one line

Sessions 1 to 6 (2026-08-31 to 2026-09-07) ran the V1 grill to 26 decisions with eight research records; session 7 (2026-09-07) reviewed the pre-authorization list with Mark, staged the assets, created the cloud environment and ran three dry runs that ended in the local ruling; session 8 (2026-09-08) confirmed the done line, filed the ADR step, and rebuilt the branch clean off `main`; session 9 (2026-09-08) swept the tickets, planned and dispatched step 1, gated its plan, and planned step 2, ending on the context watchdog with the coder still running; session 10 (2026-09-08) landed step 1's last two slices, ran the two implementation gates and the tip review, applied and filed their findings, and stalled only on the push; session 11 (2026-09-08) landed step 2 slices 5 to 12a and started deploying each slice; session 12 (2026-09-08) landed 12b, 13a and 13b, fixed the deploy recipe, filed #101 from Mark's first phone play, and took his boss-art ruling; session 13 (2026-09-08) landed and deployed ground adjustment 1 on his three phone notes and surfaced the Waking's drift as his call; session 14 (2026-09-08) built and deployed adjustment 2 on his ruling and stopped at the context limit before its design gate; session 15 (2026-09-08) ran that gate, built and deployed adjustment 3 on its finding, filed #103, folded the empty slice 15 into 16 and wrote their prompt, and stopped on the context watchdog before dispatching it; session 16 (2026-09-08) closed step 2; session 17 (2026-09-09) built, gated and closed adjustment 4 (#104), planned step 3 (design record, dispatch plan, three gates, a fixer), landed slices 1 to 4b one Opus coder each with a deploy after every one, rewrote Mark's follow-along list to be his only, and paused at Mark's request at the end of half A; sessions 18 to 20 (2026-09-09) closed step 3 and deployed it, wrote and gated step 4's design record and dispatch plan, and filed ADRs 0059 and 0060; session 21 (2026-09-09) ran step 4's baselines, landed slices A0 and A and round 0's frame budget, folded the second gate round, re-ruled ADR 0060 to precedent, struck the enemy health step on evidence, took Mark's four rulings above, and stopped at slice B0; session 22 (2026-09-14) resumed after the weekly reset, landed and deployed slice B0 and slice B with one Opus coder each and their prompts drafted ahead by Opus agents, ruled the `MOB_CAP` pull-forward and the wisp clock deferral, updated Mark's follow-along doc, and dispatched slice C.

## Suggested skills

`/domain-modeling` for the ADR step (challenge terms against `CONTEXT.md`, record decisions as ADRs), and it is the skill slice B0's vocabulary ADR runs under. `/triage` for the tickets (drafts to Mark first; body carries problem and need only). `/to-spec` output lands as a ticket comment. `/tdd` and `docs/agents/feature-playbook.md` bind any coding; dispatches go to subagents with the dispatch contract, and a non-coding dispatch prompt opens with "Non-coding dispatch:". `/stay-within-limits` between waves.
