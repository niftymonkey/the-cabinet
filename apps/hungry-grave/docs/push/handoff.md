# Continue: The Hungry Grave

> **Last updated:** 2026-09-16, session 29, **round two is closed and deployed, and step 5 "Show what you have" is the work.** **Slices 5.0 and M1 to M5 have landed and are live** (hashes in `step-5-progress.md` section 1, its note sections 6 to 11): the frame is composed across three regimes, a slim row at the field's top edge carries the score, a cushion mark, every rostered line's five rungs and the bank, a loss is watched as the score counts down and the stripped mark empties, and a stripped rung falls onto the field as a body the dive can catch. Step 5 goes slice by slice per `step-5-slice-prompts.md`, each prompt gated before its dispatch: **M5-fix is building, then M6 and M7.** **This file is trimmed on every rewrite, never only appended** (Mark, 2026-09-16), because appending without pruning took it to 89 KB by session 27: prune what is recorded elsewhere on the branch and cite where it lives.
> **Branch:** `hungry-grave-v1`, worktree `.claude/worktrees/hungry-grave-v1` (the absolute path is in FIRST ACTION), tip `b1f86d520a` at slice M5-fix's dispatch. **Every step 5 slice's hashes are in `step-5-progress.md` section 1**, round two's in `round-two-progress.md` section 1, step 4's in `step-4-progress.md`. **Step 5's four files are `docs/design/show-what-you-have.md` (the record, R1 to R12, slices 5.0 then M1 to M7, and the orchestrator's rulings inside it), `docs/research/portrait-hud-and-catchable-loss.md`, `docs/push/step-5-slice-prompts.md` and `docs/push/step-5-progress.md`.** Round two's three are `round-two-wall-belch.md` (rulings R1 to R12 in section 2, findings in section 7), `round-two-slice-prompts.md` and `round-two-progress.md`. All under `apps/hungry-grave/`. Cite their sections rather than restating them; this file is the state of play and the standing rules, nothing more.
> **Working tree:** **an M5-fix coder holds it**, editing `src/game/grave.ts`, `show-what-you-have.md` and `step-5-progress.md`; anything else should be clean. `pnpm verify` green twice on M5's committed tree (note section 11). **`WITNESS_VERSION` 11, `READINGS_VERSION` 8, `FORMAT_VERSION` 4, `GOLDEN` `-2049717150`** at `b1f86d520a`, none of the four moved since M1, which spent the step's whole witness and readings budget (note section 3); M5 left its permitted re-pin unused. **M5-fix and M6 are permitted none, so a move in either is a stop and report, and M7 moves `READINGS_VERSION` 8 to 9 by its own prompt and nothing else.** **Every tape recorded before the current witness is refused at the decode.** Run `git log --oneline -25` first anyway.
> **Deployed:** **slices M1 to M5 are live on https://hungry-grave.vercel.app** on top of round two's J3, Mark's own phone-play address; deploy over it freely. **The next deploy is after M5-fix.** Two build warnings, pre-existing and ticketed as **#50 and #51**: `@pixi/sound` sits in the main chunk whatever the dynamic import says, and one chunk is over 500 kB.
> **Session shape:** the orchestrator makes every hard call itself or in a Fable subagent, and everything else is an Opus or cheaper subagent. **Usage mid-session 29: session 41, weekly all 57, weekly Fable 42**; re-read it with the usage script rather than trusting this line. **The caution lines are waived; only the stops apply** (weekly all 85, Fable 95). One code slice per wave, and the deploy recipe is `docs/deploy.md`.

## FIRST ACTION

1. **Slice M5-fix is landing or has landed**, dispatched at `b1f86d520a` on the orchestrator's own ruling: **when there is no room below the grave, the strip's rungs fall above it and drift down past it**, which is R6's closing paragraph read against M5's batch. It edits `src/game/grave.ts`, the record `show-what-you-have.md` and `step-5-progress.md`, and its record is step 5 note section 11. **A dirty tree or a WIP commit means the coder was interrupted, and that section says where it got to.** **Deploy after it.**
2. **Enter the worktree** before anything else: EnterWorktree with `path: /home/mlo/dev/niftymonkey/the-cabinet/.claude/worktrees/hungry-grave-v1`.
3. **Run the usage script**, `bash /home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/usage.sh`, against the stops above, then **`git log --oneline -25` and `git status --short`**, one plain command per Bash call, and confirm the tip and the tree this file claims.
4. **Then M6, gated and folded already at `d06dd74930`**: the fallen-rung four-fate ledger, the grave's y per strip read from state, the memory's edge, a boss tag, strips already in `damageTaken`, and the bot seeking nearest food with no preference. **Then M7**, whose prompt and precedent research are at `fcce0b28c6` (`docs/research/score-inputs-precedent.md`: boss damage per hit at about 100 boss health per trash kill, the source kill at 24x, rich food at full power sized to one boss fight, `READINGS` 8 to 9) and **whose gates are being folded now**. **Then step 5's close**: all three gates on the step's diff, CodeRabbit on the tip, and a batch. The step's briefs are the table below and the orchestrator's rulings live inside `show-what-you-have.md`, not here.
5. **Then the tuning-record step**, draft at `docs/push/drafts/step-5-tuning-record-draft.md`, behind step 5 because his instruction is V1 first. Its eleven decisions are the orchestrator's and are ruled before any coder is dispatched; the tuning-step inputs below are its first sweep.
6. **Write the follow-along document's row for a slice when its prompt is written, not when it lands** (Mark, 2026-09-16, after he did not know what slice L was). The doc is https://md.niftymonkey.dev/api/raw/CTtB5BJJ, edited through the targeted-edits API with `MD_API_KEY` from the environment, and it is **current through M5-fix building** at revision `rv_TyT4IWhI`. Read it live before writing to it. It is for a regular human: what it is and what it means for the game, no hashes, no counts, no process jargon.
7. **#131, #132 and #130 are outside step 5 and are Mark's to schedule.** #131 is the boss's per-toll tell plus the shed armor pieces, #132 is the Waking's source taking a resisted push, and #130, typed agent roles, he wants at a pause point between pushes and never mid-push.
8. **No asking**: the work is ruled, and the stops are the stuck rule and the usage stops, nothing else. **The handoff is still written when the context watchdog fires**; only the stop and the `/clear` ask are waived.

Push records are the tracked copies in `docs/push/`. The isolation guard refuses writes to the main checkout and refuses compound Bash mentioning git even in quoted text; use plain separate commands.

**This file is the continue document and it is ephemeral**, tracked on the branch only because the worktree cannot see the main checkout's gitignored `continue-hungry-grave.md`, now a pointer here. Delete it when the push merges.

## Step 5: show what you have (#99, and #72 for M2)

**What the step is and every ruling under it**: `docs/design/show-what-you-have.md`, #72's layout with the score, the weapon levels and the falling level on screen, gated by all three gates with their findings folded and committed at `45a25ee6eb`.

| Slice | What it is | Where it stands |
| --- | --- | --- |
| 5.0 | ADR 0054 amended in place, ADR 0002 re-ruled to the composite score, ADR 0061's sixth term, `CONTEXT.md`, the visible-ladder precedent | landed, `988426e433`, `09a1cf3dcb`, `729de2b744`, note section 6 |
| M1 | a kill pays score, and a bled rung stays bled until the grave grows off the floor | landed, `2c7a281657`, `11483ecf31`, `84aabe68ec`, note section 7 |
| M2 | the frame is composed and the band is reserved: `100svh` with `resizeTo: #app`, the slack split evenly, no HUD content yet | landed, `0c7f877ad1`, `9ec1467aac`, note section 8 |
| M3 | the HUD carries the ladder and the score, a dumb view with the driver outside owning the data and the diffing | landed and live, `48383d4d68`, `ded89c5242`, note section 9 |
| M4 | the loss is watched: the score's 40-tick countdown and the stripped mark emptying, both driver-owned transients, with a field blow-up on the skull stream | landed and live, `d463dc8252`, `437ded35ba`, note section 10 |
| M5 | the stripped rung falls as a fourth food kind, one body per rung, and a swallow restores its own line | landed and live, `9aa6f83a6a`, `b1f86d520a`, note section 11 |
| M5-fix | with no room below, the rungs fall above the grave and drift down past it | building, dispatched at `b1f86d520a`; deploy after it |
| M6 | the ladder's cost is measurable: the fallen rung's four fates, the grave's y per strip, strips per run | prompt gated at `d06dd74930`, to dispatch |
| M7 | the score's other inputs, boss damage and the Waking's source and large food after full power, each a row of its own with its weight researched and annotated, never typed | prompt and research at `fcce0b28c6`, gates being folded |

**The batches so far:** M1 and M5 each bled every run at the floor and stripped 5 of 12, and M5's twelve dropped ten rungs the bot caught none of (note sections 7 and 11). Those are what M6's readings are read against.

## Round two and step 4, both closed

Every round two slice from the ADR commit through J3 and where its hashes are is the table in `round-two-progress.md` section 1; the close is that note's section 17 and the rulings are `round-two-wall-belch.md` section 2. Nothing in it is outstanding.

**What step 4 was** and all eighteen plan steps: `docs/design/mow-ladder-director.md` and `docs/design/step-4-mow-ladder-director-dispatch.md` section 10. Steps 0 to 15 are closed, A0 through G landed and deployed with every hash in `step-4-progress.md` sections 6 to 19, and step 16 was round two.

**What must not move at any point**: the fences, the invariants, replay determinism.

## Mark's reads

His phone and localhost reads through 2026-09-15 are recorded where they were acted on: the 2026-09-14 reads became tickets #122 to #125, and the 2026-09-15 evening reads on H2 and J are in `round-two-wall-belch.md` section 2 under R10 and R12.

**His 2026-09-16 sighting, bodies inside the eruption not pushed, is answered and shipped as J3** (round two note section 18, seed 1999305952). **What is owed from him now is a play of the deployed build**, which carries J3, the row, the watched loss and the falling rung: whether a press reads as throwing everything standing in it, whether the row reads at a glance on his phone, and whether picking one stripped rung out of four lands as a choice.

**The second cause behind his 12-to-15 sighting is still not ruled out**: the reach is a hard edge with no falloff and J-fix made the drawn ring and the real reach one circle, so his next play is the evidence (`round-two-wall-belch.md` section 4).

## Mark's rulings

His 2026-09-09 rulings are `mow-ladder-director.md` section 2 verbatim, and his 2026-09-15 rulings are `round-two-wall-belch.md` section 2 as R1 to R12, plus ADRs 0007, 0008 and 0042. **Three rulings of 2026-09-16 live here until they have a narrower home, and the trimming rule at the top is the fourth:**

1. **The tuning-record step sits behind step 5**, and **the follow-along document gets a plain-words row for every slice when its prompt is written**, not when it lands. FIRST ACTION carries both.
2. **The score is one number fed by several inputs**, never kills alone: kills, boss damage, the Waking's source killed, large food after full power, and more he has not named. **A friends' high score list is his wish**, ticket **#135**, after the store. ADR 0002 and decision 10 were superseded in place at `09a1cf3dcb`, M7 carries the other inputs, and memory `score-is-composite` holds it.
3. **An instrument built for one of his sightings is checked on his own play before the slice is called done**, in the standing rules with his words.

## Findings filed for Mark's read, not applied

Each is a finding against something he ruled, written down and built past, and anything closed or superseded is cut on every rewrite. The fuller versions live in `show-what-you-have.md` section 7, `round-two-wall-belch.md` section 7, `round-two-progress.md` sections 8 to 18 and `step-4-progress.md` sections 13 to 19.

**Waiting on his read.**

- **Three of five strips dropped every rung off the field on the tick it fell** (step 5 note section 11), the grave at its bottom clamp, 742.0 of 760, which is where a hand under fire usually sits. **His section 7 edge rule stands and nothing repaired it**; the orchestrator ruled M5-fix on top of it, so with no room below the rungs fall above the grave and drift down past it. **The other choice is named in the record and is his**: let a floor strip's rungs simply be gone.
- **ADR 0055 records an eyes-open cost the tree disproves.** It says a body at the bottom of the field is something the base harness policy will always walk to; the measured take rate is zero of ten across twelve runs (step 5 note section 11). The hand's rule is right and the sentence is wrong, and **it is filed rather than amended** because M6's declared take rate settles it.
- **Three of the four lines announce a strip in the HUD and nowhere on the field** (step 5 note section 10). M4 built the field blow-up for the skull stream alone: the bell's cones show only during a toll, the wisps' flight only for 90 ticks after a swallow, and Territory buys area rather than a count. **A field channel for the other three is a design job per line**, the record's section 7 sixth finding.
- **A hit costs points, so clean play scores higher** (step 5 record section 7). ADR 0003's order stands, the floor ladder spends the score before it spends a level, and under his own composite-score ruling that number is what a high-score list ranks. **Nothing acts on it**; it is his to overrule once M7's other inputs are in and the number means more than kills.
- **The second hit while small strips a level off every line, and a dive that grows the grave resets the cushion** (step 5 record section 7). It is the one behaviour in the step he did not ask for and will feel first, and it exists because a kill paying score would otherwise re-arm the bled rung about twice a second. **If it reads as too harsh the lever is one rule, not a number**, read against M6's strips per run.
- **Three Mark-ruled ADRs were amended in step 5.0 under one-push mode**, 0054, 0002 and 0061's sixth term, listed in the step 5 record's section 7. **An amendment to something he ruled is his to overrule before merge.**
- **The ladder does not open across 96 bot runs** (round two record section 7): median one rung a run, nothing bought at all in 20 and 19 of 48, level 5 on no line. The bot does not hunt carriers, so it is a floor rather than the truth. **For him: did he reach rung 3 on any line.**
- **A body costs about a third fewer hits by minute six** (round two record section 7), the evidence `mow-ladder-director.md` section 12 item 6 asked for when the per-minute enemy health step was struck; that record marks the call as his.
- **The witness has moved 7 to 11 and the readings 4 to 8 since the plan was written**, none of it in any plan, every move the orchestrator's under one-push mode, and each witness move kills every tape before it. **A plan change is his to see at merge.** M7 takes the readings to 9.
- **The push ladder has flattened and the Waking's corpse property has become dominant** (round two note section 10): rung one to rung five went 6.7 times to 1.6, the corpse property 1.51 to 6.8. **It follows from R2 exactly as he picked it**, so nothing was built past; it is what to react to when he next plays a high bell.
- **A toll's damage now steps to nothing at a line inside the cone that is never drawn** (round two note section 10), the body's travel its only tell: R10 working, and whether the step wants a visible edge is a feel question.
- **The charging control is now as loud as the ready one used to be** (round two note section 13), because ADR 0054 forbids the old alpha step; whether the corner reads too loud on a real device is his.
- **Under `svh` the belch's control sits over the field on his phone, every run**, by 17 to 108 stage units (step 5 note section 5). **M2's even slack split shipped as the mitigation and it does not reach zero.**
- **The play area on his phone gets slightly smaller and stops moving** (step 5 record section 7), because M2 sizes the canvas to `svh` rather than `dvh`. **No test can tell a field that is correctly smaller from one that is wrongly smaller**, so this one's whole verdict is his phone read, and the M3 deploy that carries it is out.
- **The bank's `+` marker and its 12-unit font are first figures** (step 5 note section 9): `BANK` does not fit the row's 23 units and the size falls out of the widest bank the stage can produce. **If the bank should read larger, the width comes off the score's own budget**, because R1 leaves the row 2.2 units of slack.
- **Where the row draws over the field, a mob passes behind the marks** (step 5 note section 9), both readable at 320 by 460: R1's no-plate arrangement working, and the first thing in this game to draw over live play.
- **A full build's doubled throughput puts the nominal run at about six and three quarter minutes against ADR 0049's eight** (step 4 note section 15), two retune asks declined because the damage lane is ruled, standing as an `it.fails` tripwire.
- **CodeRabbit's major on slice I was declined** (round two note section 9): fold `impulse.source` and bump the witness with it. **A reviewer's major built past is his to see at merge.** **All seven of M4's findings were declined as well** (step 5 note section 10), the major among them a per-tick event delivery change across both screens, left for whoever next opens event delivery.
- **A thrown corpse is food the hand never gets back** (round two note section 15): 0 swallowed of 72 carried against 15.8 expected, and the design gate filed whether that is the shove's price or a leak.
- **The bell's shoves rose 116 to 220 on slice J's conditioned tape** (round two note section 15), because cone-killed bodies now fly, and the gate asks whether the bell then reads as repel or as stealing his food.
- **A lone Drip arrives at t=44** in the cairn's answer to his mob-types question, filed and nothing built on it.

**Tuning-step inputs, its first sweep list.** Each is measured in full where it is cited and none is anyone's build now.

- **A later shove of a press throws fewer bodies than the first** (round two note section 18), because the first shove already cleared the ring. J3's rule holds at every shove; what varies is how full the ring is when each fires, a density question rather than a bug.
- **The purses are priced above what the quiet interval lets a section spend**, and the director takes three quarters of the Procession's empty ticks (round two record section 7, step 4 note 18).
- **At rungs four and five the storm kills bodies before a belch shove can carry them**, and **the living fringe is wide but shallow**, a damage reach of 0.47 for 0.7 roughly doubling its depth (round two note sections 10 and 11).
- **The belch is the least observable thing in the game and it is the thing being changed**: no tape of either kind can carry one, so measuring it wants a new named configuration (round two note sections 11 and 12).
- **Slice J-fix missed its second pass line with nothing tuned to reach it**, and **the eruption's front no longer pictures the field-wide gas** (round two record section 7, note section 12): both are candidate reasons a press can feel like it did nothing.
- **A hit costs about 46 seconds of mowing at rung 1 and about 12 at rung 5**, against about 5 on the dead baseline (step 4 note 15).
- **Territory's bottom rung inverts against his #79 dwell ruling**: two pulses is a shambler and a level-one crossing lands five. **#125 is the felt half, still unruled.**

**Open against tickets, one line each.** **#133**, `killableOutright` is a dead rule with no production reader. **#134**, no batch can press on the Wall, so the curtain's only opener is unmeasurable in a sweep. **#135**, the friends' high score list, after the store. #122, the offer is not separable at a glance in a Crowd-density field. #129, a body touching the grave does the dominant harm and there is no tell. #128, the renderer walks every pool slot at the derived caps. #66, the dev stack's FPS line overprints the score at the narrowest phone, which R12 rules a dev-build-only collision. #50 and #51, the two build warnings.

**Owed to whoever next touches the code they sit in.** `src/dev/readings/repel.ts` carries a stale sentence, "push only exists at bell levels 4 and 5", in its JSDoc at line 19 and in `__tests__/repel.test.ts` at line 54; it is wrong at every rung now, and a stale "ramp" comment survives at `digest.ts:316`. **The trash's `mobFire` stream is drawn by nothing at all in a whole run** (step 4 note section 17), so a reading treating a moving `mobFire` cursor as evidence reads a stream nothing spends.

## Open items with no other home

0. **Stop hook error in this worktree, harmless, not to be chased.** `scripts/roadmap/on-v1-change.mjs:23` imports `local/publish-instruction.mjs`, which lives only in the main checkout; copying that one file into the worktree's ignored `local/` fixes it.
1. **Open tickets against step 4, none blocking.** #118 the Undertaker deadlock, on Mark's sighting and not reproduced in 108 bot runs; #117 is Mark's alone; #116, #121, and the deferred gate findings on #39 and #109.
2. **The human-tape replay divergence** on `b1c3a584d1` is undiagnosed, evidence at `docs/push/divergence-b1c3a584d1.md`. Mark: it cannot be allowed to fail, and a test holds the cause once known.
3. **#82 and #37 stay open deliberately.** #82's fix is on this branch only, so it closes at merge; #37 (boss stories 12-14, fog story 22) waits for the path's step 2.
4. **Stamp-order trigger** on PR #84's stamp thread: a second output in `v1.yaml` `outputs:` moves the stamp into the hook. Dormant while roadmap edits are struck.
5. **The rewritten `stay-within-limits` skill files are uncommitted** at `/home/mlo/dev/niftymonkey/claude/skills/stay-within-limits/`, and the worktree guard blocks committing them from this session.
6. **Candidate ADR: a reading may not inherit a meaning from the shape its value happens to have.** It is the stated reason behind three declines (step 4 note section 19, round two note sections 8 and 9) and lives only in `comparisonDeclared.test.ts` and three notes. **The orchestrator files it when a slice next touches the readings**, which is M6, and Mark reviews it before merge.
7. **The wisp-cap silence is still open.** `launchWisps` takes a null slot from `takeSlot` and returns, so a truncated volley raises nothing and moves no counter, against the rule that nothing abnormal is ever silent. **The trigger is the next time either line cap is measured binding.** No ticket needed.

## Standing rules and facts, still true

- **Craft calls are settled by research, and precedent outranks our own record (Mark, 2026-09-09).** Naming and every craft number comes from research off the main thread, and a record that disagrees with precedent is re-ruled to precedent rather than amended to permit both. His rulings are the feel; the numbers under them are the session's arithmetic and move freely.
- **Decide craft values, never offer Mark a comparison (Mark, 2026-09-16).** His words: *"half of that feedback is solicited... We're still getting caught up on little things... I want a V1. Please help me get there."* **Pick the value with a stated reason, preferring a floor or ceiling an existing rule already implies over a number somebody liked. Where a value is genuinely open, annotate it and move on, and build no comparison knob before V1.** His unsolicited reaction after playing stays welcome and binding; what is banned is manufacturing the question.
- **Gates never overrule Mark (Mark, 2026-09-09).** A gate finds, never rules. A finding that would reverse, defer, narrow or reconsider something he decided is filed as "gate disagrees with ruling X" and built past. Questions to him only when the session cannot make the call and it is hard to reverse.
- **One-push build mode (Mark, 2026-09-07):** one autonomous push on one branch in its own worktree, CodeRabbit CLI before every code commit, gates as relevant, ADRs my call and reviewed on the branch before merge. Memory `one-push-build-mode.md` holds it in full.
- **Stuck rule:** two failed honest attempts, a decision only Mark can make, or an unauthorized irreversible step: stop the thread, write a stuck note (doing, the two attempts and why each failed, one question), send it over Discord (`DISCORD_WEBHOOK_URL`, a curl POST with `{"content": ...}`), move to independent work, and if there is none write the handoff and pause. **Discord only for stuck notes and the done message.**
- **Fable never reads (Mark, 2026-09-09).** Cheaper agents search and hand Fable the result; a small Fable job runs in the main thread, a big one as a Fable subagent carrying those results. **Spend it on the balance itself, the interaction of changes, and reading each batch to pick the next move**, never on records, prompts, coding, gates, batches, tickets, commits, deploys or the follow-along list.
- **Dispatch shape.** The gate agents are `gate-game-design`, `gate-product-vision` and `gate-tech-architecture`, exempt from the dispatch hook, and their prompts must tell them not to edit and not to leave markers. A non-coding prompt opens with the literal line `Non-coding dispatch: <reason>`. A coding prompt names `docs/agents/feature-playbook.md`, the prompts file and `docs/push/step-4-coder-contract.md`, and carries the six contract items; a coder handed a slice missing any of them stops and reports. A prompt is drafted by an Opus agent into the scratchpad while the previous slice builds, then committed as docs; **the orchestrator reads only a draft's decision parts** and rules on any stop it names before the dispatch. **A coder is never dispatched in the last hour of a session**, because it dies with the context switch.
- **Usage limits:** `stay-within-limits` thresholds are session and weekly-all caution 75 stop 85, Fable caution 85 stop 95, and the caution lines are waived for this push. At a stop, write the handoff, commit it, chain a new session on Opus.
- **Frame a craft-value question in perception terms**, what the player must see and against which ruler already on screen, and convert every candidate into the field's own units before it is a number. The post-mortem behind this rule is `docs/research/watched-pushback-duration.md` section 0.
- **An instrument built for one of his sightings is run over a real play against that sighting before the slice is called done (Mark, 2026-09-16).** J2's per-body press record had no tick axis, so it could not see a wave that pushed nothing, and the gap survived to his next play. His words: *"If you created the tape thing to account for this, why did you not check it to make sure you had it right before moving on?"*
- **A headless browser cannot photograph a short-lived state.** It draws at 3 to 5 frames a second against twenty to thirty sim ticks per drawn frame, so such a property is pinned by test in the sim and is **human-checkable only, on a real device** (round two note sections 8 and 13, step 5 note section 11).
- **Deploys run from the branch worktree between coders, never from a scratch worktree**, which the isolation guard makes unusable for a subagent.
- **Commits on this branch carry no trailer of any kind**, Mark's global single-line conventional rule overriding the harness's `Co-Authored-By` reminder.
- **Mark's operating rule (2026-08-28):** batch mechanical already-ruled work; stop only for a new product or architecture decision, an invariant conflict, scope expansion, or invalidating evidence. Commit, push, deploy and merge each need their own yes outside the pre-authorized list.
- **His issue format**, which the triage skill is not the source of: three one-line paragraphs, Problem, Need, Done when, plus a follow-up comment posted right after creation opening with a Placement line.
- **Edit-in-place ADR pattern:** same question, new answer: rewrite in place, supersession note inside, slug follows the new title, number stays, and a new number only for a new question. Supersession is dated inline bold prose plus a what-stood / what-it-replaced / what-it-could-not-have-known triple. No YAML frontmatter anywhere.
- **Vercel:** Mark is locked out of the dashboard (2FA, ticket open); the CLI login works and a token is minted. Project `hungry-grave`, org `team_rDwpau77qippLMDxVoyeq5Ev`; the worktree's `.vercel/` link and `.env.local` are ignored copies.
- **The store is provisioned and dormant**, Neon Postgres `hungry-grave-runs` and a private Blob store, keys in `.env.local`; its shape and read-after-write trap are in `docs/push/pre-authorizations.md`. **#135's high score list waits on it.**
- **Assets, agreed**, are the mapping in `step-2-stage-floor-dispatch.md`. **Every asset is a placeholder to be replaced before the real game, so licensing is never a question to raise** (Mark, 2026-09-08, repeated).
- **Commands:** the conditioned-tape, frame-budget, measure and verification lines are in `docs/push/step-4-coder-contract.md`; only `pnpm typecheck` judges diagnostics and `pnpm verify` is the repo gate. Local play: `pnpm build && pnpm exec vite preview` here. Mark saves tapes to `/mnt/c/Users/markd/Downloads/*.tape`. **`coderabbit review --agent --uncommitted` from the worktree root is the working form**; it does not see untracked files, so add by path first.
- ADR set 0001-0061 canonical, no ADR carries a combat magnitude, and a /to-spec spec lands as a comment on the existing ticket.
- **Reading big files:** `cat` over roughly 30KB gets saved to a file; Read the saved path. In zsh a bare `echo ====` fails and `--include=*.ts` needs quoting. The scratchpad is shared between subagents, so a generic filename gets clobbered; anything a later session needs goes into `apps/hungry-grave/docs/push/` on the branch.

## How we got here, one line

Sessions 1 to 28, the V1 grill of 2026-08-31 through round two's close, are in this file's git history (`git log --follow apps/hungry-grave/docs/push/handoff.md`); `7f5cb0105b` is the last version carrying the session-by-session narrative.

## Suggested skills

`/domain-modeling` for the ADR step, `/triage` for tickets with drafts to Mark first, `/to-spec` output as a ticket comment, `/tdd` bound by `docs/agents/feature-playbook.md`, `/stay-within-limits` between waves.
