# Step 2 progress: the stage floor

Written for the agents taking slices 0 to 16. The plan is `docs/design/step-2-stage-floor-dispatch.md`; everything below is what it does not say or what has moved since it was written.

## 0. Planning read

The plan was written while step 1 was mid-build and its own opening section asked for this read once step 1 landed. Every `file:line` in the plan was located by content against the tree at step 1's tip and corrected in place. About 320 citations were checked, 131 occurrences across 104 distinct citations were wrong and are now right, and nothing was left pointing at a site that could not be found.

**Citations corrected.** Grouped by file, because within a file every citation moved for one cause.

- `stage.ts`, 39 occurrences. Step 1 put `carries` on `StageRow` with its doc comment and added the carrier paragraph to `RAMP_ROWS`'s header, which pushes the whole file down: 1 line before `StageRow`, 6 more after it, 14 by the time the tables end. `PhaseName` 11 to 12; `StageRow` 13-23 to 14-29; the drain-out sentence 24-27 to 31-34; the measured drain-out 48-50 to 54-56 (two sites); `DRAIN_OUT_SECONDS` 62 to 68 (three sites) with its readers 176, 239 to 190, 255; the `BACK_HALF_ROWS` comment reader 70 to 76; `RAMP_ROWS` 81-105 to 95-117 and its heading 81 to 95, readers 152, 240 to 166, 256; the Wall's comment 107-112 to 119-126 and its row 114 to 128; `BACK_HALF_ROWS` 113-133 to 127-147 and its heading 113 to 127, readers 154, 241 to 168, 257; `Phase` 135-138 to 149-152 with its `PhaseName` readers 136, 244 to 150, 260; the boss stub's comment 145-150 to 160-163; `PHASES` 151-157 to 165-171, heading 151 to 165, readers 213,225,226,229,242 to 228,242,243,258; the banshee entry 153 to 167 and the undertaker entry 155 to 169; `phaseLengthTicks` 173-177 to 187-191 and 173 to 187 (two sites) with readers 229, 237 to 245, 253; the victory stub's comment 201-206 to 218-222 (two sites), the stub itself 214-216 to 230-232 (two sites) and `state.ending` 215 to 231; `enterNextPhase` 206-217 and 207-217 to 223-233; `advanceStage` 224-233 to 240-249 and its loop 225 to 242; the value import of `mobs.ts` 6 to 7, which was pointing at the type-only import beside it.
- `mobs.ts`, 3. `spawnMob` gained its `carries` argument and `Mob` gained the field, so everything below moved by 7. `spawnMob` 237-241 to 244-249, `cullMobs` 408 to 422, `fall` 288-298 to 297-307.
- `corpses.ts`, 10. `asSwallowable` gained the body id in slice 8, which moved everything after it by 1. `oldestEvictable` 144 to 145 (two sites) and 144-151 to 145-152; `claimSlot`'s rationale 153-159 to 154-160 and the function 160-177 to 161-178; its `oldestEvictable` reader 166 to 167 and its `corpseEvicted` emit 168-173 to 169-174 with the reader 169 to 170; `spawnFeast`'s comment 211-215 to 212-216, the function 216-237 to 217-238, and `decays: false` 233 to 234.
- `events.ts`, 6. Slices 7 and 8 added `CarrierLost` and the four offer payloads. `MobFired` 125-130 to 144-149 and its `emitter` 127 to 146; `CorpseEvicted` 146 to 165 and 146-151 to 165-170 with readers 147,278 to 166,347; the `PatchClosed` precedent 180-196 to 199-215; the phase-change comment 241 to 309 and the `PhaseName` reader 244 to 312.
- `caps.ts`, 7. Step 1's gate fix moved `TERRITORY_CAP` out to `lines/territory.ts`, which shortened the header and lengthened the file's own comment. `MOB_CAP` 22 to 29 (two sites) with its derivation comment 10-20 to 17-28; `CORPSE_CAP` 24 to 31 (two sites) and its export 123 to 110; the no-removal rule 64-67 to 62-65.
- `step.ts`, 7. Slice 8 added `chooseOfferBody` and `loseOffer` to the tick and lengthened the order comment. `hitGrave`'s caller 53 to 54 (four sites), the belch-before-every-overlap rule 150-153 to 195-198 (two sites, and the old range was the storm's advance rather than the rule), and `cullMobs`'s caller 175 to 220.
- `run.ts`, 6. `RunState` gained `roster`, `offer` and `bankedOffers`, and `createRun` gained the roster parameter. `RunState` 59-102 to 60-114; `uniformLevels` 154 and 170 to 177 (three sites, and the plan cited two different stale numbers for the same seam); `createRun`'s third parameter 190 to 218 and its signature 211-215 to 215-219.
- `witness.ts`, 2. Slice 8 added `foldOffer`. `foldStage` 267-270 to 266-269, the `WITNESS_VERSION` export 318 to 337.
- `faults.ts`, 3. Step 1 appended three identities. `FAULT_IDENTITIES` 18-31 to 18-34 (two sites), the `FaultIdentity` reader 34 to 37.
- `invariants.ts`, 1. The `CORPSE_CAP` readers 8,401 to 7,394.
- `bell.ts`, 3. Slice 6 replaced the ring with cones and renamed the function. 154 to 256, with the name (see the sentence list below).
- `territory.ts`, 3. `TERRITORY_CAP` arrived from `caps.ts` above `eligiblePoints`. 334 to 351.
- `belch.ts`, 1. Slice 5 split the gas from the burst. The full-reservoir gate 57 to 82; the old line was inside `burstNearbyMobs`.
- `layering.ts` and `layering.test.ts`, 6. Slice 9 added the bank line's width assertion. `LAYER_ORDER` 16-29 to 16-30; `ADR_0014_STACK` 73-86 to 75-88 (four sites); the container-count assertion 141 to 143 (two sites).
- `digest.ts`, 1. Slices 2 to 8 re-pinned `GOLDEN` four times and the comment above it grew each time. 276 to 297.
- `playback.ts` 265 to 266, `tapeHeader.ts` 7,82 to 6,81, `sound.test.ts` 37 to 38: each one line, from the offer's own fields and events.
- Test files under the `RAMP_ROWS` and `CORPSE_CAP` reader lists, 11. `bot.test.ts` moved furthest, because slice 8 added `REACHES_VICTORY_MAXED`, `maxedRun` and `AUTHORED_CARRIERS`: `phaseLengthTicks` 27,124,125 to 29,202,203; `RAMP_ROWS` 28,128,420 to 30,206,212,562; `BACK_HALF_ROWS` 25,128,134 to 27,206,212,218; `PHASES` 124,125,140-143 to 28,202,203; `PhaseName` 140-143 to 224-227 plus the literals at 301,303,466,470,471; the both-endings assertion 358-366 to 488-509. Then `corpses.test.ts` 29,46 to 30,47 and 9,341,352,359 to 9,363,374,381; `belch.test.ts` 13,18 to 15,25; `bell.test.ts` 15,30 to 18,37; `step.test.ts` 18,194 to 17,191; `skullStream.test.ts` 18,34 to 18,36; `wisps.test.ts` 14,29 to 14,31; `StormRenderer.test.ts` 13,26 to 17,30; `FieldRenderer.test.ts` 333 to 348.
- Records, 3. `pre-authorizations.md` 22 to 20 (item 14, the image tool); `handoff.md` 33 to 37 (the mp3 loop gaps and the LAME headers), which the note itself warns has been rewritten several times; `WebAudioInstance.mjs` 168-174 to 172-175 in `@pixi/sound` 6.0.1's installed source, where the `loopStart` and `loopEnd` assignment actually sits.
- `readings.ts`, 2, and these were wrong before step 1 rather than because of it. `readings.ts:50-66` was cited for "declares twelve readings" and is the import block; `TuningReadings` is 72-85. The file has moved by one line since the plan was written, so both citations were wrong when they were written.

**Sites not found.** None. Two citations needed more than a line move to resolve and both are recorded above and below:

- `src/game/__tests__/drops.test.ts:21,25`, in the `RAMP_ROWS` reader list. The file is gone, which the plan's own caveat 8 predicted. Searched for `drops.test.ts` and for the `run.stage.firedRows = RAMP_ROWS.length` idiom across `src`; the file that inherited it is `src/game/__tests__/offer.test.ts:28,40`, and the entry now names that.
- `bell.ts:154` (`sweepRing`). Searched for `sweepRing` across `src` (no hits) and then for every walk over `state.mobs` in `bell.ts`; slice 6 renamed the function `sweepToll` and it is at `bell.ts:256`.

**Plan sentences changed because the cited thing changed.**

- Old: "`bell.ts:154` (`sweepRing`)". New: "`bell.ts:256` (`sweepToll`)". Three sites, in section 4, section 5's module table and section 7. Slice 6 renamed the function, and a coder grepping for `sweepRing` finds nothing.
- Old: "its one row is `{ t: 2, template: 'wall', count: 22, type: 'shambler' }` at `stage.ts:114`". New: the same row with `carries: false` at `stage.ts:128`. Step 1 put the column on every row, so the quoted literal no longer compiles as written.
- Old: "**`WITNESS_VERSION` (`witness.ts:38`, 4 today, 5 after step 1): moves once**". New: "5 today, where step 1 left it: moves once to 6". The tree reads 5, so "4 today" is now false and "one past whatever step 1 left" is now a number.
- The opening section, "What this plan may claim, and what it cannot", in full. It said the numbers were read mid-build and would need re-checking; they have been re-checked, so the paragraph now says so and points here, and each of caveats 1 to 8 is restated as the tree reads rather than as a prediction. Caveat 9 was already true and is untouched.
- Old: "Everything else cited below was read directly and is in files step 1 does not touch (`stage/templates.ts`, `caps.ts`, `tuning.ts`, ...)". New: a list of what moved outside the nine named files. `caps.ts` was on the untouched list and step 1's gate fix moved it; `bell.ts`, `territory.ts`, `belch.ts` and `bot.test.ts` moved too. `templates.ts`, `tuning.ts`, `field.ts`, `clock.ts`, `grave.ts`, `mobFire.ts`, `palette.ts`, `layering.ts`, `sound.ts`, `audio.ts`, `engine.ts` and `stage.test.ts` did not.
- Old: "Registers the new reading in the three places the graph is declared (`readings.ts:50-66`, `:76-95`, `:97-110`)". New: five places, `TuningReadings` (72-85), `ReadingsAcc` (87-100), `createReadings` (109-125), `observeReadings` (135-154) and `readingsOf` (156-169). A new reading needs an entry in all five and the plan named three, none of which were the right ranges.
- Old: "**fifteen test files that import it only to silence spawns**" followed by a list of twelve. New: twelve, listed, with the sentence saying that fifteen test files import `RAMP_ROWS` in all, which is the count verification step 5 names. `carriers.test.ts` is a reader too and reads the rows as well as silencing them, so it is named on the main readers line rather than inside the twelve.
- Old: `PHASES` readers include `bot.test.ts:140-143`. New: they do not. That helper is `phaseOrder` and it reads `phaseChanged.phase`, never `PHASES`, so it is listed under `PhaseName` instead.
- Old: "`soulStream` survives only at `src/game/__tests__/witness.test.ts`". New: it survives in three test files that pin the old name on purpose (`witness.test.ts:852`, `startingLevels.test.ts:98,102,109`, `roster.test.ts:70`).

**Tests and assumptions that had no slice, and where they were placed.** Section 10 assigned every test from 1 to 131. The four the step 1 implementation gate added, 132 to 135, had none.

- **132** (a row carries or does not, and `carriers.ts` alone says which placement holds the offer) goes to **slice 4**, the carrier schedule on the new rows, beside module test 120 which is the same table's other carrier rule.
- **133** (a banked offer opens on the first permitting tick and stays banked through a phase that does not) goes to **slice 2**, because the plan's own gate block says the site and the column land "in the slice that lands the phase columns" and that is slice 2. Slice 2's text now says so.
- **134** (an offer whose three bodies are all refused at the corpse cap is banked, not lost) and **135** (a carrier the mob cap refuses is announced as `carrierLost` with the cap as its reason) go to **slice 5**, because the same gate block says both become reachable the moment slice 5's corpse cap refuses and that "slice 5 therefore lands" them.
- One seam was missing under those tests and is added: **section 4's `Phase` block did not carry `bankOpens`**, which the gate block rules and which test 133 is written against. `readonly bankOpens: boolean` is now on the interface with the reason beside it. A coder implementing section 4 as written would have had no column for the test to read.
- Slice 5's sentence also now names the `corpseEvicted` and `oldestEvictable` retirement and the two supply refusals outright. Section 9 item 6 is the assumption that retiring the event is safe, and the slice carried it only as "never evicting", which does not name `events.ts` at all.

Every other section 9 item is carried by a slice: 1 by slice 2's glossary commit, 2 by slice 3, 3 by slice 10, 4 by slice 2's ceiling rows, 5 by slice 2 declaring `PhaseMusic` and 12a filling it, 7 by slice 6, 8 by slice 12a, 9 by slice 2's test 6, 11 by slices 6 and 14, 12 by slice 6. Item 10 is a decline with a trigger and needs no slice.

**Consistency after the gate fixes.** The plan was checked against the eight tech-gate findings the handoff lists and no earlier passage contradicts them: no passage calls a ceiling an invariant, no seam parses an encoder header, `setPiece.ts` declares no magnitude anywhere it is described, `WITNESS_VERSION` moves once, slices 12 and 13 are split, `pngjs` is in the module table and in slice 13a. The slice count holds (nineteen slices, eighteen commits) and so does the test count (67 spec, 61 module, 7 fence, 135 in all), re-derived from the numbered lists rather than trusted. The pour-rows-in-`rows.ts` contradiction at the old line 591 was already fixed by step 1's tip review and no other passage carries it.

## 1. Slices committed

| Slice | Commit | Message |
| --- | --- | --- |
| 1 | `6958cfdd28` | refactor(hungry-grave): the stage tables move into rows.ts unchanged (#97) |
| 2 | `1e420aeb53` | feat(hungry-grave): the stage is three named sections with one property each (#97) |
| 3 | `4d0b156a72` | feat(hungry-grave): a phase ends on its own condition and the drain-out is a sparse row (#97) |
| 4 | `2567dd0e95` | test(hungry-grave): the carrier schedule pays a full build before the set piece (#97) |
| 5 | `b324994400` | feat(hungry-grave): the corpse cap refuses and faults instead of evicting food (#97) |
| 6 | `5eaa6d2759` | feat(hungry-grave): bosses are bought in chunks and every weapon line fires at one target seam (#97) |
| 7 | `c06f2e0c46` | feat(hungry-grave): the Banshee fights in two chunks and her death starts the Wall's clock (#97) |
| 8 | `31c48ee1e8` | feat(hungry-grave): the Undertaker fights in three chunks of curtains, spiral and diggers (#97) |
| 9 | `e024ebb7c8` | feat(hungry-grave): victory is the Undertaker's death and the ladder falls in order (#97) |
| 10 | `b0b4f4a4f5` | feat(hungry-grave): the Waking opens mid-field and pours until its budget is spent (#97) |
| 11 | `55ed3230c7` | feat(hungry-grave): a section timeline reading reports what each section held (#97) |
| 12a | `b640d4e424` | feat(hungry-grave): each phase names its loop and the app plays it (#97) |
| 12b | `f23d8eb162` | test(hungry-grave): no loop is trimmed by a header parse, and the three wraps are measured (#97) |
| 13a | `3a7a85d5b1` | feat(hungry-grave): sprites and tiles are imported grayscale, and a fence keeps them so (#97) |
| 13b | `7570270a80` | feat(hungry-grave): each section dresses the ground in its own colour family (#97) |
| Ground adjustment 1 | `21b5de181a` | feat(hungry-grave): the ground keeps what lands on it, lays as one floor, and is dressed thick (#97) |
| 14 | `c2a2ac3e52` | feat(hungry-grave): both bosses are drawn and boss fire draws in its own kind (#97) |
| Adjustment iteration 2 | `e636411950` | feat(hungry-grave): the Waking rides the ground at the field's own scroll (#97) |
| Adjustment iteration 3 | `d58448a6e3` | feat(hungry-grave): a body that appears inside the field arrives before it can touch the grave (#97) |
| 15 and 16 | `715575a37d` | test(hungry-grave): the curtain's coverage is filled and each full-build seed carries its own budget (#97) |

Slice 0 records the baseline tapes and makes no commit. Ground adjustment 1 is not in the plan's section 10: it is an iteration on slice 13b that Mark asked for on 2026-09-08 after playing the 13b deploy on his phone, and it runs as a slice of its own. Adjustment iteration 2 is not in it either: it is an iteration on slice 10 that Mark ruled on 2026-09-08 after adjustment 1 changed what the Waking's source sits in, and it runs as a slice of its own too.

## 2. GOLDEN moves

Slice 1: none, which the slice required. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 2: two fields, `mobs` 6 to 5 and `checksum` -1111652845 to -141809765, and nothing else. The scenario's 600 ticks fall inside the Procession, which owns emptiness, so its second row moved from t=8 to t=11 and one authored body that used to be on the field at tick 600 has not arrived yet. Every other field held: the grave's position and size, the reservoir, the score, the two scripted kills, and all five stream cursors. The re-pin's reason is written above `GOLDEN` in `src/dev/digest.ts`.

Slice 3: none, and the slice claimed none. The scenario's 600 ticks are inside the Procession, whose rows before t=112 did not move, and no phase can end inside them, so `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 4: none, and none was possible. The commit holds one test file and no production code at all, so nothing the digest reads can have moved; `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 5: none, and none was expected. `CORPSE_CAP` rose from 200 to 233, which lengthens a pre-allocated pool and changes no behaviour the scenario can reach: the digest's 600 ticks hold one corpse. The refusal path is unreachable in the scenario for the same reason, and `src/dev/digest.ts` is not in the commit with the digest test green.

Slice 6: one field, `checksum`, from -141809765 to -36124581, and nothing else. The witness now folds the one boss and the one set piece, and the scenario's 600 ticks meet neither, so what moved the number is the two absent-field sentinels ADR 0019 requires rather than any behaviour. Every other field held, the two scripted kills and all five stream cursors included. This is the step's one `WITNESS_VERSION` move, 5 to 6, and the re-pin's reason is written above `GOLDEN`.

Slice 7: none, and the slice claimed none. `GOLDEN`'s scenario is 600 ticks from the top of the Procession and the Banshee arrives at tick 7107 under the same still hand, so nothing this slice changed is inside the window. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 8: none, and the slice claimed none. `GOLDEN`'s scenario is 600 ticks from the top of the Procession and the Undertaker stands two sections and two fights past it, so nothing this slice changed is inside the window. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 9: none, and the slice claimed none. What moved is when the run's ending fires, and the scenario's 600 ticks are in the Procession with no boss in them and no ending reached. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 10: none, and the slice claimed none. `GOLDEN`'s scenario is 600 ticks from the top of the Procession, and the source is placed at the Crowd's own t=120, two sections and a fight past it. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 11: none, and none was possible. The commit holds one new reading, its registration, its comparison entry and two tests, and not one line the simulation runs: a reading is a fold over events the sim already emitted, so nothing the digest reads can have moved. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 12a: none, and the slice claimed none. The witness folds `RunState` and never the event list, so a payload gaining a field moves nothing the digest reads, and the only sim change beside it is where `enterNextPhase` builds the event it already pushed. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 12b: none, and none was possible. The commit holds one test file and no production code at all, so nothing the digest reads can have moved; `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 13a: none, and none was possible. The commit holds an import script, a fence test and twenty-seven PNGs, and not one line the simulation runs: nothing under `src/game` is in it and nothing the app builds imports the script. `src/dev/digest.ts` is not in the commit and the digest test is green.

Slice 13b: none, and none was possible. Nothing under `src/game` is in the commit at all: what it holds is two renderer modules, the palette, the two screens that attach them, `main.ts`, the import script and twenty-five re-imported PNGs. A renderer reads the run and never writes to it, so nothing the digest folds can have moved; `src/dev/digest.ts` is not in the commit and the digest test is green.

Ground adjustment 1: none, and none was possible. Nothing under `src/game` is in the commit at all: what it holds is the renderer, its art library, its tests, the import script, the design record and nine PNGs. A renderer reads the run and never writes to it, so nothing the digest folds can have moved; `src/dev/digest.ts` is not in the commit and the digest test is green. The adjustment's own prompt says GOLDEN moving would mean the slice is wrong, and it did not move.

Slice 14: none, and none was possible. Nothing under `src/game` is in the commit at all: what it holds is two renderer modules, the shot sprite, the field renderer's own sync, the two screens that attach the boss, and three test files. A renderer reads the run and never writes to it, so nothing the digest folds can have moved; `src/dev/digest.ts` is not in the commit and the digest test is green.

Slices 15 and 16: none, and the pass required none. The commit holds two test files and no production code at all, so nothing the digest reads can have moved; `src/dev/digest.ts` is not in the commit and both digest suites are green, `src/game/__tests__/digest.test.ts`'s seven and `src/dev/__tests__/digest.test.ts`'s one.

Adjustment iteration 2: **none, and its prompt is wrong to expect otherwise.** The commit moves three rows and two behaviour sites, all of them the Waking's, and `GOLDEN`'s scenario is 600 ticks from `SEED` 20260820 (`digest.ts:18-19`), which is inside the Procession with the source two sections and a fight past it. Nothing the digest folds can have moved; `src/dev/digest.ts` is not in the commit and the digest test is green. What did move, and what section 25 names field by field, is the seed 101 tape: the source's placement tick, its own positions, the poured bodies' positions and the reading of the Waking. Nothing before tick 15726 moved, and that is a `foldWitness` checksum measured in both trees rather than a claim.

## 3. CodeRabbit

Slices 15 and 16: one run of `coderabbit review --agent --uncommitted` over the two staged files, **zero findings**. Nothing applied and nothing declined.

Slice 1: `coderabbit review --agent --uncommitted` on the staged work, all eighteen files reviewed, **zero findings**. Nothing applied and nothing declined.

Slice 2: two runs over the thirteen staged files. The first found one Minor and it was real: `expect(STILL_PLAY.state.ending).not.toBe('sealed')` in `stage.test.ts` could never fail, because the rig clears a sealed ending every tick to keep the stage's clock running. **Applied**: the assertion is now that the still hand held more than the ceiling for over a second of ticks and that the run crossed to the `over` phase, which is what the rig's throw-on-fault actually proves. The second run, on the fixed tree, reported zero findings. Nothing declined.

Slice 3: two runs over the seven staged files.

- **Applied**, one Minor on the first run and it was real: module test 65 compared a tail counted in whole ticks against `SLOWEST_DESCENT_TICKS`, which is 1091.23. A descent that spent the whole 1092nd tick would read as over the bound. The bound is now `Math.ceil`ed with the reason written beside it.
- **Declined**, one Major on the second run, and it is a plan-shaped finding rather than a defect: it asks that `bossKilled` and `setPieceOpened` be evaluated through their own phase state rather than falling back to the phase's rows running out. That state does not exist yet. `RunState.boss` and `RunState.setPiece` are declared at slice 6, the bosses are slices 7 and 8, and the source is slice 10; implementing the finding today would stall every run in the Banshee phase and make the stage untraversable, which the plan's own slice 9 says outright ("no run reaches the Vigil before the source exists"). The stand-in is written in `phaseEnded`'s JSDoc and both halves of it are already pinned as named todos: module test 66 for a boss phase that is no longer empty, and spec test 115 for the Crowd ending on the eye opening.

Slice 4: one run of `coderabbit review --agent --uncommitted` over the one staged file, **zero findings**. Nothing applied and nothing declined.

Slice 5: one run of `coderabbit review --agent --uncommitted` over the twenty-three staged files, **zero findings**. Nothing applied and nothing declined.

Slice 6: two runs over the thirty-three staged files. The first found one Minor and it was real: fence 124's roster sweep built its expected string out of the actual one (`walks ${walksTheField}, fenced ${walksTheField || named}`), so the fenced half of it asserted nothing at all and a fifth line could arrive un-fenced. **Applied**, and not in the form the finding suggested: requiring every line's module to be in `STORM_MODULES` fails today, because the skull stream's targeting lives in `storm.ts` and its own module has nothing to reach the seam for. The property that binds every line is the narrower one, that no line's module walks the mob pool at all, and the sweep asserts that directly now. It was proved to bite by planting a walk in `skullStream.ts` and watching it go red. The second run, on the fixed tree, reported zero findings. Nothing declined.

Slice 7: one run of `coderabbit review --agent --uncommitted` over the eleven staged files, **zero findings**. Nothing applied and nothing declined.

Slice 8: one run of `coderabbit review --agent --uncommitted` over the eight staged files, **zero findings**. Nothing applied and nothing declined.

Slice 9: one run of `coderabbit review --agent --uncommitted` over the nine staged files, **zero findings**. Nothing applied and nothing declined.

Slice 10: two runs, the first over fourteen staged files and the second over sixteen. The first found one Minor and it was real: `observeTerritoryControl` records a pulse pace off any territory-sourced `mobDamaged`, and the storm now reaches a boss and the set piece's source through the same seam it reaches a mob through, so a source standing under one patch for a whole pour would report a pace no mob ever walked, and its entry would never be cleared, because what clears one is a `mobKilled`. **Applied**: a pulse is recorded only for an id standing in the mob pool, with a test that a hit on something outside it reads no pace and that the same two ticks on a real mob do. The defect is older than this slice, since a boss has reached that seam since slice 6, and what this slice did was widen it. The second run, on the fixed tree, reported zero findings. Nothing declined.

Slice 11: one run of `coderabbit review --agent --uncommitted` over the six staged files, **zero findings**. Nothing applied and nothing declined.

Slice 12a: one run of `coderabbit review --agent --uncommitted` over the fifteen staged files, one Major, **declined**. It asks that `musicChannel` and its `Assets.loadBundle` move out of `src/main.ts` into a module that owns music behaviour, leaving main.ts to composition alone. Declined for three reasons written together: the plan's own module table puts the music power in `src/main.ts` beside `playSound`; `src/app/sound.ts` cannot take it, because the boundary rule that governs that one file allows `game/events` and the `@pixi/sound` package and nothing else, and `Assets` is `pixi.js`; and a new module for an eight-line adapter fails the deletion test and would be the only power in the app not built in main.ts, where `backdropPowers`, `buttonSound` and `volumePowers` each already carry their own small mechanics. The standing rule is the one that settles it: a dependency used at one site is used directly there, and that site is the boundary (`.claude/rules/code-core.md`).

Slice 12b: one run of `coderabbit review --agent --uncommitted` over the one staged file, **zero findings**. Nothing applied and nothing declined.

Slice 13a: one run of `coderabbit review --agent --uncommitted` over the thirty-one staged files, **zero findings**. Nothing applied and nothing declined. It also read `docs/push/path-draft.md`, which is uncommitted and is not this slice's; see section 7.

Slice 13b: two runs over the thirty-nine staged files plus the two uncommitted documents named in section 22.

- **Applied**, one Minor on the first run and it was real: `BackgroundRenderer`'s texture cache keyed a cut cell by its `x` alone, so two cells of one sheet differing only in row, width or height would have shared a texture. No two cells in today's tables collide, which is why nothing was wrong on screen; the key now carries all four numbers. The first run also died on a WebSocket close after three findings, so it is not a complete review and the second one is what the slice stands on.
- **Declined**, two Minors, and both are the same finding on work that is not this slice's: `docs/push/handoff.md` and `docs/push/step-2-slice-prompts.md` are uncommitted in this worktree, written by the dispatching session, and each carries a `SLICE_13B_SUMMARY` or `SLICE_13B_HASH` placeholder for a commit that did not exist while the review ran. CodeRabbit reads the whole uncommitted tree, which is how it saw them; they are neither staged nor committed here.
- The second run, on the fixed tree, reported those two and **nothing at all on this slice's own files**.

Ground adjustment 1: two runs over the fourteen staged files.

- **Applied**, one Major on the first run and it was worth taking even though it changes nothing today: `baseGreyOf` in `grayscale-import.ts` counted every pixel's red channel when it looked for the rock's own grey, including fully transparent ones, where the stretch beside it already skips them. `Terrain/Tiles.png` is opaque in all 5120 pixels so the answer was right, and it was checked rather than argued: the baked `floor.png` is byte-identical before and after the fix, same md5. The function now says what it means and would still be right on a sheet with holes in it.
- **Nothing declined.** The second run, on the fixed tree, reported zero findings.

Slice 14: two runs over the nine staged files plus the two uncommitted documents named in section 24. The first found two and both were real.

- **Applied**, one Major: the compile-time guard that `FireKind` and `FireEmitter` are the same four members was written as `A extends B ? (B extends A ? true : never) : never`, which is a **distributive** conditional over a union, so it answers "does any member match" rather than "are these the same set". Adding a fifth member to one union alone would have left it green. Both sides are wrapped in a tuple now, and it was proved to bite the way the finding said it would not: adding `'hail'` to `FireEmitter` turns the assignment into `Type 'true' is not assignable to type 'never'`. It is the toothless-assertion shape `docs/agents/lessons.md` already names, arriving in a type this time rather than in a table.
- **Applied**, one Minor, and it is the one real defect in the slice: `FLASH_HALF_PERIOD` was 5, which at 60 Hz is **six flashes a second**, and the comment beside it claimed the opposite, that it was under the three-a-second limit. A boss body is the largest sprite on the field, so WCAG SC 2.3.1's small-area escape hatch cannot apply and it binds exactly as it binds the hit dim (`tuning.ts`'s own derivation for `INVULNERABLE_TICKS`) and the corpse flicker. A flash is a pair of opposing changes, so the period is twice the half and the floor is eleven ticks: it is **12** now, the figure the corpse flicker already takes. The number had no guard, so a new test was written for it and it reads the rate off what the renderer drew rather than off the constant. Both rendered flash frames were re-shot after the change, because the ones taken before it show a period the build no longer has.
- **Declined**, one Minor on the second run, and it is work that is not this slice's: `docs/push/handoff.md` is uncommitted in this worktree, written by the dispatching session, and the finding asks that its next-steps sequence put adjustment iteration 2 before slice 15. That is the dispatching session's own ordering to write and the file is neither staged nor committed here. CodeRabbit reads the whole uncommitted tree, which is how it saw it.
- The second run reported **nothing at all on this slice's own files**.

Adjustment iteration 2: one run of `coderabbit review --agent --uncommitted` over the eight staged files, one Major, **declined**.

- It asks that the design record's Waking boundary be reconciled with its own nominal clock: section 1's table gives the Crowd 2:45 to 5:20, 155 seconds, and the eye opens at the Crowd's second 140. The disagreement is real and it is not this commit's. **The Crowd has measured 140 seconds since slice 10** and section 18's two timeline tables record it at 140.0 on both hands; this slice did not move the Crowd's length, the opening tick, or the Waking's own 900 ticks, all three of which are identical either side of the change and are measured in section 25. Where the 155 came from is worth writing down: `rows.ts`'s own comment said the source is placed "two seconds before its own last row", which with the old twenty-second fall would have opened the eye at about second 156, and the placing row said 120 and never matched its comment. That false comment is recorded as item 72 below and is rewritten in this commit. Reconciling a nominal figure against a measured one is the tuning pass's job (#31, #36); the record's own sentence beside the table already says every figure in it is an initial row and design intent rather than a run clock, and replacing intent with today's measurement is a decision rather than a fix.

## 4. Plan claims found false against the tree

Four, all from slice 1, and each one is a slice assignment rather than a citation. The plan's intent was followed and its letter was not.

1. **Slice 1 says "the three tables move out of `stage.ts` under their old content and new names", and the tree carries two tables.** `RAMP_ROWS` and `BACK_HALF_ROWS` are all there is, and section 7 says `BACK_HALF_ROWS` is "replaced by `CROWD_ROWS` and `VIGIL_ROWS`", which is a split and therefore authoring. Slice 2 owns the authoring, so the Procession took the ramp's rows and the Crowd took the back half's, both byte-identical, and `VIGIL_ROWS` landed as an empty table with its reason on it. `PHASES` still names two spawning phases, so behaviour is unchanged.
2. **Slice 1 says module test 71 lands here, and `directed` does not exist until slice 2.** Slice 2's own sentence names `StageRow.directed` and `Phase.directed` among the columns it lands. A test cannot be green against a column that is not there, so 71 is pinned in `rows.test.ts` as a named `test.todo`, which is the playbook's own mechanism for a planned test whose seam arrives later. **Slice 2 fills it**, beside module test 119, which is its sibling over the ceiling columns.
3. **Test 67 names the pour among `peakArrivals`'s terms, and the pour's rows land at slice 5.** Slice 5 says so outright: the set piece's own rows and `POUR_SHARES` go into `rows.ts` there, "because this is the slice where `peakArrivals` first needs them". The query therefore landed with three of its four terms, and test 67 holds those three. **Slice 5 adds the pour term and widens the test with it.**
4. **Fence 111 is an existing test, not new code.** Section 6 lists `src/__tests__/boundary.test.ts` under "existing, must stay green", so slice 1 confirmed it rather than writing it. `rows.ts` value-imports nothing from `src/game` (`MobType` and `TemplateName` are both `import type`), so the cycle guard's known-cycle list is still empty.

Three more from slice 2, and each one is a seam the plan left short rather than a citation that moved.

5. **Module test 66 cannot be green in this slice, and it is pinned as a `test.todo`.** It reads "two boundaries can no longer fall on one tick, because no phase is empty," and the three boundary phases are still empty: `phaseLengthTicks` gives a rowless phase a length of zero, so the Banshee begins and ends on the tick the Procession ends, and the same holds for the Waking and the Undertaker. Slice 3 does not close it either, because `phaseEnded` reading `Phase.ends` gives a boss phase `bossKilled` and there is no boss to kill until slice 7. **The todo is named `crosses no two boundaries on one tick, because no phase is empty` in `stage.test.ts`, and the slice that makes the last boundary phase real is the one that fills it.** Everything else the plan assigns to slice 2 landed.
6. **Section 4's `PhaseEnd` union has no member for the set piece phase.** It is `'rowsSpentAndFieldClear' | 'setPieceOpened' | 'bossKilled'`, and section 4 names the end condition for three phases: the two boss boundaries, the Crowd, and the boss phases. The Waking is not among them. It authors no rows and sheds no boss, so what clears its field is the pour running out, and the union says nothing about that. The column is filled with `rowsSpentAndFieldClear` and the reason is written beside the row: nothing reads `ends` until slice 3 and nothing fills the Waking until slice 10, so **slice 10 rules whether the set piece's end wants a member of its own.** The `over` phase carries the same value for the same reason, that nothing reads it.
7. **`Phase.bankOpens` had no reader, and reading it from `step.ts` would have broken section 5's own rule.** The step 1 gate block asks for "a per-tick check in `offer.ts` ... called from `step.ts`", and the check needs the current phase's column. Section 5 says nothing outside a module indexes another's rows, so `step.ts` cannot index `PHASES`, and `offer.ts` importing `stage.ts` would put a new edge in the core's import graph for one boolean. **A seam was added: `bankOpensNow(state)` in `stage.ts`**, which is the phase table's own reader, and `step.ts` hands its answer to `openBankedOffer(state, permitted)`. The bank never learns which phase the run is in. It is named in section 9 below.

Three from slice 3, and the first of them is the one a later slice most needs to know.

8. **The `firedRows = ROWS.length` idiom no longer silences the stage, and this slice is what made it wrong.** The plan's section 7 calls it a smell and hands it to #59, which was true while a phase ended on a tick count. A phase ends now on its rows being spent and its field clearing, so a test that marks one phase's rows fired rolls into the next section's rows the tick its own field empties, and a pooled mob slot is then refilled by a spawn the test never asked for. Three rigs broke and each is fixed the same way, by standing the run in the last phase of the table, which is the one phase `advanceStage` never leaves: `quietRun` in `mobs.test.ts` and in `carriers.test.ts`, and `wallRun` in `bot.test.ts`. **Thirteen test files still set `firedRows` and all are green**, because each one's window closes before its field empties. They are left alone rather than swept: the sweep is #59's, and `offer.test.ts` in particular cannot take the same fix, since the last phase is the one phase whose `bankOpens` is false and that file is where the bank is tested. **Trigger for #59, and for any later slice whose test suddenly meets mobs it did not spawn.**
9. **Slice 2 filled the Waking's `ends` with `rowsSpentAndFieldClear` because nothing read it, and this slice reads it.** So the set piece's phase now waits for the trash the Crowd handed it to leave, about fifteen seconds under a hand that kills nothing, before the Vigil begins. It is stated rather than a surprise and it is why `stage.test.ts`'s stubbed-boss-phase test no longer names the Waking. Slice 10 still rules whether the set piece's end wants a member of its own on `PhaseEnd`.
10. **The plan deletes `phaseLengthTicks` and names its readers, and says nothing about what replaces them.** Two of those readers are budgets rather than lengths: `STAGE_TICKS` in `stage.test.ts` and `PROCESSION_TICKS`/`STAGE_TICKS` in `bot.test.ts`, which cap how long a harness plays. There is no length to read any more, so each is now each phase's own last row plus one whole descent, with the descent derived from the mob table and the field rather than written down. It is a ceiling and not a prediction, and both files say so where they declare it.

Two from slice 4, and both are about where the work had already been done rather than about a citation.

11. **Slice 4 reads as though it authors the schedule, and slice 2 authored all of it.** Section 10 says slice 4 is "the carrier schedule on the new rows. Eight, eleven and six, on File and V rows in the Procession and never on a Drip", while slice 2's own sentence says which rows may carry is a property of the table and is authored there. The tree agrees with slice 2: the three tables already stand at eight, eleven and six through `carrierRow`, every Procession carrier is on a File or a V, and no Drip anywhere carries. **So slice 4 changed no production code**; what it owed was the four tests, and they are what the commit holds. The plan's intent, that the schedule and its rules are held by tests before the corpse cap starts refusing spawns at slice 5, is met.
12. **The Crowd's last row fires on the tick that section's rows run out, so its carrier is met just inside the Waking.** Spec test 54 says "before the set piece", and the Crowd's `ends` is still slice 3's stand-in, which falls back to the rows running out; the last row is `t=138` and it carries. The run rig therefore plays on past the boundary while a carrier stands, an offer is live or an offer is banked, which is a second and a bit, and everything of the set piece is still absent from the field. **Slice 10 is what settles this**: once the Crowd ends on the eye opening and its rows keep firing under the pour, the last carriers arrive under a source that is already open, and whether the sentence wants the boundary or the source's opening is that slice's to rule.

Five from slice 5, and the first two are numbers the plan carried from before slice 2 re-authored the tables.

13. **Section 7 says `CORPSE_CAP` becomes "initially 232", and it lands at 233.** The design record derives 232 from a Crowd whose densest ten seconds was 36, which is the table as it stood before slice 2. That table now authors 39 in its densest ten seconds, so the share it keeps firing at under the pour is 13 rather than 12 and the arrivals term is 63 rather than 62. **The cap is 160 + 63 + 10 = 233.** The number moving with the rows is the whole point of deriving it, and no test pins either figure: both sides of every assertion are computed from the same tables.
14. **Section 4 names `SET_PIECE_POUR_TICKS`, and a module that value-imports nothing cannot know how long a tick is.** Section 5's own row for `rows.ts` says it value-imports nothing from `src/game`, and slice 3 already met this and took the same answer for the sparse row's spacing. The row lands as **`SET_PIECE_POUR_SECONDS`, 0.2**, which is the plan's twelve ticks in the table's own clock. `setPiece.ts` holds the clock and converts at slice 10, which is the direction section 5 already sets: behaviour there, data here.
15. **Section 7 says `FAULT_IDENTITIES` gains three, and slice 5 alone gains three.** The corpse refusal, the carrier refusal and the offer refusal are the three the step 1 gate block asks for by name, and they take wire codes 16, 17 and 18. Slice 6's own invariants, module tests 95 and 96, need two more, so **the step appends five and the codes run to 20.** Append-only, so nothing is lost, but a later slice reading section 7's "three appended" as the step's total would be wrong.
16. **`SET_PIECE_PLACED_AT` is in section 4's `rows.ts` block and is not in this slice.** Slice 5's own sentence lands "the pour's own rows and `POUR_SHARES`", and module test 128 names the budget, the interval, the health, the sweep bounds and the share. The placement second is none of those: it is the phase-local time of a Crowd row that slice 10 authors, and an exported constant with no caller and no test is dead weight. **Slice 10 lands it beside the row that reads it.**
17. **Module test 121's other half cannot be green in this slice.** It reads "no boss's authored add cadence exceeds `BOSS_ADD_ALLOWANCE`, and no build holds more rungs than `RUNG_ALLOWANCE`". The rung half is green and derived, `RUNG_ALLOWANCE` against `carriersForFullBuild()`. The cadence half needs a boss module, which lands at slice 8, so it is pinned as the named todo `holds every add a boss's authored cadence sheds inside a freshness window` in `rows.test.ts`. **Slice 8 fills it.**

Six from slice 6, and the first of them is a ruling the plan set aside rather than a citation that moved.

18. **ADR 0008 asks for boss damage from the belch's burst, and the plan builds none.** The ADR's own sentence is that the burst "deals its big chunk of boss damage only when the boss is inside that radius, and never pushes a boss", and `belch.ts` carried a comment saying the boss half was a stub the boss dispatch would fill. The plan rules the other way, in section 4's `killableOutright` block: the burst is a kill rule rather than a damage number, a kill rule reaching a chunked body would break a chunk outright, and the alternative it names is "a belch damage number against a chunked target, which is a magnitude nobody has measured". **The plan's ruling is what landed in slice 6**: a boss read as not killable outright, the burst skipped it, and what a belch bought in a fight was the gas. **Slice 7 closed the gap under the dispatching session's own ruling, which is in section 5 below**: the burst deals a data row of damage to a chunked target inside its radius, through the seam, never the kill rule and never a push. `killableOutright` keeps meaning that the kill rule does not apply; what changed is what lands instead of it. The row is `BELCH_CHUNK_DAMAGE` in `belch.ts` and it is no longer unowned.
19. **`Boss` needs an id, and section 4's block does not declare one.** The same section reasons about "a boss's id" three paragraphs later, where it rules that `timeToKill` and `replayTallies` stay mob-only, so the id was assumed and never written into the interface. `mobDamaged` carries the join key and module test 79 asks for a `mobDamaged` on an invincible boss, so without an id there is nothing to report under. It is `readonly id`, drawn from `nextEntityId` like every other entity, and the witness excludes it as spawn identity exactly as `mobs[].id` is excluded.
20. **The plan says the two readings that join `mobDamaged.id` against the mob pool "stay mob-only and say so in a comment", and a comment would not have been enough.** `timeToKill.ts`'s `typeOfMob` **throws** on an id with no pool slot, with its own comment calling that a bug rather than a reading. A boss's damage is exactly that id, so the reading would have thrown inside the harness on the first boss hit. It returns null now and the engagement is not opened, and **the guard it used to carry has not been given up**: an engagement that never closes is caught on the kill side by `closeEngagement`, whose event is a mob's by construction because a boss's death is a `bossKilled`.
21. **Section 4's `chunks.ts` block declares no body and no standing point, and the seam cannot work without them.** `bossHitbox` needs half extents and `spawnBoss` needs somewhere to put the boss, or the storm has nothing to overlap and spec test 25 cannot be green. `BOSS_HALF_WIDTH`, `BOSS_HALF_HEIGHT` and `BOSS_ARRIVAL_Y` land as initial rows in `chunks.ts`, one size for both bosses rather than a row each, because nothing this slice builds tells them apart by shape: the two silhouettes are the renderer's at slice 14. **Slice 7 or 8 takes a per-boss row the day a pattern needs one.**
22. **Section 4's `CHUNK_HP` block says "Four fields on the row and no more", and neither thing it could be describing has four fields.** `CHUNK_HP` is a `Record<BossKind, readonly number[]>` and has no fields at all; `Boss` as the same block declares it has seven. It was read as what the block plainly intends, that the seam is implemented as written and nothing is added to it without a caller today, and the one addition is the id above, which has one.
23. **`RunState.refusals` was already decided in the witness partition, so this slice had nothing to rule.** The dispatch asks the slice that moves `WITNESS_VERSION` to decide whether the refusal ledger folds or is excluded. Slice 5 decided it and wrote the reason: all three paths are in `witness.test.ts`'s `EXCLUDED`, because every refusal is decided by the pools the fold already walks. The version move does not change that, and the exclusion is confirmed rather than re-argued.

Five from slice 7, and the first is a claim about the tree rather than about a slice.

24. **The plan says the two Wall policies from ADR 0042 land in this slice, and both already stood.** `unloadedPolicy` and `belchingPolicy` are in `bot.ts`, and `bot.test.ts`'s `the Wall's two-sided property (ADR 0042)` has carried both halves against a hand-built curtain since before this step began. What was missing was never the policies: it was the **anchor**, the Wall's row firing two seconds after a real Banshee died rather than two seconds into a phase a stub fell through. That is what spec test 33 now plays, through the boundary itself. The hand-built rig is untouched, because what it holds is the curtain's own property at a pinned build and neither half of that moved.
25. **Section 4's `chunks.ts` block declares no seam for reaching a boss's own module, and both halves of the fight need one.** The block says `advanceBoss` is "delegated to the boss's own module" and `banshee.ts` declares `bansheeDied`, and nothing in either says how `chunks.ts` reaches them. A module-private `BOSS_GRAMMARS` table keyed by `BossKind` is what landed, holding one `{ advance, died }` record per boss, with two readers exported beside it: `bossChunks(kind)`, so `stage.ts` can announce the fight's shape without indexing another module's rows, and `bossIsAuthored(kind)` for the item below.
26. **A boss whose grammar is not written must not arrive, and no document says so.** Wiring the arrival on `Phase.boss` alone puts the Undertaker on the field at slice 7 with no pattern to dodge and 5100 points of health no birthright storm can empty, which stalls every run in his phase and makes the stage untraversable end to end. So `arriveBoss` reads `bossIsAuthored` and a boss with no grammar does not arrive at all, which leaves that phase standing on exactly the stand-in slice 3 gave it: nothing on the field means `phaseEnded` is met on the tick the phase begins. **Slice 8 deletes the gate by writing `undertaker.ts`**, and nothing else has to move for it.
27. **Four rigs outside the boss modules had to be told what to do with a boss, and the plan names none of them.** Every one of them plays a run across a boundary for a reason that is not the fight, so each empties whatever boss arrives and says why in its own words: `playToTheWaking` in `rows.test.ts` (spec 54, which is about carriers), the sparse-row boundary test in `bot.test.ts` (which is about what the rows put on the field at a boundary), and `bansheePhaseHeldFor` in `stage.test.ts` (which is spec 7's own instrument). The fourth is `corpses.test.ts`'s cap-binding run, which used to get its traffic by rolling out of the Procession into the Crowd's rows and now stands in the Crowd outright. It is the same shape as slice 3's finding 8: **a rig that crosses a boundary now crosses a fight**, and the next slice's rigs will meet it again with the Undertaker.
28. **Module test 66 still cannot be green, and it needs the Undertaker.** It reads "two boundaries can no longer fall on one tick, because no phase is empty". The Banshee's phase is no longer empty, and the Undertaker's still is: it authors no rows and no boss arrives in it, so the Vigil, the Undertaker and the over phase still cross on one tick. **Slice 8 fills it**, and the half that is already true is asserted meanwhile in `holds a boss phase open for its fight, and ends a stubbed one on the tick it begins`.

Four from slice 8, and the first is the seam the plan left short rather than a citation that moved.

29. **Section 4 declares `CURTAIN_ROWS` and `SPIRAL_ROWS` as `readonly CurtainRow[]` and `readonly SpiralRow[]`, and his chunks do not divide that way.** Chunk one throws curtains and no spiral, chunk two turns the arm and throws no curtain, and chunk three does both, so neither table has one row per chunk unless a chunk that does not run a shape can say so. **Both tables are `readonly (CurtainRow | null)[]`, three long, indexed by chunk**, which makes the chunk count one fact in three tables rather than a length in `CHUNK_HP` and two shorter lists nobody can index. The alternative, tables keyed by chunk index, hides the same fact behind a lookup and gives no place for a fourth chunk to be missing from.
30. **Section 4 names `GAP_MARGIN` and nothing else the two patterns need, and four more rows had to be authored.** `GAP_WALK` is how far the way through walks between curtains when nothing else places it, `GAP_LAG_TICKS` is how far behind the arm it sits in the locked overlap, `ARM_REACH` is how far the arm reaches and so how much of the field it sweeps, and `DIGGER_TYPE` is what he digs up. All four are initial rows in his own module beside the two tables, which is where section 5 puts a boss's numbers, and each is exported because his tests read the row rather than a figure written twice.
31. **The design record's "one emit" is the pattern's own cycle and not the clod's flight**, and reading it the other way would have made the curtain faster than a trash shot. Section 8 derives the chunk-health floor from "one clod curtain falling from the top edge with its gap sweeping once, four seconds", and a clod crossing the field's 760 units in four seconds travels at 190 a second against the trash shot's 110, which contradicts "slow clod curtains" in the same sentence. **The four seconds is the curtain's period**, which is what the test reads as one emit; the clod's own speed is a separate initial row at 95 a second, slower than a trash shot and a little faster than a tear.
32. **A boss test cannot switch the storm off by zeroing the birthright, and the rig found it the honest way.** Standing a run at `skullStream` level zero raises the fatal `levels in range` invariant on the first tick, because the birthright is what a run is born holding (ADR 0045). The pattern rig holds the live chunk full instead, which is the same intent with none of the lying: the storm still works and what it takes off a chunk stays chunks.ts's question.

Four from slice 9, and the first is what the plan's own two halves asked for together.

33. **Victory on his death and seven phases on the tape cannot both hold if the ending waits for the crossing.** Section 1 says a headless caller "sees seven phases in order" and verification step 7 says victory "fires on `bossKilled` for the Undertaker and not on a phase index". A phase crosses at the top of a tick and `advance.ts:49` executes no further ticks once a run has ended (#52), so an ending fired on the death leaves the stage standing in his phase forever and the tape shows six phases. **The last boundary is therefore announced from the ending's own seam**: `winStage` in `stage.ts` reads the tick's `bossKilled`, crosses into `over` and fires `victory`, all on the death's tick. The observable timeline did not move at all, which is what says the shape is right: every assertion in `stage.test.ts` about the `over` boundary stayed green, and only the cause moved.
34. **Section 5 says `bot.ts` changes for "a hit-taking policy so the floor ladder is reachable in a boss fight", and the policy needed no edit.** `hitTakingPolicy` has existed since before this step and steers into the nearest threat, which is all the ladder needs. What was missing was never the policy: it was the run handed to it, standing in a fight rather than in the Procession and carrying something above the birthright to be stripped of. The commit's only change to `bot.ts` is its JSDoc, which said the whole ladder "must not be asked" of it and now says what it walks is a fact about the run it is handed.
35. **The ladder's first rung cannot be earned inside the fight, and that is measured rather than assumed.** Score arrives only as overflow from a swallow at the size ceiling, and under this policy in the Undertaker's phase the first hit lands at tick 88 while the fight's first corpse arrives at tick 307: a grave that dives is off the ceiling long before the fight pays it anything, so it reaches the floor with score zero and the bleed never fires. The score is therefore pinned on the run beside the build, both as arrival conditions with their reasons written on them, because a rig standing in the last fight is not the run that earned them. The figure itself carries nothing: the score tier is one rung and bleeds whole, which the test asserts against the pinned number.
36. **The plan's spec 45 does not say what happens when both endings fall on one tick, and one tick can hold both.** Hits resolve in `resolveOverlaps` and deaths in `resolveDeaths`, so a grave the fight sealed and a last chunk that emptied are one tick apart in the same tick's order. `winStage` returns nothing when an ending already stands, and the run that sealed stays sealed; the test that holds it measures the fall's own tick first and stands the hit on it, because the two have to land together for the rule to be about anything.

Seven from slice 10, and the first is the ruling slice 3's note left here.

37. **Section 4's `PhaseEnd` union has no member for the Waking, and the phase needs one.** Slice 2 filled the column with `rowsSpentAndFieldClear` because nothing read it and slice 3's finding 9 handed the ruling here. The union gains `setPieceClosed`: the Waking is the source's own moment and ends the tick the source is gone. Keeping the stand-in would put a fifteen-second drain in front of the Vigil, and ADR 0051 says only the two boss boundaries need the field empty.
38. **The plan's `SetPiece` block declares no id, and the target seam cannot work without one.** It is the same shape as slice 6's finding 19 about `Boss` and the same answer: `readonly id`, drawn from `nextEntityId`, excluded from the witness fold as spawn identity exactly as `boss.id` is. The fold's shape is unchanged, so **`WITNESS_VERSION` did not move**, which is the plan's own ruling.
39. **`POUR_SHARES` had no reader that fires a row, because the Waking authored none.** Section 4 says the share is "read by the stage's spawner" and the phase slice 2 left carries `rows: []`, so there was nothing to thin. `WAKING_ROWS` is what the share now builds, in `rows.ts` beside it: the Crowd's own last groups over the pour's own length, re-timed to zero and thinned. Without it spec test 116 has nothing to be about and the pour arrives into whatever the Crowd left falling and then into silence.
40. **Spec test 118 landed at slice 5 and this slice landed its other half.** The plan puts it in `setPiece.test.ts`; slice 5's `pours faster than the densest ten seconds the sections author` in `rows.test.ts` is the data-row form of the same sentence. What this slice added is the measured form, counted off the pour's own arrivals rather than off the row it was authored from.
41. **Fence 110 is in no slice's list at all.** Section 6 declares it, "no boss module and no set piece module names a weapon line", and section 10 never assigns it. It landed here, because this is the slice that creates the second of the two module families it covers, and it reads the boss half off the folder so a third boss joins it without an edit.
42. **Test 19's sentence is stronger than the code, and what landed is the quantity the code supports.** "Nothing the set piece does fills the reservoir": swallowing an ordinary corpse charges the reservoir a little, as every corpse does, so a pour the player eats does raise it. What ADR 0050 rules out is a gift, and that is what the test holds: the source sheds no treasure and charges nothing itself, a whole pour nobody eats leaves the reservoir where it found it, and a feast in the same rig does charge.
43. **The Waking's property is a tendency across seeds rather than a per-seed law.** Section 6's tests 16 and 17 read as per-seed claims, like the Wall's, and the Wall's are survival claims where these are a quantity. Measured over twelve seeds, eleven went the committing way and one the other, and which one moved with the window the trail was read over. The test therefore holds the total across its seeds and pins the exceptions as a set, which is the `STRIPS_A_RUNG` idiom; section 17 carries the figures.

Four from slice 11, and the first is a place the plan counts and the tree guards.

44. **The readings graph is declared in six places and the plan names five.** The planning read corrected the count from three to five (`TuningReadings`, `ReadingsAcc`, `createReadings`, `observeReadings`, `readingsOf`), and the sixth is `READING_COMPARISONS` in `src/dev/compareRuns.ts`, whose own comment says a reading with no entry there is a hole. `src/dev/__tests__/comparisonDeclared.test.ts` went red the moment the reading joined `TuningReadings`, naming `tuning.sectionTimeline.spans` as undeclared, and it was green again once the entry landed. **It is declared as a list and not a series**: the spans are one entry per phase crossed, two runs that crossed a different number of phases have no index to pair, and the count is how far through the stage each side reached.
45. **Section 6 puts both tests in `src/dev/readings/__tests__/sectionTimeline.test.ts`, and test 101 cannot live there.** It is a claim about what a measured tape reports, so it reaches `src/dev/measure.ts`, and the test-span fence in `src/__tests__/boundary.test.ts` refuses a test under `dev/readings` that imports from outside that subtree: it named both `../../bot` and `../../measure` the first time it was run. **Test 101 landed in `src/dev/__tests__/measure.test.ts`**, which is the test folder of the lowest folder containing everything it spans and where the up-field traffic reading's own claim already sits, and it reads the file's existing rich fixture rather than recording a second long run: that fixture already stops at 9000 ticks inside the Banshee's fight, which is exactly the tape the test needs. Test 100 stayed beside the module.
46. **Verification step 9 says to record a full run with `record-conditioned.ts`, and no tape that tool records crosses a boundary.** Its steering script is a fixed wander (`scripts/record-conditioned.ts:165-170`), and a wandering hand takes fire from every row it crosses: at every line pinned to level 5 on seed 101 it sealed shut at tick 3453, inside the Procession, and the reading reported the one open span that says so. **The step ran on a tape recorded through the same authority under the maxed dodge bot**, which is the hand the plan's own verification step 7 already uses for the same reason, that only a pinned build crosses the stage headlessly. The tables are in section 18.
47. **Verification step 9's eight-to-ten-minute criterion holds on neither hand, and section 17 had already measured why.** The maxed dodge bot's whole run is 25862 ticks, 7.18 minutes, and the birthright still hand's is 38510, 10.70. Every other criterion of the step passes on the taped run: seven phases in order, the Procession shorter than the Crowd, the Vigil shorter than the Procession, and both boss phases beginning on a tick with zero live mobs. The two hands are the band's own edges rather than a defect here, which is the reading section 17 already took from the other side, and what stands between them is spec test 3's derived nominal of 8.2 minutes. **It is the tuning pass's question and not this slice's**, and it is now measured rather than intended, which is what the step exists for.


Seven from slice 12a, and the first two are work the plan asks for that was already done or never needed.

48. **The plan changes both assetpack configs for the `music` bundle, and neither needed a line.** Section 5's row names `.assetpack.js` and `scripts/assetpack-vite-plugin.ts` as changed "because `.assetpack.js:1-4` says the two configs must mirror each other". The bundle comes from the folder tag alone: `raw-assets/music{m}` produces a `music` bundle in `src/manifest.json` exactly as `main{m}` and `preload{m}` do, with no config entry anywhere. **Both files are untouched**, and the mirror rule still holds because there was nothing to mirror.
49. **`Phase.music` was already filled across all seven phases, by slice 2.** Slice 12a's sentence lists it among what it lands and slice 2's sentence lists it too; the tree agrees with slice 2. This slice authored no phase row, which is the same shape as slice 4's finding 11.
50. **Section 4's `MusicOutput.play` takes a volume the engine throws away.** `BGM.play` starts a track, sets its volume to zero and animates it to the music volume the player set, so a volume passed in its options is overwritten on the next line. The seam is `play(alias: string): void`, which `BGM.play` still satisfies, and how loud the music is stays the player's own setting.
51. **`musicFor` cannot resolve a phase name to a loop, because `sound.ts` may reach `game/events` and nothing else.** The boundary rule narrowed to that one file (`src/__tests__/boundary.test.ts`) allows `game/events` alone, so it cannot read `PHASES` to find what a phase named. **`PhaseChanged` therefore carries `music: PhaseMusic | null`** and `events.ts` re-exports the type: the sim says which loop and `src/app` resolves that name to a file, which is what section 4's own `Phase.music` block asks for. The alternative was widening the fence to let the sound module read the stage machine, which is the exact reach it exists to forbid.
52. **Nothing announces the phase a run opens in, so the first section would have played silent.** `enterNextPhase` is the only site that pushes `phaseChanged` and a run begins already inside the first phase, which slice 11's reading works around from the other side. **`phaseUnderway(state)` in `stage.ts`** reports the phase the run stands in as the event a crossing would carry, `enterNextPhase` is built on it so an opening and a boundary are one fact rather than two, and `GameScreen.prepare` announces it once per run. Without it a run is silent through the Procession and her whole fight and the music starts at the Banshee's death.
53. **The music bundle can be behind the first cue, and the plan does not say who waits.** Section 8 rules the loops into their own bundle, background-loaded and declared by no screen, and `sound.find` throws on an alias the library has not registered yet. So the channel `main.ts` builds awaits the bundle before it plays and reports anything past that, which is the design record's "the section cue is applied both on `phaseChanged` and again when the bundle resolves" in its simplest form: the cue is what waits, so nothing has to be reapplied.
54. **Test 131 cannot live in `stage.test.ts`, and 58 and 108 cannot live under `app/screens/game`.** 131 spans the phase table and the app's alias table, and a test under `src/game` may reach `src/dev` and nothing else outside the sim; 58 and 108 reach `app/sound`, which the test-span fence refuses from `app/screens/game/__tests__`, whose whole allowance is `app/palette` and `app/layout`. **All three landed in `src/app/__tests__/sound.test.ts`**, the test folder of the lowest folder containing everything they span, which is the same finding slice 11 made about test 101.

Two from slice 12b, and the first is what the measurement had to widen before it could answer the question at all.

55. **Verification step 13 says "the three loops" and the app ships six files, of which the ogg is the one a browser here actually plays.** Every entry in the `music` bundle carries `src: ['music/<name>.ogg', 'music/<name>.mp3']`, the resolver takes the first it can decode, and the built app under `vite preview` in Chromium fetched `seek-n-slaughter.ogg`, `bells-of-death.ogg` and `a-hollow-call.ogg` and never an mp3. The mp3 is what a WebKit browser would fall back to, because it decodes no Ogg Vorbis, so **all six were measured and the step's three rows are six**. Reading the step as three would have measured the format the game does not play on the engine it was measured on.
56. **The plan's `@pixi/sound` citation is right in content and off by a line.** Section 4 and verification step 13 both say `WebAudioInstance.mjs:172-175` maps `start` and `end` to the source node's `loopStart` and `loopEnd`; in 6.0.1 it is `:173-174`, and `BGM.play` passes a `PlayOptions` straight through to `Sound.play`, so the route the step names is real and open. It is recorded because the route is what the slice's own test allows and a header parse is what it forbids.

Three from slice 13a, and the first is a claim about the machine rather than about the tree.

57. **The plan says ImageMagick and ffmpeg are both installed, and ImageMagick is not.** Section 8 and the design record's section 7 both name `magick in.png -colorspace Gray` as the mechanism with ffmpeg's `hue=s=0` beside it, "both installed". There is no `magick` and no `convert` on any path here and no ImageMagick package; ffmpeg is real, at `/home/linuxbrew/.linuxbrew/bin/ffmpeg`. The intent, that the art is desaturated on import by a committed script, is followed and the mechanism is neither of the two named: `scripts/grayscale-import.ts` decodes and re-encodes through `pngjs`, the decoder the same slice adds for the fence, and takes each pixel's grey from `luma` in `src/app/color.ts`. Three reasons together. A pipeline that runs on every restage should not rest on a second system binary, which is the failure ImageMagick's absence has just demonstrated. ffmpeg re-encodes with its own encoder and its bytes are not stable across versions, while this output is committed. And `luma` is the quantity ADR 0014's value ceiling is measured in, so a sprite's brightness and a palette entry's are one number rather than two conventions that happen to agree. **ffmpeg was measured rather than waved away**: `hue=s=0` over `undead.png` returned exact R equals G equals B on all 15360 pixels with the alpha channel intact, so it would have worked, and the three reasons above are why it was not used.
58. **The Banshee has no art of her own in the packs, and the mapping reads as though she does.** It names "big ghost Banshee" beside "ghost revenant", and `scarymobs/undead.png` holds exactly one ghost, four frames of it at 32 by 32. So the ghost is imported once as `sprites{fix}/ghost.png` and she is that art drawn large rather than a second file with the same pixels in it. There is a live disagreement above this: section 8's craft call says the two bosses are vector silhouettes and not pixel art at all, while slice 14's own prompt says both bosses are drawn "from slice 13a's grayscale sprites". **Slice 14 rules it**, and either way both bodies it could want are in this commit: the ghost, and `sprites{fix}/tall-figure.png` cut from `cosmic_horror_dude.png`, which is the tall figure the mapping names.
59. **The golem carrier lives in a sheet that also carries three rows the mapping rules out**, so the scarymobs sheets are cut rather than copied whole. `beasts.png` is four columns by five rows of 48 by 64 cells: rows 0 to 2 are the grey werebeast, the blue werebeast and the brown flesh giant, and rows 3 and 4 are the pale and the dark skull golem. `sprites{fix}/golem.png` is row 3 alone, three poses, and nothing above it enters the repo. The pale shade is the one taken because a tint multiplies and so can only darken what it is given. `smallbeasts.png`, `werebeasts.png` and `forklift.png` are not imported from at all: the first two are the wolves, gorillas and werewolves the mapping rules out, and the third is a forklift.

Five from slice 13b, and the first is the one that made the whole slice worth rendering.

60. **The staged art cannot be coloured by a palette entry as imported, and only the rendered check could see it.** A PixiJS tint multiplies, so an entry is a ceiling and never a value: what draws is the art's own grey times the entry. Slice 13a's import wrote each pixel's `luma` faithfully, and this pack is a dark one, so nineteen of the twenty-seven files peaked between 17 and 72 of 255. Tinted at `standInGroundDressCold`'s own channels that put a statue's brightest pixel at grey 15, two luma points off night, and the first screenshot of the built app showed a ground that was there and invisible. The design record's own numbers only mean anything if the entry is what draws: it says the tile wears `nightSpeckle` at luma 14, not luma 1.6. **So the import gained a stretch and the twenty-five files it moves are in this commit**, pinned first as a red fence in `palette.test.ts` and only then fixed, which is the stuck rule's own order. The stretch takes its top from the 98th percentile of a file's opaque greys rather than from its brightest pixel, because the packs put a handful of speculars at the top: stretching to the maximum left `eye.png`, `tentacle.png` and `waking-awake.png` where they were and scaled the Waking's two frames 3.5 apart from each other, one bright and one dark, which is the case that chose the percentile. Every file now reaches the top of the range at p98, the fence holds it, and fence 122's no-hue guard is untouched because one factor over three equal channels leaves them equal.
61. **A boundary phase wears the section it ends, and the design record's two changes are the music's rather than the ground's.** Decision 22's amendment names the Banshee's death and the eye opening, and slice 12a mapped the three loops on exactly those two. The ground cannot take the same map: the Waking's phase is 900 ticks and the drift window is 3064, so a dressing change at the eye opening would still have had the Crowd's own sprites falling when the Vigil began, and nothing the renderer is handed could tell it which family they belonged to. **`DRESSING_BY_PHASE` therefore puts the Waking under the Crowd and starts the Vigil's teal at the Vigil**, which is what the design record's section 7 asks for from the other side: the teal is the run's one colour addition, "the addition has to be the event", and spending it under the Crowd's own boundary event would spend it a section early with the source in the Crowd's family beside it. What the mapping buys is that every phase inheriting its predecessor's set is more than one whole drift window past the change before it, so which family a placement wears is a function of the tick, `phaseIndex` and `phaseTick` alone and the renderer holds nothing across frames. Nothing goes into the held-transient registry, so `REPLAY_LEAD_IN_TICKS` does not move.
62. **The five palette entries are not the design record's five rows, because one of those rows is `nightSpeckle`.** The table names `groundTile` first and gives its target as "the `nightSpeckle` family", and the paragraph under it says outright that `nightSpeckle` "is a ready-made ground colour with no consumer, and the ground tile is its consumer". So the ground tile declares nothing and the five new entries are `standInGroundDressCold`, `standInGroundDressWet`, `standInVigilTint`, `standInWaking` and `standInWakingDark`, which is the record's own count. All five carry the `standIn` prefix decision 13 asks for.
63. **Two of the dressing files the record names are not single sprites, and the rendered check is what said so.** `Cracks` is 32 by 32 of fully opaque pixels spanning two greys, so stretched it draws as a solid rectangle of the section's colour, and it is a terrain overlay rather than a detail sprite: **it is dropped from the Procession's set**, which keeps the four statues and the cliff. `Cliff` is a two-piece sheet whose top right cell is a solid black block, which drew as a hole in the rock, so it is **cut to its left column at 48 by 112**. Four more of the named files are animation sheets and are cut to one cell each: the Crowd's `Little Eyes` opens across the middle of a 36-cell sheet whose first eleven cells are empty, and the Vigil's eye, tentacle and floating rocks take their first frame. Every rectangle was read off the imported PNG rather than guessed.
64. **The stand-in texture lookup is a power the two screens hand in, and both `dressField` sites are wired here.** The bundle is background loaded and no screen declares it (slice 13a's own shape), so a texture can be missing for the first frames of a run; `BackgroundProps.standInArt` answers null until it is there and the renderer asks again, which is the late-start shape slice 12a already gave the music. The power itself is eight lines in `src/main.ts` beside `musicChannel`, for the reason slice 12a's declined finding records: a dependency used at one site is used directly there. **`ReplayScreen` takes it too**, because a renderer wired into one of the two `dressField` sites and not the other is how a renderer ships unseen, and the plan gives that wiring to slice 14 only for the boss renderer.

Three from ground adjustment 1, and all three are rulings of Mark's that supersede what the plan and the record say rather than citations that moved.

65. **The plan's section 8 and the design record both say the ground scrolls at half the field's scroll, and that is superseded.** The record's bullet reads "tiled from `Terrain/Tiles.png`, scrolling at half the field's scroll so it reads as parallax depth under the mobs", and section 8 says the same. **Mark's ruling of 2026-09-08, after playing the 13b deploy: the ground runs at the field's own scroll.** Territory is lobbed onto the ground and becomes hands pulling at mobs, so a patch belongs to the ground; it drifts in the sim at `SCROLL_SPEED` (`lines/territory.ts`, `patch.y += SCROLL_SPEED`), and at half the rock slid out from under every landed patch at half its speed. `GROUND_PARALLAX` is gone rather than set to one. The design record's bullet is amended in place with a dated line and its old sentence stands above it. The test that pinned the half, `runs the ground at half the field, so the rock reads as depth under the bodies`, is replaced by one whose promise is that a landed patch and the rock under it move at the same rate, asserted by drifting a real patch through `advanceTerritory` rather than against either constant.
66. **A known consequence of that ruling, decided by the dispatching session and not fixed here: the Waking's source now slides against the ground.** `SET_PIECE_DRIFT_SHARE` is 0.5 (`rows.ts:734`), so the source drifts at half the scroll and therefore at half the ground's rate where it used to match it. That is a sim row under an ADR and it goes to Mark; the share was not touched.
67. **The record says the ground is tiled from `Terrain/Tiles.png`, and it no longer is.** Mark's second note was that the tiles read as placed at random and spun, and the sheet is why: 80 by 64 pixels, a five by four grid of twenty 16-pixel blocks, every one of them bordered on all four sides by its own one-pixel groove at grey 17 against a rock of 33, and every one lit at its own angle. Tiled whole, that is a lattice of loose blocks repeating every 150 by 120 field units. The pack ships no edge or corner cells to fit together with, which was read rather than assumed: `Tileset.png` is 128 by 880 and its only terrain is those same twenty blocks at (32, 80) plus a borderless fill at (0, 80) of three greys with a mean of 13, three luma points off black, whose own motif repeats every 32 by 64 and reads as glyphs on a field. **So the layout is authored rather than assembled**, and the record's bullet is amended in place: `scripts/grayscale-import.ts` bakes one 256 by 256 floor from an authored map of fourteen slabs three to six cells a side, laid from the five cells whose streaks run one way, with each cell's groove dissolved into rock on every side that is not a slab edge. The floor's own outer edge dissolves too, so it joins itself where it tiles. The runtime still draws one texture through the `TilingSprite` and knows nothing of the map.

Four from slice 14, and the first is a reader list that is short by exactly the reader that would have broken.

68. **Section 7 says the fire kind's readers are "`mobFireSprite.ts:57` and `:99`, and nothing else, because the kind exists for the draw", and there is a third.** `FieldRenderer.syncShots` decides **whether** to call `drawShot` at all, and it decided on `shot.halfExtent` alone. A boss shot and a trash shot can be the same size, so a pooled slot recycled from one kind to the other kept the colour it was last drawn in and the read never arrived. The look is `${shot.kind}|${shot.halfExtent}` now, which is the shape `mobLook` already takes. That is the playbook's own rule 2 firing on the far side: the readers of a value include whoever decides when the value is read.
69. **Section 4 gives the fire kind to `Shot`, and `drawScatter` is handed no shot.** The scatter is drawn a whole cancel after the shot stopped being alive, off the renderer's own `ShotMemory` and `Scatter` records, so what is scattering has already left the pool. Both gained a `kind` beside the extent they already carried, and `drawScatter` takes the kind rather than the shot. Without it the belch smothering a boss pattern would have recoloured the whole of it to trash on the tick it landed.
70. **Slice 13a's finding 58 hands slice 14 a disagreement to rule, and there is none between the two records.** It reads "section 8's craft call says the two bosses are vector silhouettes and not pixel art at all, while slice 14's own prompt says both bosses are drawn from slice 13a's grayscale sprites". The plan's section 8 and the design record's dressing section (`stage-floor.md:328`) agree with each other word for word; what disagreed was an earlier draft of the slice's prompt, and the prompt as dispatched already rules for the records. **The two creature cut-outs 13a imported, `sprites{fix}/ghost.png` and `sprites{fix}/tall-figure.png`, are not used and nothing in this commit reads them.** The construction that landed honours both halves of the record's sentence: the pair is the body colour and the detail colour (`banshee`/`bansheeDark`, `undertaker`/`undertakerDark`), and the near-black companion is `foodOutline`, which is what `SPRITE_OUTLINE` names for both bosses and what the three mob types already wear.

71. **Section 5's module table names `BossRenderer.ts` alone, and the folder's own split wants two files.** What a kind draws lives beside its tables and what syncs it lives in the renderer, which is the split `mobSprite.ts`, `mobFireSprite.ts` and `foodSprite.ts` already give this folder against `FieldRenderer.ts`, and which slice 13b took again for `groundDressing.ts` against `BackgroundRenderer.ts`. **`bossSprite.ts` landed beside `BossRenderer.ts`**: the two silhouettes as point tables in unit space, the pair each boss wears, the flash read and the drawing; the renderer is the sync and nothing else, twenty lines of it. Named here because it is a seam the plan did not list, not because it is a departure from its intent.

Five from adjustment iteration 2, and all five are the dispatching session's own prompt or an old comment rather than the plan's.

72. **`SET_PIECE_PLACED_AT`'s comment said the source is placed "two seconds before its own last row", and 120 is eighteen seconds before the Crowd's last row at 138.** The comment's stated reason, that the eye opens a beat after the last group falls, was right and its arithmetic was never true of the value beside it. The row is 135 now and the comment states its derivation, five seconds of fall at the field's own scroll to the opening depth, authored rather than computed because `rows.ts` value-imports nothing. It is also where CodeRabbit's declined finding above comes from: the nominal 2:35 in the design record's clock table is what that comment would have produced.
73. **The prompt says the property is held "in `bot.test.ts` against `COMMITTING_PAYS_OVER`", and it is in `setPiece.test.ts`** (`setPiece.test.ts:575` and `:600`). `bot.test.ts` names neither constant. The property was re-measured where it actually lives.
74. **The prompt says GOLDEN moves, and it cannot.** Section 2 above carries the whole of it. The prompt's own list of fields is a list of the seed 101 tape's, and those are measured in section 25.
75. **The prompt says ADR 0050 names the slower parallax and it does not, which it half suspected.** Nor does it name the mid-field opening: both phrases are decision 25's (`decisions.md:103`), and ADR 0050 is two kilobytes that mention neither. No ADR moved. Two code comments cited ADR 0050 for "it opens around mid-field", in `rows.ts` and `setPiece.ts`, and both are rewritten in this commit, because the sentence they quoted is the one Mark's ruling supersedes and it was never in the ADR they credited.
76. **The prompt's parenthetical that thirty units of margin is "under one pour interval" is the wrong way round**: thirty units is forty-seven ticks of fall against a twelve-tick interval, so it is nearly four. The rule the same sentence states, at least one pour interval of margin, is what the new test holds. Separately, the prompt's "the run's clock before and after the Waking is unchanged" is true before it and false after it, and section 25 measures how much and says why.

Five from slices 15 and 16, and the first is the assignment the prompt already suspected.

77. **The plan's slice 15 assigns an extension to fence 110 that had already landed**, which item 41 above records from the other side. `AUTHORED_MOMENT_MODULES` in `lineAgnosticPolicies.test.ts` sweeps `src/game/bosses/*.ts` off the folder and names `src/game/stage/setPiece.ts`, and the five tests under `no boss and no set piece names a weapon line` are green, the sweep guard among them. Slice 15 wrote no code, which is why it was dispatched with 16.
78. **`boundary.test.ts:247` is not a stale todo, it is a dormant guard, and it names no folder today.** The prompt reads it as "a todo for a folder that does not exist yet"; it is an `it.todo` inside the `BOUNDARIES` loop, reached only when a declared root's folder is absent (`if (!existsSync(root))`). All six declared roots exist, `game`, `input`, `dev`, `tape`, the `app/sound.ts` single-file entry and `app/ui`, so the branch never runs and `boundary.test.ts` reports no todo at all. Both todos the suite counts are `palette.test.ts`'s. Nothing was left; there was nothing there to leave.
79. **The prompt's `local/step2/tests-baseline.txt` is at the worktree root and `apps/hungry-grave/local/step2` is a different folder with the same name.** The app-level one holds slice 11's and 13b's tapes and measure output. Both baselines were located before the diff was read, and the diff is against the worktree-root file the note's section 6 names.
80. **`fireCore` does not clear APCA Lc 45 against the Banshee's own body, and no test asks it to.** `BACKGROUNDS` in `palette.test.ts` lists the four grounds and the three stand-in ground entries, and it was written before anything drew a boss; a boss body sits in `mobBodies`, which is under `mobFire` in `LAYER_ORDER`, so every tear she throws crosses it. Measured at the tip: `fireCore` against `banshee` is **Lc 39.59** against the file's own `CORE_MIN_LC` of 45, and against `bansheeDark` 69.92, `undertaker` 72.35, `undertakerDark` 93.96. The curtain's own pair is the half that passes and is what the filled todo holds; the Banshee's is a finding and is not fixed here, because moving a palette entry is ADR 0014's decision and not a verification pass's. **Trigger: the art pass at #38, or the first time Mark loses a tear on her body.**
81. **Verification step 9's eight-to-ten-minute band still holds on no hand, and the maxed one now misses it from below.** Item 47 measured 7.18 minutes and 10.70 at slice 11; the tip's seed 101 tape under the same hand is **26691 ticks, 7.41 minutes**, and step 7's own rig (`runPolicy`, which feeds the policy the tick's own events) runs seeds 404 and 505 to **47455 and 51600 ticks, 13.2 and 14.3 minutes**. The stage data did not move: the Procession, the Crowd and the Waking are identical to adjustment 2's measurement. What varies is the Vigil and the Undertaker fight, which are play. **It is the tuning pass's question (#31, #36) and nothing here retuned a row.**

## 5. Gate corrections

### Dispatching session, after slice 6

**The belch's burst deals a data row of boss damage, and slice 7 built it.** Slice 6's note recorded ADR 0008's clause, that the burst "deals its big chunk of boss damage only when the boss is inside that radius, and never pushes a boss", as unbuilt: the plan's `killableOutright` block makes the belch skip a chunked target outright, on the reason that a kill-rule burst would break a chunk in one go. The dispatching session ruled that both stand, because the ADR asks for damage and not for a kill. So the burst asks the seam whether its kill rule applies and lands the row where it does not, through `damageStormTarget` and never through a push.

- The row is `BELCH_CHUNK_DAMAGE` in `belch.ts`, beside `BELCH_BURST_RADIUS`, one row for every boss rather than a row each, because what it prices is the press and not the fight.
- Its initial value is **400**, derived from the two figures the design record already carries: a full build lands about 50 points a second on a boss standing at the top of the field, so one earned press is worth about eight seconds of that. It sits well under one chunk of the shortest boss's health, 1100, so a single belch never breaks a fresh chunk and spec test 44's one-full-emit property is not something a press can take away. The harness at step 4 measures it.
- Two tests carry it. `takes its own chunk of boss damage from a boss inside the burst, and none from one outside it` in `chunks.test.ts` holds all three halves of the ADR's sentence, the amount, the radius, and that the boss never moves, and it holds the row under the smallest chunk in `CHUNK_HP` so a retune cannot quietly make one press a chunk break. `smothers a whole ring and leaves her standing` in `banshee.test.ts` is the gas half against a real boss pattern, which is the first time that clause has had one to be about.

## 6. The baseline tapes

Recorded at the branch tip `ed2ad14108` before any step 2 edit, saved outside the repo under `local/step2/` (gitignored). They are the input to verification steps 5 and 10.

- `local/step2/baseline-a.tape`: seed 2093383922, 12000 ticks, the birthright only (`skullStream=1 territory=0 wisps=0 bell=0`).
- `local/step2/baseline-b.tape`: the same seed and ticks, every line at level 5 (`skullStream=5 territory=5 wisps=5 bell=5`).
- `local/step2/baseline-a.measure.txt`: `measure.ts` against baseline-a. The run ends `sealed` at tick 1746.
- `local/step2/baseline-b.measure.txt`: `measure.ts` against baseline-b. The run ends `sealed` at tick 3393.

The recorder is `scripts/record-conditioned.ts`; its argument names are the current weapon lines (`skullStream`, `territory`, `wisps`, `bell`), read from `WEAPON_LINES` in `src/game/lines/roster.ts` rather than trusted from the handoff, whose own example still says `soulStream`. Neither recording raised a fault.

The test-name baseline for verification step 5: `local/step2/tests-baseline.json` (`vitest list --json`, with the AssetPack log lines the command prints before the array stripped so the file parses as JSON) and `local/step2/tests-baseline.txt` (a sorted plain list, one `file :: name` per line, for a later agent's diff). 1423 tests, all unique.

## 7. Verification steps run

Slice 0: the step 5 and step 10 baselines recorded, see section 6.

Slice 1, from the plan's section 3:

- **Step 1, unit tests.** Green. 110 files, 1418 passed, 10 expected fail, 4 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`: **five names added, none removed, none renamed.** The five are `rows.test.ts`'s. Renaming a constant moves no test name, which is why the fifteen edited files show nothing here, and that is the point of the step: a suite that failed to load would have shown as removals. **`vitest list` does not report a `test.todo`**, so the sixth test in `rows.test.ts` is invisible to this instrument and the counts read 1428 against the baseline's 1423.
- **Step 6, GOLDEN.** Did not move, which the slice required. See section 2.
- **Steps 3, 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.**

Slice 2, from the plan's section 3:

- **Step 1, unit tests.** Green. 110 files, 1437 passed, 10 expected fail, 4 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are both standing: `@pixi/sound` statically imported alongside its dynamic import (#50) and the pixi chunk over 500 kB.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`: **32 names added, 8 removed, and every one of the 8 is a rename whose new name is among the added.** The renames are the five `survives the ramp on seed N` in `bot.test.ts`, which become `survives the Procession on seed N`, and three in `stage.test.ts`: `chains the phases in order` becomes `chains the seven phases in order`, `lands the Wall two seconds into the back half` becomes `into the Crowd`, and `holds only Drips and one File in the ramp's first 45 seconds` becomes `in the Procession's first 45 seconds`. Five of the 32 additions are slice 1's `rows.test.ts` names, because the baseline predates slice 1. **Nothing was removed without a replacement.** `vitest list` still does not report a `test.todo`, so the two in `rows.test.ts` and `stage.test.ts` are invisible to it.
- **Step 6, GOLDEN.** Moved, in the same commit as the behaviour. See section 2.
- **Steps 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.**

Slice 3, from the plan's section 3:

- **Step 1, unit tests.** Green. 110 files, 1443 passed, 10 expected fail, 6 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`, read net of slices 1 and 2, which the baseline predates. **Slice 3's own share is 8 names added, 6 renamed and 2 removed with no replacement.** The renames: five in `bot.test.ts`, `the drain-out's property (plan 6.29) > leaves the field empty at every phase boundary on seed N` becomes `the sparse last row and its two boundaries (ADR 0051) > meets each boss on an empty field and the set piece in traffic on seed N`; and one in `rows.test.ts`, `marks the Wall's own row the same way` becomes `marks the Wall's own row and every sparse last row`. **The two removed are the drain-out's own tests and they retire with the ruling that superseded it**: `gives a phase a length of its last row's time plus the drain-out`, which pinned a constant that no longer exists, and `leaves the drain-out silent: no row falls inside it`, whose claim ADR 0051 reverses and whose replacement is `leaves no spawn silence anywhere in the stage`. The two new todos are invisible to `vitest list` as always, and show only in the suite's own count, 6 against slice 2's 4.
- **Step 6, GOLDEN.** Did not move, which the slice claimed. See section 2.
- **Steps 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.**

Slice 4, from the plan's section 3:

- **Step 1, unit tests.** Green. 110 files, 1447 passed, 10 expected fail, 6 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`, read net of slices 1 to 3, which the baseline predates. **Slice 4's own share is 4 names added, none removed and none renamed**, all four in `rows.test.ts` under `the carrier schedule across the sections (ADR 0002, ADR 0048)`. The whole diff against the baseline is 49 added and 15 removed, and every one of the 15 is slice 2's or slice 3's, already accounted for above.
- **Step 6, GOLDEN.** Did not move, and could not: the commit holds no production code. See section 2.
- **Steps 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.**

Slice 5, from the plan's section 3:

- **Step 1, unit tests.** Green. 110 files, 1461 passed, 10 expected fail, 7 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`, read net of slices 1 to 4, which the baseline predates. **Slice 5's own share is 19 names added, 5 removed, and one of the 5 is a rename whose new name is among the added**: `holds fifteen identities against eighteen checks` becomes `holds eighteen identities against nineteen checks`. **The other four are the eviction path's own tests, and they retire with the ruling that superseded it**: `takes the oldest live corpse under, reports an eviction rather than an expiry, and gives the new corpse its slot` and `drops by id and not by slot index, which a recycled slot is what proves` in `caps.test.ts`, whose claim ADR 0056 reverses outright, and `evicts a corpse rather than a drop when the pool is full` and `refuses the spawn outright when every slot holds treasure` in `corpses.test.ts`, whose two cases are now one: `removes nothing already on the field whichever kind of food asks for the slot`. The whole diff against the baseline is 68 added and 20 removed, and the other 49 and 15 are slices 1 to 4's, already accounted for above. The new todo is invisible to `vitest list` as always, and shows only in the suite's own count, 7 against slice 4's 6.
- **Step 6, GOLDEN.** Did not move, and the slice claimed none. See section 2.
- **Steps 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.**

Slice 6, from the plan's section 3:

- **Step 1, unit tests.** Green. 112 files, 1530 passed, 10 expected fail, 8 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 137 added and 20 removed, and **all 20 removals belong to slices 2, 3 and 5 and are already accounted for in this section**: five bot `survives the ramp on seed N` and three `stage.test.ts` renames from slice 2, five bot drain-out renames and the two retired drain-out tests from slice 3, and the four retired eviction tests from slice 5. **Slice 6's own share is 70 added and one renamed, with nothing removed without a replacement.** The rename is the fault count, `holds eighteen identities against nineteen checks` becoming `holds twenty identities against twenty-one checks`. The 70: 15 in `chunks.test.ts`, 15 in `invariants.test.ts` (four on the two new identities and eleven no-NaN cases), 13 in `witness.test.ts`, 10 in `stormTargets.test.ts`, 8 in `lineAgnosticPolicies.test.ts`, 5 in `mobFire.test.ts`, 2 in `step.test.ts`, and one each in `damageTaken.test.ts` and `faults.test.ts`. The new todo is invisible to `vitest list` as always, and shows only in the suite's own count, 8 against slice 5's 7.
- **Step 6, GOLDEN.** Moved, in the same commit as the fold that moved it, and the slice claimed exactly that field. See section 2.
- **Step 10, the old-tape decode check.** **Ran, and passed.** `measure.ts` on both slice 0 baseline tapes, recorded at `ed2ad14108` before any step 2 edit, returns `{"outcome":"witnessVersionMismatch","tapeWitnessVersion":5,"readerWitnessVersion":6}`. The tape decodes, the tool names both versions in its own vocabulary, and it refuses replay precisely rather than throwing a format error or silently coercing (ADR 0043's precise-refusal contract, ADR 0019's witness). `FORMAT_VERSION` did not move.
- **Steps 7 to 9 and 11 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.**

Slice 7, from the plan's section 3:

- **Step 1, unit tests.** Green. 113 files, 1541 passed, 10 expected fail, 8 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones. **Lint caught a real defect the tests could not**: the ring's trigonometry called `Math.sin` and `Math.cos` directly, which ADR 0015 forbids because they are implementation-approximated, so a ring would have been a slightly different ring on another engine. Both go through `math.ts` now, and the test that reads bearings goes through `atan2` and `normalize` for the same reason.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 154 added and 26 removed; 20 of the 26 belong to slices 2, 3 and 5 and are accounted for above. **Slice 7's own share is 17 added and 6 removed, and every one of the 6 is a rename whose new name is among the added**: the five `dodgePolicy from the size ceiling > ends the run one way or the other on seed N`, which become `is held in the Banshee's fight on seed N, ending neither way`, and `begins and ends a stubbed boss phase on the same tick`, which becomes `holds a boss phase open for its fight, and ends a stubbed one on the tick it begins`. **Nothing was removed without a replacement.** The 17 added are 9 in `banshee.test.ts`, 1 in `chunks.test.ts` (the belch's boss damage), 1 in `stage.test.ts` (spec test 7), and the six renames' new names. `vitest list` still does not report a `test.todo`, so the 8 todos show only in the suite's own count.
- **Step 6, GOLDEN.** Did not move, and the slice claimed none. See section 2.
- **Steps 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open.** Verification step 11's Banshee shot, her mid-chunk-one rings, is slice 14's: nothing draws a boss yet, and her tears still draw in the trash read until `mobFireSprite` reads the shot's own kind.

Slice 8, from the plan's section 3:

- **Step 1, unit tests.** Green. 114 files, 1557 passed, 10 expected fail, 6 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 170 added and 26 removed, and all 26 belong to slices 2, 3, 5 and 7 and are accounted for above. **Slice 8's own share is 17 added and one removed, and the one is a rename whose new name is among the added**: `holds a boss phase open for its fight, and ends a stubbed one on the tick it begins` becomes `holds each boss phase open for its own fight`, because there is no stubbed boss phase left to be about. The 17 added are 14 in `undertaker.test.ts`, one in `rows.test.ts` and one in `stage.test.ts` (the two todos this slice fills), and the rename's new name. **Nothing was removed without a replacement**, and the two filled todos are why the suite's todo count reads 6 against slice 7's 8.
- **Step 6, GOLDEN.** Did not move, and the slice claimed none. See section 2.
- **Steps 7 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open**, and verification step 17, whether chunk three's twist is legible, is the one this slice's work is most directly waiting on: no test can see whether the way through reads as following the arm.

Slice 9, from the plan's section 3:

- **Step 1, unit tests.** Green. 115 files, 1562 passed, 10 expected fail, 6 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones. Lint caught one thing and it was formatting: prettier wants the widened export block over six lines.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 176 added and 27 removed, and 170 of the additions and 26 of the removals belong to slices 1 to 8 and are accounted for above. **Slice 9's own share is 6 added and one removed, and the one removed is a rename whose new name is among the added**: `ends the run in victory when the over phase is reached` becomes `ends the run on the tick the last boss falls, and crosses behind him on it`, because the phase being reached is no longer the cause. The other five additions are `endings.test.ts`'s. **Nothing was removed without a replacement**, and the todo count is unchanged at 6.
- **Step 6, GOLDEN.** Did not move, and the slice claimed none. See section 2.
- **Steps 7 and 8, the headless runs to victory and to sealed shut**, are slice 10's in their full-run form and were not run here. What this slice ran is their pinned-phase halves, which is what section 10 assigns it: victory on his death and the whole ladder inside his fight, both in `src/__tests__/endings.test.ts`.
- **Steps 9 to 13 and the rendered checks** belong to later slices and were not run. **Steps 16 to 22 are Mark's and stay open**, and none of them is about this slice: nothing it changed is visible before the last chunk empties.

Slice 10, from the plan's section 3:

- **Step 1, unit tests.** Green. 116 files, 1593 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 207 added and 27 removed, and **all 27 removals belong to slices 2, 3, 5, 7, 8 and 9 and are already accounted for above**. **Slice 10's own share is 31 added, nothing removed and nothing renamed**: 17 in `setPiece.test.ts`, 5 in `lineAgnosticPolicies.test.ts` (fence 110 over the three boss modules and the source, plus its own sweep guard), 4 in `stage.test.ts` (the two todos slice 3 pinned, spec 3, and the Crowd's own end condition), 2 in `stormTargets.test.ts` (the todo slice 6 pinned, and the seam's damage half), and one each in `rows.test.ts`, `bot.test.ts` and `territoryControl.test.ts`. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and the slice claimed none. See section 2.
- **Step 7, the headless run to victory.** **Ran, and passed.** All five seeds reach victory from a build pinned through `createRun`'s third parameter with `uniformLevels(MAX_LEVEL)`, the seed set did not move, and on every one of them victory fires behind the Undertaker's own `bossKilled` on the same tick rather than on a phase index, with no fault recorded. `maxedRun` now takes its build through that parameter rather than writing the levels after `createRun`, which is the plan's own wording and moved no seed.
- **Step 8, the headless run to sealed shut.** **Ran, and passed on all five seeds**, through `scoreBled`, then `weaponStripped`, then `sealed`, in that order, with every line back at the birthright. It is not at the birthright loadout, which the plan's wording asks for, and section 17 says why: slice 9 measured that a birthright run reaches the floor with nothing to bleed, so the run is pinned at level two with score on it, the same two pins `endings.test.ts` carries.
- **Steps 9 to 14 and the rendered checks** belong to later slices and were not run, though section 17 carries the whole-run clock this slice measured while writing spec test 3. **Steps 16 to 22 are Mark's and stay open**, and step 19, whether the Waking reads as heaven rather than as a wall, is the one this slice is waiting on: the corpse figures it can report are in section 17 and whether it feels like a reward is a hand question.

Slice 11, from the plan's section 3:

- **Step 1, unit tests.** Green. 117 files, 1595 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included, and its two warnings are the two standing ones. **It failed first, on the throwaway instrument this slice wrote under `local/`, which is section 17's own recorded finding firing on the trigger it names.** The app's `eslint.config.mjs` ignored `dist` alone where the repo root's config already names `**/local/**`, so a folder deliberately kept out of git and out of prettier could break the standing checks on a tree that is otherwise green. The ignore list now names `local` with the reason beside it, and the finding is closed rather than worked around by deleting the instrument.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 209 added and 27 removed, and **all 27 removals and 207 of the additions belong to slices 1 to 10 and are already accounted for above**. **Slice 11's own share is 2 added, nothing removed and nothing renamed**: `section timeline > reports one span per phase, in order, with the tick each began on` in the new `sectionTimeline.test.ts`, and `measure > reports one span per section crossed, the live one open where the tape stops` in `measure.test.ts`. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and could not. See section 2.
- **Step 9, the section timeline read off a tape.** **Ran.** Four of its five criteria pass on the taped maxed run and the clock does not, which section 4's item 47 records and section 18 tables.
- **Steps 7, 8 and 10 to 14 and the rendered checks** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and step 20, whether nine minutes is bearable, is the one this slice's numbers speak to without answering: a hand can be shown 7.18 minutes or 10.70 depending on what it is holding, and which of those a person will play is not a measurement.


Slice 12a, from the plan's section 3:

- **Step 1, unit tests.** Green. 117 files, 1603 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 216 added and 27 removed, and **all 27 removals and 209 of the additions belong to slices 1 to 11 and are already accounted for above**. **Slice 12a's own share is 7 added, nothing removed and nothing renamed**: six in `sound.test.ts` under `the section music (spec 58, module 108, 131)` and one in `stage.test.ts` under the phase machine. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and the slice claimed none. See section 2.
- **Step 11, the rendered check, for the audible half of it.** **Ran, on the built app through `vite preview` and never the dev server**, and section 19 says what it could and could not see. The title screen and a live run at tick 258 were both read as screenshots, and the run started with **no new console entry of any kind**, which is the boundary instrument this change has: a bundle that failed to load or an alias the library could not find both reach a `console.warn` the channel writes. What it cannot show is sound, and the one instrument that would have (patching `AudioBufferSourceNode.prototype.start` from the page) could not be installed, because `playwright-cli eval` is refused by this session's isolation guard whatever the expression. **Whether the loops are audible, whether the change lands where it should and whether three loops is enough are Mark's ear and nothing else** (steps 16 and 22).
- **Steps 7 to 10 and 12 to 14** belong to other slices and were not re-run here. **Step 13, the loop gap, is slice 12b's and runs on exactly the three files this slice staged.** **Steps 16 to 22 are Mark's and stay open**, and step 22, whether three loops is enough or the bosses want their own, is the one this slice is waiting on: nothing that can hear it is automated and the change costs three strings in the table.

Slice 12b, from the plan's section 3:

- **Step 1, unit tests.** Green. 117 files, 1606 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 220 added and 27 removed. **Slice 12b's own share is 3 added, nothing removed and nothing renamed**, all three in `sound.test.ts` under `no loop is trimmed by a header parse (module 129)`. **Slice 12a's own share was 7 and its note says 6**: the diff against 12a's saved list turns up a fourth name, `screenLifecycle.test.ts`'s `cues the section a run opens in, and cues it again on the next run out of the pool`, which is one of 12a's own additions and which its note names in section 19. The test is in the tree and green; what was one short was the list the count came off, so the whole diff at 12a was 217 added rather than 216 and nothing was lost. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and could not: the commit holds no production code. See section 2.
- **Step 13, the music loop gap, measured before anything is built for it.** **Ran, and the expected result held: the browser trims and nothing was built.** The six rows and how they were taken are in section 20.
- **Steps 7 to 12 and 14** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and step 22, whether three loops is enough or the bosses want their own, is untouched by this slice: what was measured is the wrap and never the music.

Slice 13a, from the plan's section 3:

- **Step 1, unit tests.** Green. 117 files, 1607 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 221 added and 27 removed, and **all 27 removals and 220 of the additions belong to slices 1 to 12b and are already accounted for above**. **Slice 13a's own share is 1 added, nothing removed and nothing renamed**: `the grayscale import over the stand-in art (#38, ADR 0014) > leaves no pixel with a hue in any sprite or tile it staged` in `palette.test.ts`. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and could not: the commit holds no simulation code. See section 2.
- **Step 12, the grayscale check, for the half a test can see.** The pixel half ran and is fence 122 itself; the half that is a look at target density is slice 16's, on the Crowd's densest moment and the Waking's pour, and needs the renderers slice 13b has not written yet. What this slice can say is that every one of the twenty-seven imported files carries no hue at all, and that the webp copies AssetPack ships beside them do not either, which was checked rather than assumed: `waking-dormant.webp` decodes to 33280 pixels with a maximum channel spread of zero, so the format the browser actually loads is as grey as the PNG the fence reads.
- **Steps 7 to 11, 13 and 14** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and none of them is about this slice on its own: nothing new is drawn until slice 13b takes these textures.
- **One anomaly, preserved rather than absorbed.** `docs/push/path-draft.md` was modified in this worktree while the slice ran, by another session and not by this one: it gains an item 8, the iPhone silent switch (#101), dated 2026-09-08. It is deliberately not in this commit, it is still uncommitted at the tip, and it is named here so the next agent does not read it as this slice's stray edit. CodeRabbit saw it because it reads the whole uncommitted tree.

Slice 13b, from the plan's section 3:

- **Step 1, unit tests.** Green. 118 files, 1626 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 240 added and 27 removed, and all 27 removals and 221 of the additions belong to slices 1 to 13a and are already accounted for above. **Slice 13b's own share is 19 added, nothing removed and nothing renamed**: thirteen in the new `BackgroundRenderer.test.ts` and six in `palette.test.ts` under `the stand-in ground (ADR 0049, decision 22, #38)` and the grayscale block. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and could not: nothing under `src/game` is in the commit. See section 2.
- **Step 11, the rendered check, its per-section half.** **Ran, on the built app through `pnpm exec vite preview` and never the dev server, through the pinned-replay path.** Five shots taken and read, all of them off one recorded tape rendered at a chosen tick, so each is deterministic and each reaches the renderer. What they showed is in section 22. The per-boss half is slice 14's.
- **Step 12, the grayscale check, its target-density half.** Not run as its own step, and it is slice 16's; what this slice can say is that finding 60's stretch was measured on every file and that the Crowd's and the Waking's shots were read at their own densest moments with no hue on screen but the entries'.
- **Step 14, every layer name unchanged.** Green: `src/app/__tests__/layering.test.ts` passes with `ADR_0014_STACK` and `LAYER_ORDER`'s length untouched, and the commit does not contain `layering.ts`. That is fence 113, and module test 105 holds the other half from the renderer's side.
- **Steps 7 to 10 and 13** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and three of them are what this slice is waiting on: step 16, whether the three sections are tellable; step 21, whether the dormant source reads apart from the Crowd's eye dressing; and step 12's own look at target density.
- **Two anomalies, preserved rather than absorbed.** `docs/push/handoff.md` and `docs/push/step-2-slice-prompts.md` were modified in this worktree while the slice ran, by the dispatching session and not by this one, and both carry a placeholder for this slice's own hash. Neither is staged or committed here, and CodeRabbit found them because it reads the whole uncommitted tree (section 3). Separately, **two full-suite runs went red on 5000 ms timeouts in `bot.test.ts` and `rows.test.ts` while another agent's `codex` processes held the machine at load average 6**; both files pass alone in 36 seconds and three full runs on an idle machine are green, so it is contention rather than flakiness, which is the reading `docs/agents/lessons.md` asks for before anything else.


Ground adjustment 1, from the plan's section 3:

- **Step 1, unit tests.** Green. 118 files, 1628 passed, 10 expected fail, 3 todo.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 242 added and 27 removed, and **all 27 removals belong to slices 1 to 13a and are already accounted for above**. **This adjustment's own share is 3 added and 1 removed, and the one removed is the superseded parallax test**, `the stand-in ground (module 105) > runs the ground at half the field, so the rock reads as depth under the bodies`, which the ruling in section 4 item 65 retires. It never reached the baseline, so it shows here as one of 13b's additions no longer present rather than as a removal. The three added are `moves the ground at the rate a landed patch drifts, so a patch stays on its rock`, `lays the floor from the one baked at import and never from the pack tile sheet` and `dresses the ground thickly, at an interval derived from the density row`, all three in `BackgroundRenderer.test.ts`.
- **Step 6, GOLDEN.** Did not move, and could not. See section 2.
- **Step 11, the rendered check.** **Ran, on the built app through `pnpm exec vite preview` and never the dev server, through the pinned-replay path.** One shot per section plus a pair with a landed territory patch on screen, all off one tape recorded under the maxed dodge bot at seed 101. What they showed is in section 23. The per-boss half is still slice 14's.
- **Step 12, the grayscale check.** Its pixel half is fence 122 and it is green over all 34 imported files, the baked floor among them: no hue anywhere and every file's body reaching the top of the range. Its target-density half is still slice 16's.
- **Step 14, every layer name unchanged.** Green, and `layering.ts` is not in the commit.
- **Steps 7 to 10 and 13** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and this adjustment adds one of his own: whether the ground now reads right on his phone, which is the deploy that produced these three notes in the first place.

Slice 14, from the plan's section 3:

- **Step 1, unit tests.** Green. 120 files, 1647 passed, 10 expected fail, 3 todo. Run alone, on an idle machine.
- **Step 2, `pnpm typecheck`.** Green. It is load-bearing in this slice rather than a formality: module 127's cross-boundary half is a type-level guard and typecheck is the only thing that runs it.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones. Lint caught formatting in two files and nothing else.
- **Step 4, `pnpm verify` at the repo root.** Green, run from inside the worktree, exit 0.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is 261 added and 27 removed, and **all 27 removals belong to slices 2, 3, 5, 7, 8 and 9 and are already accounted for above**. **Slice 14's own share is 19 added, nothing removed and nothing renamed**: 12 in the new `BossRenderer.test.ts`, 5 in the new `mobFireSprite.test.ts`, and 2 in `FieldRenderer.test.ts`. The three todos left in the suite are `swallow.test.ts`'s one and `palette.test.ts`'s two, none of them this step's.
- **Step 6, GOLDEN.** Did not move, and could not: nothing under `src/game` is in the commit. See section 2.
- **Step 11, the rendered check, its per-boss half.** **Ran, on the built app through `pnpm exec vite preview` and never the dev server, through the pinned-replay path.** Eight shots taken and read, all off tapes recorded under the maxed dodge bot at seed 101. What they showed, and how the frames were pinned, is in section 24.
- **Steps 7 to 10, 12 to 14** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and two of them are what this slice is most directly waiting on: **step 17, whether chunk three's twist is legible**, which is exactly what nothing here can see (the shot shows the gap and the arm in one frame, and whether a person reads one as following the other is motion); and **step 19, whether the Waking reads as heaven rather than as a wall**, untouched by this slice and still open from slice 10.

Adjustment iteration 2, from the plan's section 3:

- **Step 1, unit tests.** Green. 120 files, 1649 passed, 10 expected fail, 3 todo. Run alone, twice.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones.
- **Step 4, `pnpm verify` at the repo root. Green once and red three times, on one pre-existing timeout that is not this slice's, and the proof of that is a run at the branch tip.** The red is `rows.test.ts`'s `pays a full build to a run that kills every carrier before the set piece` at 5051 to 5583 ms against its default 5000 ms budget. Alone that test's file runs its case in 1.65 seconds here and **1.81 seconds at the tip**, so this slice made it faster rather than slower; under `pnpm verify` the two workspaces' suites run at once and it lands on the edge. The same test times out the same way at the tip: a detached worktree at `1d11300431` with `node_modules` symlinked was built and `pnpm verify` run in it, and it failed on that one test at **5155 ms** with none of this slice's code in the tree. It is the file `docs/agents/lessons.md` and section 22 both already name as contention rather than flakiness, and what is new here is that it now reproduces under `verify` rather than only under an outside load. **It is not fixed here: the fix is a timeout on a test this slice does not touch, and bumping one is the move that rule warns about.** Trigger: the next slice that touches `rows.test.ts`, or the first time `verify` fails on it in CI.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is **263 added and 27 removed**, and **all 27 removals belong to slices 2, 3, 5, 7, 8 and 9 and are already accounted for above**. This adjustment's own share is **3 added and 1 gone**, exactly the shape adjustment 1's was. The three added are `falls at the field's own scroll, so it stays in the rock it sits in` and `spends its whole budget before the bottom edge, with a body of margin` in `setPiece.test.ts`, and `reads the source travelling at the fall the source actually takes` in `stormTargets.test.ts`. **The one gone is the superseded drift test**, `drifts slower than the field, so it is on screen longer than a mob`, which the ruling retires; it never reached the baseline, so it shows here as one of slice 10's additions no longer present rather than as a removal.
- **Step 6, GOLDEN.** Did not move, and could not. See section 2, and note that this adjustment's own prompt says it does.
- **Step 11, the rendered check.** **Ran, on the built app through `pnpm exec vite preview` and never the dev server, through the pinned-replay path.** Three shots off three tapes recorded under the maxed dodge bot at seed 101, two of the dormant approach and one mid-pour, with the dormant pair matched over every offset in both axes rather than eyeballed. What they showed, and the one criterion in the prompt that no build keeping the authored sweep could meet, is in section 25.
- **Steps 7 to 10 and 12 to 14** belong to other slices and were not re-run here. **Steps 16 to 22 are Mark's and stay open**, and this adjustment sharpens one of them: **step 19, whether the Waking reads as heaven rather than as a wall**, is now about a moment that lasts twenty seconds rather than thirty-five, and whether that is still a moment is a hand question. It also adds one of Mark's own: whether the eye reads as sitting in the ground on his phone, which is what the ruling was for.

Slices 15 and 16, the step's closing pass. Every agent-actor step of the plan's section 3, all fifteen, run at the branch tip so the step's record is one measurement of one tree. Section 27 carries the tables and the reads; what is below is each step and its result.

- **Step 1, unit tests.** Green. 120 files, 1654 passed, 10 expected fail, 2 todo. Run alone on a machine at load average 2.6.
- **Step 2, `pnpm typecheck`.** Green.
- **Step 3, `pnpm build`.** Green, lint and typecheck included. Its two warnings are the two standing ones, `@pixi/sound` statically imported beside its dynamic import (#50) and the pixi chunk over 500 kB.
- **Step 4, `pnpm verify` at the repo root. Green twice, exit 0, and the timeout the prompt hands forward is fixed at the cause rather than waited out.** The pre-existing red was `rows.test.ts`'s `pays a full build to a run that kills every carrier before the set piece`, one test playing three whole two-section runs: 1619 ms alone at the tip and 5051 to 6150 ms under `verify`, where the two workspaces' suites run at once. **What it is now is one test per seed**, the shape `bot.test.ts`'s whole-run claims already take, at 612, 473 and 445 ms each. The promise is unchanged: the same three seeds, the same three assertions, and a per-seed name so a failure says which seed. A stated timeout was the other option the prompt allows and `bot.test.ts` has the precedent for it (`FIVE_MAXED_RUNS_MS`, 30000, on five cached maxed runs); it was not taken because these three runs are independent and splitting them needs no number to justify, where a timeout would have invented one.
- **Step 5, the test-name diff.** `vitest list --json` against `local/step2/tests-baseline.txt`. The whole diff is **268 added and 27 removed**, and **all 27 removals belong to slices 2, 3, 5, 7, 8 and 9 and are already accounted for above**. This pass's own share is **4 added and 1 gone**. The four: three per-seed `pays a full build to a run that kills every carrier before the set piece on seed N` in `rows.test.ts`, and `covers the Undertaker's curtain, which draws in the clod emitter and reddens nothing` in `palette.test.ts`. **The one gone is the un-split name**, which slice 4 added and which never reached the baseline, so it shows as one of slice 4's additions no longer present rather than as a removal. The suite's todo count falls 3 to 2 with the filled todo; `vitest list` still reports no `test.todo`.
- **Step 6, GOLDEN.** Did not move, and could not: the commit holds no production code. See section 2.
- **Step 7, the headless run to victory, under the dodge bot at maxed levels.** **Ran, and passed on all five seeds.** Victory fires behind the Undertaker's own `bossKilled` on the same tick on every one, with no fault recorded. Ticks: seed 101 **27984**, 202 **26250**, 303 **26422**, 404 **47455**, 505 **51600**.
- **Step 8, the headless run to sealed shut.** **Ran, and passed on all five seeds**, in the order `scoreBled`, `weaponStripped`, `sealed`. The rig stands in the Undertaker's fight above the birthright with score on it, which slice 9's finding 35 measured the reason for; every line ends back at the birthright. Ticks, identical on all five seeds because the pinned fight is what varies and the seed is not: `scoreBled` at 1233, `weaponStripped` at 1258 and again at 1298, `sealed` at 1328.
- **Step 9, the section timeline, read off a tape recorded at the tip.** **Ran.** Four of its five criteria pass and the clock does not, from below this time. The table is in section 27. `record-conditioned.ts` was run first and slice 11's finding 46 still holds at the tip: its fixed wander sealed inside the Procession on seed 101 with every line at level 5, and the reading reported the one open span that says so.
- **Step 10, the old-tape decode check.** **Ran, and passed.** `measure.ts` on both slice 0 baseline tapes returns `{"outcome":"witnessVersionMismatch","tapeWitnessVersion":5,"readerWitnessVersion":6}`. `WITNESS_VERSION` is 6 and did not move.
- **Step 11, the rendered check, per section and per boss.** **Ran, on the built app through `pnpm exec vite preview` and never the dev server, through the pinned-replay path.** All eight shots taken and read, each off its own tape recorded at the tip to end on the tick the shot wanted. Section 27 has each read and its pixel counts, and which reads were not obtained.
- **Step 12, the grayscale check at target density.** **Ran**, on the Crowd's densest moment and the Waking's pour, both frames rewritten in Rec.709 luma alone. The separation is measured rather than eyeballed and is in section 27.
- **Step 13, the music loop gap.** **Not re-run, and nothing asks for it.** The audio path has not changed since slice 12b measured it: `git diff f23d8eb162 HEAD` names no file under `src/app/sound.ts`, `src/engine/audio/`, or `raw-assets/music`, and the one change to `src/main.ts` is the stand-in art power slice 13b added beside `musicChannel`, which the channel does not read. Section 20's six rows stand.
- **Step 14, every layer name unchanged.** Green. `src/app/__tests__/layering.test.ts` passes with `ADR_0014_STACK` and `LAYER_ORDER`'s length untouched, and `layering.ts` is not in the commit.
- **Step 15, fences and invariants.** Green, and this is slice 15. Each fence is named by its own test title in section 27.
- **Steps 16 to 22 are Mark's and stay open.** Section 27 says which of them the deployed build lets him answer now.

## 8. Slice 1, the rows module

`peakArrivals` is the one thing in the commit that is not a move, so what it does is written down here rather than left to be re-derived.

**It is a maximum over windows and never a sum of them.** For each window length it takes the densest window in each section table, and the largest of those against `BOSS_ADD_ALLOWANCE + RUNG_ALLOWANCE`, which is what a boss phase's own window holds. That is the shape the design record's derivation already uses: it prices the Waking window at 62 (the pour plus the Crowd's reduced share) and the Undertaker's at 26 (7 diggers plus 19 rungs) and takes the larger, and it does not add a rung term to the Waking. Adding the allowances to every window instead would put the initial cap at 251 rather than the 232 the record derives, so the record settles it.

A window is half-open, `[t, t + seconds)`, and only a window that opens on a row can be the densest. Against slice 1's tables that reproduced the record's own figure: `peakArrivals(10)` was 36, the Crowd's four rows from t=50, which is the "densest 36" the derivation reads off the same table. **Slice 2 re-authored all three tables and the figure moved with them**: it is 39 now, the Crowd's four rows from t=130, the climb into the Waking. The Procession's densest ten seconds is 13 and the Vigil's 16, both under the allowance floor of 26, so the Crowd's table is still what the maximum falls on.

**The allowances are flat rows sized for the freshness window**, not scaled by the argument, which is what section 4's seam comment says ("inside a freshness window"). `BOSS_ADD_ALLOWANCE` is 7 and `RUNG_ALLOWANCE` is 19, both from the design record's derivation.

**The tests were proved to bite rather than assumed to.** Three mutations were run against `rows.ts` and reverted: dropping the zero-window guard, dropping the allowance term from the maximum, and widening the window's upper bound to inclusive. Each turned exactly one of the three query tests red and nothing else. The two table tests carry their own proof inline, each asserting that its rule catches a hand-made bad row, because both otherwise assert an absence.

**`StageRow` now lives in `rows.ts` and `stage.ts` does not re-export it.** A consumer imports the type from `rows.ts`; `stage.ts` imports it for `Phase` and its own export block does not name it. `stage.ts` keeps `PhaseName`, `Phase`, `StageState`, `PHASES`, `DRAIN_OUT_SECONDS`, `phaseLengthTicks`, `createStage` and `advanceStage`.

**One warning for anyone running the standing checks.** `pnpm format` run from the shared checkout at `/home/mlo/dev/niftymonkey/the-cabinet` reformats this worktree's `scripts/roadmap/` files, because that root's `.prettierignore` names `scripts/roadmap/` relative to itself and cannot see the same entry in the nested worktree's own ignore file. It churned five ignored files here and they were reverted before the commit; nothing outside the worktree was touched, checked by mtime. Step 1's note already said to run the gate from inside the worktree, and this is the mechanism behind that instruction.

## 9. Slice 2, the three sections

**The seam that was added: `bankOpensNow(state)` in `stage.ts`.** Section 4's `Phase` gained `bankOpens` and the step 1 gate block asks for the per-tick opening site in `offer.ts`, called from `step.ts`. Neither document says who reads the column. `step.ts` indexing `PHASES` breaks section 5's rule that nothing outside a module indexes another's rows, and `offer.ts` importing `stage.ts` adds an import edge to the core for one boolean, so the phase table reads its own column and hands the answer over: `openBankedOffer(state, bankOpensNow(state))`, called beside `advanceLines` where the gate block puts it.

**`bankOpens` is true on every phase but `over`.** The gate block says the default is true and a phase is set false only where the record names a reason. The record names none for a boss phase or the sparse row, so none is set false there. The `over` phase is the one exception and its reason is written beside the row: the run has ended, so there is no run left to spend an offer in.

**The per-tick site finds nothing to do today, and that is stated in its own comment.** Every offer clears through a take or a loss, and each of those opens the bank itself. It becomes live at slice 5, where an offer whose three bodies are all refused at the corpse cap banks rather than disappearing.

**The three properties are measured under two named hands, and each test states its own.** The plan leaves the killing policy to the test and both hands live in `stage.test.ts`.

- The **sharp hand** kills every mob on the first tick the game lets it be read, its arriving beat spent and its body fully on the field (ADR 0016). It is what the Procession's ceiling and the Vigil-against-Crowd food rate are read under. Under it the maximum live templates is 1 in all three sections and growth paid per second is exactly the section's own authored rate, 0.26 for the Procession, 0.87 for the Crowd and 0.52 for the Vigil, in size units.
- The **still hand** is the rig this file already used: a grave held immortal that never moves and fires only its birthright. It is what the Crowd's floor is read under, and what the Procession's deliberate absence is read under. Under it the Procession reaches two live templates and stays there for 2625 of its 7200 ticks with no fault raised anywhere, which is the deliberate absence spec test 4 asks for.

**Why the ceiling needs the sharp hand and the floor needs the still one, measured rather than assumed.** Four seeds were run under each hand at three build levels. Under any still-grave hand, birthright or maxed, the Procession reaches two live templates: a parked grave never engages the edges, so a body it never touches lives its full roughly fifteen seconds against rows nine seconds apart. Under the sharp hand every section holds one, because a hand that deletes a group as it arrives makes the ceiling true by construction and tells the three sections apart not at all. So the ceiling is asserted under the sharp hand and given its teeth by a second assertion under the still one: the Procession's field is empty more than four times the share of ticks the Crowd's is. Those two are the pair the plan describes, the property under a hand that plays it and the absence under one that does not.

**The Crowd's floor drove two authoring changes.** Measured across four seeds at build levels 1, 3 and 5, the floor broke in two places: at 16.0 to 17.7 seconds, where only the lone ghoul Drip stood between the Wall and the first Pincer, and at 84.0 to 86.7 seconds, climbing out of the trough. The Pincer moved from t=17 to t=15 and a Drip of three was added at t=79, and with those two the Crowd holds two or more live templates from the tick it first has them through to its last row firing, on every seed and at every build level. The holes before the first two rows have fired and after the last one are outside the window and are what the sparse row and the eye opening answer at slices 3 and 10.

**The clock is 120, 155 and 75 seconds, which is 2:00, 2:35 and 1:15.** The design record's nominal clock exactly, and the boss phases still end on the tick they begin, so a run of authored rows is 350 seconds. `phaseLengthTicks` is still last row plus the drain-out, so the last rows are t=103, t=138 and t=58.

**The carrier count is held at 25 and its distribution is the record's eight, eleven and six.** Slice 4 owns the placement and this slice owns which rows may carry, so `carriersScheduled()` is met without `carriers.test.ts:228`'s literal pin having to move. `authoredCarriers()` in that file gained `VIGIL_ROWS`, which it needed either way.

**The Procession's first carrier now stands at t=21 rather than t=2, and that has a measured cost.** The design record holds the first two Drips clear on purpose, the first because the first kill of the run teaches the swallow and the second because the lone revenant teaches the tell. `hitTakingPolicy` seals shut at tick 1132 on every seed, before that first carrier, so it never swallows a rung and never loses one: `weaponStripped` fell from every seed to none. **The assertion was re-pinned rather than weakened**, as `STRIPS_A_RUNG` in `bot.test.ts` with the cause written on it and asserted as an equality, so the day a seed reaches the rung again the file goes red and says which. **ADR 0003's whole ladder in order is spec test 47's, at slice 9, on a run that reaches a boss fight, and that is where it has to be re-established.** If it cannot be reached there either, the honest answer is a pinned build on the hit-taking policy rather than a thinner opening.

**The other bot pins that moved, all re-measured and all with their cause written in the file.** `REACHES_VICTORY_FRESH` went from four seeds to two (404 and 505); `REACHES_VICTORY_FROM_THE_CEILING` from 101, 303 and 404 to 101 and 202; the storm's measured floors from 66 kills and 7 offers to 20 and 1. One cause runs through all of them: the stage is 21000 ticks of authored rows where it was 12421, and a dodger is paid only by the carriers its own lane happens to cross, so a longer, emptier opening pays it less and gives it longer to be ground down. `REACHES_VICTORY_MAXED` did not move, all five seeds, and `SEALS_IN_THE_PROCESSION`, `NEVER_FEEDS` and `MEETS_THE_TIMELINE` are all still empty.

**One test gained a stated timeout, and it is a budget rather than a flake fix.** `reaches victory from a maxed build on the seeds the set names` is the one test in `bot.test.ts` that pays for five whole-stage runs nothing else has warmed, and five runs of 21000 ticks at a maxed build no longer fit inside vitest's default five seconds. It is `FIVE_MAXED_RUNS_MS` on that one test, with the reason on the constant, rather than a suite-wide setting. It was also moved above the both-endings test in its own describe, which restores that block's own stated claim that every run it reads has already been paid for.

**The row tables are now 8 lines per row and `rows.ts` is 658 lines.** Prettier breaks a `StageRow` literal at 80 characters and `directed` is the field that pushes it over. The cost is real: 57 rows spread over 456 lines is a timeline whose shape can no longer be read at a glance, which is the thing the tuning pass at step 4 will want most. It was taken rather than worked around, because `// prettier-ignore` has no precedent anywhere in this tree and a formatting escape hatch is not a coding agent's call. **If a later slice wants the tables scannable again, that directive is the answer and it costs three lines.**

**Test 130 reads `stage.ts` through a Vite `?raw` import rather than `node:fs`.** A source-text fence needs the file's text, and `boundary.test.ts` allows a test under `src/game` to import exactly one bare package, vitest. `import stageSource from '../stage.ts?raw'` is a relative import the fence resolves inside `game`, so the guard stays green and the test stays in the file the plan puts it in.

**One stale sentence was found and corrected in `bot.test.ts`.** `SEALS_IN_THE_PROCESSION`'s opening line read "and there is one: 505, at 5466 ticks" while the constant has been an empty array since the offer of three landed; its own later paragraph says 505 left the set. The opening line now says the set is empty. Its historical paragraphs still say "the ramp", because they record measurements taken on the stage that was the ramp, and a note at the top says the ramp is the phase now called the Procession.

## 10. Slice 3, the sparse last row and the end condition

**The seam that was added: `sparseLastRow(from, shape)` and `SPARSE_LAST_ROW` in `rows.ts`.** Section 4's `rows.ts` block does not carry the sparse row, and spec test 12 asks that its count, type and spacing be stage data that a test can move and watch the row follow. Four literal rows twice would be a shape nothing could move, so the row is built from a `SparseShape` record: four bodies, shamblers, one every ninety ticks, which is the design record's initial row restated in the table's own unit as 1.5 seconds. Both boss-bound sections call it with the same shape, the test calls it with a different one, and nothing else in the tree reads it. `rows.ts` still value-imports nothing, which is why the spacing is seconds and not ticks: `TICK_HZ` would have been the module's first value import from `src/game`.

**Where the two sparse rows stand, and why there.** The Procession's is at t=112, 116.5 and the four bodies between, which is its own nine-second cadence carried past the last group; the Vigil's is at t=63 to 67.5 on its own five-second one. Both sit inside what the retired drain-out used to be, so the sections still end near the record's nominal 2:00 and 1:15 under a hand that kills what arrives, and both are `directed: false`, which is what put them beside the Wall in `rows.test.ts`'s off-limits list.

**What `phaseEnded` does with a condition whose machinery does not exist.** It falls back to the phase's own rows running out, and that is one line with the reason in its JSDoc. A boss phase authors no rows, so it still ends on the tick it begins, exactly the stub slice 2 left; the Crowd ends when its last row has fired, with the field still full, which is the half of ADR 0051 that already holds. **Both halves are pinned as named todos rather than left to be remembered**: module test 66 goes green when a boss phase is real, spec test 115 when the eye can open. CodeRabbit's Major finding on this is section 3's decline.

**Module test 65 needed a bound, and the bound is derived rather than written down.** The tail after a section's last row is however long the last bodies take to leave: the field's height plus the deepest a template places above it plus the body's own half height, over the scroll plus the slowest descent any type holds, which is 1091 ticks against a measured tail of about 830 under a hand that kills nothing. Every term is read from `MOB_TYPES`, `SPAWN_MARGIN`, `FIELD_HEIGHT` and `SCROLL_SPEED`, so the bound follows the mob table. The same derivation is in `bot.test.ts` for its run budget, which is two copies and stays two.

**Two bot seed sets moved and both were re-measured, with the cause on the constant.** `REACHES_VICTORY_FRESH` goes from 404 and 505 to 202, 303 and 404, running 21166, 21641 and 20812 ticks at 58, 65 and 118 kills, with 101 sealing at 13355 in the Crowd and 505 at 15988 in the Waking. `REACHES_VICTORY_FROM_THE_CEILING` gains 404 and is 101, 202 and 404 at 21575, 21526 and 21087 ticks. `REACHES_VICTORY_MAXED` did not move, all five seeds. The cause is the file's own standing one, a dodger steering off the field it stands in: a boundary now falls on the tick the field clears rather than on a count, so it lands a few seconds either side of where it did and the run is a different run from there on. Length moved for everyone, by the two sparse rows and by the Waking waiting for the Crowd's trash. The measurements were taken with a throwaway test file under `local/`, deleted after both readings.

## 11. Slice 4, the carrier schedule

**The slice changed no production code, and that is the finding rather than a shortcut.** Section 4 above says why: slice 2 authored the carrier column across all three tables, so the schedule already stood at eight, eleven and six through `carrierRow`, on File and V rows in the Procession and on no Drip anywhere. What was owed was the four tests, and they are the commit. Nothing in `rows.ts`, `carriers.ts` or `stage.ts` moved, so `GOLDEN` could not.

**Where the four went, and why one of them plays a run.** Spec 53, spec 54, module 120 and module 132 are all in `src/game/stage/__tests__/rows.test.ts`, which is where the plan's section 6 puts them, and module 69 was re-checked in place. Three of the four read the tables. Test 54 cannot: it says a run that kills every carrier before the set piece holds a full build, and how much one carrier pays is the offer's business rather than the row's, so the tables are the subject and a run is the fixture. That is the same reading `boundary.test.ts`'s own `game: ['game']` allowance is written on, that a stage's test building a run is building a fixture, and the file's header now says so.

**The hand the run is played under.** Every carrier is put one point from death under a skull of its own so the sim's own deaths phase kills it, because a kill the test makes outside the tick pays nothing: `step.ts:183` opens the offer over the tick's accumulated kills. Everything else on the field dies the way `stage.test.ts`'s sharp hand kills it, so the field clears and the sections reach their own boundaries. The take is the game's own: the grave is put over a body of the live offer and the swallow does the rest. **An earlier version called `resolveOffer` directly and it was wrong**, and the invariant harness said so on tick 1357: a take that skips the swallow leaves the body it took standing on the field, so `one live offer` faults with a drop carrying a line for no live offer. The swallow is what removes the body, and only it.

**The run reaches a full build on every seed it was read on.** Three seeds are pinned in the file (101, 202, 303) and eight were run while writing it, all with the same reading: nineteen carriers killed, nineteen offers taken, and every line at `MAX_LEVEL` before the Waking has anything in it. Nineteen is `carriersForFullBuild()` and it is also what the Procession and the Crowd author between them, which is decision 10's condition holding by construction rather than by luck. Three rather than eight because each seed is a whole run of two sections, at about 0.4 seconds each.

**The four tests were proved to bite rather than assumed to.** Five mutations, each reverted: a Procession File row set to carry nothing turned spec 53 and spec 54 red and nothing else; a Procession Drip set to carry turned module 120 red; a comment holding the word `carrying` added to `rows.ts` turned module 132 red, which is the source half of it firing; the rig's dive for the offer body disabled turned spec 54 red at the take; and the rig's arming of carriers disabled turned it red at the kill, one carrier reached instead of nineteen. The last two are the guard the lessons file asks for on a test that could otherwise pass over an empty set.

**Module 69's re-check.** It was green as written and it is stronger now: it asserted that some row somewhere carries, which was true when only two tables did, and it now asserts that each of the three sections carries, which is what the authored schedule says. Nothing else in it moved and the hand-made bad row it already carried is untouched.

**One reference in the dispatch prompt does not lead anywhere.** It names ADR 0048 and ADR 0036 as the slice's rulings. ADR 0036 is the bell, and the only thread from it to a carrier is step 1's third gate correction, that the carrier's stand-in mark is a tint rather than a ring because the ring is the boss's shape. The rulings the tests actually pin are ADR 0002 (carriers meter power) and ADR 0048 (a missed carrier is missed), and those are the two the new describe names.

## 12. Slice 5, the corpse cap

**The seam that was added: `RunState.refusals`, a three-count ledger cleared at the top of every tick.** The plan asks the invariant harness to raise a fault on a refused corpse spawn (module test 97), and a fault can only come from `checkInvariants`, which reads the state at the end of the tick. **A refusal is invisible there.** The body that was refused is in no pool, and the slot that was full when it happened is often free again by the time the tick ends: corpses spawn in `resolveDeaths`, and `advanceCorpses` and `cullCorpses` both run after it. A check over a full pool would therefore miss the refusal it exists to catch, and it could not tell the corpse refusal from the carrier's or the offer's, which the step 1 gate block asks for as three identities. So `claimSlot`, `spawnRow` and `standOffer` each count their own refusal, `step` clears the ledger first thing, and `checkRefusals` reads it last thing. Nothing in the rules reads it.

**It is excluded from the witness fold rather than folded, and `WITNESS_VERSION` did not move.** Slice 6 moves it once, which is the plan's ruling, and a fold widened here would be a second fold shape under one version. The exclusion is honest rather than convenient: every refusal is decided by the pools the fold already walks, so a divergence in the ledger is a divergence the witness already sees. The three paths are named in `witness.test.ts`'s `EXCLUDED` with that reason, and in `invariants.test.ts`'s own no-NaN `EXCLUDED` with a second one, that a counter cleared to zero and only ever incremented by one has no arithmetic that could produce a NaN.

**The cap is 233 and every term of it is computed.** `MOB_CAP` 160, `peakArrivals(FRESHNESS_SECONDS)` 63, `TREASURE_ALLOWANCE` 10. The 63 is the Waking's window: 50 poured bodies in ten seconds plus the Crowd's 39-body densest ten seconds at its third share, rounded up to 13. The rounding is up on purpose and the reason is on the function: a fraction of a body is a whole body arriving, and a cap that rounds its own worst case down can bind on the beat the worst case named. The other three terms of the query stand where slice 1 left them, and the Undertaker's window, 7 adds plus 19 rungs, is 26 and sits well inside the 63, which is the thing the third and fourth terms exist to prove.

**`CarrierLost` gained a reason and it is a closed union of two.** `leftField` is the ordinary one the cull already emitted, and `cap` is a carrying placement the mob pool refused, which `spawnDueRows` used to drop on the floor with `spawnMob`'s null. One event with a reason rather than two events, on the `PatchClosed` precedent the plan cites for `SetPieceClosing`. A refused body that carries nothing is deliberately silent: that is density the player never met and the director's problem, and there is a test that says so.

**An offer whose bodies are all refused banks, and `openBanked` is what hands it back.** `standOffer` increments the bank and reports `offerBanked`; the per-tick site slice 2 landed opens it on the first tick with room. The bank is never left short by the path through `openBanked`, which decrements before it stands the offer and re-increments if the pool refuses it.

**The tests were proved to bite rather than assumed to.** Four mutations, each reverted. Putting the eviction back into `claimSlot` turned six tests red across three files, including the behavioural one that watches every body leaving the field. Dropping the pour term from `peakArrivals` turned the query test red and nothing else, which is right: the cap's own test computes both sides from the same function on purpose, so what guards the term is the query's test and not the cap's. Inverting the carrier guard in `spawnRow` turned both stage tests red, and dropping the bank from `standOffer` turned the offer test red.

**The one test that plays a run drives it through the authority rather than through `stepping`.** `stepping` throws on any fault, and the run under test starts with a full corpse pool on purpose, so the corpse cap's own fault is expected there. It builds an `Execution` instead, asserts every body that stopped being alive was matched by what its tick reported, and asserts at the end that the run really did lose food all three ways and that the only fault recorded was the cap's. Without the full pool the assertion would pass over an empty set, which is the trap `docs/agents/lessons.md` names.

**`FieldRenderer` needed no edit.** It already sizes both food layers from `CORPSE_CAP`, so the pool followed the cap on its own; module test 109 reads the two sprite layers against the run's own corpse pool rather than against the constant, which is the stronger form and what the plan's warning about the two pools disagreeing is about.

## 13. Slice 6, the boss machine and the target seam

**The seam that was added: `stormTargets.ts`, and its contract is a sentence a later slice has to obey.** THE LIST IS ANSWERED FOR THE MOMENT IT IS ASKED FOR, AND NO CALLER RETAINS IT PAST ITS OWN PASS. The slots are a module-owned buffer refilled in place, so a target held across a later call is a reading of whatever now stands in that place. `launchWisps` is the one site that needed it: it over-commits surplus wisps onto the last target it assigned, and it carries that target by id and asks the seam again rather than holding the record. A pass that needs one target across two calls does the same.

**What each of the five sites became, and the two that changed shape rather than just their walk.** `storm.ts`'s `mobUnder` is `targetUnder` and asks the seam afresh per skull, exactly as the old walk did, because the pass kills as it walks. `bell.ts`'s `pushMob` is `pushTarget` and keeps its own push arithmetic, asking the seam to apply the move and reading the displacement back off the target, which is what makes a boss report no shove at all rather than a shove of zero. `territory.ts` moved three walks, not the one the plan lists: `eligiblePoints`, `controlMobs` (now `controlTargets`, which takes the list once for the whole pass because nothing in it kills) and `pulseWithPatch`. Its `mobIsOverPatch` became `patchHolds(patch, box)` so that `holdingPatch`, which is `src/dev`'s reading seam and takes a `Mob`, shares the one overlap rule. `wisps.ts`'s `targetOf` is now one call to `stormTarget`. `belch.ts`'s `burstNearbyMobs` is `burstNearbyTargets` and reads `entered` and `killableOutright` off the target instead of branching.

**`moveStormTarget` carries the target's box with the move, and that is behaviour parity rather than tidiness.** Territory's control displaces a body under one patch and then asks the next patch whether it is over that one; the old code rebuilt `mobHitbox(mob)` on every ask, so the box followed. A seam that handed out a box filled when the list was filled would have left every second patch testing a position one displacement stale. There is a test for it, and it is the only guard: no existing Territory test noticed the mutation.

**`pushable` and the mob handle are two guards on the same behaviour, and only the first is a decision.** Making a boss pushable in the seam does not move it, because `moveStormTarget` applies the move through the mob handle and a boss has none. The mutation round found this: the seam's own flag test went red and spec 123 did not. Both tests stand, the reason is written at the guard, and **spec 123 was strengthened** to assert that the toll reached the boss at all, so it is a statement about a boss rather than about an empty field.

**What the boss machine does and does not do in this slice.** `spawnBoss` puts a boss on the field and nothing calls it from the stage: a boss phase still ends on its rows running out, which is slice 3's stand-in, and wiring the arrival here would stall every run in the Banshee phase with no pattern to kill and no ending to reach. **Slices 7 and 8 wire it**, and spec test 23 is pinned at the seam instead, driving each boss phase's own column and asserting the field it arrives on is empty. `advanceBoss` runs the flash down and the pattern clock up, and the pattern itself is delegated to `banshee.ts` and `undertaker.ts` when they exist. `bossArrived` and the three set-piece events are declared with no producer in this slice, each naming the module that will fire it, which is the cited-future form: the boss ones are slices 7 and 8's and the set piece's are slice 10's.

**`RunState.setPiece` is declared and folded here and filled at slice 10, which is the plan's own ruling, and it needed a type.** `stage/setPiece.ts` exists holding the `SetPiece` record and nothing else; slice 10 fills the module. The record is folded whole, and its invariant (`set piece budget not negative`) is live code over a field that exists rather than a check waiting for one.

**The fold appends and never reshuffles.** `foldBoss` and `foldSetPiece` run after `foldOffer`, each folding its own absent sentinel when nothing stands there, which is what moved `GOLDEN`'s checksum on a scenario that meets neither. `boss.kind` folds through `BOSS_KIND_CODES`, append-only and read by name like every other union in that file. `boss.id` is excluded as spawn identity, `mobFire[].kind` as a field written once at spawn and never mutated.

**The two new fault identities take wire codes 19 and 20, which is what slice 5's note predicted.** `boss chunk only increases` and `set piece budget not negative`, both recoverable: a chunk that came back replays a pattern the player has already beaten and a budget below zero pours nothing, and neither poisons a number anything downstream reads. The chunk check needs memory across ticks, so `StageWatch` gained `seenBoss`, which holds the id as well as the chunk: two bosses run in one run, and a memory of the chunk alone would read the second one's arrival at chunk zero as the index falling.

**The tests were proved to bite rather than assumed to.** Eight mutations, each reverted. A pushable boss (the seam's flag test alone, which is the finding above). A belch-killable boss (the flag test and the belch guard). The boss dropped from the seam's list (seven tests across both new files, spec 25 among them, which is the storm-must-always-matter property). The flash guard removed (three). The chunk-break feast removed (two). A directed shot re-aimed at the grave (two). The box left behind by a push (one, and nothing else in the tree saw it). A walk planted in `skullStream.ts` (fence 124's roster sweep).

**`FieldRenderer` and `mobFireSprite` needed no edit, and one of them is stated rather than a surprise.** `mobFireSprite.ts` still names `MOB_FIRE.trash` for both the shot and its scatter, so **boss fire draws in the trash body colour until slice 14**, which the plan puts there because that is the slice that draws bosses at all. The three sprites `palette.ts` has been carrying since before any boss existed are still unused.

## 14. Slice 7, the Banshee

**She arrives because the phase says so, and her phase ends because she is gone.** `enterNextPhase` reads `Phase.boss` and calls `spawnBoss`, announcing `bossArrived` with the chunk count `bossChunks` reads off her own health row; `phaseEnded` answers a `bossKilled` column with `state.boss === null`. Those two lines are the whole wiring, and together they are what makes the Wall's clock her death: the Crowd's phase-local zero is the tick after she falls, and its own first row is the curtain at t=2.

**What her rings are, as data.** `RING_ROWS` is one row per chunk: a period, a spoke count, how many adjacent spokes the opening replaces, how far the opening turns from one ring to the next, a jitter, and the sources it fires from. Chunk one is one source at her centre; chunk two is two, offset seventy units either side with their openings half a turn apart and the drift reversed. Initial rows every one of them, and what is not tuning is the shape: one clean opening per source, an opening that walks rather than stands still, and two sources whose openings never point the same way.

**The jitter is the one place chance enters her pattern, and it is one draw per ring.** All the sources of a tick share it, so the offset between two openings is exactly the authored one rather than two independent rolls that could quietly close the gap between them. It is drawn from the fire stream, because a ring is fire and because a sixth stream name would widen the witness fold and `WITNESS_VERSION` moved once already, at slice 6.

**The delegation table is how a boss's grammar reaches the machine.** `BOSS_GRAMMARS` in `chunks.ts` holds `{ advance, died }` per kind, `advanceBoss` runs the pattern on the clock it can read and then moves it, and `killBoss` announces the death before it asks the boss's own module what the death sheds. The Undertaker is absent from the table, which is what keeps the stage crossable until slice 8; section 4's item 26 is the whole of that argument.

**What the Banshee costs the headless birthright runs, measured rather than assumed.** This is the biggest thing in the slice that is not code. A policy that only dodges has nothing to kill a boss with, so a fresh run now ends inside her phase or just past it rather than crossing the stage:

- Fresh, five seeds: none reaches victory, none seals, all five run to the budget. Three of them (101, 303, 404) do empty her on the birthright storm alone, in ten to fifteen thousand ticks, and then run out of budget inside the Crowd; 202 and 505 are still in her phase at the end. Kills fall from 20 to 112 down to 11 to 32, and offers from 1 to 4 down to 0 to 4.
- From the size ceiling, five seeds: not one empties her, so every run is still in her phase when the budget ends, ending neither way.
- Maxed, five seeds: all five still reach victory, which is why that set exists, but they take 22000 to 48000 ticks against the 27000 the rows alone bound. `MAXED_RUN_TICKS` is three times the authored ceiling for exactly that reason.

**That last spread is the harness's input at step 4 and not a defect here.** A maxed dodger spends one to seven minutes on a boss authored for forty-five seconds, because it never parks under her and its storm reaches her only when its dodging happens to; a parked still hand on the birthright alone empties her in about 4900 ticks. Her health per chunk is the design record's derived 1100 and is tuning data by ADR 0052, so nothing here was retuned to make a bot's number nicer.

**Where the coverage went.** `reaches both endings across the five seeds` used to read its sealed half off a dodger that got ground down before the Vigil, and no dodging birthright run ends at all now. It reads the maxed set for victory and the hit-taking policy for sealed, which is the policy written to reach it; `sealedRun` caches those five runs because two suites now read them. **The honest summary is that between this slice and the tuning pass, the birthright loadouts no longer cross the stage headlessly, and what covers the whole stage is the pinned maxed build the plan's verification step 7 already names.**

**Two rig budgets grew and each says why on itself.** `STAGE_TICKS` in `stage.test.ts` adds a `BOSS_FIGHT_TICKS` allowance per boss phase, because a fight has no authored length and the still hand's own 4900 ticks left the old ceiling with six per cent of slack; and `bounds the tail after a section's last row` took a stated timeout, because it plays two whole stages nothing else has warmed and a stage is a fight longer than it was. The bot file's birthright budget deliberately did **not** grow: those runs do not empty a boss, so a bigger budget buys minutes and nothing else.

**The tests were proved to bite rather than assumed to.** Eleven mutations, each reverted, and every one turned at least one of this slice's tests red: firing every spoke (the one-clean-gap test), giving chunk two one source and lining the two openings up (the offset test, twice), dropping the stream draw and moving it to the spawns stream (the stream test, twice), drawing a tear in the trash read (three tests, including the gas one), shedding nothing on her death (all four death and Wall tests), ending a boss phase on its rows again (spec 7, the stubbed-phase test, the Wall test and all five ceiling seeds), never arriving at all (nineteen), skipping a chunked target in the burst and applying the kill rule to it (the two belch tests). Two of the mutations also moved bot seed outcomes, which is the path effect that file has documented four times: a different fight is a different run from there on.

**One thing this slice does not do, stated so it is not read later as a bug.** Her tears carry the `tear` fire kind and `mobFireSprite` still draws every shot in `MOB_FIRE.trash`, so boss fire is drawn in the trash colour until slice 14, exactly as slice 6's note said. Nothing draws her body at all yet either; `BossRenderer` is slice 14's.

## 15. Slice 8, the Undertaker

**The arrival gate is gone, and its going is the whole of the stage change.** `BOSS_GRAMMARS` is a total `Record<BossKind, BossGrammar>` now rather than a `Partial`, `bossIsAuthored` is deleted with its caller, and `arriveBoss` reads `phase.boss` and nothing else. What the totality buys is the thing the gate was standing in for: a kind added to `BossKind` with no module to fight with is a compile error rather than a boss who never arrives and a phase nothing ever leaves.

**What his three chunks are, as data.** `CURTAIN_ROWS` and `SPIRAL_ROWS` are each three long and each carries a null where its chunk does not run that shape: the burial throws curtains, the exhumation turns the arm, the last chunk does both. A curtain row is a period and a clod count; a spiral row is the arm's revolution, its shot interval and its digger cadence. Every number is an initial row and what is not tuning is the shape.

**How a curtain is built, and why there is always a way through.** The clods are spread evenly across everything the opening does not take, so the count is the row's whatever the grave's size is: a bigger grave buys a wider way through and never a thinner curtain, and the opening exists by construction rather than by arithmetic that happens to work out. The opening is held far enough from either edge that all of it is on the field, because half an opening against a wall is not the width the gap rule promised.

**Where the opening sits is the one branch in the module, and it is the locked overlap.** A chunk with no arm turning walks its opening across the field on its own; a chunk with one opens it where the arm has just swept, a quarter of a revolution behind. Both read the one pattern clock, which is what makes "in phase for the whole chunk" true by construction rather than by a second clock that has to be kept in step.

**The arm is the line the spiral's shots leave along, and the curtain reads where that line reaches.** That is what `ARM_REACH` is for and why it is a row rather than a drawing decision: at 180 of the field's 540 the arm sweeps between x 90 and 450 and never needs the edge clamp, so the opening tracks it exactly. The diggers come up at the same place, which is what makes them an exhumation rather than a spawn.

**The lattice is why the phase tests carry a tolerance, and the tolerance is derived.** A curtain's clods stand on a lattice of their own spacing, so an opening measured between two of them can only land within half a step of where the pattern aimed it. Both overlap tests compute that half step from the row and the current gap rather than writing a number down, and the follows-rather-than-leads half asserts the arm's current position is further off than the tolerance, so a lag of zero fails.

**What the storm floor is computed from, and never pinned.** Spec test 44 reads `COLUMNS_BY_LEVEL`, `SKULL_DAMAGE` and `STREAM_INTERVAL` off the skull stream and `BELL_DAMAGE_NEAR` and `BELL_PERIOD` off the bell, at their top level, and derives about 147 points a second. Territory and the wisps are left out exactly as the design record leaves them out, so the figure is a floor rather than a prediction. The chunk-health property is then that each chunk's row clears that floor across one emit of its own longest cycle, which at 240 and 300 ticks is 588 and 735 against rows of 1700.

**A boss test cannot switch the storm off, so the rig holds the chunk instead.** Zeroing `skullStream` raises the fatal `levels in range` invariant, because the birthright is what a run is born holding. The pattern rig restores the live chunk's health after each tick and holds the grave immortal at whatever size the test set, and it stands the run in his own phase, which authors no rows, so everything that arrives on the field in these tests is his.

**What he costs the headless runs, measured rather than assumed.** All five maxed seeds still reach victory and the set did not move, but victory now means his death rather than a phase a stub fell through. The runs take 26000 to 52000 ticks and his fight is 3600 to 4800 of them on every one of the five, against the Banshee's 1280 to 27230 on the same runs. That tightness is worth reading: he is fought where the Vigil has left the field nearly empty, so a dodger stands in front of him and its storm lands, where her fight is spent dodging across a field she shares with nothing. Sixty seconds against ADR 0052's ninety to a hundred and twenty is what a maxed build buys, and it is the harness's input at step 4 rather than a defect here.

**One rig budget grew and it says why on itself.** `BOSS_FIGHT_TICKS` in `stage.test.ts` is 19500 against 7500, because the still hand's own measurement moved: a parked grave on the birthright alone empties the Banshee in about 4900 ticks and the Undertaker in about 13000, his three chunks against her two. It is a ceiling and not a prediction, and every run in that file still ends on victory long inside it.

**One test beyond the plan's list, and it is what a mutation asked for.** Spec 38 holds that a curtain is never a wall, and the first mutation, clods spread across the whole field with no opening cut, left an opening against the right-hand edge wide enough to pass it. That opening is real, so the measurement is right and the test list was short: nothing held that the way through **moves**. `walks the way through rather than opening it in the same place twice` is the missing half of game-concept.md:70's "one moving gap", and it holds both sides, that the opening moves at all and that it never moves further than the row says it walks.

**The tests were proved to bite rather than assumed to.** Ten mutations, each reverted, and every one turned at least one of this slice's tests red: a curtain with no opening cut (the walk test and both overlap tests, which is the finding above); a gap rule reading a fixed size (the gap-rule test and both grave-size tests); the overlap reading the arm's current tick instead of the lag (both overlap tests); the last chunk's curtain at chunk one's clod count (the thinner-curtain test); the last chunk's digger cadence removed (the keeps-digging test); a whole curtain fired from one point (six tests, the no-ring one among them); a chunk's health dropped under its emit floor (spec 44); a digger cadence over `BOSS_ADD_ALLOWANCE` (module 121 in `rows.test.ts`); the burial turning an arm as well (the one-shape-at-a-time test and the walk test); and the arrival gate put back for the Undertaker alone (both stage tests this slice touched).

**Two things this slice does not do, stated so neither is read later as a bug.** His clods and spiral shots carry the `clod` and `spiral` fire kinds and `mobFireSprite` still draws every shot in `MOB_FIRE.trash`, so his patterns draw in the trash colour until slice 14, exactly as slices 6 and 7 said. And victory still fires on entering the `over` phase rather than on his death: the stub is slice 9's to retire, and what changed here is that reaching that phase now costs a fight.

## 16. Slice 9, both endings

**Victory is announced from its own seam, and the last boundary rides with it.** `winStage(state, tickEvents)` in `stage.ts` reads the tick's own `bossKilled`, checks it against the boss the table's last fight carries, crosses into `over` and fires `victory`, all on the tick he falls. `step.ts` calls it straight after `resolveDeaths`, which is the last phase of a tick that can empty a boss. The stub in `enterNextPhase` is gone and that function now only announces a phase and stands up whatever boss it carries. Section 4's item 33 is the whole of why the crossing moved in here rather than being left to the next tick's `advanceStage`.

**Which boss ends the stage is read off the phases and never written down.** `FINAL_BOSS` folds `PHASES` for the last phase carrying a boss, so a stage that gains a section ends on that section's boss with no edit in this module. The second half of the check is the current phase's own boss column, which is what keeps a boss stood up outside his phase by a rig from ending anything: `chunks.test.ts` spawns bosses wherever it likes and none of it moved.

**What the ladder needed was the run and not the policy.** `hitTakingPolicy` steers into the nearest threat and is unchanged. Standing it in the Undertaker's phase on a run pinned at level two with score on it, ADR 0003's rungs fall in order: the whole score bled on the first floor hit, then two strips down to the birthright, then sealed, with the boss still standing at 1331 of his last chunk's 1700. Level two rather than five is deliberate and stated on the row: a full build empties a chunk faster than his pattern grinds a grave to the floor, and a fight that ends in victory cannot also seal.

**`STRIPS_A_RUNG` was re-measured and is still empty**, which the equality in `bot.test.ts` asserts on every run. Nothing in this slice touches the Procession, where that policy seals at tick 1132 before the first carrier at t=21, so its second rung is still out of a birthright run's reach there. The constant's comment now points at `endings.test.ts` as where the whole ladder is walked instead of at a slice that had not happened yet.

**A warning for slice 10, which owns module test 104.** That test asks the bot to reach sealed "through the whole ladder" on at least one seed, and `sealedRun` plays `hitTakingPolicy` from the ceiling on a birthright loadout in the Procession: it bleeds score once and seals, and there is no rung between them. Reaching the whole ladder from that file needs the same two pins this slice used, a build above the birthright through `createRun`'s third parameter and a fight to stand in, or the test measures the policy rather than the ladder.

**The lifecycle test's victory rig had to become a real fight.** `screenLifecycle.test.ts`'s "complete run in both directions" stood the run in the last boss phase with nobody in it and let the crossing win it, which is exactly the route this slice retired. It now spawns him on his last point of health and plays until the birthright storm reaches him, which is 79 ticks from a parked grave on every seed measured. It is the only test outside the new file that the ruling moved, and the rest of the suite was green on the first run after the change.

**The tests were proved to bite rather than assumed to.** Six mutations, each reverted. The stub put back in `enterNextPhase` (three tests, two of them `endings.test.ts`'s and one the stage's). Any boss's death ending the run (seventeen, the Banshee's half of the payout test among them, because a stage that ends at the Banshee never reaches the Vigil). The ending without the crossing behind it (six, including the seven-phase chain and both boundary-tick tests, which is the measurement behind item 33). His death shedding a feast like hers (three, spread across `endings.test.ts` and `undertaker.test.ts`). The ladder reordered to strip before it bleeds (two, this file's and `grave.test.ts`'s own). The already-ended guard dropped from `winStage` (one, the same-tick test, which is the only thing that holds it).

## 17. Slice 10, the set piece

**The Waking is a phase as long as its own source, and that is the ruling slice 3 left here.** `PhaseEnd` gains `setPieceClosed` beside `setPieceOpened`, so the Crowd ends the tick the eye opens and the Waking ends the tick the source is gone, whichever of its three ends took it. Neither waits for a field to clear, which is ADR 0051's "only the two boss boundaries need the field empty" read in both directions: the Crowd hands the set piece a field with trash on it and the set piece hands the Vigil the trail it poured. The alternative slice 2 left in the table, `rowsSpentAndFieldClear`, would have put a fifteen-second drain in front of the Vigil that no ruling asks for. Both conditions terminate by construction and `phaseEnded`'s JSDoc says why: the source is placed inside the field and drifts downward at a fixed rate, so it always reaches its opening depth, and once open it spends a finite budget on a fixed interval.

**Which phase places the source is read off the column that says it ends on the eye.** `placeDueSetPiece` fires when `phase.ends === 'setPieceOpened'`, so the section that ends on the eye opening is the section the eye is placed from and no phase name is tested. It is placed once, because a source on the field is what ends that phase.

**`setPiece.ts` declares no magnitude and derives its whole geometry from rows.** The drift is `SCROLL_SPEED` times `SET_PIECE_DRIFT_SHARE`, the opening depth is `FIELD_HEIGHT` times `SET_PIECE_OPEN_DEPTH`, and the pour's interval is `SET_PIECE_POUR_SECONDS` in ticks. Both shares are new rows in `rows.ts` rather than a speed and a depth, for the reason slice 5 already met with `SET_PIECE_POUR_SECONDS`: that module value-imports nothing, so it cannot read the field or the scroll, and what is authored is that the source is slower than everything else and opens in the middle of the player's view.

**The sweep is a function of the depth and never a heading.** `sweptTo(y)` puts the source at its authored bounds' fraction of the fall, so it cannot sweep twice, cannot turn back, and cannot come out of step with its own drift. Test 22's assertion is then about the bounds rather than about a state machine.

**The mouth pours from alternating lips, and that is the narrow no-stack answer (#81 stays unowned).** `POUR_LIP_X` and `POUR_JITTER_X` are rows: the lips stand far enough apart that the widest trash body fits between them and the jitter is bounded under that gap, so two bodies in a row are never stacked. The jitter is drawn from the spawns stream, which is the one place chance enters the moment and is what module test 76 holds; a pour is a spawn, so it draws where every other placement scatter does. The type it pours is `POUR_TYPE` in `rows.ts` and not a name in the module, because ADR 0042 rules that a set piece names the property and never the cast: the shambler, because the ghoul closes and would turn a trail into a chase and every revenant is armed, which would make the moment a dodge.

**`WAKING_ROWS` is what `POUR_SHARES` was waiting for.** The plan says the share is read by the stage's spawner, and the Waking authored no rows at all, so there was nothing for it to thin. The rows are now derived: the Crowd's own last groups over the pour's own length, re-timed to zero and each count taken to the share, never below one, carrying no offer and open to no director. `peakArrivals` is untouched and the corpse cap did not move, because the Waking's own densest window sits inside what `pourWindow` already prices, which `rows.test.ts` now asserts at every window length from one to twenty seconds.

**The dodge became steerable, and every policy in `bot.ts` is now the same hand wanting different things.** `bestMoveToward(state, point, enough)` is what `bestDodge` calls with `HOME`, and `divingPolicy` and `waitingPolicy` call with the nearest food and with the nearest food's column at the drifting height. `dodgePolicy` is behaviour-identical and every pinned bot seed set held. The one row the diving hand adds is `COMMITTING_CLEARANCE`, half a trash body's width against the whole body's width the drifting hand keeps, and it is load-bearing rather than a knob: above the cap two moves tie on room and where the hand wants to be decides, and inside a pour every move leaves less than a body's width, so a hand that only took the roomiest move never reached anything and measured as a dodger. Squeezing past a body to reach a corpse is what a person does, and it is `VISION.md:21`'s own bet.

**The property holds as a tendency across seeds and not as a per-seed law, and that is measured rather than assumed.** Over twelve seeds, reading the trail from the source's opening to one freshness window past its leaving, eleven went the committing way and one the other, and which seed went the other way moved with the window the trail was read over; the totals were 132 corpses to 58. The test therefore asserts the total across its seeds against `COMMITTING_PAYS_OVER`, half again, and pins the seeds that go the other way as `WAITING_EATS_MORE`, empty today over the standing five, which is the same idiom `STRIPS_A_RUNG` already uses. Neither hand seals on any seed and the hits are nearly zero on both, so the moment is heaven at its loudest rather than a dose of hell.

**What the pour costs the storm is worth reading before the tuning pass.** Under a pinned full build at the size ceiling, about twenty-five of the seventy-five bodies poured are killed inside the pour and its trail; the rest fall past. "The storm shreds the swarm" is not what the initial rows produce, and the property the tests hold is comparative rather than absolute, so nothing here is red. **It is the harness's first question at step 4** and it moves on three rows: the budget, the interval, and the source's own depth.

**The clock, measured end to end, and the one figure spec test 3 writes down.** Under a parked hand firing only the birthright the whole run is 38432 ticks, 10.68 minutes: the Procession 118 seconds, the Banshee 81, the Crowd 140, the Waking 15, the Vigil 69, the Undertaker 216. Under a parked full build it is 23924 ticks, 6.65 minutes, with the two fights at 17 and 39 seconds. Neither is a player, and the two are the band's own edges. What spec 3 holds is ADR 0049's own sentence, that the nominal clock inside the band is stage data: the sections are as long as their own rows take under a hand that plays them, and a fight's nominal length is its health rows against a full build's storm at the share the design record sizes `CHUNK_HP` under. That share, one third, is the one figure in the file written down rather than read off a row, and it has to be, because how much of a player's storm is pointed at a boss is a fact about the player. The sum is 8.2 minutes.

**Verification step 8 is met with two pins and not at the birthright, which slice 9's warning named.** `ladderRun` stands the hit-taking policy in the Undertaker's phase on a run pinned at level two with score on it, and the whole ladder falls in order on all five seeds. The birthright half of the plan's own wording cannot be met: score arrives only as ceiling overflow from a swallow and this policy never dives, so a birthright run reaches the floor with nothing to bleed and seals on its first rung, which is what `sealedRun` already measures in the Procession.

**Two findings outside the slice, neither fixed here.**

- **The app's own eslint config ignores only `dist`, so `apps/hungry-grave/local/` is linted by `pnpm build`.** The repo root config has `**/local/**`, `.gitignore` and `.prettierignore` both exclude it, and `eslint.config.mjs:55` does not. It bit this slice because its throwaway instruments were the first `.ts` files anyone had put there; every earlier baseline in `local/step2/` is `.txt` or `.json`. It is `docs/agents/lessons.md`'s own "check every ignore list the standing checks read", still open for the app's own config. **Trigger: the next slice that writes a TypeScript instrument under `local/`.**
- **`pnpm verify` was run once from the shared checkout at `/home/mlo/dev/niftymonkey/the-cabinet` by mistake.** It is read-only, it failed on the worktree's own `scripts/roadmap/` files exactly as slice 1's note predicts, and nothing was written; it was re-run from inside the worktree and is green. The failure mode is still live and the note's instruction still stands.

**The tests were proved to bite rather than assumed to.** Nine mutations, each reverted, and every one turned at least one of this slice's tests red: a dormant source that takes damage (the immunity test); the source dropped from the target seam's list (three, including the property itself); a pour from one point rather than alternating lips (the no-stack test); a pour with no draw behind it (the stream test); the Crowd ending on its rows again (both Crowd tests); a sweep across the whole field rather than its authored bounds (three, the property among them); a killed source that keeps its budget (three); a pour row that carries the offer (the share test); and the Waking draining before the Vigil (the parking rule's own test).

## 18. Slice 11, the section timeline

**A span's `to` is the tick the next section began, and it is the convention the tree already reads spans by.** Section 4 declares `from` and a nullable `to` and says nothing about which tick the boundary belongs to. The two spans either side of a boundary both name it, so a section held `to - from` ticks, which is exactly what `spanOf` and `foodPerSecond` in `stage.test.ts` already do with the same boundaries (`[from, boundary.tick]`, then `foodPaid.slice(from, to)`). The alternative, a `to` one tick before the successor's `from`, reads well in a table and produces a span of minus one ticks the day two boundaries fall on one tick, which is a thing the stage forbade at slice 8 rather than a thing the reading can rely on.

**The first span is the reading's own, because the stage announces only crossings.** `enterNextPhase` is the one site that pushes `phaseChanged`, so the Procession has no event of its own and a fold over the event stream alone would report a run that began in the Banshee. The accumulator therefore opens at tick 0 on the table's first phase, which is where `createStage` puts a run and where a measured tape begins with it: a tape is a recording of a run from its own first tick.

**Every bound is the event's own tick and never the observer's.** The two are one apart: `step` announces the boundary while it is spending tick N and increments the counter at the end, so the listener is told N+1. The reading takes `event.tick`, which is the clock the arrivals, the deaths and `stage.test.ts`'s own timeline are all read on.

**The reading joins no entity id, so slice 6's null rule has nothing to bind here.** The dispatch carried the warning that the two readings joining `mobDamaged.id` return null on an id with no pool slot rather than throwing. This one folds `phaseChanged` and reads no pool at all, so there is no id to miss.

**Verification step 9, on the maxed dodge bot, recorded as a tape and read through `scripts/measure.ts`.** Seed 101, every line pinned to level 5, 25862 ticks, ending victory, no fault recorded. Food per second is growth paid by the tick's kills in size units, which is the design record's scarcity measure and the same quantity `stage.test.ts` reads. The live-mob column is the report's own `mobsAlivePerTick` at each span's first tick, which is what verification step 9's empty-field criterion asks for.

| phase | from | to | ticks | seconds | food/second | live mobs as it began |
| --- | --- | --- | --- | --- | --- | --- |
| procession | 0 | 7284 | 7284 | 121.4 | 0.24 | 0 |
| banshee | 7284 | 8526 | 1242 | 20.7 | 0.00 | 0 |
| crowd | 8526 | 16927 | 8401 | 140.0 | 0.78 | 0 |
| waking | 16927 | 17827 | 900 | 15.0 | 0.74 | 21 |
| vigil | 17827 | 22022 | 4195 | 69.9 | 0.61 | 19 |
| undertaker | 22022 | 25861 | 3839 | 64.0 | 0.16 | 0 |
| over | 25861 | open | 1 | 0.0 | 0.00 | 4 |

**And on the birthright still hand, which is a rig run and not a tape.** Seed 101, parked, firing only what it was born with, with the grave held immortal exactly as `stage.test.ts`'s rig holds it. It is not a tape and cannot be one: a recording replays commands from a fresh run, so a rig that restores the grave every tick would diverge from its own witness. A birthright hand that is not held seals inside the Procession, which is what `stage.test.ts` says in as many words and what the wandering recorder measured from the other side.

| phase | from | to | ticks | seconds | food/second |
| --- | --- | --- | --- | --- | --- |
| procession | 0 | 7107 | 7107 | 118.5 | 0.06 |
| banshee | 7107 | 12056 | 4949 | 82.5 | 0.00 |
| crowd | 12056 | 20457 | 8401 | 140.0 | 0.04 |
| waking | 20457 | 21357 | 900 | 15.0 | 0.27 |
| vigil | 21357 | 25526 | 4169 | 69.5 | 0.07 |
| undertaker | 25526 | 38509 | 12983 | 216.4 | 0.05 |
| over | 38509 | open | 1 | 0.0 | 0.00 |

**What the two tables say, beyond the reading working.** Under the bot the Vigil pays less per second than the Crowd, 0.61 against 0.78, which is the section's own property. Under the still hand the same pair reads 0.07 against 0.04 and the order inverts, because a hand that kills nothing is measuring what wanders into its birthright storm rather than what the section serves: the scarcity property is spec test 8's and is held under the sharp hand the design record derives it under, so nothing here contradicts it and the still hand's column is a reading of the rig. **The Crowd is the best-fed section under the hand that plays, and the Banshee's fight pays nothing at all on either hand**, because her feast falls on the tick she dies and that tick is the tick her phase gives way, so the payout lands in the Crowd's first tick. Worth knowing before the tuning pass reads any of these as a section's own rate.

**The still hand's timeline reproduces section 17's independently measured clock.** That section recorded 38432 ticks and 118, 81, 140, 15, 69 and 216 seconds, measured while writing spec test 3 with no reading in the tree. This slice's reading reports 38510 ticks and 118.5, 82.5, 140.0, 15.0, 69.5 and 216.4 on the same hand. Two instruments written a slice apart agreeing to a second is the strongest thing said about the reading here, and it is why these tables are worth more than the two tests are.

**The instrument is `local/step2/slice11-timeline.ts` in the app, with its tape and its measured report beside it.** It is a throwaway in the ignored folder, kept rather than deleted because it is the only thing that reproduces these tables, and it is what found the lint hole section 7 records.

**The tests were proved to bite rather than assumed to.** Four mutations, each reverted. Dropping the opening span turned both tests red. Never closing a span, and a span that forgets which tick it began on, each turned test 100 red. Dropping `observeSectionTimeline` from `observeReadings` turned test 101 red and nothing else, which is the mutation that matters: **it turned nothing red at all against the first draft of test 101**, whose tape was sixty ticks long and crossed no boundary, so a test claiming a span is never closed at the last tick was asserting an absence its own input could not produce. That is `docs/agents/lessons.md`'s named trap, and the fix was to read a tape that had already closed one.

## 19. Slice 12a, the music bundle and the seam

**Three loops, re-encoded, and what they cost.** The staged pack's `bells-of-death.mp3`, `seek-n-slaughter.mp3` and `a-hollow-call.mp3` run 118.20, 103.42 and 147.72 seconds and were staged at 44.1 kHz stereo, 182, 161 and 178 kbps, 7.7 MB in all. They are in `raw-assets/music{m}` at 96 kbps mono, sample rate untouched and metadata stripped, which is 1.42, 1.24 and 1.77 MB, **4.33 MB against the design record's estimate of about 4.4**. Every duration is unchanged to the sample the encoder reports.

**The re-encode is load-bearing rather than tidy.** AssetPack's audio pipe emits an mp3 and an ogg per source and its mp3 output carries `recompress: false`, so an mp3 input is **copied verbatim** and only the ogg is encoded. The staged file's own encoding is therefore the shipped one, and the pack as staged would have shipped at 7.7 MB whatever the pipe's settings say. The pipe's own target is 96 kbps mono at 48 kHz, which is what the figure above matches. The shipped pair is 4.33 MB of mp3 and 1.48 MB of ogg at the pipe's 32 kbps, **5.81 MB against the record's estimated 8.9**.

**The loop the phase names rides the event, and that is what the sound fence forces.** `src/app/sound.ts` is governed by a boundary rule of its own that allows `game/events` and nothing else, so it cannot read `PHASES` to turn a phase name into a loop. `PhaseChanged` carries `music: PhaseMusic | null` and `events.ts` re-exports the type beside it. The split ADR 0049 asks for is exactly preserved: the section's music is authored on the phase in the sim, and only the filename lives in `src/app`. The alternative, widening the fence, is the one thing that rule was written to stop.

**`phaseUnderway` is the announcement a crossing never made.** `enterNextPhase` is the only site that pushes `phaseChanged`, so the phase a run opens in has no event and the music would have started at the Banshee's death, two minutes and a whole fight in. The reader is in `stage.ts` beside `bankOpensNow`, `enterNextPhase` now builds its own event through it, and `GameScreen.prepare` announces it once per run. A pooled screen says it again on the next run, which is its own half of the test, because the channel is still holding the loop the last run ended on.

**The bundle is what the cue waits on, and the wait is the whole of the gap handling this slice does.** No screen declares the music bundle, so the first cue can land before the file is registered and `sound.find` throws on an alias it has not seen. The channel `src/main.ts` builds awaits `Assets.loadBundle('music')` before every play and reports anything that fails past that. Cues resolve in the order they were made, so a run that crossed a boundary while the bundle was still coming still ends on the loop its own phase named. **No loop-gap handling of any kind is built**, which is slice 12b's to rule on after it measures.

**What the seam does not carry, and why.** `MusicOutput.play` takes an alias and no volume: `BGM.play` starts every track at zero and animates it to the music volume the player set, so a volume passed in would be overwritten a line later. `playMusicFor` catches nothing, because `BGM.play` is async and a throw inside it is a rejected promise rather than something a caller can catch; the catching is on the channel's own promise chain in `main.ts`, which is also where the bundle is awaited.

**Three tests beyond the plan's list, each one guarding something no other test can see.** `ships every loop in its own bundle` reads the manifest, because a filename that drifts sounds exactly like a section that plays nothing and the closed union cannot see a string. `cues the section a run opens in` in `screenLifecycle.test.ts` is the wiring, and it is the only thing that fails if the screen stops asking. `says which loop plays on every crossing` in `stage.test.ts` holds the crossings and the opening as one fact. The plan's own three are 58, 108 and 131, all in `sound.test.ts`, and 108 is held in two halves: the cue is issued on every crossing including the ones that ask for the loop already playing, and the engine's own `BGM` turns those six cues into three started tracks, the opening and the two changes.

**The tests were proved to bite rather than assumed to.** Five mutations, each reverted. Dropping the opening announcement from `GameScreen.prepare` turned the lifecycle test red and nothing else, which is the point of having it. Making every phase answer the first phase's loop turned five red across both files. A typo in one filename in the table turned the bundle test red. Making `playMusicFor` skip a cue whose alias had not changed, which is the app deciding what an audible change is, turned the every-crossing test red. Giving the Banshee's phase the Crowd's loop turned the two-changes test red. The engine's no-op is asserted against the real `BGM` with the sound library and the tween stood in for, so the sentence about three aliases and two changes is held end to end rather than at the table.

## 20. Slice 12b, the loop gap measured

**The measurement, and what it settles.** Verification step 13 exists to decide whether any gap handling gets written, and the answer is no. Every one of the six files the `music` bundle ships decodes, in Chromium, to exactly the length its own header states minus the encoder's delay and padding, to the sample. The browser trims on decode, the earlier premise that `decodeAudioData` hands back an untrimmed buffer is stale, and no loop gains a `start` or an `end`. **The commit holds one test file and no production code.**

**The rows.** Taken in Chromium 1234 against `pnpm exec vite preview` on the built `dist`, so the bytes measured are the bytes the app serves. Every decode lands at 48 kHz because `decodeAudioData` resamples to its context's rate, which is what a device does too, so the header's sample counts are compared at 48 kHz throughout.

| Loop | File | The header's own count, at 48 kHz | Decoded | Quiet at the wrap |
| --- | --- | --- | --- | --- |
| Procession | `bells-of-death.ogg` | granule 2605293 at 22050, so 5671386 | 5671386, 118.153875 s | 0 samples |
| Procession | `bells-of-death.mp3` | 4525 frames, 5673796 raw, 5671385 trimmed | 5671385, 118.153854 s | 0 samples |
| Crowd | `seek-n-slaughter.ogg` | granule 2279631 at 22050, so 4962462 | 4962462, 103.384625 s | 27 samples, 0.563 ms |
| Crowd | `seek-n-slaughter.mp3` | 3959 frames, 4964101 raw, 4962462 trimmed | 4962462, 103.384625 s | 5 samples, 0.104 ms |
| Waking | `a-hollow-call.ogg` | granule 3256616 at 22050, so 7089232 | 7089232, 147.692333 s | 0 samples |
| Waking | `a-hollow-call.mp3` | 5655 frames, 7090678 raw, 7089231 trimmed | 7089231, 147.692313 s | 0 samples |

**Every mp3 decodes short of its raw length by exactly its delay plus its padding.** All three were written by `Lavc61.19` with an `Info` tag, a delay of 576 and paddings of 1639, 930 and 753. `bells-of-death.mp3` is 2215 samples short of 5212800 at 44.1 kHz, which is 576 plus 1639 to the sample, and the other two match their own pair the same way. That is the trim, done by the decoder, and it is the whole of the answer.

**The quiet at the two Crowd wraps is the loop's own first note and not a gap.** The rendered wrap was measured off a four second loop through the same node `@pixi/sound` uses, an `AudioBufferSourceNode` with `loop` set, rendered in an `OfflineAudioContext` and read sample by sample. The quiet run spanning the wrap is 27 samples in the ogg and 5 in the mp3, and in both the decoded buffer's own leading quiet is that same count with a trailing quiet of zero: the silence is the file's first samples being under the threshold, and there is nothing of the encoder's in it. Half a millisecond is two orders of magnitude under anything a person hears as a break.

**The seam at the wrap is no louder than the music around it.** A gap is silence and a click is a step, so the step across the wrap was read against the largest step the same music makes in the fifty milliseconds either side of it. Five of the six wrap steps are smaller than their neighbourhood's largest. The sixth, `a-hollow-call.mp3` at 0.031 against 0.019, is a hair over, on the file a Chromium browser does not load, at an amplitude that is a thirtieth of full scale.

**What was not obtained, said plainly.** Only Chromium was measured: no Firefox and no WebKit is installed here, so the plan's claim that Firefox has trimmed since 83 and WebKit does the same stands on its own citations and not on anything read in this slice. The recording is the offline render of the loop's own source node rather than captured device audio, which is the same node the engine plays through but not the same as a person listening. Whether the loops sound right is still Mark's ear, verification steps 16 and 22.

**Test 129 is a sweep with a plant, and the plant was run against the real tree.** The guard walks every production `.ts` under `src`, skipping prototypes and test folders, and looks for the nine marks a header parse cannot be written without: `Xing`, `VBRI`, `LAME`, `OggS`, `ID3`, the layer III frame's 1152 samples, `encoderDelay`, `encoderPadding` and `granulePosition`. None of the nine appears anywhere in `src` today. Planting `const FRAME_SAMPLES = 1152` in `app/sound.ts` and `const XING_TAG = 'Xing'` in `game/step.ts` turned it red naming both files and both marks, and both were reverted; the second test proves the same detector against four planted lines and asserts the walk really reaches `app/sound.ts`, `main.ts` and `engine/audio/audio.ts`, because a sweep is its detector plus its file list.

**The third test is the half that would have held whichever way the measurement went.** It records what `playMusicFor` hands the channel and holds that every value is one the `LOOPS` table declares, so a loop that ever needs points declares them beside its file and a number the app worked out for itself cannot get through. Making the cue pass `{ start, end }` turned it red on the argument count, and it was reverted. The route for the data-row answer is open and was checked rather than assumed: `BGM.play` takes a `PlayOptions` and passes it to `Sound.play`, and `@pixi/sound` 6.0.1 sets `loopEnd` and `loopStart` on the source node from it (`WebAudioInstance.mjs:173-174`).

## 21. Slice 13a, the grayscale import

**What landed, and its size.** Twenty-seven PNGs under `raw-assets/standIn{m}`, **103451 bytes in all**, written by `scripts/grayscale-import.ts` out of the two gitignored packs. Twenty-two are the ground, its dressing and the Waking's own source, copied whole from `crawling-depths` as the design record's section 7 names them, and five are creatures cut out of `scarymobs` sheets. Nothing else in the two packs is in the repo, and the packs themselves still are not.

**The import table is the record of which staged art the game uses**, because the packs are gitignored and there is nowhere else that fact can live. It sits in the script as two tables with the roles written above them: the skeleton is the shambler, the ghost is the revenant and the Banshee both, the cat is the ghoul, the golem is the carrier, and the tall figure is the Undertaker. The cut rectangles were read off the sheets rather than guessed: every cell's opaque pixel count was counted before a rectangle was written, which is what says `undead.png` is five by three cells of 32 and that its last cell is empty, and that `beasts.png` is four by five of 48 by 64 with the golem on rows 3 and 4.

**Two folder tags, both load-bearing, both AssetPack's.** `standIn{m}` makes one bundle no screen declares, so the stand-ins background-load and nothing waits on them, which is the shape slice 12a already gave the music. `{fix}` on the two subfolders turns off the half-resolution mipmap: without it AssetPack writes an `@0.5x` copy of every PNG (`@assetpack/core/dist/image/mipmap.js`), and a half-size copy of pixel art is a blurred one the renderer may pick over the real thing, which is the smoothing the nearest-neighbour craft call exists to prevent. The generated `src/manifest.json` was read after the build to confirm both: a `standIn` bundle with 27 entries and no `@0.5x` anywhere in it.

**The grayscale value is `luma` and not a convention of the tool's.** `grayOf` packs the three channels into a hex, asks `src/app/color.ts` for its Rec.709 luma, and writes that back to all three. Alpha is untouched. That makes a sprite's brightness the same quantity `FIELD_LUMA_CEILING` and every other assertion in `palette.test.ts` is stated in, which is the reason the import is not a shell-out to an image tool; section 4's item 57 carries the rest of it.

**Fence 122 was proved to bite rather than assumed to.** It went red before the import existed, naming the missing folder, and green after. Then `assets-staging/scarymobs/undead.png` was copied into `sprites{fix}/` untouched and it went red again on that one file, and the copy was deleted. It refuses to pass over an empty folder for the same reason the source scan above it does: a fence whose input is a directory listing passes loudest when the directory is gone.

**The fence reads the folder and not the script's own output.** That is the point of it: a PNG somebody drops in by hand, or a restage that skipped a file, is caught the same as a bad import. It reads through `hsv` from the app's own colour module, where saturation is zero exactly when the three channels agree, so it measures what the rest of the file measures rather than comparing bytes on its own terms.

**`pngjs` is 7.0.0 and `@types/pngjs` is 6.0.5, both pinned exactly**, with the lockfile in the same commit. The plan asks for a declared decoder rather than a transitive one and the reason holds here in full: `sharp` is in the tree, but only because `@assetpack/core`'s mipmap pipe pulls it, and a fence whose only decoder arrives that way stops running the day that package changes, silently and with every test still green.

**What no test in this slice can see.** Whether the grey art reads at all once it is tinted, which is verification step 12's other half and slice 16's, and whether the three dressing sets tell the three sections apart, which is step 16 and Mark's. Nothing draws any of these textures yet: slice 13b takes the ground and the dressing, and slice 14 rules whether the two bosses want the ghost and the tall figure or the vector silhouettes section 8 calls for.

**The worktree's dependencies had to be reinstalled before anything could run, and the next slice should expect it.** Every symlink under `node_modules` and `apps/hungry-grave/node_modules` pointed into `/tmp/claude-1000/.../018b7216-.../scratchpad/deploy/node_modules`, a folder that no longer exists, so `vite`, `vitest` and `tsc` all resolved to nothing and `pnpm exec vite preview` died on a missing module rather than on anything in the tree. `CI=true pnpm install --frozen-lockfile` at the worktree root recreated them from the lockfile, which changed no tracked file and left `git status` clean. The `CI` variable is needed because pnpm refuses to recreate a modules directory with no TTY.

## 22. Slice 13b, the ground and the dressing

**What draws, and where.** `BackgroundRenderer.ts` is the view and `groundDressing.ts` is its art library, which is the split `mobSprite.ts` and `foodSprite.ts` already give this folder: what a kind draws lives beside the tables, what syncs it lives in the renderer. Everything goes into `ground` and nothing else, so `LAYER_ORDER` is untouched and ADR 0014's stack is unchanged.

**The five entries, each measured in tree before it was written down**, under this repo's own `color.ts` and never carried from the plan.

| Name | Hex | Luma | Hue | Saturation | What it is |
| --- | --- | --- | --- | --- | --- |
| `nightSpeckle` (already declared) | `0x1d2434` | 13.99 | 221.74 | 0.442 | the ground tile, every section |
| `standInGroundDressCold` | `0x303947` | 22.00 | 216.52 | 0.324 | the Procession's statues and cliff |
| `standInGroundDressWet` | `0x2d423d` | 23.99 | 165.71 | 0.318 | the Crowd's urns and veins |
| `standInVigilTint` | `0x2e545c` | 30.00 | 190.43 | 0.500 | the Vigil's rocks, eyes and tentacles: the one departure |
| `standInWaking` | `0x47766a` | 42.02 | 164.68 | 0.398 | the Waking's source, dormant and awake |
| `standInWakingDark` | `0x13221f` | 12.00 | 168.00 | 0.441 | its dark companion, drawn as a rim past its body |

**What the measurements cost and bought, since a ground is a background for everything above it.** Every mob-fire core clears APCA Lc 70.9 over the worst of them and 83.4 over the departure, so all four joined `BACKGROUNDS` in `palette.test.ts` and the band's own check now binds on the ground the game actually draws. What it costs is real and is written down rather than discovered later: a corpse over the Procession's statues reads Lc 42.4 against 47.0 over bare ground, and a corpse over the Waking's own body reads 22.8, which is the price of a source bright enough to be the loudest beat in the run at Lc 23.45 over the tile it drifts down. The four ground colours are in `NOT_SPRITES` with `night` and `nightSpeckle`, because they are what those two were, and the Waking's pair is held instead by a check of its own, exactly as the field's boundary is.

**The drift is a function of the tick and nothing else.** A placement's index is `tick / DRESSING_INTERVAL_TICKS` on a global grid, its x and its piece are two draws off that index, and its y is how far it has fallen; which family it wears is the phase's set if it was placed inside this phase and the previous phase's set otherwise. So the renderer holds no per-run memory at all, a replay rendering a pinned tape at a chosen tick draws the ground the run drew, and nothing joins the held-transient registry. The drift window falls out of the arithmetic rather than being enforced: past `DRIFT_WINDOW_TICKS` every visible placement is younger than `phaseTick` anyway.

**The rows, all initial and all marked stand-in.** The ground runs at half the field's scroll (`GROUND_PARALLAX` 0.5), a placement every five seconds, twelve slots, and `DRESSING_SCALE` 1.875 field units per texture pixel. The scale is a row rather than a derivation on purpose, so the two ends it was chosen to meet are assertions: the Crowd's 16-pixel eye cell draws at 30 units, and the Waking's source draws at three times that, which is the 90 its own hitbox is across. Both are held by tests, and both were proved to bite by moving the scale and the multiple; written as a derivation, the source's size agreed with itself whatever the multiple said.

**Nearest-neighbour is set per texture as the renderer takes it**, never as a global default, so the create-pixi UI art is untouched. A test drives a run and reads the scale mode off every texture the ground asked for.

**What the five rendered shots showed, each read.** All off one tape recorded under the maxed dodge bot at seed 101, the same run slice 11 measured, rendered through `#/replay?tape=...&at=`.

- **The Procession at tick 4303.** Tiled rock, four statues and a cliff piece drifting down in the cold blue-grey, mob fire and the grave's rim clearly above them. The tile lattice is regular and reads as a stone floor.
- **The Crowd at tick 12398.** Vein columns and urns in the green-teal, plainly a different place from the Procession, with the section's own overlap on top of it.
- **The Vigil at tick 20626.** Floating rocks in the teal departure and tentacles as dark curling shapes. The departure reads as a colour that was not there before.
- **The drift at tick 10006, a thousand ticks after the Banshee died.** Green-teal urns and a vein column at the top, blue-grey cliff and a statue below them, both families on screen at once with the incoming above the outgoing. That is Einhänder's answer with no card, no fade and no cut, and it is the one shot that shows the mechanism rather than a result.
- **The Waking at tick 17206.** The source open mid-field in the Crowd's own family with the pour streaming out below it, its body clearly the brightest thing on the ground layer.

**What no test in this slice can see, said plainly.** Whether the three sections are tellable (verification step 16), whether the dormant source reads apart from the Crowd's eye dressing (step 21), and whether the tile lattice is scenery or noise. Driving the app through a CLI browser is not a substitute for playing it: every read above is a still frame taken seconds apart, and what a person watching would see of the drift is motion.

**The tests were proved to bite rather than assumed to.** Eleven mutations, each reverted. Dropping the drift so a placement always wears the current set turned module 106 red. Giving the Waking the Vigil's teal turned the two-changes test red. Running the ground at the field's own scroll, never setting the scale mode, drawing into a layer that is not `ground`, writing a colour literal into the renderer, counting frames instead of reading a placement's index, and moving the departure's hue out of the empty band each turned exactly the test written for it red. Two mutations turned nothing red the first time and both were the same defect, an assertion built from the constant it was checking: the parallax read against the renderer's own `GROUND_SPEED` and the source's size against the scale it is derived from. Both were rewritten against an independent source, the sim's own `SCROLL_SPEED` and a dressing eye the renderer actually drew, and both bite now.

## 23. Ground adjustment 1

Three notes Mark took off the slice 13b deploy on his phone, 2026-09-08: the territory splotch moved at a different rate than the ground under it, the tiles were placed at random and spun, and the dressing was too sparse. All three land on the render side and nothing under `src/game` is in the commit.

**The ground runs at the field's own scroll, and the parallax row is gone rather than set to one.** The ruling and what it supersedes are in section 4, items 65 to 67. What is worth adding here is the test's shape: the promise is that a landed patch and the rock under it move at the same rate, and the only honest source of truth for the patch's rate is the sim, so the test lays a patch on a real run, drifts it through `advanceTerritory` for 120 ticks, and holds the renderer's `tilePosition.y` over the same 120 ticks against what the patch actually fell. It names neither `SCROLL_SPEED` nor `GROUND_SPEED`, which is what slice 13b's own note asks for after two assertions there were built from the constants they were checking.

**The floor is baked, and the layout is authored because the pack has nothing to assemble.** The measurements are in section 4 item 67. Two mechanisms were prototyped and rendered before one was chosen, both drawn the way the game draws them, after the import's percentile stretch, tiled at `DRESSING_SCALE` and tinted `nightSpeckle` over a 540 by 760 field. The borderless fill laid as the whole floor was rejected: its own motif repeats every 32 by 64 and reads as rows of glyphs. The fill as a bed with stone slabs set into it was rejected too, and for a reason worth writing down: the fill's greys are 7 to 14 where the blocks' rock is 33, so the fill is the dark rock those blocks are set into and the two are two value bands rather than one. Stones on it read as bright patches floating on black, which is the opposite of one floor. What was chosen is the blocks themselves with their grooves dissolved everywhere except at slab edges, which keeps the whole floor in one value band and lets the grooves do what grooves do.

**Which five cells the floor is laid from was read rather than picked.** A contact sheet drew each of the twenty cells stripped and tiled three by three, and only the five in the sheet's first column plus the cell at (2, 1) tile into continuous rock lit from one direction. The other fifteen show their own 16-pixel rhythm at three by three, which is the "spun" read.

**The floor is brighter than the sheet was, measured rather than discovered later.** The import's stretch takes its top from the 98th percentile of a file's own opaque greys, and the calm cells carry fewer speculars than the whole sheet did, so the top lands at 55 where it landed at 72 and the factor is 4.64 against 3.54. The floor's mean grey is 142.7 of 255 where the tiled sheet's was 104.5, so the ground draws about a third brighter under the same `nightSpeckle`. That is the stretch's own rule doing what it was written to do on a different body of art, not a defect, and it is written here because a ground is the background for everything above it and slice 16's grayscale check at target density is where it will be judged.

**The density is a row and the interval falls out of it.** `DRESSING_ON_SCREEN` is 30 placements in flight, initial and marked stand-in, and `DRESSING_INTERVAL_TICKS` is the drift window divided by it. That order is the whole point of the row: the crossing halved when the ground took the field's own scroll, and the old five-second interval held fixed would have halved the dressing at the same time Mark was asking for more of it. What draws runs a few under the row, because the window is the tallest piece's crossing and a short piece leaves the bottom edge earlier; the test holds both the derivation and the count, and the count is Mark's own twenty-five to thirty rather than anything derived.

**The three sets grew with the row, in their own families and colours.** The Procession takes `Statue C1/C2` and `Book Altar` and stands at eight, the Crowd takes one still from each of the `Vein A` and `Vein B` sheets and stands at nine, and the Vigil takes `Amalgam Arc 1/2` and stands at eight. The `Vein A2` and `Vein B2` sheets were staged and then dropped: measured frame by frame they are the same two veins one animation frame along, so a still cut from either is the still already cut from A1 and B1. The five palette entries did not move and no colour changed.

**What the rendered check showed, each read.** All off one tape recorded under the maxed dodge bot at seed 101 for 22000 ticks, rendered through `#/replay?tape=...&at=`. Its spans: Procession 0 to 7284, Banshee to 8526, Crowd to 16927, Waking to 17827, Vigil open at 22000.

- **Does the patch stay on its rock? Yes, and it was measured rather than eyeballed.** Two frames in the Procession, ticks 6198 and 6485. The ground's own shift between them was found by matching the two frames over every offset: 202 pixels, which is what 287 ticks at the field's scroll comes to on this viewport. Frame A shifted down by that 202 and laid over frame B in two colour channels leaves the ground, the dressing and **the landed patch's whole ring** grey, meaning the two frames agree on all of them; the mobs, the shots and the grave come out red and cyan, because they moved on their own. A patch that slid against its rock would have shown as two rings.
- **Does the floor read as one floor? Yes.** In all three section shots the ground reads as a paving of large slabs with grooves only where two slabs meet, at a stone size a grave could stand on. The 16-pixel lattice that ran across the whole field in the 13b shots is gone, and so is the double groove where two blocks abutted.
- **Does the dressing read as thick? Yes.** The Procession carries statues, altars and cliff pieces on every part of the screen rather than the four statues and one cliff the 13b shot showed, and the Crowd and the Vigil are as full. If anything the honest risk is now the other way, that the ground competes for attention, and that is a look rather than a test.

**Driving the app through a CLI browser is not a substitute for playing it.** Every read above is a still frame or a pair of them, and the one thing these notes came from is Mark watching the ground move on a phone. Three reads were not obtained: whether the drift between two families still reads with the window halved, whether the thicker dressing crowds the field in motion, and whether the brighter floor costs anything a player notices.

**The tests were proved to bite rather than assumed to.** Three mutations, each reverted. Putting `GROUND_SPEED` back to half the scroll turned the patch test red. Pointing the floor at the pack's own `tiles.png` alias turned the baked-floor test red. Halving `DRESSING_ON_SCREEN` turned the density test red and nothing else.

**One thing left behind by an earlier slice, named rather than touched.** `apps/hungry-grave/local/slice13b-record.ts` is still there. This adjustment wrote its own recorder beside it and deleted it; 13b's is not mine to remove, and `local/` is gitignored so neither ever reached a commit.

## 24. Slice 14, the boss renderers and boss fire's own read

**What draws, and where.** `bossSprite.ts` is the art and `BossRenderer.ts` is the sync, which is section 4's item 71. Everything goes into `mobBodies` and nothing else, so `LAYER_ORDER` is untouched and ADR 0014's stack is unchanged. One sprite and never a pool, for the same reason the sim holds one record: there is exactly one boss at a time, ever. It attaches after the mob pool, so a boss draws over the adds it summons.

**The two bosses are vector silhouettes and the record's disagreement was never between the two records** (section 4, item 70). Each is a point table in units of its own half extents, so the drawn body **is** the hitbox by construction: moving `BOSS_HALF_WIDTH` moves the drawing with it, and a test reads the polygon off the drawing rather than off the table. The Banshee is a dome over a ragged hem, the Undertaker a brimmed hat over a coat with a shovel across it; every edge of his is straight where every edge of hers curves, which is ADR 0014's first discriminator spent on the thing it is for.

**The pair is the body and the detail, and the near-black companion is `foodOutline`.** The design record asks for "the palette entries that already exist for them ... by the same construction the three mob types use: a body plus a near-black companion", and those are two different colours: `SPRITE_OUTLINE` names `foodOutline` for both bosses, and `bansheeDark` and `undertakerDark` are declared in `palette.test.ts`'s `DARK_HALVES` as each boss's own dark half. So the body fills in the boss's colour, the eyes and mouth and shovel fill in its dark half, and the rim strokes in `foodOutline`. No palette entry moved and none was measured again.

**The chunk flash inverts the pair rather than dimming it.** On the inverted beat the body takes the dark half and the detail **and the rim** take the bright one, so an invincible boss never darkens into the night it stands against: a dark body with a near-black rim would be a boss that disappears for a third of every chunk break. It runs off `boss.flash` and never off `run.tick`, so a replay rendering a pinned tape draws the fight the run drew. Its period is a safety floor before it is a feel number and the number that shipped first was wrong: section 3 has the whole of it.

**The rim joins round rather than mitered, and that is a measurement rather than a preference.** The shroud's hem meets at acute angles, and a mitered join there threw a spike four times the rim's own width past the body: the drawn bounds came to 136.8 units against a 120-unit hitbox. Round brings it to exactly the hitbox plus the rim, and the test holds that equality so a spike cannot come back.

**`mobFireSprite` reads the shot's own kind at both sites, and a third reader had to move with them** (section 4, items 68 and 69). Between slice 6 and here, every boss shot in the game drew in the trash body colour, which slices 6, 7 and 8 each stated rather than left to be found.

**What the rendered check showed, each read, and how each frame was pinned.** Playback stops at the last verified checkpoint, and the shipped spacing is 60 ticks (`RECORDER_CHECKPOINT_SPACING`), so a thirty-tick flash falls between two of them and a full tape simply plays past whatever tick was asked for. Each shot therefore came off its own tape, recorded to end on the tick the shot wanted, and the two flash tapes were recorded with a checkpoint every tick. The spans of the full run, seed 101 under the maxed dodge bot: Procession 0 to 7284, Banshee to 8526, Crowd to 16927, Waking to 17827, Vigil to 22022, Undertaker to 25861, victory at 25862.

Every read below was **counted rather than eyeballed**: a throwaway `pngjs` script counted, over the whole frame, the pixels matching each declared hex. The counts are what the sentences say.

- **The Banshee mid-chunk-one, tick 7560.** She reads at a glance: a pale mint shroud with a ragged hem, two dark eye hollows and the dark oval of the wail, high in the field with her tear-ring expanding around her and one clean gap in it. 11963 pixels of `banshee` and 1840 of `bansheeDark`. **1488 pixels of `fireTear` and not one of `fireTrash`.**
- **The Banshee mid-flash, tick 7756.** The inversion is unmistakable beside the frame above: 13180 pixels of `bansheeDark` as the body and 5862 of `banshee` as the rim, the eyes and the mouth, and `foodOutline` down from 3518 to 756. She goes from pale-with-dark-eyes to dark-with-pale-eyes, and she is never hard to find.
- **The Banshee in chunk two, tick 8280.** Two ring sources offset either side of her with their openings pointing different ways, which is what the chunk is for. 3714 pixels of `fireTear`, still no trash.
- **The Undertaker in chunk one, tick 22560.** The burial: two clod curtains falling across the field, each with a way through, over the Vigil's teal. 13066 pixels of `undertaker`, 1366 of `undertakerDark` for the eye slits and the shovel, **2479 of `fireClod` and none of any other kind.**
- **The Undertaker mid-flash, tick 23032.** 13185 pixels of `undertakerDark` as the body against 4857 of `undertaker`. **This is the weakest of the eight reads and it is a finding rather than a colour to change:** his dark half is luma 16.56 and the Vigil's ground now carries a lit floor and a teal dressing, so on the inverted beat only the bright rim, the eye slits and the shovel hold his shape. It is legible and it is a twelve-tick beat, and the palette entries are ADR 0014's and do not move in this slice. **Trigger: the art pass at #38, or the first time Mark says a boss vanished mid-fight.**
- **The Undertaker in chunk two, tick 23700.** The exhumation: the arm's shots leaving him along one line down and to the left, green diggers coming up under it. **1002 pixels of `fireSpiral` and zero clods**, which is the one-shape-at-a-time rule visible in colour.
- **The Undertaker in chunk three, tick 25500.** The locked overlap: **1682 pixels of `fireClod` and 952 of `fireSpiral` in the same frame**, a curtain across the field with its way through on the left and the arm throwing at the same time. The twist is the only place in the game where two fire kinds are on screen at once, and the count is what says so.
- **The Procession, tick 4260, as the control.** 93 pixels of `fireTrash` and not one pixel of any boss or boss-fire colour. The field a player already knows is unchanged.

**Does a boss read over the thicker ground?** Yes, both, in every frame. The ground adjustment's floor is a paving of large slabs and its dressing runs about thirty pieces in flight, and neither boss is lost in it: the Banshee at luma 67.32 is the brightest thing on her screen, and the Undertaker at 41.39 sits well above the Vigil's `standInVigilTint` at 30.00 and reads as a solid body against it. The one caveat is the flash frame above.

**What no test in this slice can see, said plainly.** Whether chunk three's twist reads as the gap following the arm (verification step 17) is the one this slice is most waiting on: a still frame shows the gap and the arm together and cannot show one tracking the other. Whether the Undertaker's silhouette reads as a gravedigger rather than as a wide-hatted shape is a look. **Driving the app through a CLI browser is not a substitute for playing it**: every read above is a still frame, and the flash, the ring's walk and the arm's sweep are all motion. Three reads were not obtained: whether the flash reads as invincibility rather than as damage, whether the two silhouettes are told apart in the second a player has, and whether the four fire kinds are told apart at a shot's real size, which is the honest risk in the colour work here because the kinds differ only in the body ring between the near-black rim and the near-white core.

**The tests were proved to bite rather than assumed to.** Thirteen mutations, each reverted, and every one turned at least one of this slice's tests red: the shot drawing `MOB_FIRE.trash` again and the scatter doing the same; the shot look dropping the kind; the cancel forgetting the kind; the boss detail taking the body colour; the flash never inverting; the boss drawing into `mobFire`; both bosses taking one silhouette; the silhouette scaling by a fixed number instead of the half extents; the rim going back to a miter join; the boss sprite arriving visible; the flash period back to five, which is the guard added after CodeRabbit; and a fifth member added to `FireEmitter` alone, which fails typecheck at the tuple guard and passed under the distributive one.

**One anomaly, preserved rather than absorbed.** `docs/push/handoff.md` and `docs/push/path-draft.md` were modified in this worktree while the slice ran, by the dispatching session and not by this one: the handoff records Mark's ruling on the Waking's drift and both carry ticket #102, the phone browser's swipe-back gesture. Neither is staged or committed here, and CodeRabbit found the handoff because it reads the whole uncommitted tree (section 3). It is the third slice running to meet this; it is not a stray edit of the slice's.

**One thing left behind, named rather than touched.** `apps/hungry-grave/local/slice13b-record.ts` is still there. This slice wrote its own recorder beside it and deleted it, the same as the ground adjustment did; `local/` and `dist/` are both gitignored, so neither the recorder nor the eight tapes it wrote ever reached a commit.

## 25. Adjustment iteration 2, the Waking rides the ground

Mark's ruling of 2026-09-08, taken after ground adjustment 1 put the ground at the field's own scroll and left the Waking's source drifting at half of it: the source rides the ground at the field's own scroll too, its on-screen time shortens, and the design gate reviews the commit. Unlike adjustment 1 this one is all sim: three rows in `rows.ts` and two behaviour sites, in `setPiece.ts` and `stormTargets.ts`.

**The share is gone rather than set to one, and two rows moved with it.** `SET_PIECE_DRIFT_SHARE` is deleted the way adjustment 1 deleted `GROUND_PARALLAX`: the drift in `setPiece.ts` and the velocity the storm's seam reads in `stormTargets.ts` are both `SCROLL_SPEED` itself now, with the reason written at the drift, that the source is a place on the ground and the ground moves at the scroll. At the field's own scroll the source crosses the field in twenty seconds instead of forty, so **`SET_PIECE_OPEN_DEPTH` moved 0.5 to 0.25** and **`SET_PIECE_PLACED_AT` moved 120 to 135**. The depth is what keeps the pour's fifteen seconds inside the field: it opens at y 190, spends its last body at y 760, and the `scrolled` close does not fire until y 790, so thirty units of fall are left, which is forty-seven ticks against the pour's own twelve-tick interval. The placement is the opening second minus the fall, five seconds at 0.25, authored rather than computed because `rows.ts` value-imports nothing.

**What is different for the player.** The dormant eye is on screen about five seconds before it opens instead of twenty, and about twenty seconds in all instead of thirty-five: one mob's crossing, no longer "several times longer than a mob". Mark chose that cost. The sweep is a function of depth, so the open source now crosses three quarters of its authored x range while it pours where it crossed under two fifths, and the trail is a wider, faster diagonal.

**Measured on the seed 101 tape, under the maxed dodge bot.**

| | before | after |
| --- | --- | --- |
| placed | tick 15726 | tick 16626 |
| opened | tick 16927 | tick 16927 |
| closed, `spent` | tick 17827 | tick 17827 |
| dormant | 1200 ticks, 20.0 s | 300 ticks, 5.0 s |
| on screen | 2100 ticks, 35.0 s | 1200 ticks, 20.0 s |

**Nothing before the placement moved, and that is a checksum rather than a claim.** `foldWitness` over the whole run state at tick 15726, the last tick on which both trees agree there is no source at all, is `-475416322` in both. It was measured by building a detached worktree at the branch tip with `node_modules` symlinked and running one instrument in each tree, which is verification step 5's own recipe used for a different question. The boundaries up to and including the Waking are identical either side: Banshee 7284, Crowd 8526, Waking 16927, Vigil 17827.

**The run's clock after the Waking is not unchanged, and the prompt's expectation that it would be is wrong.** The Undertaker now arrives at 22703 where it arrived at 22022, and victory lands at 26691 where it landed at 25861: 830 ticks later, thirteen seconds. Nothing in the Vigil's own rows moved. What moved is the run the bot plays: a different trail is a different set of corpses in different places, so the hand dodges differently from tick 17827 onward and the Vigil's `rowsSpentAndFieldClear` end arrives later. The section lengths that are stage data are unchanged; the ones that are play are not, and the Vigil's is play.

**The property holds and is slightly stronger, re-measured rather than assumed.** Over the six seeds `PROPERTY_SEEDS` names, the committing hand swallowed **63 corpses to the waiting hand's 25**, where it swallowed **60 to 25** before: a ratio of 2.52 against 2.40. No seed went the other way either side, so `WAITING_EATS_MORE` stays empty and `COMMITTING_PAYS_OVER` is untouched. Section 17's figure of 132 to 58 was read over twelve seeds whose identities that note does not record, so it cannot be reproduced; what is above is the six the test itself holds, read before and after with one instrument.

**Density was counted, not assumed, and nothing moved near a cap.** Inside the source's own span on the seed 101 tape: 75 poured events before and after, every one of them reaching the field; the most mobs alive at once fell from 49 to 41 against `MOB_CAP` 160, so no mob-cap refusal was possible either side; the most corpses alive at once rose from 27 to 40 against `CORPSE_CAP` 233; and `refusals.food` summed over the span is **zero before and zero after**. The corpse count rising by half is the same trail arriving into a shorter window, which is what the shorter stay means, and it is still a sixth of the cap. No cap moved.

**One defect the change exposed, fixed at the cause rather than patched.** `pourBody` clamped each body to the sweep's bounds after adding the lip offset. With the pour now running from y 190 to y 760 it reaches the sweep's far end, where the clamp folded the outer lip back onto the edge and put two consecutive bodies 19.5 units apart against a widest trash body of 26. The no-stack test, `never lands two bodies on the same point`, turned red for that reason and not for the ruling's, which is what said so. The clamp now holds the **mouth's centre** a lip's full reach inside the bounds and the two lips are placed off that centre unclamped, so every body still lands inside the authored bounds and the gap the alternating lips exist to keep cannot be closed. `POUR_REACH` is `POUR_LIP_X + POUR_JITTER_X` and is derived rather than authored. The defect was latent before this slice: the old pour ran from y 380 to y 665 and never reached the far end.

**GOLDEN did not move and could not.** The prompt says it does. `GOLDEN`'s scenario is 600 ticks from `SEED` 20260820 (`digest.ts:18-19`), inside the Procession, and the source is placed two sections and a fight past it. Nothing this slice changed is inside that window, `src/dev/digest.ts` is not in the commit, and the digest test is green. The fields the prompt names, the source's own positions, the poured bodies' positions and the Waking's own reading, are the seed 101 tape's rather than the digest's, and they are measured above.

**What the rendered check showed, each read.** Off three tapes recorded under the maxed dodge bot at seed 101 and rendered through `#/replay?tape=...&at=`, each recorded to end on the tick its shot wanted so playback stops there, which is slice 14's own recipe. The dormant window is only 300 ticks now, so the widest pair inside it that clears the readout overlay is 120 ticks apart, at ticks 16800 and 16920.

- **Does the eye stay in its rock? In the axis the ground moves, exactly, and it was measured rather than eyeballed.** Matching the two frames over every vertical offset puts the ground's own shift at **137 pixels**, which is what 120 ticks at the field's scroll comes to on this viewport. Matching the eye's own box over every offset in both axes puts the eye at **dy 137 and dx 58**: it fell exactly as far as the rock did, to the pixel. The 58 pixels sideways are the authored sweep, 32.3 field units in two seconds against the 32.4 the sweep's own bounds predict, and the ground does not move sideways at all. **So a plain two-channel overlay of the pair shows two eyes side by side, and the prompt's criterion, that the eye come out grey with the rock, cannot be met by any build that keeps the sweep decision 25 asks for ("the source drags across").** The question that criterion is for is whether the eye slides against its rock in the axis the rock moves, and the answer is no, in the overlay and in the module test both. Everything else in the overlay is grey: the floor, its grooves, the dressing.
- **Does the pour still read as a trail across the field? Yes, and more diagonally than before.** At tick 17400, mid-pour, the open source stands about two thirds down with its bodies streaming out behind it in one clean diagonal to the lower left and the corpses trailing under them. It reads as a curve a dive can follow rather than a column or a wall.
- **Driving the app through a CLI browser is not a substitute for playing it.** Every read above is a still frame or a pair of them. Three reads were not obtained: whether twenty seconds of eye reads as a moment rather than as a mob, whether the faster diagonal is still a trail a hand wants to swim up, and whether the eye reads as sitting in the ground while both are moving.

**Findings against the tree, beyond those already named.**

- **The prompt says the property is held "in `bot.test.ts` against `COMMITTING_PAYS_OVER`", and it is in `setPiece.test.ts`** (`setPiece.test.ts:575` and `:600`). `bot.test.ts` holds the dodge policy's own seeds and names neither constant.
- **The prompt's parenthetical that thirty units of margin is "under one pour interval" is the wrong way round.** Thirty units is forty-seven ticks of fall and a pour interval is twelve, so the margin is nearly four intervals. The rule the same sentence states, that the margin be at least one pour interval, is what the new test holds.
- **ADR 0050 names neither the slower parallax nor the mid-field opening**, which the prompt suspected and which was settled by reading the whole file: both phrases are decision 25's (`decisions.md:103`). No ADR moved. Two comments cited ADR 0050 for "it opens around mid-field", in `rows.ts` and in `setPiece.ts`, and both are rewritten, because the sentence they quoted is the one this slice supersedes and it was never in the ADR they credited.
- **The storm's seam reads the source's `vx` as zero while the sweep carries it sideways** (`stormTargets.ts:193`). It was already so before this slice and the slice doubles the rate, so a line leading a travelling target now under-leads the source by twice as much laterally as it did. It is not fixed here: `vx` is the seam's own contract and what a target moving on a curve should report is a decision rather than a retune. **Trigger: the first weapon line that leads its target in x, or the tuning pass at step 4.**

**Records amended in place, dated, none deleted.** `stage-floor.md` in all three places the prompt names (section 1's placement paragraph, section 2's identity paragraph, section 7's background-art sentence), `game-concept.md:50`, and `CONTEXT.md`'s Waking entry. Each amendment names what stood (the pour, its budget and rate, the opening tick, the property), what changed (the source rides the ground at the field's scroll, it opens a quarter down, it is on screen about one crossing), and what the record could not have known (the ground was at half scroll when it was written, so "rides the ground" and "slower parallax" were one sentence until adjustment 1 split them). `docs/push/decisions.md` is frozen and was not touched.

**The tests were proved to bite rather than assumed to.** Five mutations, each reverted.

- The drift back to half the scroll turned both new tests red, the fall test and the storm seam's coupling test.
- The seam's velocity back to half the scroll, with the drift left alone, turned **only** the coupling test red, which is the mutation that matters: the coupling test exists so the two sites cannot part.
- The opening depth back to 0.5 turned five red, the margin test among them, and the close reason on that run is `scrolled`.
- The placing row back to 120 turned three in `stage.test.ts` red, `ends the Crowd on the eye opening and never on an empty field` among them, so the row was already guarded before this slice and no new test was needed for it.
- The pour's clamp back to clamping the body rather than the centre turned the no-stack test red and nothing else.

**One thing left behind, named rather than touched.** `apps/hungry-grave/local/slice13b-record.ts` is still there, and it is what recorded this adjustment's three tapes rather than a fourth recorder being written beside it. The two measuring instruments this adjustment did write, `local/adj2-measure.ts` and `local/adj2-property.ts`, are deleted, and so are the tapes under `dist/`; `local/` and `dist/` are both gitignored, so none of them reached a commit.

## 26. Adjustment iteration 3, a poured body arrives before it can touch

The apply-now finding of the game design gate that reviewed adjustment iteration 2 (marker on #97, 2026-09-08). Since adjustment 2 the pour runs to the bottom edge, so its bodies materialise inside the box of a grave parked there, and a body that materialised inside the box could touch on the tick it existed.

**The shape, and there is no new number in it.** `Mob` gains one boolean, `appearedInside`, written once in `spawnMob` from `hasEntered(mob)`, which is the same predicate that starts the beat, and never written again. `canTouchGrave(mob)` in `mobs.ts` is the rule, guard first: a body that did not appear inside touches as it always has, and one that did touches when its beat is zero. `resolveMobContact` in `step.ts` reads it and owns nothing of the rule. The window is `ARRIVE_TICKS`, the clock the beat already keeps, so nothing is tuned and nothing is reconciled.

**The slice's own definition was wrong, and the record already said so.** The dispatched definition was "a grave parked at the bottom edge takes no contact hit from a poured body", and no implementation of the ruling can meet it. A body falls 42.75 units during its 45-tick beat, at its own half of the scroll plus the field's scroll; the grave's box at `SIZE_CEILING` is 135 units tall and at `SIZE_FLOOR` 36; so a body that materialises anywhere but the bottom 42.75 units of the box is still in the box when its beat runs out, and it touches then, with three quarters of a second of warning. Section 2 of `stage-floor.md` had ruled this from the start: a grave parked under the mouth "sits in the densest incoming traffic it will meet all run, taking contact shrink from bodies". Zero was never the promise. The session ruled the promise on the measurements below: **no contact hit lands from a body that has not finished arriving.**

**The run-seam spec test is not written, and the reason is not its cost.** `graveHit` names its source as `contact` and never the body, so a run-seam test can only attribute a hit by rebuilding the overlap pass out of `graveHitbox`, `mobHitbox` and `overlaps` and then asking `canTouchGrave` whether the pass was right, which is the implementation-coupled tautology the playbook forbids in as many words. It is also racy, measured rather than feared: on seed 20260910 with the grave parked under the mouth, the first contact hit of the run landed at tick 706 and nothing at all overlapped the grave when the tick ended, because `resolveDeaths` runs after `resolveOverlaps` and the storm took the body that touched. So the ruling is carried by the two module tests, both of which step whole ticks through the one execution authority (ADR 0017) on a body placed where the pour places one. The run-seam numbers are recorded here instead.

**Measured, before and after, with one instrument run in both trees.** The before tree is a detached worktree at `7b00c15551` with `node_modules` symlinked, which is verification step 5's own recipe used for a different question.

| Reading | before | after |
| --- | --- | --- |
| Contact hits, grave parked at the bottom edge under the mouth, `SIZE_CEILING`, six `PROPERTY_SEEDS` seeds | 36 | 31 |
| The same at `SIZE_FLOOR` | 30 | 29 |
| The same at a fixed x of 270, `SIZE_CEILING` | 0 | 0 |
| The same at a fixed x of 404, `SIZE_CEILING` | 26 | 17 |
| The property, corpses to the committing hand against the waiting one | 63 to 25 | 63 to 25 |
| Seed 101 under the maxed dodge bot, hits inside the Waking's span | one shambler shot, no contact | one shambler shot, no contact |
| Seed 101, hits in the whole run | 2 shambler, 4 revenant, 8 undertaker | 2 shambler, 4 revenant, 8 undertaker |
| Seed 101, phase boundaries and ending | 7284, 8526, 16927, 17827, 22703, 26691, victory | identical |

**What that says.** The residual hits are the whole point: they are hits with three quarters of a second to read, which is what the gate asked for, and what is gone is the hit on the tick a body appeared. In play the change costs nothing at all: the seed 101 run under the maxed dodge bot is identical either side, boundary for boundary and hit for hit, because that hand never parks in the trail. The property is untouched at 63 to 25, the same figures section 25 measured, and `WAITING_EATS_MORE` stays empty.

**The new field is excluded from the fold, and `WITNESS_VERSION` stays 6.** ADR 0019's rule is that the fold covers everything the rules mutate, and this field is written once at the spawn from the y the placement asked for and never mutated, exactly as `mobs[].type` and `patches[].level` are. It is in the excluded half of the partition with that reason beside it. Folding it would have moved the version, which the prompt hands forward as not moving.

**GOLDEN did not move and could not.** Its scenario is 600 ticks from seed 20260820 inside the Procession, where the only bodies placed inside the field are the digest's own scripted ones: the shambler at the grave is killed on the tick it is placed, and the ghoul placed at y 20 reaches the grave hundreds of ticks after its beat has run. `src/dev/digest.ts` is not in the commit and `digest.test.ts` is green.

**No rendered check is owed, and this says so rather than skipping it silently.** Nothing draws differently. The beat is already drawn as the placement it is, and the change moves no pixel: it only decides whether an overlap that is already on screen becomes a hit.

**CodeRabbit: one applied, one declined.**

- **Applied, major, `stage-floor.md`.** The "Budget and end condition" paragraph still said the source drifts "at half the scroll", which adjustment iteration 2 left stale when it amended three other places in the same file. It is amended in place, dated, and the margin behind its conclusion is written down: the fall from the opening depth to the close is 947 ticks against the pour's 900.
- **Declined, major, `witness.test.ts`.** The reviewer asked for `mobs[].appearedInside` to move from the excluded half of the partition to the folded half. ADR 0019's criterion is what the rules mutate, not what matters: this field is written once at the spawn and never mutated, so it sits beside `mobs[].type`, and any divergence in it is a divergence in the spawn's own y, which the fold already carries. Folding it would move `WITNESS_VERSION`, which this slice is handed forward as not moving.

**Plan claims found false against the tree.**

- **The pour is not the one spawner that puts a body inside the field.** `digUpBody` in `bosses/undertaker.ts` places a dug-up body at `boss.y`, and its own JSDoc says why: "at his own depth so it arrives on the field rather than above it". The ruling is written as the general fact rather than as the pour's own, so it covers the Undertaker's adds deliberately, and a digger that materialises on the grave now waits out its beat too. Nothing in the seed 101 fight moved, so it cost that fight nothing.
- **The prompt's "takes no contact hit" reading of the record was wrong**, and the paragraph it read past is in the same section: "Parking under the mouth" already rules that a grave under the source takes contact shrink from bodies as the price of standing in the densest traffic in the run.
- **Every template places above the top edge**, which the prompt assumed and which is verified: every `y` in `templates.ts` is `-ENTRY_DEPTH` or deeper. So the two inside spawners are the pour and the diggers, and no authored row is affected.
- **ADR 0037 is not changed and is not cited here.** It rules that contact never kills a mob and that live mobs are never food, and this slice touches neither: the body is not consumed either way, and what changed is only when a body can hurt the grave. It was tempting to cite it because `resolveMobContact`'s JSDoc already does.

**Seams that moved.** `Mob` gains `appearedInside`; `mobs.ts` exports `canTouchGrave`. `resolveMobContact` keeps its signature. One existing test needed its planted body taken past its beat: the presence half of `never touches the grave, whatever is parked under it` spawns a body on the grave and expects a hit on the next tick, which is the same repair `mobOnGrave` in `step.test.ts` has always carried with the same comment.

**The tests were proved to bite rather than assumed to.** Four mutations, each reverted.

- The contact guard removed from `resolveMobContact` turned the holding test red, which is how it was written: it was red before the rule existed and green after.
- `appearedInside` written as `true` for every spawn turned **only** the top-edge test red, which is the mutation that matters: that test is the fence under the glossary's "movement only" for a body that crosses the edge.
- `canTouchGrave` returning `mob.beat < ARRIVE_TICKS`, so a body touches after one tick of its beat rather than after all of it, turned the holding test red.
- The excluded entry dropped from the witness partition turned `every nested field is either folded or excluded with a reason beside it` red, naming the field.

**Verification steps run.** 1, unit tests, `pnpm vitest run`, 1651 passed beside the one pre-existing timeout below. 2, `pnpm typecheck` green. 3, `pnpm build` green. 4, `pnpm verify` at the repo root, green on a quiet machine, all 120 files and 1651 tests. 5, the test-name diff against `local/step2/tests-baseline.txt`: 27 names gone, every one of them an earlier slice's and already accounted for in that slice's note, none from a file this slice touched, and the two names added are this slice's own. 6, the golden digest, unmoved. Step 11's rendered check is not owed and the paragraph above says why. Steps 16 to 22 are Mark's and stay open.

**Three verify runs, and what separates them is the machine and nothing else.** The first, before this note was written, was green: 120 files, 1651 tests. The second failed seven tests and the third one, and every single failure in both was `Test timed out in 5000ms`, with not one assertion among them. What was happening beside them is in `ps`: a Blender render out of the `cabinet-app` worktree taking nine cores, which put the load average at 12 to 14 where the green run had it near 3. The seven were spread over four files that have nothing in common but their length. The one that survives on a settling machine is `pays a full build to a run that kills every carrier before the set piece` in `rows.test.ts`, at 6150 ms against the 5000 ms limit; run alone it passes twice in 2.3 seconds. That is exactly the pre-existing failure the prompt names as slice 16's, and this slice adds nothing to it. `docs/agents/lessons.md` carries the rule this is an instance of: contention is not flakiness, and a red gate arriving beside running work is contention until a run on its own says otherwise.

**One thing the gate cannot see, so it is written here.** `apps/hungry-grave/docs/` is on the prettier ignore list, with the reason beside it: the design record is hand-shaped prose and prettier's table padding makes it worse to edit. Running `prettier --write` on a named path under it still rewrites the file, ignore list or not, and it repadded every row of section 1's table. The churn was reverted rather than committed. **Nothing under `docs/` is ever handed to prettier by name.**

**Instruments deleted.** `local/adj3-probe.ts` and `local/adj3-play.ts` are gone, and so is the detached before-tree; `local/` is excluded from version control, so neither reached a commit. `local/slice13b-record.ts` is still there, untouched, as sections 24 and 25 record.

## 27. Slices 15 and 16, the fences and the verification pass

The step's closing pass. It writes no production feature: it confirms the fences, fills one stale todo, fixes at the cause the one timeout the branch tip carried, and runs every agent-actor step of the plan's section 3 at the tip so the step's record is one measurement of one tree.

### Slice 15, the seven fences, each named by its own test

All four files green together: 109 passed, 2 todo. No fence was edited.

| Fence | File | The test, by title |
| --- | --- | --- |
| 110 | `src/__tests__/lineAgnosticPolicies.test.ts` | `no boss and no set piece names a weapon line` over `banshee.ts`, `chunks.ts`, `undertaker.ts` and `setPiece.ts`, plus `holds every boss module in the sweep, so one cannot arrive outside it` |
| 111 | `src/__tests__/boundary.test.ts` | `the core has no import cycle > carries no value-import cycle beyond the ones written down` |
| 112 | `src/__tests__/boundary.test.ts` | `the rendering-import boundary > src/game imports only from src/game` |
| 113 | `src/app/__tests__/layering.test.ts` | `LAYER_ORDER (ADR 0014) > reversed is the ADR's stack as the ADR states it, top to bottom`, with `FieldLayers > holds one container per name, in LAYER_ORDER order` holding the count |
| 114 | `src/app/__tests__/palette.test.ts` | `the source scan over the modules that draw during a run (ADR 0014) > reaches no MENU colour, writes no colour literal, and sets no blendMode` |
| 122 | `src/app/__tests__/palette.test.ts` | `the grayscale import over the stand-in art (#38, ADR 0014) > leaves no pixel with a hue in any sprite or tile it staged`, beside `leaves every sprite and tile reaching the top of the value range, so a tint is a colour` |
| 124 | `src/__tests__/lineAgnosticPolicies.test.ts` | `no weapon line walks the mob pool` over `storm.ts`, `belch.ts`, `bell.ts`, `territory.ts` and `wisps.ts`, plus `holds every line in the pool, so a fifth cannot arrive outside the fence` |

Fence 110 needed no extension: item 77 in section 4 records that the plan's slice 15 assigned work slice 6 had already done.

### The stale todo filled, and what it now holds

`palette.test.ts`'s `covers the Undertaker's curtain, which arrives at dispatch 6 and must redden nothing` was dated when `clod` was a palette entry with no drawer. The Undertaker has thrown curtains since slice 8 and they draw in their own kind since slice 14, so the entry has a drawer and the coverage is due. It is filled as **`covers the Undertaker's curtain, which draws in the clod emitter and reddens nothing`**, and it holds two things: that `clod` is one of the emitters the band's assertions sweep and satisfies each of them, and that the curtain's core clears assertion 8's own Lc 45 against **the body it is thrown from**, which is a background `BACKGROUNDS` does not carry because that list was written before anything drew a boss. `undertaker` reads Lc 72.35 and `undertakerDark` 93.96, so the curtain passes. The Banshee's own body does not, at Lc 39.59, and that is item 80 in section 4 with its trigger rather than a palette entry moved here.

`boundary.test.ts:247` is item 78: a dormant guard rather than a stale todo, naming no folder today. `palette.test.ts`'s `covers src/app/ui, whose widgets draw over the field and are dressed at #38` stays a todo; #38 owns it.

### The timeout, fixed at the cause

`pays a full build to a run that kills every carrier before the set piece` played **three whole two-section runs inside one test**: 1619 ms alone and 5051 to 6150 ms under `pnpm verify`, where the two workspaces' suites run at once and it crossed vitest's 5000 ms default. It is one test per seed now, 612, 473 and 445 ms, which is the `on seed N` shape `bot.test.ts` already gives every whole-run claim. Nothing was weakened: the same three seeds, the same three assertions, one budget each, and a failure that names its seed. `pnpm verify` is green twice at the tip, exit 0.

### The section timeline, off a tape recorded at the tip

Seed 101, 40000 ticks under the maxed dodge bot through `local/slice13b-record.ts`, measured with `scripts/measure.ts`.

| Phase | From | To | Ticks | Seconds | Alive at its first tick |
| --- | --- | --- | --- | --- | --- |
| procession | 0 | 7284 | 7284 | 121.4 | 0 |
| banshee | 7284 | 8526 | 1242 | 20.7 | **0** |
| crowd | 8526 | 16927 | 8401 | 140.0 | 0 |
| waking | 16927 | 17827 | 900 | 15.0 | 21 |
| vigil | 17827 | 22703 | 4876 | 81.3 | 7 |
| undertaker | 22703 | 26691 | 3988 | 66.5 | **0** |
| over | 26691 | victory | | | |

Seven phases in order; the Procession (121.4 s) shorter than the Crowd (140.0 s); the Vigil (81.3 s) shorter than the Procession; both boss phases beginning on a tick with zero live mobs, and the Waking beginning in traffic, which is ADR 0051's own asymmetry visible in one column. **The clock is the criterion that fails**: 26691 ticks is 7.41 minutes against the eight-to-ten band. Item 81 carries it with the other hands' figures and its trigger; nothing was retuned.

Every boundary is identical to adjustment iteration 2's and adjustment 3's measurement of the same seed, which is what says those two cost this hand nothing: 7284, 8526, 16927, 17827, 22703, 26691.

### What the rendered check showed, each read

Eight shots, each off its own tape recorded at the tip to end on the tick it wanted, rendered through `#/replay?tape=...&at=` on the built app under `pnpm exec vite preview` at 1440 by 900. Every read below was **counted rather than eyeballed**: a `pngjs` script counted, over the whole frame, the pixels matching each declared hex.

- **The Procession, tick 4260, one template live over its own dressing.** A file of green bodies down the middle over grey statues, urns and a cliff, with corpses behind them. **1847 pixels of `mob`, 33 of `fireTrash`, and not one of any boss, boss-fire or ground-tint colour.** The section a player already knows.
- **The Crowd, tick 16500, two templates overlapping over its dressing.** Four groups on screen at once, two of them overlapping top right, over the Crowd's own green vein and eye dressing. **9956 pixels of `mob`, 5.4 times the Procession's, and 212 of `fireTrash`.** Density is the difference and the count is what says so.
- **The Vigil, tick 20983, with the tint departure.** **7016 pixels of `standInVigilTint`** in amalgam arcs, tentacles, floating rocks and eyes, against **837 of `mob`**, a fifth of the Crowd's. Fewer, tougher bodies over the run's one colour addition, and it is unmistakably a third place.
- **The Banshee mid-chunk-one, tick 7560.** A pale mint shroud with a ragged hem, two dark eye hollows and the dark oval of the wail, high in an empty field, with her tear-ring expanding around her and one clean gap in it at the lower left. **5974 pixels of `banshee`, 914 of `bansheeDark`, 611 of `fireTear` and zero of `fireTrash`.**
- **The Wall arriving, tick 8700.** A rank of twenty-two bodies across the whole top edge, **9727 pixels of `mob`**, with her last tears still in flight beneath it, **841 of `fireTear` and zero `fireTrash`**. The reservoir is full and that is measured in the sim rather than read off the shot: it first fills at tick 3791 and the maxed dodge bot never belches. **The replay screen draws no readout**, so the reservoir line the plan's shot asks for is on no frame this path can take. Stated rather than skipped.
- **The dormant source riding down through the Crowd, tick 16800.** The source is a large tentacled face high in the field over the Crowd's own smaller eye and vein dressing, **390 pixels of `standInWaking` and 28 of `standInWakingDark`** against 4366 of `mob`. It is plainly larger than the dressing pieces and wears the same family on purpose, which is exactly the read step 21 is Mark's.
- **The Waking open and pouring, tick 17400.** The open source about three fifths down with bodies streaming out behind it in one clean diagonal to the lower left and corpses trailing under them. **11130 pixels of `mob`, the most of any frame in this pass**, with 326 of `standInWaking`. It reads as a curve a dive can follow.
- **The Undertaker in chunk three, tick 25980.** The locked overlap: two clod curtains across the field, each with a way through, the arm throwing at the same time, over the Vigil's teal. **719 pixels of `fireClod` and 502 of `fireSpiral` in one frame, with zero `fireTrash` and zero `fireTear`**, plus 6122 of `undertaker`, 613 of `undertakerDark` and 4911 of `standInVigilTint`. Two fire kinds on screen at once is the only place in the game that happens, and the count is what says so.

### The grayscale check at target density

Both frames rewritten in Rec.709 luma alone, which is the quantity `src/app/color.ts`'s `luma` measures, so anything separated by hue alone collapses into the ground.

**Threats still read apart from the ground, and the margin is measured.** Over ground-only rectangles in both frames the floor and its dressing span grey **21 to 61 of 255**, p50 21.6. The threats drawn over it: `fireTrash` 111.5, `fireClod` 118.0, `fireTear` 136.2, `fireSpiral` 152.4, `mob` 169.9, `fireCore` 239.6. **The darkest threat sits 50 grey points above the ground's brightest pixel**, at the Crowd's densest moment (28 alive, tick 16500) and at the Waking's pour (41 alive, tick 17400) alike. Section 23 flagged that the baked floor draws about a third brighter than the tiled sheet did and said this is where it would be judged: it has not closed the gap, and the answer is that the stretch cost the separation nothing.

The one entry that sits closer to the ground than the rest is the Waking's source at grey 107.1, which is the moment's centrepiece rather than a threat and carries its own dark companion. It reads as a distinct silhouette in the grayscale frame.

### Driving the app through a CLI browser is not a substitute for playing it

Every read above is a still frame. Four reads were not obtained and none of them can be from a frame: whether the three sections read as three places while they move past, whether chunk three's gap reads as following the arm, whether the pour reads as a reward rather than a wall, and whether the floor's brightness costs anything a player notices in motion.

### Verification steps 16 to 22 are Mark's and stay open

The deployed build lets him answer **16** (whether the three sections are tellable), **17** (whether chunk three's twist is legible), **19** (whether the Waking reads as heaven), **20** (whether the length is bearable), **21** (whether the dormant source reads apart from the Crowd's eye dressing) and **22** (whether three loops is enough), because all six are in a run he can play end to end. **18**, whether one belch plus play beats the Undertaker, needs a hand that levels and belches, which no headless policy does; the agent owes him belch count per fight and time to kill by loadout, and nothing here has measured either.

### The tests were proved to bite rather than assumed to

Three mutations, each reverted.

- `MOB_FIRE.clod`'s core pointed at `fireClod` instead of `fireCore` turned the filled curtain test red, along with four of the band's own.
- `PALETTE.undertaker` moved to a near-white hex turned the curtain test red on the pair it adds and **named `undertaker` in the failure**, which is the half no other test in the file holds.
- One Procession row's `carries` flipped to false turned **all three** per-seed full-build tests red, which is the split doing what the un-split test did and naming which seed besides.

### Instruments deleted, and what is left behind

`local/slice16-endings.ts`, `local/slice16-marks.ts`, `local/slice16-chunk3.ts`, `local/slice16-count.mjs`, `local/slice16-grayscale.mjs`, `local/slice16-separation.mjs`, `local/slice16/` and the eight tapes under `dist/` are gone; `local/` and `dist/` are both excluded from version control, so none of them reached a commit. Screenshots are in the session scratchpad and never in the repo. **`local/slice13b-record.ts` is still there** and is what recorded this pass's tapes, as sections 24, 25 and 26 record. **`local/step2/slice11-timeline.ts` is still there too**, left by slice 11 and named here for the first time; neither is mine to remove.
